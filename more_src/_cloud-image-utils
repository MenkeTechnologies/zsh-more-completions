#compdef cloud-image-utils

# cloud-image-utils: package launcher - real binaries: cloud-localds,
# cloud-publish-image, cloud-publish-tarball, cloud-publish-ubuntu,
# growpart, ubuntu-cloudimg-query
local curcontext="$curcontext" ret=1
_arguments -C \
  '*::cloud-image utility and args:_files' && ret=0
return ret
