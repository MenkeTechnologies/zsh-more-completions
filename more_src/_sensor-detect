#compdef sensors-detect
# Detect hardware monitoring sensors

local ret=1

_arguments -s -S \
  '--auto[answer yes to all questions]' \
  '--stat[print statistics]' \
  '--bus-list[scan I2C/SMBus adapters]' && ret=0

return ret
