#compdef jack_impulse_grabber
# JACK toolkit client to grab an impulse response

local ret=1

_arguments -s -S \
  '-d+[capture period length / duration]:duration' \
  '-f+[output format]:format:(C gnuplot)' && ret=0

return ret
