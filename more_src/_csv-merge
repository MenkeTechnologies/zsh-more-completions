#compdef csv-merge
# Documentation: <none verified — multiple unrelated tools share this name>
# Reference accessed: 2026-05-17
# Stub: no single canonical upstream found for `csv-merge`. Tool is ambiguous
# between several language ecosystems (PyPI, Cargo, npm, ad-hoc scripts).
# Completion falls back to file matching with common CSV-ish CLI flags.

_arguments -s -S \
  '(- *)'{-h,--help}'[show help]' \
  '(- *)'{-V,--version}'[show version]' \
  '*:csv_file:_files -g "*.csv"'
