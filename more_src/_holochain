#compdef holochain

_arguments -s -S \
  '--structured=[Structured log output]:format:(log compact json none)' \
  '(-c --config-path)'{-c,--config-path}'=[Conductor YAML config path]:file:_files -g "*.yml(Y)(-.)"' \
  '--config-schema[Print conductor config JSON schema]' \
  '(-p --piped)'{-p,--piped}'[Read keystore passphrase from stdin]' \
  '--build-info[Print detailed build information]' \
  '--create-config[Generate default conductor configuration files]' \
  '--danger-print-db-secrets[Print database decryption secrets (dangerous)]'
