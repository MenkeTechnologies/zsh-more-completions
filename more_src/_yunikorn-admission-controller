#compdef yunikorn-admission-controller k8s_yunikorn_admission_controller
# _yunikorn-admission-controller — Apache YuniKorn admission controller (validates/mutates pods + validates config ConfigMaps)
# upstream: https://github.com/apache/yunikorn-k8shim
# Source: pkg/cmd/admissioncontroller/main.go on tip; no CLI flags — runtime config via ConfigMap admissionController.* keys (webHook.*, filtering.*, accessControl.*, processNs.*, bypassControllers.*).

_yunikorn-admission-controller() {
    _arguments \
        '(- *)'{-h,--help}'[show help]'
}

_yunikorn-admission-controller "$@"
