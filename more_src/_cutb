#compdef cutb
# Documentation: https://hashcat.net/wiki/doku.php?id=hashcat_utils
# extract word segments by offset and length for combinator attacks

local ret=1

_arguments -s -S \
  '1:offset (negative counts from end):' \
  '2:length (optional):' && ret=0

return ret
