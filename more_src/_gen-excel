#compdef gen-excel
# _gen-excel — generate an Excel workbook from a LinkML schema (linkml/linkml)
# upstream: https://github.com/linkml/linkml
# Flags from linkml/generators/excelgen.py (click).

_gen-excel() {
    local -a args
    args=(
        '(-o --output)'{-o+,--output=}'[output .xlsx file]:file:_files -g "*.xlsx"'
        '--split-workbook-by-class[emit one workbook per class instead of a single workbook]'
        '--include-mixins[include mixin classes in the workbook]'
        '--metadata[include schema metadata in output]'
        '--no-metadata[do not include schema metadata]'
        '(-im --importmap)'{-im+,--importmap=}'[import mapping file]:file:_files'
        '(-v --verbose)'{-v,--verbose}'[verbose output]'
        '--mergeimports[merge imports into output]'
        '--no-mergeimports[do not merge imports]'
        '--stacktrace[print full stack traces on error]'
        '--no-stacktrace[suppress stack traces on error]'
        '(- *)'{-V,--version}'[show version]'
        '(- *)--help[show help]'
        '1:LinkML schema YAML:_files -g "*.(yaml|yml)"'
    )
    _arguments -s -S $args
}

_gen-excel "$@"
