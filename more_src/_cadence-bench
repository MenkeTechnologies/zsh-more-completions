#compdef cadence-bench
# _cadence-bench — Cadence load/benchmark worker (cadence-workflow/cadence)
# upstream: https://github.com/cadence-workflow/cadence
# Subcommands from cmd/bench/main.go (urfave/cli/v2).

_cadence-bench() {
    local context state state_descr line
    typeset -A opt_args

    _arguments -C \
        '(- *)'{-h,--help}'[show help]' \
        '(- *)'{-v,--version}'[show version]' \
        '(-r --root)'{-r+,--root=}'[root directory of execution environment]:dir:_files -/' \
        '(-c --config)'{-c+,--config=}'[config directory relative to root]:dir:_files -/' \
        '(-e --env)'{-e+,--env=}'[runtime environment]:env:(development production)' \
        '(--az --zone)'{--az+,--zone=}'[availability zone]:az:' \
        '1: :->cmd' \
        '*:: :->args'

    case $state in
        cmd)
            local -a cmds=(
                start:'start cadence bench worker'
            )
            _describe -t commands command cmds
            ;;
    esac
}

_cadence-bench "$@"
