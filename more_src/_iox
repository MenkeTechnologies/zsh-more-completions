#compdef iox
# Documentation: https://man.archlinux.org/search?q=iox
# Reference accessed: 2026-05-16

local curcontext="$curcontext" state line ret=1
local -a subcommands

subcommands=(
  'fwd:port forwarding'
  'proxy:socks5 proxy'
)

_arguments -C \
  '(- *)'{-h}'[show help]' \
  '1: :->subcommand' \
  '*::arg:->args' && ret=0

case $state in
  (subcommand) _describe -t subcommands 'iox subcommand' subcommands && ret=0 ;;
  (args)
    _arguments -s \
      '*-l[local listen address]:host\:port:' \
      '*-r[remote address]:host\:port:' \
      '-k[pre-shared key hex]:hex:' \
      '-u[UDP forward]' \
      '-t[timeout ms]:ms:' \
      '-v[verbose]' \
      '-h[help]' && ret=0 ;;
esac

return ret
