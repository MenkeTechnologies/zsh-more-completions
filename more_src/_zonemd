#compdef zonemd
# Documentation: (ZONEMD is a DNS record type per RFC 8976; no canonical CLI)
# Reference accessed: 2026-05-16
# Verified: no upstream standalone CLI binary for "zonemd". Tool kept per
# "do not delete files" directive with minimal stub.

# zonemd: no canonical standalone CLI (ZONEMD is an RFC 8976 DNS record type)
local curcontext="$curcontext" ret=1
_arguments -C \
  '*::extra args:_files' && ret=0
return ret
