#compdef vipw
# _vipw — OpenBSD vipw (edit /etc/master.passwd under a lock with $EDITOR (default vi(1)); runs pwd_mkdb(8) on the new file and rejects mangled entries)
# upstream: https://github.com/openbsd/src (usr.sbin/vipw)
# Synopsis from usr.sbin/vipw/vipw.8 on tip.

_vipw() {
    _arguments
}

_vipw "$@"
