#compdef km
# Source: cargo binstall kimun@0.21.0
# Probed via: completions zsh
# Repository: https://github.com/lnds/kimun

autoload -U is-at-least

_km() {
    typeset -A opt_args
    typeset -a _arguments_options
    local ret=1

    if is-at-least 5.2; then
        _arguments_options=(-s -S -C)
    else
        _arguments_options=(-s -C)
    fi

    local context curcontext="$curcontext" state line
    _arguments "${_arguments_options[@]}" : \
'-h[Print help]' \
'--help[Print help]' \
'-V[Print version]' \
'--version[Print version]' \
":: :_km_commands" \
"*::: :->km" \
&& ret=0
    case $state in
    (km)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:km-command-$line[1]:"
        case $line[1] in
            (loc)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format\: table (default), json, short, terse, github, or codeclimate. \`short\` emits a single compact line of key\:value pairs (AI-friendly). \`terse\` emits a single headline metric value (for piping/embedding). \`github\` emits GitHub Actions warning annotations (only cycom, cogcom, smells). \`codeclimate\` (alias\: \`gitlab\`) emits CodeClimate JSON for GitLab Code Quality (only cycom, cogcom, smells)]:FORMAT:((table\:"Human-readable table (default)"
json\:"Machine-readable JSON"
short\:"Single compact line of key\:value pairs (AI-friendly)"
terse\:"Single headline metric value (for piping/embedding)"
github\:"GitHub Actions warning annotations (CI)"
codeclimate\:"CodeClimate JSON report (GitLab Code Quality, Codacy, etc.) Also accepted as \`--format gitlab\` for discoverability"))' \
'(--exclude-ext)*--include-ext=[Only include files with these extensions (case-insensitive, leading dot optional). When set, all other extensions are excluded. Cannot be combined with --exclude-ext. Repeatable\: --include-ext rs --include-ext toml]:EXT:_default' \
'*--exclude-ext=[Exclude files by extension, case-insensitive (leading dot optional\: "js" and ".js" are equivalent). Repeatable\: --exclude-ext js --exclude-ext ts]:EXT:_default' \
'*--exclude-dir=[Exclude directories by exact name (case-sensitive). Matches directory names at any depth in the tree. Repeatable\: --exclude-dir vendor --exclude-dir dist]:DIR:_default' \
'*-E+[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'*--exclude=[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'--include-tests[Include test files and directories in analysis (excluded by default)]' \
'--list-excluded[Print files that would be excluded by the current filter and exit. Useful for debugging filter rules before running an analysis]' \
'-v[Show summary stats (files read, unique, ignored, elapsed time)]' \
'--verbose[Show summary stats (files read, unique, ignored, elapsed time)]' \
'--by-author[Break down lines of code by git author (requires a git repository)]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::path -- Directory to analyze (default\: current directory):_files' \
&& ret=0
;;
(dups)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format\: table (default), json, short, terse, github, or codeclimate. \`short\` emits a single compact line of key\:value pairs (AI-friendly). \`terse\` emits a single headline metric value (for piping/embedding). \`github\` emits GitHub Actions warning annotations (only cycom, cogcom, smells). \`codeclimate\` (alias\: \`gitlab\`) emits CodeClimate JSON for GitLab Code Quality (only cycom, cogcom, smells)]:FORMAT:((table\:"Human-readable table (default)"
json\:"Machine-readable JSON"
short\:"Single compact line of key\:value pairs (AI-friendly)"
terse\:"Single headline metric value (for piping/embedding)"
github\:"GitHub Actions warning annotations (CI)"
codeclimate\:"CodeClimate JSON report (GitLab Code Quality, Codacy, etc.) Also accepted as \`--format gitlab\` for discoverability"))' \
'(--exclude-ext)*--include-ext=[Only include files with these extensions (case-insensitive, leading dot optional). When set, all other extensions are excluded. Cannot be combined with --exclude-ext. Repeatable\: --include-ext rs --include-ext toml]:EXT:_default' \
'*--exclude-ext=[Exclude files by extension, case-insensitive (leading dot optional\: "js" and ".js" are equivalent). Repeatable\: --exclude-ext js --exclude-ext ts]:EXT:_default' \
'*--exclude-dir=[Exclude directories by exact name (case-sensitive). Matches directory names at any depth in the tree. Repeatable\: --exclude-dir vendor --exclude-dir dist]:DIR:_default' \
'*-E+[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'*--exclude=[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'--min-lines=[Minimum lines for a duplicate block (default\: 6, overridable via .kimun.toml)]:MIN_LINES:_default' \
'--max-duplicates=[Exit with code 1 if duplicate groups exceed this limit. Useful as a CI quality gate\: --max-duplicates 0 fails on any duplicate. Overridable via .kimun.toml \[dups\] max_duplicates]:N:_default' \
'--max-dup-ratio=[Exit with code 1 if the duplicated-lines ratio exceeds this percentage. Useful for ratcheting down duplication over time\: --max-dup-ratio 5.0 fails when more than 5% of code lines are duplicated. Overridable via .kimun.toml \[dups\] max_dup_ratio]:PERCENT:_default' \
'--fail-on-increase=[Exit with code 1 if the current duplication ratio is higher than at the given git ref. Prevents duplication debt from growing silently in CI\: --fail-on-increase origin/main fails if this branch added more duplicates]:REF:_default' \
'--include-tests[Include test files and directories in analysis (excluded by default)]' \
'--list-excluded[Print files that would be excluded by the current filter and exit. Useful for debugging filter rules before running an analysis]' \
'-r[Show detailed report with duplicate locations]' \
'--report[Show detailed report with duplicate locations]' \
'--show-all[Show all duplicate groups (default\: top 20)]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::path -- Directory to analyze (default\: current directory):_files' \
&& ret=0
;;
(indent)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format\: table (default), json, short, terse, github, or codeclimate. \`short\` emits a single compact line of key\:value pairs (AI-friendly). \`terse\` emits a single headline metric value (for piping/embedding). \`github\` emits GitHub Actions warning annotations (only cycom, cogcom, smells). \`codeclimate\` (alias\: \`gitlab\`) emits CodeClimate JSON for GitLab Code Quality (only cycom, cogcom, smells)]:FORMAT:((table\:"Human-readable table (default)"
json\:"Machine-readable JSON"
short\:"Single compact line of key\:value pairs (AI-friendly)"
terse\:"Single headline metric value (for piping/embedding)"
github\:"GitHub Actions warning annotations (CI)"
codeclimate\:"CodeClimate JSON report (GitLab Code Quality, Codacy, etc.) Also accepted as \`--format gitlab\` for discoverability"))' \
'(--exclude-ext)*--include-ext=[Only include files with these extensions (case-insensitive, leading dot optional). When set, all other extensions are excluded. Cannot be combined with --exclude-ext. Repeatable\: --include-ext rs --include-ext toml]:EXT:_default' \
'*--exclude-ext=[Exclude files by extension, case-insensitive (leading dot optional\: "js" and ".js" are equivalent). Repeatable\: --exclude-ext js --exclude-ext ts]:EXT:_default' \
'*--exclude-dir=[Exclude directories by exact name (case-sensitive). Matches directory names at any depth in the tree. Repeatable\: --exclude-dir vendor --exclude-dir dist]:DIR:_default' \
'*-E+[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'*--exclude=[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'--include-tests[Include test files and directories in analysis (excluded by default)]' \
'--list-excluded[Print files that would be excluded by the current filter and exit. Useful for debugging filter rules before running an analysis]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::path -- Directory to analyze (default\: current directory):_files' \
&& ret=0
;;
(hal)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format\: table (default), json, short, terse, github, or codeclimate. \`short\` emits a single compact line of key\:value pairs (AI-friendly). \`terse\` emits a single headline metric value (for piping/embedding). \`github\` emits GitHub Actions warning annotations (only cycom, cogcom, smells). \`codeclimate\` (alias\: \`gitlab\`) emits CodeClimate JSON for GitLab Code Quality (only cycom, cogcom, smells)]:FORMAT:((table\:"Human-readable table (default)"
json\:"Machine-readable JSON"
short\:"Single compact line of key\:value pairs (AI-friendly)"
terse\:"Single headline metric value (for piping/embedding)"
github\:"GitHub Actions warning annotations (CI)"
codeclimate\:"CodeClimate JSON report (GitLab Code Quality, Codacy, etc.) Also accepted as \`--format gitlab\` for discoverability"))' \
'(--exclude-ext)*--include-ext=[Only include files with these extensions (case-insensitive, leading dot optional). When set, all other extensions are excluded. Cannot be combined with --exclude-ext. Repeatable\: --include-ext rs --include-ext toml]:EXT:_default' \
'*--exclude-ext=[Exclude files by extension, case-insensitive (leading dot optional\: "js" and ".js" are equivalent). Repeatable\: --exclude-ext js --exclude-ext ts]:EXT:_default' \
'*--exclude-dir=[Exclude directories by exact name (case-sensitive). Matches directory names at any depth in the tree. Repeatable\: --exclude-dir vendor --exclude-dir dist]:DIR:_default' \
'*-E+[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'*--exclude=[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'--top=[Show only the top N files (default\: 20)]:TOP:_default' \
'--sort-by=[Sort by metric\: effort, volume, or bugs (default\: effort)]:SORT_BY:(effort volume bugs)' \
'--include-tests[Include test files and directories in analysis (excluded by default)]' \
'--list-excluded[Print files that would be excluded by the current filter and exit. Useful for debugging filter rules before running an analysis]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::path -- Directory to analyze (default\: current directory):_files' \
&& ret=0
;;
(cycom)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format\: table (default), json, short, terse, github, or codeclimate. \`short\` emits a single compact line of key\:value pairs (AI-friendly). \`terse\` emits a single headline metric value (for piping/embedding). \`github\` emits GitHub Actions warning annotations (only cycom, cogcom, smells). \`codeclimate\` (alias\: \`gitlab\`) emits CodeClimate JSON for GitLab Code Quality (only cycom, cogcom, smells)]:FORMAT:((table\:"Human-readable table (default)"
json\:"Machine-readable JSON"
short\:"Single compact line of key\:value pairs (AI-friendly)"
terse\:"Single headline metric value (for piping/embedding)"
github\:"GitHub Actions warning annotations (CI)"
codeclimate\:"CodeClimate JSON report (GitLab Code Quality, Codacy, etc.) Also accepted as \`--format gitlab\` for discoverability"))' \
'(--exclude-ext)*--include-ext=[Only include files with these extensions (case-insensitive, leading dot optional). When set, all other extensions are excluded. Cannot be combined with --exclude-ext. Repeatable\: --include-ext rs --include-ext toml]:EXT:_default' \
'*--exclude-ext=[Exclude files by extension, case-insensitive (leading dot optional\: "js" and ".js" are equivalent). Repeatable\: --exclude-ext js --exclude-ext ts]:EXT:_default' \
'*--exclude-dir=[Exclude directories by exact name (case-sensitive). Matches directory names at any depth in the tree. Repeatable\: --exclude-dir vendor --exclude-dir dist]:DIR:_default' \
'*-E+[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'*--exclude=[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'--min-complexity=[Minimum max-complexity to include a file (default\: 1)]:MIN_COMPLEXITY:_default' \
'--top=[Show only the top N files (default\: 20)]:TOP:_default' \
'--sort-by=[Sort by metric\: total, max, or avg (default\: total)]:SORT_BY:(total max avg)' \
'--include-tests[Include test files and directories in analysis (excluded by default)]' \
'--list-excluded[Print files that would be excluded by the current filter and exit. Useful for debugging filter rules before running an analysis]' \
'--per-function[Show per-function breakdown]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::path -- Directory to analyze (default\: current directory):_files' \
&& ret=0
;;
(cogcom)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format\: table (default), json, short, terse, github, or codeclimate. \`short\` emits a single compact line of key\:value pairs (AI-friendly). \`terse\` emits a single headline metric value (for piping/embedding). \`github\` emits GitHub Actions warning annotations (only cycom, cogcom, smells). \`codeclimate\` (alias\: \`gitlab\`) emits CodeClimate JSON for GitLab Code Quality (only cycom, cogcom, smells)]:FORMAT:((table\:"Human-readable table (default)"
json\:"Machine-readable JSON"
short\:"Single compact line of key\:value pairs (AI-friendly)"
terse\:"Single headline metric value (for piping/embedding)"
github\:"GitHub Actions warning annotations (CI)"
codeclimate\:"CodeClimate JSON report (GitLab Code Quality, Codacy, etc.) Also accepted as \`--format gitlab\` for discoverability"))' \
'(--exclude-ext)*--include-ext=[Only include files with these extensions (case-insensitive, leading dot optional). When set, all other extensions are excluded. Cannot be combined with --exclude-ext. Repeatable\: --include-ext rs --include-ext toml]:EXT:_default' \
'*--exclude-ext=[Exclude files by extension, case-insensitive (leading dot optional\: "js" and ".js" are equivalent). Repeatable\: --exclude-ext js --exclude-ext ts]:EXT:_default' \
'*--exclude-dir=[Exclude directories by exact name (case-sensitive). Matches directory names at any depth in the tree. Repeatable\: --exclude-dir vendor --exclude-dir dist]:DIR:_default' \
'*-E+[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'*--exclude=[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'--min-complexity=[Minimum max-complexity to include a file (default\: 1)]:MIN_COMPLEXITY:_default' \
'--top=[Show only the top N files (default\: 20)]:TOP:_default' \
'--sort-by=[Sort by metric\: total, max, or avg (default\: total)]:SORT_BY:(total max avg)' \
'--include-tests[Include test files and directories in analysis (excluded by default)]' \
'--list-excluded[Print files that would be excluded by the current filter and exit. Useful for debugging filter rules before running an analysis]' \
'--per-function[Show per-function breakdown]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::path -- Directory to analyze (default\: current directory):_files' \
&& ret=0
;;
(mi)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format\: table (default), json, short, terse, github, or codeclimate. \`short\` emits a single compact line of key\:value pairs (AI-friendly). \`terse\` emits a single headline metric value (for piping/embedding). \`github\` emits GitHub Actions warning annotations (only cycom, cogcom, smells). \`codeclimate\` (alias\: \`gitlab\`) emits CodeClimate JSON for GitLab Code Quality (only cycom, cogcom, smells)]:FORMAT:((table\:"Human-readable table (default)"
json\:"Machine-readable JSON"
short\:"Single compact line of key\:value pairs (AI-friendly)"
terse\:"Single headline metric value (for piping/embedding)"
github\:"GitHub Actions warning annotations (CI)"
codeclimate\:"CodeClimate JSON report (GitLab Code Quality, Codacy, etc.) Also accepted as \`--format gitlab\` for discoverability"))' \
'(--exclude-ext)*--include-ext=[Only include files with these extensions (case-insensitive, leading dot optional). When set, all other extensions are excluded. Cannot be combined with --exclude-ext. Repeatable\: --include-ext rs --include-ext toml]:EXT:_default' \
'*--exclude-ext=[Exclude files by extension, case-insensitive (leading dot optional\: "js" and ".js" are equivalent). Repeatable\: --exclude-ext js --exclude-ext ts]:EXT:_default' \
'*--exclude-dir=[Exclude directories by exact name (case-sensitive). Matches directory names at any depth in the tree. Repeatable\: --exclude-dir vendor --exclude-dir dist]:DIR:_default' \
'*-E+[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'*--exclude=[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'--top=[Show only the top N files (default\: 20)]:TOP:_default' \
'--sort-by=[Sort by metric\: mi, volume, complexity, or loc (default\: mi)]:SORT_BY:(mi volume complexity loc)' \
'--include-tests[Include test files and directories in analysis (excluded by default)]' \
'--list-excluded[Print files that would be excluded by the current filter and exit. Useful for debugging filter rules before running an analysis]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::path -- Directory to analyze (default\: current directory):_files' \
&& ret=0
;;
(report)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format\: table (default), json, short, terse, github, or codeclimate. \`short\` emits a single compact line of key\:value pairs (AI-friendly). \`terse\` emits a single headline metric value (for piping/embedding). \`github\` emits GitHub Actions warning annotations (only cycom, cogcom, smells). \`codeclimate\` (alias\: \`gitlab\`) emits CodeClimate JSON for GitLab Code Quality (only cycom, cogcom, smells)]:FORMAT:((table\:"Human-readable table (default)"
json\:"Machine-readable JSON"
short\:"Single compact line of key\:value pairs (AI-friendly)"
terse\:"Single headline metric value (for piping/embedding)"
github\:"GitHub Actions warning annotations (CI)"
codeclimate\:"CodeClimate JSON report (GitLab Code Quality, Codacy, etc.) Also accepted as \`--format gitlab\` for discoverability"))' \
'(--exclude-ext)*--include-ext=[Only include files with these extensions (case-insensitive, leading dot optional). When set, all other extensions are excluded. Cannot be combined with --exclude-ext. Repeatable\: --include-ext rs --include-ext toml]:EXT:_default' \
'*--exclude-ext=[Exclude files by extension, case-insensitive (leading dot optional\: "js" and ".js" are equivalent). Repeatable\: --exclude-ext js --exclude-ext ts]:EXT:_default' \
'*--exclude-dir=[Exclude directories by exact name (case-sensitive). Matches directory names at any depth in the tree. Repeatable\: --exclude-dir vendor --exclude-dir dist]:DIR:_default' \
'*-E+[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'*--exclude=[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'--top=[Show only the top N files per section (default\: 20)]:TOP:_default' \
'--min-lines=[Minimum lines for a duplicate block (default\: 6, overridable via .kimun.toml)]:MIN_LINES:_default' \
'--include-tests[Include test files and directories in analysis (excluded by default)]' \
'--list-excluded[Print files that would be excluded by the current filter and exit. Useful for debugging filter rules before running an analysis]' \
'--full[Show all files instead of truncating to top N]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::path -- Directory to analyze (default\: current directory):_files' \
&& ret=0
;;
(miv)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format\: table (default), json, short, terse, github, or codeclimate. \`short\` emits a single compact line of key\:value pairs (AI-friendly). \`terse\` emits a single headline metric value (for piping/embedding). \`github\` emits GitHub Actions warning annotations (only cycom, cogcom, smells). \`codeclimate\` (alias\: \`gitlab\`) emits CodeClimate JSON for GitLab Code Quality (only cycom, cogcom, smells)]:FORMAT:((table\:"Human-readable table (default)"
json\:"Machine-readable JSON"
short\:"Single compact line of key\:value pairs (AI-friendly)"
terse\:"Single headline metric value (for piping/embedding)"
github\:"GitHub Actions warning annotations (CI)"
codeclimate\:"CodeClimate JSON report (GitLab Code Quality, Codacy, etc.) Also accepted as \`--format gitlab\` for discoverability"))' \
'(--exclude-ext)*--include-ext=[Only include files with these extensions (case-insensitive, leading dot optional). When set, all other extensions are excluded. Cannot be combined with --exclude-ext. Repeatable\: --include-ext rs --include-ext toml]:EXT:_default' \
'*--exclude-ext=[Exclude files by extension, case-insensitive (leading dot optional\: "js" and ".js" are equivalent). Repeatable\: --exclude-ext js --exclude-ext ts]:EXT:_default' \
'*--exclude-dir=[Exclude directories by exact name (case-sensitive). Matches directory names at any depth in the tree. Repeatable\: --exclude-dir vendor --exclude-dir dist]:DIR:_default' \
'*-E+[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'*--exclude=[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'--top=[Show only the top N files (default\: 20)]:TOP:_default' \
'--sort-by=[Sort by metric\: mi, volume, complexity, or loc (default\: mi)]:SORT_BY:(mi volume complexity loc)' \
'--include-tests[Include test files and directories in analysis (excluded by default)]' \
'--list-excluded[Print files that would be excluded by the current filter and exit. Useful for debugging filter rules before running an analysis]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::path -- Directory to analyze (default\: current directory):_files' \
&& ret=0
;;
(churn)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format\: table (default), json, short, terse, github, or codeclimate. \`short\` emits a single compact line of key\:value pairs (AI-friendly). \`terse\` emits a single headline metric value (for piping/embedding). \`github\` emits GitHub Actions warning annotations (only cycom, cogcom, smells). \`codeclimate\` (alias\: \`gitlab\`) emits CodeClimate JSON for GitLab Code Quality (only cycom, cogcom, smells)]:FORMAT:((table\:"Human-readable table (default)"
json\:"Machine-readable JSON"
short\:"Single compact line of key\:value pairs (AI-friendly)"
terse\:"Single headline metric value (for piping/embedding)"
github\:"GitHub Actions warning annotations (CI)"
codeclimate\:"CodeClimate JSON report (GitLab Code Quality, Codacy, etc.) Also accepted as \`--format gitlab\` for discoverability"))' \
'(--exclude-ext)*--include-ext=[Only include files with these extensions (case-insensitive, leading dot optional). When set, all other extensions are excluded. Cannot be combined with --exclude-ext. Repeatable\: --include-ext rs --include-ext toml]:EXT:_default' \
'*--exclude-ext=[Exclude files by extension, case-insensitive (leading dot optional\: "js" and ".js" are equivalent). Repeatable\: --exclude-ext js --exclude-ext ts]:EXT:_default' \
'*--exclude-dir=[Exclude directories by exact name (case-sensitive). Matches directory names at any depth in the tree. Repeatable\: --exclude-dir vendor --exclude-dir dist]:DIR:_default' \
'*-E+[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'*--exclude=[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'--top=[Show only the top N files (default\: 20)]:TOP:_default' \
'--sort-by=[Sort by\: commits (default), rate (commits/month), or file]:SORT_BY:(commits rate file)' \
'--since=[Only consider commits since this time (e.g. 6m, 1y, 30d)]:SINCE:_default' \
'--include-tests[Include test files and directories in analysis (excluded by default)]' \
'--list-excluded[Print files that would be excluded by the current filter and exit. Useful for debugging filter rules before running an analysis]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::path -- Directory to analyze (default\: current directory):_files' \
&& ret=0
;;
(hotspots)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format\: table (default), json, short, terse, github, or codeclimate. \`short\` emits a single compact line of key\:value pairs (AI-friendly). \`terse\` emits a single headline metric value (for piping/embedding). \`github\` emits GitHub Actions warning annotations (only cycom, cogcom, smells). \`codeclimate\` (alias\: \`gitlab\`) emits CodeClimate JSON for GitLab Code Quality (only cycom, cogcom, smells)]:FORMAT:((table\:"Human-readable table (default)"
json\:"Machine-readable JSON"
short\:"Single compact line of key\:value pairs (AI-friendly)"
terse\:"Single headline metric value (for piping/embedding)"
github\:"GitHub Actions warning annotations (CI)"
codeclimate\:"CodeClimate JSON report (GitLab Code Quality, Codacy, etc.) Also accepted as \`--format gitlab\` for discoverability"))' \
'(--exclude-ext)*--include-ext=[Only include files with these extensions (case-insensitive, leading dot optional). When set, all other extensions are excluded. Cannot be combined with --exclude-ext. Repeatable\: --include-ext rs --include-ext toml]:EXT:_default' \
'*--exclude-ext=[Exclude files by extension, case-insensitive (leading dot optional\: "js" and ".js" are equivalent). Repeatable\: --exclude-ext js --exclude-ext ts]:EXT:_default' \
'*--exclude-dir=[Exclude directories by exact name (case-sensitive). Matches directory names at any depth in the tree. Repeatable\: --exclude-dir vendor --exclude-dir dist]:DIR:_default' \
'*-E+[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'*--exclude=[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'--top=[Show only the top N files (default\: 20)]:TOP:_default' \
'--sort-by=[Sort by metric\: score, commits, or complexity (default\: score)]:SORT_BY:(score commits complexity)' \
'--since=[Only consider commits since this time (e.g. 6m, 1y, 30d)]:SINCE:_default' \
'--complexity=[Complexity metric\: indent (default, Thornhill), cycom (cyclomatic), or cogcom (cognitive). Overridable via .kimun.toml \[hotspots\] complexity]:COMPLEXITY:(indent cycom cogcom)' \
'--include-tests[Include test files and directories in analysis (excluded by default)]' \
'--list-excluded[Print files that would be excluded by the current filter and exit. Useful for debugging filter rules before running an analysis]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::path -- Directory to analyze (default\: current directory):_files' \
&& ret=0
;;
(knowledge)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format\: table (default), json, short, terse, github, or codeclimate. \`short\` emits a single compact line of key\:value pairs (AI-friendly). \`terse\` emits a single headline metric value (for piping/embedding). \`github\` emits GitHub Actions warning annotations (only cycom, cogcom, smells). \`codeclimate\` (alias\: \`gitlab\`) emits CodeClimate JSON for GitLab Code Quality (only cycom, cogcom, smells)]:FORMAT:((table\:"Human-readable table (default)"
json\:"Machine-readable JSON"
short\:"Single compact line of key\:value pairs (AI-friendly)"
terse\:"Single headline metric value (for piping/embedding)"
github\:"GitHub Actions warning annotations (CI)"
codeclimate\:"CodeClimate JSON report (GitLab Code Quality, Codacy, etc.) Also accepted as \`--format gitlab\` for discoverability"))' \
'(--exclude-ext)*--include-ext=[Only include files with these extensions (case-insensitive, leading dot optional). When set, all other extensions are excluded. Cannot be combined with --exclude-ext. Repeatable\: --include-ext rs --include-ext toml]:EXT:_default' \
'*--exclude-ext=[Exclude files by extension, case-insensitive (leading dot optional\: "js" and ".js" are equivalent). Repeatable\: --exclude-ext js --exclude-ext ts]:EXT:_default' \
'*--exclude-dir=[Exclude directories by exact name (case-sensitive). Matches directory names at any depth in the tree. Repeatable\: --exclude-dir vendor --exclude-dir dist]:DIR:_default' \
'*-E+[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'*--exclude=[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'--top=[Show only the top N files (default\: 20)]:TOP:_default' \
'--sort-by=[Sort by\: concentration, diffusion, or risk (default\: concentration)]:SORT_BY:(concentration diffusion risk)' \
'--since=[Only consider recent activity since this time for knowledge loss detection (e.g. 6m, 1y, 30d)]:SINCE:_default' \
'--author=[Show only files owned by this author (substring match, case-insensitive)]:NAME:_default' \
'--include-tests[Include test files and directories in analysis (excluded by default)]' \
'--list-excluded[Print files that would be excluded by the current filter and exit. Useful for debugging filter rules before running an analysis]' \
'--risk-only[Show only files with knowledge loss risk (primary owner inactive)]' \
'--summary[Aggregate by author\: files owned, lines, languages, worst risk]' \
'--bus-factor[Compute the project bus factor\: the minimum number of contributors whose combined ownership covers 80% of the code. A bus factor of 1 means one person holds most knowledge — extremely high risk]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::path -- Directory to analyze (default\: current directory):_files' \
&& ret=0
;;
(tc)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format\: table (default), json, short, terse, github, or codeclimate. \`short\` emits a single compact line of key\:value pairs (AI-friendly). \`terse\` emits a single headline metric value (for piping/embedding). \`github\` emits GitHub Actions warning annotations (only cycom, cogcom, smells). \`codeclimate\` (alias\: \`gitlab\`) emits CodeClimate JSON for GitLab Code Quality (only cycom, cogcom, smells)]:FORMAT:((table\:"Human-readable table (default)"
json\:"Machine-readable JSON"
short\:"Single compact line of key\:value pairs (AI-friendly)"
terse\:"Single headline metric value (for piping/embedding)"
github\:"GitHub Actions warning annotations (CI)"
codeclimate\:"CodeClimate JSON report (GitLab Code Quality, Codacy, etc.) Also accepted as \`--format gitlab\` for discoverability"))' \
'(--exclude-ext)*--include-ext=[Only include files with these extensions (case-insensitive, leading dot optional). When set, all other extensions are excluded. Cannot be combined with --exclude-ext. Repeatable\: --include-ext rs --include-ext toml]:EXT:_default' \
'*--exclude-ext=[Exclude files by extension, case-insensitive (leading dot optional\: "js" and ".js" are equivalent). Repeatable\: --exclude-ext js --exclude-ext ts]:EXT:_default' \
'*--exclude-dir=[Exclude directories by exact name (case-sensitive). Matches directory names at any depth in the tree. Repeatable\: --exclude-dir vendor --exclude-dir dist]:DIR:_default' \
'*-E+[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'*--exclude=[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'--top=[Show only the top N file pairs (default\: 20)]:TOP:_default' \
'--sort-by=[Sort by\: strength or shared (default\: strength)]:SORT_BY:(strength shared)' \
'--since=[Only consider commits since this time (e.g. 6m, 1y, 30d)]:SINCE:_default' \
'--min-degree=[Minimum commits per file to be included (default\: 3, overridable via .kimun.toml)]:MIN_DEGREE:_default' \
'--min-strength=[Filter results\: show only pairs with strength >= threshold (e.g. 0.5 for strong coupling only). Overridable via .kimun.toml \[tc\] min_strength]:MIN_STRENGTH:_default' \
'--include-tests[Include test files and directories in analysis (excluded by default)]' \
'--list-excluded[Print files that would be excluded by the current filter and exit. Useful for debugging filter rules before running an analysis]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::path -- Directory to analyze (default\: current directory):_files' \
&& ret=0
;;
(smells)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format\: table (default), json, short, terse, github, or codeclimate. \`short\` emits a single compact line of key\:value pairs (AI-friendly). \`terse\` emits a single headline metric value (for piping/embedding). \`github\` emits GitHub Actions warning annotations (only cycom, cogcom, smells). \`codeclimate\` (alias\: \`gitlab\`) emits CodeClimate JSON for GitLab Code Quality (only cycom, cogcom, smells)]:FORMAT:((table\:"Human-readable table (default)"
json\:"Machine-readable JSON"
short\:"Single compact line of key\:value pairs (AI-friendly)"
terse\:"Single headline metric value (for piping/embedding)"
github\:"GitHub Actions warning annotations (CI)"
codeclimate\:"CodeClimate JSON report (GitLab Code Quality, Codacy, etc.) Also accepted as \`--format gitlab\` for discoverability"))' \
'(--exclude-ext)*--include-ext=[Only include files with these extensions (case-insensitive, leading dot optional). When set, all other extensions are excluded. Cannot be combined with --exclude-ext. Repeatable\: --include-ext rs --include-ext toml]:EXT:_default' \
'*--exclude-ext=[Exclude files by extension, case-insensitive (leading dot optional\: "js" and ".js" are equivalent). Repeatable\: --exclude-ext js --exclude-ext ts]:EXT:_default' \
'*--exclude-dir=[Exclude directories by exact name (case-sensitive). Matches directory names at any depth in the tree. Repeatable\: --exclude-dir vendor --exclude-dir dist]:DIR:_default' \
'*-E+[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'*--exclude=[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'--top=[Show only the top N files (default\: 20)]:TOP:_default' \
'--max-lines=[Maximum function length before flagging (default\: 50, overridable via .kimun.toml)]:MAX_LINES:_default' \
'--max-params=[Maximum parameter count before flagging (default\: 4, overridable via .kimun.toml)]:MAX_PARAMS:_default' \
'(--since-ref)*--files=[Analyze only these specific files (repeatable). Useful for scripting\: km smells --files src/foo.rs --files src/bar.ex]:FILE:_files' \
'--since-ref=[Analyze only files changed since this git ref (e.g. origin/main, HEAD~1). Runs git diff internally — no need to pipe file lists. Ideal for CI\: km smells --since-ref origin/main]:REF:_default' \
'--include-tests[Include test files and directories in analysis (excluded by default)]' \
'--list-excluded[Print files that would be excluded by the current filter and exit. Useful for debugging filter rules before running an analysis]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::path -- Directory to analyze (default\: current directory):_files' \
&& ret=0
;;
(score)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format\: table (default), json, short, terse, github, or codeclimate. \`short\` emits a single compact line of key\:value pairs (AI-friendly). \`terse\` emits a single headline metric value (for piping/embedding). \`github\` emits GitHub Actions warning annotations (only cycom, cogcom, smells). \`codeclimate\` (alias\: \`gitlab\`) emits CodeClimate JSON for GitLab Code Quality (only cycom, cogcom, smells)]:FORMAT:((table\:"Human-readable table (default)"
json\:"Machine-readable JSON"
short\:"Single compact line of key\:value pairs (AI-friendly)"
terse\:"Single headline metric value (for piping/embedding)"
github\:"GitHub Actions warning annotations (CI)"
codeclimate\:"CodeClimate JSON report (GitLab Code Quality, Codacy, etc.) Also accepted as \`--format gitlab\` for discoverability"))' \
'(--exclude-ext)*--include-ext=[Only include files with these extensions (case-insensitive, leading dot optional). When set, all other extensions are excluded. Cannot be combined with --exclude-ext. Repeatable\: --include-ext rs --include-ext toml]:EXT:_default' \
'*--exclude-ext=[Exclude files by extension, case-insensitive (leading dot optional\: "js" and ".js" are equivalent). Repeatable\: --exclude-ext js --exclude-ext ts]:EXT:_default' \
'*--exclude-dir=[Exclude directories by exact name (case-sensitive). Matches directory names at any depth in the tree. Repeatable\: --exclude-dir vendor --exclude-dir dist]:DIR:_default' \
'*-E+[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'*--exclude=[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'--bottom=[Number of worst files to show in "needs attention" (default\: 10)]:BOTTOM:_default' \
'--min-lines=[Minimum lines for a duplicate block (default\: 6, overridable via .kimun.toml)]:MIN_LINES:_default' \
'--model=[Scoring model\: cogcom (default, v0.14+) or legacy (MI + cyclomatic, v0.13). Overridable via .kimun.toml \[score\] model]:MODEL:(cogcom legacy)' \
'--trend=[Compare current score against a git ref (default\: HEAD). Shows how the score changed\: "B- → B (+2.3)". Useful for PR review\: --trend origin/main]::REF:_default' \
'--fail-below=[Exit with code 1 if the score is below GRADE (requires --trend). Example\: --trend origin/main --fail-below B- Valid grades\: A++, A+, A, A-, B+, B, B-, C+, C, C-, D+, D, D-, F, F-, F-- Overridable via .kimun.toml \[score\] fail_below]:GRADE:_default' \
'--include-tests[Include test files and directories in analysis (excluded by default)]' \
'--list-excluded[Print files that would be excluded by the current filter and exit. Useful for debugging filter rules before running an analysis]' \
'--fail-if-worse[Exit with code 1 if the score is worse than the ref (requires --trend). Example\: --trend origin/main --fail-if-worse]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::path -- Directory to analyze (default\: current directory):_files' \
":: :_km__score_commands" \
"*::: :->score" \
&& ret=0

    case $state in
    (score)
        words=($line[2] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:km-score-command-$line[2]:"
        case $line[2] in
            (diff)
_arguments "${_arguments_options[@]}" : \
'--git-ref=[Git ref to compare against (default\: HEAD)]:REF:_default' \
'--format=[Output format\: table (default), json, short, or terse]:FORMAT:((table\:"Human-readable table (default)"
json\:"Machine-readable JSON"
short\:"Single compact line of key\:value pairs (AI-friendly)"
terse\:"Single headline metric value (for piping/embedding)"
github\:"GitHub Actions warning annotations (CI)"
codeclimate\:"CodeClimate JSON report (GitLab Code Quality, Codacy, etc.) Also accepted as \`--format gitlab\` for discoverability"))' \
'(--exclude-ext)*--include-ext=[Only include files with these extensions (case-insensitive, leading dot optional). When set, all other extensions are excluded. Cannot be combined with --exclude-ext. Repeatable\: --include-ext rs --include-ext toml]:EXT:_default' \
'*--exclude-ext=[Exclude files by extension, case-insensitive (leading dot optional\: "js" and ".js" are equivalent). Repeatable\: --exclude-ext js --exclude-ext ts]:EXT:_default' \
'*--exclude-dir=[Exclude directories by exact name (case-sensitive). Matches directory names at any depth in the tree. Repeatable\: --exclude-dir vendor --exclude-dir dist]:DIR:_default' \
'*-E+[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'*--exclude=[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'--bottom=[Number of worst files to show in "needs attention" (default\: 10)]:BOTTOM:_default' \
'--min-lines=[Minimum lines for a duplicate block (default\: 6, overridable via .kimun.toml)]:MIN_LINES:_default' \
'--model=[Scoring model\: cogcom (default, v0.14+) or legacy (MI + cyclomatic, v0.13). Overridable via .kimun.toml \[score\] model]:MODEL:(cogcom legacy)' \
'--include-tests[Include test files and directories in analysis (excluded by default)]' \
'--list-excluded[Print files that would be excluded by the current filter and exit. Useful for debugging filter rules before running an analysis]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::path -- Directory to analyze (default\: current directory):_files' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_km__score__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:km-score-help-command-$line[1]:"
        case $line[1] in
            (diff)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(age)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format\: table (default), json, short, terse, github, or codeclimate. \`short\` emits a single compact line of key\:value pairs (AI-friendly). \`terse\` emits a single headline metric value (for piping/embedding). \`github\` emits GitHub Actions warning annotations (only cycom, cogcom, smells). \`codeclimate\` (alias\: \`gitlab\`) emits CodeClimate JSON for GitLab Code Quality (only cycom, cogcom, smells)]:FORMAT:((table\:"Human-readable table (default)"
json\:"Machine-readable JSON"
short\:"Single compact line of key\:value pairs (AI-friendly)"
terse\:"Single headline metric value (for piping/embedding)"
github\:"GitHub Actions warning annotations (CI)"
codeclimate\:"CodeClimate JSON report (GitLab Code Quality, Codacy, etc.) Also accepted as \`--format gitlab\` for discoverability"))' \
'(--exclude-ext)*--include-ext=[Only include files with these extensions (case-insensitive, leading dot optional). When set, all other extensions are excluded. Cannot be combined with --exclude-ext. Repeatable\: --include-ext rs --include-ext toml]:EXT:_default' \
'*--exclude-ext=[Exclude files by extension, case-insensitive (leading dot optional\: "js" and ".js" are equivalent). Repeatable\: --exclude-ext js --exclude-ext ts]:EXT:_default' \
'*--exclude-dir=[Exclude directories by exact name (case-sensitive). Matches directory names at any depth in the tree. Repeatable\: --exclude-dir vendor --exclude-dir dist]:DIR:_default' \
'*-E+[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'*--exclude=[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'--active-days=[Files modified within this many days are Active (default\: 90, overridable via .kimun.toml)]:ACTIVE_DAYS:_default' \
'--frozen-days=[Files not modified for more than this many days are Frozen (default\: 365, overridable via .kimun.toml)]:FROZEN_DAYS:_default' \
'--sort-by=[Sort by\: date (oldest first, default), status, or file]:SORT_BY:(date status file)' \
'--status=[Show only files with this status\: active, stale, or frozen]:STATUS:(active stale frozen)' \
'--include-tests[Include test files and directories in analysis (excluded by default)]' \
'--list-excluded[Print files that would be excluded by the current filter and exit. Useful for debugging filter rules before running an analysis]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::path -- Directory to analyze (default\: current directory):_files' \
&& ret=0
;;
(deps)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format\: table (default), json, short, terse, github, or codeclimate. \`short\` emits a single compact line of key\:value pairs (AI-friendly). \`terse\` emits a single headline metric value (for piping/embedding). \`github\` emits GitHub Actions warning annotations (only cycom, cogcom, smells). \`codeclimate\` (alias\: \`gitlab\`) emits CodeClimate JSON for GitLab Code Quality (only cycom, cogcom, smells)]:FORMAT:((table\:"Human-readable table (default)"
json\:"Machine-readable JSON"
short\:"Single compact line of key\:value pairs (AI-friendly)"
terse\:"Single headline metric value (for piping/embedding)"
github\:"GitHub Actions warning annotations (CI)"
codeclimate\:"CodeClimate JSON report (GitLab Code Quality, Codacy, etc.) Also accepted as \`--format gitlab\` for discoverability"))' \
'(--exclude-ext)*--include-ext=[Only include files with these extensions (case-insensitive, leading dot optional). When set, all other extensions are excluded. Cannot be combined with --exclude-ext. Repeatable\: --include-ext rs --include-ext toml]:EXT:_default' \
'*--exclude-ext=[Exclude files by extension, case-insensitive (leading dot optional\: "js" and ".js" are equivalent). Repeatable\: --exclude-ext js --exclude-ext ts]:EXT:_default' \
'*--exclude-dir=[Exclude directories by exact name (case-sensitive). Matches directory names at any depth in the tree. Repeatable\: --exclude-dir vendor --exclude-dir dist]:DIR:_default' \
'*-E+[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'*--exclude=[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'--sort-by=[Sort by\: fan-out (default), fan-in]:SORT_BY:(fan-out fan-in)' \
'--top=[Show only the top N files (default\: 20)]:TOP:_default' \
'--include-tests[Include test files and directories in analysis (excluded by default)]' \
'--list-excluded[Print files that would be excluded by the current filter and exit. Useful for debugging filter rules before running an analysis]' \
'--cycles-only[Show only files with dependency cycles]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::path -- Directory to analyze (default\: current directory):_files' \
&& ret=0
;;
(authors)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format\: table (default), json, short, terse, github, or codeclimate. \`short\` emits a single compact line of key\:value pairs (AI-friendly). \`terse\` emits a single headline metric value (for piping/embedding). \`github\` emits GitHub Actions warning annotations (only cycom, cogcom, smells). \`codeclimate\` (alias\: \`gitlab\`) emits CodeClimate JSON for GitLab Code Quality (only cycom, cogcom, smells)]:FORMAT:((table\:"Human-readable table (default)"
json\:"Machine-readable JSON"
short\:"Single compact line of key\:value pairs (AI-friendly)"
terse\:"Single headline metric value (for piping/embedding)"
github\:"GitHub Actions warning annotations (CI)"
codeclimate\:"CodeClimate JSON report (GitLab Code Quality, Codacy, etc.) Also accepted as \`--format gitlab\` for discoverability"))' \
'(--exclude-ext)*--include-ext=[Only include files with these extensions (case-insensitive, leading dot optional). When set, all other extensions are excluded. Cannot be combined with --exclude-ext. Repeatable\: --include-ext rs --include-ext toml]:EXT:_default' \
'*--exclude-ext=[Exclude files by extension, case-insensitive (leading dot optional\: "js" and ".js" are equivalent). Repeatable\: --exclude-ext js --exclude-ext ts]:EXT:_default' \
'*--exclude-dir=[Exclude directories by exact name (case-sensitive). Matches directory names at any depth in the tree. Repeatable\: --exclude-dir vendor --exclude-dir dist]:DIR:_default' \
'*-E+[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'*--exclude=[Exclude files matching a glob pattern against the relative path from the analysis root. Use this for compound extensions (e.g. "*.min.js") or path patterns (e.g. "vendor/**"). For simple extensions prefer --exclude-ext; for directory names prefer --exclude-dir. Repeatable\: --exclude "*.min.js" --exclude "generated/**"]:PATTERN:_default' \
'--since=[Only consider activity since this time (e.g. 6m, 1y, 30d)]:SINCE:_default' \
'--include-tests[Include test files and directories in analysis (excluded by default)]' \
'--list-excluded[Print files that would be excluded by the current filter and exit. Useful for debugging filter rules before running an analysis]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::path -- Directory to analyze (default\: current directory):_files' \
&& ret=0
;;
(ai)
_arguments "${_arguments_options[@]}" : \
'-h[Print help]' \
'--help[Print help]' \
":: :_km__ai_commands" \
"*::: :->ai" \
&& ret=0

    case $state in
    (ai)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:km-ai-command-$line[1]:"
        case $line[1] in
            (analyze)
_arguments "${_arguments_options[@]}" : \
'--model=[Model to use (default\: claude-sonnet-4-5-20250929)]:MODEL:_default' \
'-o+[Save the report to a file]:OUTPUT:_files' \
'--output=[Save the report to a file]:OUTPUT:_files' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':provider -- AI provider to use (e.g. claude):_default' \
'::path -- Directory to analyze (default\: current directory):_files' \
&& ret=0
;;
(skill)
_arguments "${_arguments_options[@]}" : \
'--with-permissions[Also configure permissions so km commands run without prompting]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':provider -- Provider for the skill (e.g. claude):_default' \
&& ret=0
;;
(permissions)
_arguments "${_arguments_options[@]}" : \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':provider -- Provider for permissions (e.g. claude):_default' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_km__ai__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:km-ai-help-command-$line[1]:"
        case $line[1] in
            (analyze)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(skill)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(permissions)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(completions)
_arguments "${_arguments_options[@]}" : \
'-h[Print help]' \
'--help[Print help]' \
':shell -- Shell to generate completions for:(bash elvish fish powershell zsh)' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_km__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:km-help-command-$line[1]:"
        case $line[1] in
            (loc)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(dups)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(indent)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(hal)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(cycom)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(cogcom)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(mi)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(report)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(miv)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(churn)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(hotspots)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(knowledge)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(tc)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(smells)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(score)
_arguments "${_arguments_options[@]}" : \
":: :_km__help__score_commands" \
"*::: :->score" \
&& ret=0

    case $state in
    (score)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:km-help-score-command-$line[1]:"
        case $line[1] in
            (diff)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(age)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(deps)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(authors)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(ai)
_arguments "${_arguments_options[@]}" : \
":: :_km__help__ai_commands" \
"*::: :->ai" \
&& ret=0

    case $state in
    (ai)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:km-help-ai-command-$line[1]:"
        case $line[1] in
            (analyze)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(skill)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(permissions)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(completions)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
}

(( $+functions[_km_commands] )) ||
_km_commands() {
    local commands; commands=(
'loc:Count lines of code (blank, comment, code) by language' \
'dups:Detect duplicate code across files' \
'indent:Analyze indentation complexity (stddev and max depth per file)' \
'hal:Analyze Halstead complexity metrics per file' \
'cycom:Analyze cyclomatic complexity per file and per function' \
'cogcom:Analyze cognitive complexity per file and per function (SonarSource method)' \
'mi:Compute Maintainability Index per file (Visual Studio variant, 0-100 scale)' \
'report:Generate a comprehensive report combining all code metrics' \
'miv:Compute Maintainability Index per file (verifysoft variant, with comment weight)' \
'churn:Analyze code churn\: pure change frequency per file (git commits only)' \
'hotspots:Find hotspots\: files that change frequently and have high complexity' \
'knowledge:Analyze code ownership patterns via git blame (knowledge maps)' \
'tc:Analyze temporal coupling\: files that change together in commits' \
'smells:Detect common code smells per file' \
'score:Compute an overall code health score for the project (A++ to F--)' \
'age:Analyze code age\: classify files as active, stale, or frozen by last git modification' \
'deps:Analyze internal module dependencies\: fan-in, fan-out, and dependency cycles' \
'authors:Summarize code ownership by author\: files owned, lines, languages, last active date' \
'ai:AI-powered code analysis and tooling' \
'completions:Generate shell completion scripts (zsh, bash, fish, ...)' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'km commands' commands "$@"
}
(( $+functions[_km__age_commands] )) ||
_km__age_commands() {
    local commands; commands=()
    _describe -t commands 'km age commands' commands "$@"
}
(( $+functions[_km__ai_commands] )) ||
_km__ai_commands() {
    local commands; commands=(
'analyze:Analyze repository using an AI provider' \
'skill:Install a Claude Code skill for km' \
'permissions:Configure Claude Code permissions for km commands' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'km ai commands' commands "$@"
}
(( $+functions[_km__ai__analyze_commands] )) ||
_km__ai__analyze_commands() {
    local commands; commands=()
    _describe -t commands 'km ai analyze commands' commands "$@"
}
(( $+functions[_km__ai__help_commands] )) ||
_km__ai__help_commands() {
    local commands; commands=(
'analyze:Analyze repository using an AI provider' \
'skill:Install a Claude Code skill for km' \
'permissions:Configure Claude Code permissions for km commands' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'km ai help commands' commands "$@"
}
(( $+functions[_km__ai__help__analyze_commands] )) ||
_km__ai__help__analyze_commands() {
    local commands; commands=()
    _describe -t commands 'km ai help analyze commands' commands "$@"
}
(( $+functions[_km__ai__help__help_commands] )) ||
_km__ai__help__help_commands() {
    local commands; commands=()
    _describe -t commands 'km ai help help commands' commands "$@"
}
(( $+functions[_km__ai__help__permissions_commands] )) ||
_km__ai__help__permissions_commands() {
    local commands; commands=()
    _describe -t commands 'km ai help permissions commands' commands "$@"
}
(( $+functions[_km__ai__help__skill_commands] )) ||
_km__ai__help__skill_commands() {
    local commands; commands=()
    _describe -t commands 'km ai help skill commands' commands "$@"
}
(( $+functions[_km__ai__permissions_commands] )) ||
_km__ai__permissions_commands() {
    local commands; commands=()
    _describe -t commands 'km ai permissions commands' commands "$@"
}
(( $+functions[_km__ai__skill_commands] )) ||
_km__ai__skill_commands() {
    local commands; commands=()
    _describe -t commands 'km ai skill commands' commands "$@"
}
(( $+functions[_km__authors_commands] )) ||
_km__authors_commands() {
    local commands; commands=()
    _describe -t commands 'km authors commands' commands "$@"
}
(( $+functions[_km__churn_commands] )) ||
_km__churn_commands() {
    local commands; commands=()
    _describe -t commands 'km churn commands' commands "$@"
}
(( $+functions[_km__cogcom_commands] )) ||
_km__cogcom_commands() {
    local commands; commands=()
    _describe -t commands 'km cogcom commands' commands "$@"
}
(( $+functions[_km__completions_commands] )) ||
_km__completions_commands() {
    local commands; commands=()
    _describe -t commands 'km completions commands' commands "$@"
}
(( $+functions[_km__cycom_commands] )) ||
_km__cycom_commands() {
    local commands; commands=()
    _describe -t commands 'km cycom commands' commands "$@"
}
(( $+functions[_km__deps_commands] )) ||
_km__deps_commands() {
    local commands; commands=()
    _describe -t commands 'km deps commands' commands "$@"
}
(( $+functions[_km__dups_commands] )) ||
_km__dups_commands() {
    local commands; commands=()
    _describe -t commands 'km dups commands' commands "$@"
}
(( $+functions[_km__hal_commands] )) ||
_km__hal_commands() {
    local commands; commands=()
    _describe -t commands 'km hal commands' commands "$@"
}
(( $+functions[_km__help_commands] )) ||
_km__help_commands() {
    local commands; commands=(
'loc:Count lines of code (blank, comment, code) by language' \
'dups:Detect duplicate code across files' \
'indent:Analyze indentation complexity (stddev and max depth per file)' \
'hal:Analyze Halstead complexity metrics per file' \
'cycom:Analyze cyclomatic complexity per file and per function' \
'cogcom:Analyze cognitive complexity per file and per function (SonarSource method)' \
'mi:Compute Maintainability Index per file (Visual Studio variant, 0-100 scale)' \
'report:Generate a comprehensive report combining all code metrics' \
'miv:Compute Maintainability Index per file (verifysoft variant, with comment weight)' \
'churn:Analyze code churn\: pure change frequency per file (git commits only)' \
'hotspots:Find hotspots\: files that change frequently and have high complexity' \
'knowledge:Analyze code ownership patterns via git blame (knowledge maps)' \
'tc:Analyze temporal coupling\: files that change together in commits' \
'smells:Detect common code smells per file' \
'score:Compute an overall code health score for the project (A++ to F--)' \
'age:Analyze code age\: classify files as active, stale, or frozen by last git modification' \
'deps:Analyze internal module dependencies\: fan-in, fan-out, and dependency cycles' \
'authors:Summarize code ownership by author\: files owned, lines, languages, last active date' \
'ai:AI-powered code analysis and tooling' \
'completions:Generate shell completion scripts (zsh, bash, fish, ...)' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'km help commands' commands "$@"
}
(( $+functions[_km__help__age_commands] )) ||
_km__help__age_commands() {
    local commands; commands=()
    _describe -t commands 'km help age commands' commands "$@"
}
(( $+functions[_km__help__ai_commands] )) ||
_km__help__ai_commands() {
    local commands; commands=(
'analyze:Analyze repository using an AI provider' \
'skill:Install a Claude Code skill for km' \
'permissions:Configure Claude Code permissions for km commands' \
    )
    _describe -t commands 'km help ai commands' commands "$@"
}
(( $+functions[_km__help__ai__analyze_commands] )) ||
_km__help__ai__analyze_commands() {
    local commands; commands=()
    _describe -t commands 'km help ai analyze commands' commands "$@"
}
(( $+functions[_km__help__ai__permissions_commands] )) ||
_km__help__ai__permissions_commands() {
    local commands; commands=()
    _describe -t commands 'km help ai permissions commands' commands "$@"
}
(( $+functions[_km__help__ai__skill_commands] )) ||
_km__help__ai__skill_commands() {
    local commands; commands=()
    _describe -t commands 'km help ai skill commands' commands "$@"
}
(( $+functions[_km__help__authors_commands] )) ||
_km__help__authors_commands() {
    local commands; commands=()
    _describe -t commands 'km help authors commands' commands "$@"
}
(( $+functions[_km__help__churn_commands] )) ||
_km__help__churn_commands() {
    local commands; commands=()
    _describe -t commands 'km help churn commands' commands "$@"
}
(( $+functions[_km__help__cogcom_commands] )) ||
_km__help__cogcom_commands() {
    local commands; commands=()
    _describe -t commands 'km help cogcom commands' commands "$@"
}
(( $+functions[_km__help__completions_commands] )) ||
_km__help__completions_commands() {
    local commands; commands=()
    _describe -t commands 'km help completions commands' commands "$@"
}
(( $+functions[_km__help__cycom_commands] )) ||
_km__help__cycom_commands() {
    local commands; commands=()
    _describe -t commands 'km help cycom commands' commands "$@"
}
(( $+functions[_km__help__deps_commands] )) ||
_km__help__deps_commands() {
    local commands; commands=()
    _describe -t commands 'km help deps commands' commands "$@"
}
(( $+functions[_km__help__dups_commands] )) ||
_km__help__dups_commands() {
    local commands; commands=()
    _describe -t commands 'km help dups commands' commands "$@"
}
(( $+functions[_km__help__hal_commands] )) ||
_km__help__hal_commands() {
    local commands; commands=()
    _describe -t commands 'km help hal commands' commands "$@"
}
(( $+functions[_km__help__help_commands] )) ||
_km__help__help_commands() {
    local commands; commands=()
    _describe -t commands 'km help help commands' commands "$@"
}
(( $+functions[_km__help__hotspots_commands] )) ||
_km__help__hotspots_commands() {
    local commands; commands=()
    _describe -t commands 'km help hotspots commands' commands "$@"
}
(( $+functions[_km__help__indent_commands] )) ||
_km__help__indent_commands() {
    local commands; commands=()
    _describe -t commands 'km help indent commands' commands "$@"
}
(( $+functions[_km__help__knowledge_commands] )) ||
_km__help__knowledge_commands() {
    local commands; commands=()
    _describe -t commands 'km help knowledge commands' commands "$@"
}
(( $+functions[_km__help__loc_commands] )) ||
_km__help__loc_commands() {
    local commands; commands=()
    _describe -t commands 'km help loc commands' commands "$@"
}
(( $+functions[_km__help__mi_commands] )) ||
_km__help__mi_commands() {
    local commands; commands=()
    _describe -t commands 'km help mi commands' commands "$@"
}
(( $+functions[_km__help__miv_commands] )) ||
_km__help__miv_commands() {
    local commands; commands=()
    _describe -t commands 'km help miv commands' commands "$@"
}
(( $+functions[_km__help__report_commands] )) ||
_km__help__report_commands() {
    local commands; commands=()
    _describe -t commands 'km help report commands' commands "$@"
}
(( $+functions[_km__help__score_commands] )) ||
_km__help__score_commands() {
    local commands; commands=(
'diff:Compare the current code health score against a git ref' \
    )
    _describe -t commands 'km help score commands' commands "$@"
}
(( $+functions[_km__help__score__diff_commands] )) ||
_km__help__score__diff_commands() {
    local commands; commands=()
    _describe -t commands 'km help score diff commands' commands "$@"
}
(( $+functions[_km__help__smells_commands] )) ||
_km__help__smells_commands() {
    local commands; commands=()
    _describe -t commands 'km help smells commands' commands "$@"
}
(( $+functions[_km__help__tc_commands] )) ||
_km__help__tc_commands() {
    local commands; commands=()
    _describe -t commands 'km help tc commands' commands "$@"
}
(( $+functions[_km__hotspots_commands] )) ||
_km__hotspots_commands() {
    local commands; commands=()
    _describe -t commands 'km hotspots commands' commands "$@"
}
(( $+functions[_km__indent_commands] )) ||
_km__indent_commands() {
    local commands; commands=()
    _describe -t commands 'km indent commands' commands "$@"
}
(( $+functions[_km__knowledge_commands] )) ||
_km__knowledge_commands() {
    local commands; commands=()
    _describe -t commands 'km knowledge commands' commands "$@"
}
(( $+functions[_km__loc_commands] )) ||
_km__loc_commands() {
    local commands; commands=()
    _describe -t commands 'km loc commands' commands "$@"
}
(( $+functions[_km__mi_commands] )) ||
_km__mi_commands() {
    local commands; commands=()
    _describe -t commands 'km mi commands' commands "$@"
}
(( $+functions[_km__miv_commands] )) ||
_km__miv_commands() {
    local commands; commands=()
    _describe -t commands 'km miv commands' commands "$@"
}
(( $+functions[_km__report_commands] )) ||
_km__report_commands() {
    local commands; commands=()
    _describe -t commands 'km report commands' commands "$@"
}
(( $+functions[_km__score_commands] )) ||
_km__score_commands() {
    local commands; commands=(
'diff:Compare the current code health score against a git ref' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'km score commands' commands "$@"
}
(( $+functions[_km__score__diff_commands] )) ||
_km__score__diff_commands() {
    local commands; commands=()
    _describe -t commands 'km score diff commands' commands "$@"
}
(( $+functions[_km__score__help_commands] )) ||
_km__score__help_commands() {
    local commands; commands=(
'diff:Compare the current code health score against a git ref' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'km score help commands' commands "$@"
}
(( $+functions[_km__score__help__diff_commands] )) ||
_km__score__help__diff_commands() {
    local commands; commands=()
    _describe -t commands 'km score help diff commands' commands "$@"
}
(( $+functions[_km__score__help__help_commands] )) ||
_km__score__help__help_commands() {
    local commands; commands=()
    _describe -t commands 'km score help help commands' commands "$@"
}
(( $+functions[_km__smells_commands] )) ||
_km__smells_commands() {
    local commands; commands=()
    _describe -t commands 'km smells commands' commands "$@"
}
(( $+functions[_km__tc_commands] )) ||
_km__tc_commands() {
    local commands; commands=()
    _describe -t commands 'km tc commands' commands "$@"
}

if [ "$funcstack[1]" = "_km" ]; then
    _km "$@"
else
    compdef _km km
fi
