#compdef xdeptree
# Documentation: https://man.voidlinux.org/xtools.1
# generate a tree of a package's dependencies (xtools)

local ret=1

_arguments -s -S \
  '-L[limit recursion depth]:depth:' \
  '-R[use remote repositories]' \
  '*:package:' && ret=0

return ret
