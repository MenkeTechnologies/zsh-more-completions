#compdef surfaceFeatureExtract
# Extracts and writes surface features to file

local ret=1

_arguments -s -S \
  '-help[print the usage]' \
  '-dict+[read control dictionary from specified location]:dict:_files' \
  '-case+[specify alternate case directory]:dir:_files -/' \
  '-fileHandler+[override the fileHandler]:handler:(uncollated collated masterUncollated)' \
  '-libs+[pre-load libraries]:libs:' && ret=0

return ret
