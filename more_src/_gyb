#compdef gyb
# Documentation: https://github.com/GAM-team/got-your-back/wiki
# Reference accessed: 2026-05-18
# Verified: upstream docs at the URL above.

# GAM-team/got-your-back (Gmail backup CLI)

local ret=1
_arguments -s -S   '--email=[mailbox]:email:'   '--action=[mode]:mode:(backup estimate restore purge)'   '--local-folder=[dir]:dir:_files -/'   '--service-account[use service account keys]'   '--fast-incremental[skip unchanged messages]'   '*:: :_default' && ret=0
return ret
