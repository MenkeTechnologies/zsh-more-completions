#compdef mcp-publisher

# https://github.com/modelcontextprotocol/registry — `publisher` Go CLI (cobra)

_arguments -s -S \
  '(-h --help)'{-h,--help}'[help]' \
  '--version[version]' \
  '*::arguments:_default'
