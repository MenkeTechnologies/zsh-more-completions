#compdef fibertools-rs
# Documentation: https://man.archlinux.org/search?q=fibertools-rs
# Reference accessed: 2026-05-16
# Source: cargo binstall fibertools-rs@0.8.2
# Probed via: completion zsh
# Repository: https://github.com/fiberseq/fibertools-rs

autoload -U is-at-least

_fibertools-rs() {
    typeset -A opt_args
    typeset -a _arguments_options
    local ret=1

    if is-at-least 5.2; then
        _arguments_options=(-s -S -C)
    else
        _arguments_options=(-s -C)
    fi

    local context curcontext="$curcontext" state line
    _arguments "${_arguments_options[@]}" : \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'-h[Print help]' \
'--help[Print help]' \
'-V[Print version]' \
'--version[Print version]' \
":: :_fibertools-rs_commands" \
"*::: :->fibertools-rs" \
&& ret=0
    case $state in
    (fibertools-rs)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:fibertools-rs-command-$line[1]:"
        case $line[1] in
            (predict-m6a)
_arguments "${_arguments_options[@]}" : \
'-F+[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'--filter=[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'-x+[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ftx=[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ml=[Minium score in the ML tag to use or include in the output]:MIN_ML_SCORE:_default' \
'--strip-starting-basemods=[strip basemods in the first or last X bp of the read]:STRIP_STARTING_BASEMODS:_default' \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'-n+[Minium nucleosome length]:NUCLEOSOME_LENGTH:_default' \
'--nucleosome-length=[Minium nucleosome length]:NUCLEOSOME_LENGTH:_default' \
'-c+[Minium nucleosome length when combining over a single m6A]:COMBINED_NUCLEOSOME_LENGTH:_default' \
'--combined-nucleosome-length=[Minium nucleosome length when combining over a single m6A]:COMBINED_NUCLEOSOME_LENGTH:_default' \
'--min-distance-added=[Minium distance needed to add to an already existing nuc by crossing an m6a]:MIN_DISTANCE_ADDED:_default' \
'-d+[Minimum distance from the end of a fiber to call a nucleosome or MSP]:DISTANCE_FROM_END:_default' \
'--distance-from-end=[Minimum distance from the end of a fiber to call a nucleosome or MSP]:DISTANCE_FROM_END:_default' \
'-a+[Most m6A events we can skip over to get to the nucleosome length when using D-segment algorithm. 2 is often a good value, negative values disable D-segment for the simple caller]:ALLOWED_M6A_SKIPS:_default' \
'--allowed-m6a-skips=[Most m6A events we can skip over to get to the nucleosome length when using D-segment algorithm. 2 is often a good value, negative values disable D-segment for the simple caller]:ALLOWED_M6A_SKIPS:_default' \
'--force-min-ml-score=[Force a different minimum ML score]:FORCE_MIN_ML_SCORE:_default' \
'-b+[Number of reads to include in batch prediction]:BATCH_SIZE:_default' \
'--batch-size=[Number of reads to include in batch prediction]:BATCH_SIZE:_default' \
'-u[Output uncompressed BAM files]' \
'--uncompressed[Output uncompressed BAM files]' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'-k[Keep hifi kinetics data]' \
'--keep[Keep hifi kinetics data]' \
'--all-calls[Keep all m6A calls regardless of how low the ML value is]' \
'--fake[Skip the actual prediction step to allow for testing the speed of other parts of the code]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'-V[Print version]' \
'--version[Print version]' \
'::bam -- Input BAM file. If no path is provided stdin is used. For m6A prediction, this should be a HiFi bam file with kinetics data. For other commands, this should be a bam file with m6A calls:_files' \
'::out -- Output bam file with m6A calls in new/extended MM and ML bam tags:_default' \
&& ret=0
;;
(m6A)
_arguments "${_arguments_options[@]}" : \
'-F+[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'--filter=[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'-x+[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ftx=[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ml=[Minium score in the ML tag to use or include in the output]:MIN_ML_SCORE:_default' \
'--strip-starting-basemods=[strip basemods in the first or last X bp of the read]:STRIP_STARTING_BASEMODS:_default' \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'-n+[Minium nucleosome length]:NUCLEOSOME_LENGTH:_default' \
'--nucleosome-length=[Minium nucleosome length]:NUCLEOSOME_LENGTH:_default' \
'-c+[Minium nucleosome length when combining over a single m6A]:COMBINED_NUCLEOSOME_LENGTH:_default' \
'--combined-nucleosome-length=[Minium nucleosome length when combining over a single m6A]:COMBINED_NUCLEOSOME_LENGTH:_default' \
'--min-distance-added=[Minium distance needed to add to an already existing nuc by crossing an m6a]:MIN_DISTANCE_ADDED:_default' \
'-d+[Minimum distance from the end of a fiber to call a nucleosome or MSP]:DISTANCE_FROM_END:_default' \
'--distance-from-end=[Minimum distance from the end of a fiber to call a nucleosome or MSP]:DISTANCE_FROM_END:_default' \
'-a+[Most m6A events we can skip over to get to the nucleosome length when using D-segment algorithm. 2 is often a good value, negative values disable D-segment for the simple caller]:ALLOWED_M6A_SKIPS:_default' \
'--allowed-m6a-skips=[Most m6A events we can skip over to get to the nucleosome length when using D-segment algorithm. 2 is often a good value, negative values disable D-segment for the simple caller]:ALLOWED_M6A_SKIPS:_default' \
'--force-min-ml-score=[Force a different minimum ML score]:FORCE_MIN_ML_SCORE:_default' \
'-b+[Number of reads to include in batch prediction]:BATCH_SIZE:_default' \
'--batch-size=[Number of reads to include in batch prediction]:BATCH_SIZE:_default' \
'-u[Output uncompressed BAM files]' \
'--uncompressed[Output uncompressed BAM files]' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'-k[Keep hifi kinetics data]' \
'--keep[Keep hifi kinetics data]' \
'--all-calls[Keep all m6A calls regardless of how low the ML value is]' \
'--fake[Skip the actual prediction step to allow for testing the speed of other parts of the code]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'-V[Print version]' \
'--version[Print version]' \
'::bam -- Input BAM file. If no path is provided stdin is used. For m6A prediction, this should be a HiFi bam file with kinetics data. For other commands, this should be a bam file with m6A calls:_files' \
'::out -- Output bam file with m6A calls in new/extended MM and ML bam tags:_default' \
&& ret=0
;;
(m6a)
_arguments "${_arguments_options[@]}" : \
'-F+[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'--filter=[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'-x+[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ftx=[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ml=[Minium score in the ML tag to use or include in the output]:MIN_ML_SCORE:_default' \
'--strip-starting-basemods=[strip basemods in the first or last X bp of the read]:STRIP_STARTING_BASEMODS:_default' \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'-n+[Minium nucleosome length]:NUCLEOSOME_LENGTH:_default' \
'--nucleosome-length=[Minium nucleosome length]:NUCLEOSOME_LENGTH:_default' \
'-c+[Minium nucleosome length when combining over a single m6A]:COMBINED_NUCLEOSOME_LENGTH:_default' \
'--combined-nucleosome-length=[Minium nucleosome length when combining over a single m6A]:COMBINED_NUCLEOSOME_LENGTH:_default' \
'--min-distance-added=[Minium distance needed to add to an already existing nuc by crossing an m6a]:MIN_DISTANCE_ADDED:_default' \
'-d+[Minimum distance from the end of a fiber to call a nucleosome or MSP]:DISTANCE_FROM_END:_default' \
'--distance-from-end=[Minimum distance from the end of a fiber to call a nucleosome or MSP]:DISTANCE_FROM_END:_default' \
'-a+[Most m6A events we can skip over to get to the nucleosome length when using D-segment algorithm. 2 is often a good value, negative values disable D-segment for the simple caller]:ALLOWED_M6A_SKIPS:_default' \
'--allowed-m6a-skips=[Most m6A events we can skip over to get to the nucleosome length when using D-segment algorithm. 2 is often a good value, negative values disable D-segment for the simple caller]:ALLOWED_M6A_SKIPS:_default' \
'--force-min-ml-score=[Force a different minimum ML score]:FORCE_MIN_ML_SCORE:_default' \
'-b+[Number of reads to include in batch prediction]:BATCH_SIZE:_default' \
'--batch-size=[Number of reads to include in batch prediction]:BATCH_SIZE:_default' \
'-u[Output uncompressed BAM files]' \
'--uncompressed[Output uncompressed BAM files]' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'-k[Keep hifi kinetics data]' \
'--keep[Keep hifi kinetics data]' \
'--all-calls[Keep all m6A calls regardless of how low the ML value is]' \
'--fake[Skip the actual prediction step to allow for testing the speed of other parts of the code]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'-V[Print version]' \
'--version[Print version]' \
'::bam -- Input BAM file. If no path is provided stdin is used. For m6A prediction, this should be a HiFi bam file with kinetics data. For other commands, this should be a bam file with m6A calls:_files' \
'::out -- Output bam file with m6A calls in new/extended MM and ML bam tags:_default' \
&& ret=0
;;
(add-nucleosomes)
_arguments "${_arguments_options[@]}" : \
'-F+[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'--filter=[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'-x+[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ftx=[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ml=[Minium score in the ML tag to use or include in the output]:MIN_ML_SCORE:_default' \
'--strip-starting-basemods=[strip basemods in the first or last X bp of the read]:STRIP_STARTING_BASEMODS:_default' \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'-n+[Minium nucleosome length]:NUCLEOSOME_LENGTH:_default' \
'--nucleosome-length=[Minium nucleosome length]:NUCLEOSOME_LENGTH:_default' \
'-c+[Minium nucleosome length when combining over a single m6A]:COMBINED_NUCLEOSOME_LENGTH:_default' \
'--combined-nucleosome-length=[Minium nucleosome length when combining over a single m6A]:COMBINED_NUCLEOSOME_LENGTH:_default' \
'--min-distance-added=[Minium distance needed to add to an already existing nuc by crossing an m6a]:MIN_DISTANCE_ADDED:_default' \
'-d+[Minimum distance from the end of a fiber to call a nucleosome or MSP]:DISTANCE_FROM_END:_default' \
'--distance-from-end=[Minimum distance from the end of a fiber to call a nucleosome or MSP]:DISTANCE_FROM_END:_default' \
'-a+[Most m6A events we can skip over to get to the nucleosome length when using D-segment algorithm. 2 is often a good value, negative values disable D-segment for the simple caller]:ALLOWED_M6A_SKIPS:_default' \
'--allowed-m6a-skips=[Most m6A events we can skip over to get to the nucleosome length when using D-segment algorithm. 2 is often a good value, negative values disable D-segment for the simple caller]:ALLOWED_M6A_SKIPS:_default' \
'-u[Output uncompressed BAM files]' \
'--uncompressed[Output uncompressed BAM files]' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'-h[Print help]' \
'--help[Print help]' \
'-V[Print version]' \
'--version[Print version]' \
'::bam -- Input BAM file. If no path is provided stdin is used. For m6A prediction, this should be a HiFi bam file with kinetics data. For other commands, this should be a bam file with m6A calls:_files' \
'::out -- Output bam file with nucleosome calls:_default' \
&& ret=0
;;
(fire)
_arguments "${_arguments_options[@]}" : \
'-F+[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'--filter=[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'-x+[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ftx=[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ml=[Minium score in the ML tag to use or include in the output]:MIN_ML_SCORE:_default' \
'--strip-starting-basemods=[strip basemods in the first or last X bp of the read]:STRIP_STARTING_BASEMODS:_default' \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'--min-msp=[Skip reads without at least \`N\` MSP calls]:MIN_MSP:_default' \
'--min-ave-msp-size=[Skip reads without an average MSP size greater than \`N\`]:MIN_AVE_MSP_SIZE:_default' \
'-w+[Width of bin for feature collection]:WIDTH_BIN:_default' \
'--width-bin=[Width of bin for feature collection]:WIDTH_BIN:_default' \
'-b+[Number of bins to collect]:BIN_NUM:_default' \
'--bin-num=[Number of bins to collect]:BIN_NUM:_default' \
'--best-window-size=[Calculate stats for the highest X bp window within each MSP Should be a fair amount higher than the expected linker length]:BEST_WINDOW_SIZE:_default' \
'--min-msp-length-for-positive-fire-call=[Minium length of msp to call a FIRE]:MIN_MSP_LENGTH_FOR_POSITIVE_FIRE_CALL:_default' \
'--model=[Optional path to a model json file. If not provided ft will use the default model (recommended)]:MODEL:_default' \
'--fdr-table=[Optional path to a FDR table]:FDR_TABLE:_default' \
'-u[Output uncompressed BAM files]' \
'--uncompressed[Output uncompressed BAM files]' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'--ont[Use a ONT heuristic adjustment for FIRE calling. This adjusts the observed number of m6A counts by adding pseudo counts to account for the single stranded nature of ONT data]' \
'--human[Use a human model for FIRE calling]' \
'--yeast[Use a yeast model for FIRE calling]' \
'-e[Output just FIRE elements in bed9 format]' \
'--extract[Output just FIRE elements in bed9 format]' \
'--all[When extracting bed9 format include all MSPs and nucleosomes]' \
'-f[Output FIREs features for training in a table format]' \
'--feats-to-text[Output FIREs features for training in a table format]' \
'-s[Don'\''t write reads with no m6A calls to the output bam]' \
'--skip-no-m6a[Don'\''t write reads with no m6A calls to the output bam]' \
'-u[Use 5mC data in FIREs]' \
'--use-5mc[Use 5mC data in FIREs]' \
'-h[Print help]' \
'--help[Print help]' \
'-V[Print version]' \
'--version[Print version]' \
'::bam -- Input BAM file. If no path is provided stdin is used. For m6A prediction, this should be a HiFi bam file with kinetics data. For other commands, this should be a bam file with m6A calls:_files' \
'::out -- Output file (BAM by default, table of MSP features if `--feats-to-text` is used, and bed9 + if `--extract`` is used):_default' \
&& ret=0
;;
(extract)
_arguments "${_arguments_options[@]}" : \
'-F+[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'--filter=[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'-x+[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ftx=[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ml=[Minium score in the ML tag to use or include in the output]:MIN_ML_SCORE:_default' \
'--strip-starting-basemods=[strip basemods in the first or last X bp of the read]:STRIP_STARTING_BASEMODS:_default' \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'--m6a=[Output path for m6a bed12]:M6A:_default' \
'-c+[Output path for 5mC (CpG, primrose) bed12]:CPG:_default' \
'--cpg=[Output path for 5mC (CpG, primrose) bed12]:CPG:_default' \
'--msp=[Output path for methylation sensitive patch (msp) bed12]:MSP:_default' \
'-n+[Output path for nucleosome bed12]:NUC:_default' \
'--nuc=[Output path for nucleosome bed12]:NUC:_default' \
'-a+[Output path for a tabular format including "all" fiberseq information in the bam]:ALL:_default' \
'--all=[Output path for a tabular format including "all" fiberseq information in the bam]:ALL:_default' \
'-u[Output uncompressed BAM files]' \
'--uncompressed[Output uncompressed BAM files]' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'-r[Report positions in reference sequence coordinates]' \
'--reference[Report positions in reference sequence coordinates]' \
'--molecular[Report positions in the molecular sequence coordinates]' \
'-q[Include per base quality scores in "fiber_qual"]' \
'--quality[Include per base quality scores in "fiber_qual"]' \
'-s[Simplify output by removing fiber sequence]' \
'--simplify[Simplify output by removing fiber sequence]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'-V[Print version]' \
'--version[Print version]' \
'::bam -- Input BAM file. If no path is provided stdin is used. For m6A prediction, this should be a HiFi bam file with kinetics data. For other commands, this should be a bam file with m6A calls:_files' \
&& ret=0
;;
(ex)
_arguments "${_arguments_options[@]}" : \
'-F+[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'--filter=[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'-x+[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ftx=[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ml=[Minium score in the ML tag to use or include in the output]:MIN_ML_SCORE:_default' \
'--strip-starting-basemods=[strip basemods in the first or last X bp of the read]:STRIP_STARTING_BASEMODS:_default' \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'--m6a=[Output path for m6a bed12]:M6A:_default' \
'-c+[Output path for 5mC (CpG, primrose) bed12]:CPG:_default' \
'--cpg=[Output path for 5mC (CpG, primrose) bed12]:CPG:_default' \
'--msp=[Output path for methylation sensitive patch (msp) bed12]:MSP:_default' \
'-n+[Output path for nucleosome bed12]:NUC:_default' \
'--nuc=[Output path for nucleosome bed12]:NUC:_default' \
'-a+[Output path for a tabular format including "all" fiberseq information in the bam]:ALL:_default' \
'--all=[Output path for a tabular format including "all" fiberseq information in the bam]:ALL:_default' \
'-u[Output uncompressed BAM files]' \
'--uncompressed[Output uncompressed BAM files]' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'-r[Report positions in reference sequence coordinates]' \
'--reference[Report positions in reference sequence coordinates]' \
'--molecular[Report positions in the molecular sequence coordinates]' \
'-q[Include per base quality scores in "fiber_qual"]' \
'--quality[Include per base quality scores in "fiber_qual"]' \
'-s[Simplify output by removing fiber sequence]' \
'--simplify[Simplify output by removing fiber sequence]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'-V[Print version]' \
'--version[Print version]' \
'::bam -- Input BAM file. If no path is provided stdin is used. For m6A prediction, this should be a HiFi bam file with kinetics data. For other commands, this should be a bam file with m6A calls:_files' \
&& ret=0
;;
(e)
_arguments "${_arguments_options[@]}" : \
'-F+[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'--filter=[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'-x+[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ftx=[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ml=[Minium score in the ML tag to use or include in the output]:MIN_ML_SCORE:_default' \
'--strip-starting-basemods=[strip basemods in the first or last X bp of the read]:STRIP_STARTING_BASEMODS:_default' \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'--m6a=[Output path for m6a bed12]:M6A:_default' \
'-c+[Output path for 5mC (CpG, primrose) bed12]:CPG:_default' \
'--cpg=[Output path for 5mC (CpG, primrose) bed12]:CPG:_default' \
'--msp=[Output path for methylation sensitive patch (msp) bed12]:MSP:_default' \
'-n+[Output path for nucleosome bed12]:NUC:_default' \
'--nuc=[Output path for nucleosome bed12]:NUC:_default' \
'-a+[Output path for a tabular format including "all" fiberseq information in the bam]:ALL:_default' \
'--all=[Output path for a tabular format including "all" fiberseq information in the bam]:ALL:_default' \
'-u[Output uncompressed BAM files]' \
'--uncompressed[Output uncompressed BAM files]' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'-r[Report positions in reference sequence coordinates]' \
'--reference[Report positions in reference sequence coordinates]' \
'--molecular[Report positions in the molecular sequence coordinates]' \
'-q[Include per base quality scores in "fiber_qual"]' \
'--quality[Include per base quality scores in "fiber_qual"]' \
'-s[Simplify output by removing fiber sequence]' \
'--simplify[Simplify output by removing fiber sequence]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'-V[Print version]' \
'--version[Print version]' \
'::bam -- Input BAM file. If no path is provided stdin is used. For m6A prediction, this should be a HiFi bam file with kinetics data. For other commands, this should be a bam file with m6A calls:_files' \
&& ret=0
;;
(center)
_arguments "${_arguments_options[@]}" : \
'-F+[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'--filter=[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'-x+[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ftx=[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ml=[Minium score in the ML tag to use or include in the output]:MIN_ML_SCORE:_default' \
'--strip-starting-basemods=[strip basemods in the first or last X bp of the read]:STRIP_STARTING_BASEMODS:_default' \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'-b+[Bed file on which to center fiberseq reads. Data is adjusted to the start position of the bed file and corrected for strand if the strand is indicated in the 6th column of the bed file. The 4th column will also be checked for the strand but only after the 6th is. If you include strand information in the 4th (or 6th) column it will orient data accordingly and use the end position of bed record instead of the start if on the minus strand. This means that profiles of motifs in both the forward and minus orientation will align to the same central position]:BED:_default' \
'--bed=[Bed file on which to center fiberseq reads. Data is adjusted to the start position of the bed file and corrected for strand if the strand is indicated in the 6th column of the bed file. The 4th column will also be checked for the strand but only after the 6th is. If you include strand information in the 4th (or 6th) column it will orient data accordingly and use the end position of bed record instead of the start if on the minus strand. This means that profiles of motifs in both the forward and minus orientation will align to the same central position]:BED:_default' \
'-d+[Set a maximum distance from the start of the motif to keep a feature]:DIST:_default' \
'--dist=[Set a maximum distance from the start of the motif to keep a feature]:DIST:_default' \
'-u[Output uncompressed BAM files]' \
'--uncompressed[Output uncompressed BAM files]' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'-w[Provide data in wide format, one row per read]' \
'--wide[Provide data in wide format, one row per read]' \
'-r[Return relative reference position instead of relative molecular position]' \
'--reference[Return relative reference position instead of relative molecular position]' \
'-s[Replace the sequence output column with just "N"]' \
'--simplify[Replace the sequence output column with just "N"]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'-V[Print version]' \
'--version[Print version]' \
'::bam -- Input BAM file. If no path is provided stdin is used. For m6A prediction, this should be a HiFi bam file with kinetics data. For other commands, this should be a bam file with m6A calls:_files' \
&& ret=0
;;
(c)
_arguments "${_arguments_options[@]}" : \
'-F+[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'--filter=[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'-x+[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ftx=[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ml=[Minium score in the ML tag to use or include in the output]:MIN_ML_SCORE:_default' \
'--strip-starting-basemods=[strip basemods in the first or last X bp of the read]:STRIP_STARTING_BASEMODS:_default' \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'-b+[Bed file on which to center fiberseq reads. Data is adjusted to the start position of the bed file and corrected for strand if the strand is indicated in the 6th column of the bed file. The 4th column will also be checked for the strand but only after the 6th is. If you include strand information in the 4th (or 6th) column it will orient data accordingly and use the end position of bed record instead of the start if on the minus strand. This means that profiles of motifs in both the forward and minus orientation will align to the same central position]:BED:_default' \
'--bed=[Bed file on which to center fiberseq reads. Data is adjusted to the start position of the bed file and corrected for strand if the strand is indicated in the 6th column of the bed file. The 4th column will also be checked for the strand but only after the 6th is. If you include strand information in the 4th (or 6th) column it will orient data accordingly and use the end position of bed record instead of the start if on the minus strand. This means that profiles of motifs in both the forward and minus orientation will align to the same central position]:BED:_default' \
'-d+[Set a maximum distance from the start of the motif to keep a feature]:DIST:_default' \
'--dist=[Set a maximum distance from the start of the motif to keep a feature]:DIST:_default' \
'-u[Output uncompressed BAM files]' \
'--uncompressed[Output uncompressed BAM files]' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'-w[Provide data in wide format, one row per read]' \
'--wide[Provide data in wide format, one row per read]' \
'-r[Return relative reference position instead of relative molecular position]' \
'--reference[Return relative reference position instead of relative molecular position]' \
'-s[Replace the sequence output column with just "N"]' \
'--simplify[Replace the sequence output column with just "N"]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'-V[Print version]' \
'--version[Print version]' \
'::bam -- Input BAM file. If no path is provided stdin is used. For m6A prediction, this should be a HiFi bam file with kinetics data. For other commands, this should be a bam file with m6A calls:_files' \
&& ret=0
;;
(ct)
_arguments "${_arguments_options[@]}" : \
'-F+[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'--filter=[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'-x+[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ftx=[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ml=[Minium score in the ML tag to use or include in the output]:MIN_ML_SCORE:_default' \
'--strip-starting-basemods=[strip basemods in the first or last X bp of the read]:STRIP_STARTING_BASEMODS:_default' \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'-b+[Bed file on which to center fiberseq reads. Data is adjusted to the start position of the bed file and corrected for strand if the strand is indicated in the 6th column of the bed file. The 4th column will also be checked for the strand but only after the 6th is. If you include strand information in the 4th (or 6th) column it will orient data accordingly and use the end position of bed record instead of the start if on the minus strand. This means that profiles of motifs in both the forward and minus orientation will align to the same central position]:BED:_default' \
'--bed=[Bed file on which to center fiberseq reads. Data is adjusted to the start position of the bed file and corrected for strand if the strand is indicated in the 6th column of the bed file. The 4th column will also be checked for the strand but only after the 6th is. If you include strand information in the 4th (or 6th) column it will orient data accordingly and use the end position of bed record instead of the start if on the minus strand. This means that profiles of motifs in both the forward and minus orientation will align to the same central position]:BED:_default' \
'-d+[Set a maximum distance from the start of the motif to keep a feature]:DIST:_default' \
'--dist=[Set a maximum distance from the start of the motif to keep a feature]:DIST:_default' \
'-u[Output uncompressed BAM files]' \
'--uncompressed[Output uncompressed BAM files]' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'-w[Provide data in wide format, one row per read]' \
'--wide[Provide data in wide format, one row per read]' \
'-r[Return relative reference position instead of relative molecular position]' \
'--reference[Return relative reference position instead of relative molecular position]' \
'-s[Replace the sequence output column with just "N"]' \
'--simplify[Replace the sequence output column with just "N"]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'-V[Print version]' \
'--version[Print version]' \
'::bam -- Input BAM file. If no path is provided stdin is used. For m6A prediction, this should be a HiFi bam file with kinetics data. For other commands, this should be a bam file with m6A calls:_files' \
&& ret=0
;;
(footprint)
_arguments "${_arguments_options[@]}" : \
'-F+[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'--filter=[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'-x+[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ftx=[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ml=[Minium score in the ML tag to use or include in the output]:MIN_ML_SCORE:_default' \
'--strip-starting-basemods=[strip basemods in the first or last X bp of the read]:STRIP_STARTING_BASEMODS:_default' \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'-b+[BED file with the regions to footprint. Should all contain the same motif with proper strand information, and ideally be ChIP-seq peaks]:BED:_default' \
'--bed=[BED file with the regions to footprint. Should all contain the same motif with proper strand information, and ideally be ChIP-seq peaks]:BED:_default' \
'-y+[yaml describing the modules of the footprint]:YAML:_default' \
'--yaml=[yaml describing the modules of the footprint]:YAML:_default' \
'-o+[Output bam]:OUT:_default' \
'--out=[Output bam]:OUT:_default' \
'-u[Output uncompressed BAM files]' \
'--uncompressed[Output uncompressed BAM files]' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'-h[Print help]' \
'--help[Print help]' \
'-V[Print version]' \
'--version[Print version]' \
'::bam -- Input BAM file. If no path is provided stdin is used. For m6A prediction, this should be a HiFi bam file with kinetics data. For other commands, this should be a bam file with m6A calls:_files' \
&& ret=0
;;
(qc)
_arguments "${_arguments_options[@]}" : \
'-F+[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'--filter=[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'-x+[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ftx=[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ml=[Minium score in the ML tag to use or include in the output]:MIN_ML_SCORE:_default' \
'--strip-starting-basemods=[strip basemods in the first or last X bp of the read]:STRIP_STARTING_BASEMODS:_default' \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'--acf-max-lag=[maximum lag for the ACF calculation]:ACF_MAX_LAG:_default' \
'--acf-min-m6a=[Minimum number of m6A marks to use a read in the ACF calculation]:ACF_MIN_M6A:_default' \
'--acf-max-reads=[maximum number of reads to use in the ACF calculation]:ACF_MAX_READS:_default' \
'--acf-sample-rate=[After sampling the first "acf-max-reads" randomly sample one of every "acf-sample-rate" reads and replace one of the previous reads at random]:ACF_SAMPLE_RATE:_default' \
'--n-reads=[Only process the first "n" reads in the input bam file]:N_READS:_default' \
'-u[Output uncompressed BAM files]' \
'--uncompressed[Output uncompressed BAM files]' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'--acf[Calculate the auto-correlation function of the m6A marks in the fiber-seq data]' \
'-m[In the output include a measure of the number of m6A events per MSPs of a given size. The output format is\: "m6a_per_msp_size\\t{m6A count},{MSP size},{is a FIRE}\\t{count}" e.g. "m6a_per_msp_size\\t35,100,false\\t100"]' \
'--m6a-per-msp[In the output include a measure of the number of m6A events per MSPs of a given size. The output format is\: "m6a_per_msp_size\\t{m6A count},{MSP size},{is a FIRE}\\t{count}" e.g. "m6a_per_msp_size\\t35,100,false\\t100"]' \
'-h[Print help]' \
'--help[Print help]' \
'-V[Print version]' \
'--version[Print version]' \
'::bam -- Input BAM file. If no path is provided stdin is used. For m6A prediction, this should be a HiFi bam file with kinetics data. For other commands, this should be a bam file with m6A calls:_files' \
'::out -- Output text file with QC metrics. The format is a tab-separated file with the following columns\: "statistic\tvalue\tcount" where "statistic" is the name of the metric, "value" is the value of the metric, and "count" is the number of times the metric was observed:_default' \
&& ret=0
;;
(track-decorators)
_arguments "${_arguments_options[@]}" : \
'-F+[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'--filter=[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'-x+[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ftx=[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ml=[Minium score in the ML tag to use or include in the output]:MIN_ML_SCORE:_default' \
'--strip-starting-basemods=[strip basemods in the first or last X bp of the read]:STRIP_STARTING_BASEMODS:_default' \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'-b+[Output path for bed12 file to be decorated]:BED12:_default' \
'--bed12=[Output path for bed12 file to be decorated]:BED12:_default' \
'-d+[Output path for decorator bed file]:DECORATOR:_default' \
'--decorator=[Output path for decorator bed file]:DECORATOR:_default' \
'-u[Output uncompressed BAM files]' \
'--uncompressed[Output uncompressed BAM files]' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'-h[Print help]' \
'--help[Print help]' \
'-V[Print version]' \
'--version[Print version]' \
'::bam -- Input BAM file. If no path is provided stdin is used. For m6A prediction, this should be a HiFi bam file with kinetics data. For other commands, this should be a bam file with m6A calls:_files' \
&& ret=0
;;
(pileup)
_arguments "${_arguments_options[@]}" : \
'-F+[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'--filter=[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'-x+[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ftx=[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ml=[Minium score in the ML tag to use or include in the output]:MIN_ML_SCORE:_default' \
'--strip-starting-basemods=[strip basemods in the first or last X bp of the read]:STRIP_STARTING_BASEMODS:_default' \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'-o+[Output file]:OUT:_default' \
'--out=[Output file]:OUT:_default' \
'--shuffle=[Shuffle the fiber-seq data according to a bed file of the shuffled positions of the fiber-seq data]:SHUFFLE:_default' \
'--rolling-max=[Output a rolling max of the score column over X bases]:ROLLING_MAX:_default' \
'-u[Output uncompressed BAM files]' \
'--uncompressed[Output uncompressed BAM files]' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'-m[include m6A calls]' \
'--m6a[include m6A calls]' \
'-c[include 5mC calls]' \
'--cpg[include 5mC calls]' \
'--haps[For each column add two new columns with the hap1 and hap2 specific data]' \
'-k[Keep zero coverage regions]' \
'--keep-zeros[Keep zero coverage regions]' \
'-p[Write output one base at a time even if the values do not change]' \
'--per-base[Write output one base at a time even if the values do not change]' \
'--fiber-coverage[Calculate coverage starting from the first MSP/NUC to the last MSP/NUC position instead of the complete span of the read alignment]' \
'--no-msp[No MSP columns]' \
'--no-nuc[No NUC columns]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'-V[Print version]' \
'--version[Print version]' \
'::bam -- Input BAM file. If no path is provided stdin is used. For m6A prediction, this should be a HiFi bam file with kinetics data. For other commands, this should be a bam file with m6A calls:_files' \
'::rgn -- Region string to make a pileup of. e.g. chr1\:1-1000 or chr1\:1-1,000 If not provided will make a pileup of the whole genome:_default' \
&& ret=0
;;
(clear-kinetics)
_arguments "${_arguments_options[@]}" : \
'-F+[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'--filter=[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'-x+[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ftx=[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ml=[Minium score in the ML tag to use or include in the output]:MIN_ML_SCORE:_default' \
'--strip-starting-basemods=[strip basemods in the first or last X bp of the read]:STRIP_STARTING_BASEMODS:_default' \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'-u[Output uncompressed BAM files]' \
'--uncompressed[Output uncompressed BAM files]' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'-h[Print help]' \
'--help[Print help]' \
'-V[Print version]' \
'--version[Print version]' \
'::bam -- Input BAM file. If no path is provided stdin is used. For m6A prediction, this should be a HiFi bam file with kinetics data. For other commands, this should be a bam file with m6A calls:_files' \
'::out -- Output bam file without hifi kinetics:_default' \
&& ret=0
;;
(strip-basemods)
_arguments "${_arguments_options[@]}" : \
'-F+[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'--filter=[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'-x+[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ftx=[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ml=[Minium score in the ML tag to use or include in the output]:MIN_ML_SCORE:_default' \
'--strip-starting-basemods=[strip basemods in the first or last X bp of the read]:STRIP_STARTING_BASEMODS:_default' \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'-b+[base modification to strip out of the bam file]:BASEMOD:(m6A 6mA 5mC CpG)' \
'--basemod=[base modification to strip out of the bam file]:BASEMOD:(m6A 6mA 5mC CpG)' \
'--ml-m6a=[filter out m6A modifications with less than this ML value]:ML_M6A:_default' \
'--ml-5mc=[filter out 5mC modifications with less than this ML value]:ML_5MC:_default' \
'-u[Output uncompressed BAM files]' \
'--uncompressed[Output uncompressed BAM files]' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'--drop-forward[Drop forward strand of base modifications]' \
'--drop-reverse[Drop reverse strand of base modifications]' \
'-h[Print help]' \
'--help[Print help]' \
'-V[Print version]' \
'--version[Print version]' \
'::bam -- Input BAM file. If no path is provided stdin is used. For m6A prediction, this should be a HiFi bam file with kinetics data. For other commands, this should be a bam file with m6A calls:_files' \
'::out -- Output bam file:_default' \
&& ret=0
;;
(ddda-to-m6a)
_arguments "${_arguments_options[@]}" : \
'-F+[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'--filter=[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'-x+[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ftx=[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ml=[Minium score in the ML tag to use or include in the output]:MIN_ML_SCORE:_default' \
'--strip-starting-basemods=[strip basemods in the first or last X bp of the read]:STRIP_STARTING_BASEMODS:_default' \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'-u[Output uncompressed BAM files]' \
'--uncompressed[Output uncompressed BAM files]' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'-h[Print help]' \
'--help[Print help]' \
'-V[Print version]' \
'--version[Print version]' \
'::bam -- Input BAM file. If no path is provided stdin is used. For m6A prediction, this should be a HiFi bam file with kinetics data. For other commands, this should be a bam file with m6A calls:_files' \
'::out -- Output bam file:_default' \
&& ret=0
;;
(fiber-hmm)
_arguments "${_arguments_options[@]}" : \
'-F+[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'--filter=[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'-x+[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ftx=[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ml=[Minium score in the ML tag to use or include in the output]:MIN_ML_SCORE:_default' \
'--strip-starting-basemods=[strip basemods in the first or last X bp of the read]:STRIP_STARTING_BASEMODS:_default' \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'-n+[Minium nucleosome length]:NUCLEOSOME_LENGTH:_default' \
'--nucleosome-length=[Minium nucleosome length]:NUCLEOSOME_LENGTH:_default' \
'-u[Output uncompressed BAM files]' \
'--uncompressed[Output uncompressed BAM files]' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'-h[Print help]' \
'--help[Print help]' \
'-V[Print version]' \
'--version[Print version]' \
'::bam -- Input BAM file. If no path is provided stdin is used. For m6A prediction, this should be a HiFi bam file with kinetics data. For other commands, this should be a bam file with m6A calls:_files' \
'::out -- Output bam file with nucleosome calls:_default' \
&& ret=0
;;
(validate)
_arguments "${_arguments_options[@]}" : \
'-F+[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'--filter=[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'-x+[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ftx=[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ml=[Minium score in the ML tag to use or include in the output]:MIN_ML_SCORE:_default' \
'--strip-starting-basemods=[strip basemods in the first or last X bp of the read]:STRIP_STARTING_BASEMODS:_default' \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'-r+[Number of reads to validate]:READS:_default' \
'--reads=[Number of reads to validate]:READS:_default' \
'-m+[The fraction of reads that must have m6A calls to pass validation]:M6A:_default' \
'--m6a=[The fraction of reads that must have m6A calls to pass validation]:M6A:_default' \
'-n+[The fraction of reads that must have nucleosome and MSP calls to pass validation]:NUC:_default' \
'--nuc=[The fraction of reads that must have nucleosome and MSP calls to pass validation]:NUC:_default' \
'-a+[Check for the fraction of reads with alignment to a reference genome]:ALIGNED:_default' \
'--aligned=[Check for the fraction of reads with alignment to a reference genome]:ALIGNED:_default' \
'-p+[Check for the fraction of reads with phasing information]:PHASED:_default' \
'--phased=[Check for the fraction of reads with phasing information]:PHASED:_default' \
'-k+[Check for the fraction of reads with kinetics information]:KINETICS:_default' \
'--kinetics=[Check for the fraction of reads with kinetics information]:KINETICS:_default' \
'-u[Output uncompressed BAM files]' \
'--uncompressed[Output uncompressed BAM files]' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'-f[Check for FIRE calls in the reads, there must be at least one FIRE call to pass validation]' \
'--fire[Check for FIRE calls in the reads, there must be at least one FIRE call to pass validation]' \
'-h[Print help]' \
'--help[Print help]' \
'-V[Print version]' \
'--version[Print version]' \
'::bam -- Input BAM file. If no path is provided stdin is used. For m6A prediction, this should be a HiFi bam file with kinetics data. For other commands, this should be a bam file with m6A calls:_files' \
&& ret=0
;;
(pg-inject)
_arguments "${_arguments_options[@]}" : \
'-o+[Output BAM file]:OUT:_default' \
'--out=[Output BAM file]:OUT:_default' \
'-s+[Split contigs into multiple BAM records every N base pairs (0 = no splitting)]:SPLIT_SIZE:_default' \
'--split-size=[Split contigs into multiple BAM records every N base pairs (0 = no splitting)]:SPLIT_SIZE:_default' \
'-b+[Optional BED file with annotations to add to mock BAM records]:BED:_default' \
'--bed=[Optional BED file with annotations to add to mock BAM records]:BED:_default' \
'--header-out=[Additionally write the output BAM header to this file]:HEADER_OUT:_default' \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'(--strip)-p+[panSN-spec prefix to add to contig names (e.g., "HG002#1#") If provided, adds the prefix. Mutually exclusive with --strip]:PREFIX:_default' \
'(--strip)--prefix=[panSN-spec prefix to add to contig names (e.g., "HG002#1#") If provided, adds the prefix. Mutually exclusive with --strip]:PREFIX:_default' \
'-d+[Delimiter character to use when stripping panSN-spec (default\: '\''#'\'')]:DELIMITER:_default' \
'--delimiter=[Delimiter character to use when stripping panSN-spec (default\: '\''#'\'')]:DELIMITER:_default' \
'-1+[Tag to identify haplotype 1 contigs (e.g., "haplotype1") If both hap1-tag and hap2-tag are provided, reads will be tagged with HP based on contig names]:HAP1_TAG:_default' \
'--hap1-tag=[Tag to identify haplotype 1 contigs (e.g., "haplotype1") If both hap1-tag and hap2-tag are provided, reads will be tagged with HP based on contig names]:HAP1_TAG:_default' \
'-2+[Tag to identify haplotype 2 contigs (e.g., "haplotype2") If both hap1-tag and hap2-tag are provided, reads will be tagged with HP based on contig names]:HAP2_TAG:_default' \
'--hap2-tag=[Tag to identify haplotype 2 contigs (e.g., "haplotype2") If both hap1-tag and hap2-tag are provided, reads will be tagged with HP based on contig names]:HAP2_TAG:_default' \
'-q+[The mapping quality must be greater than or equal to this value for haplotag assignment]:MIN_MAPQ:_default' \
'--min-mapq=[The mapping quality must be greater than or equal to this value for haplotag assignment]:MIN_MAPQ:_default' \
'-c+[BAM file to copy header from (excluding SQ and HD tags)]:COPY_HEADER:_default' \
'--copy-header=[BAM file to copy header from (excluding SQ and HD tags)]:COPY_HEADER:_default' \
'-u[Uncompressed BAM output (default\: compressed)]' \
'--uncompressed[Uncompressed BAM output (default\: compressed)]' \
'-e[Extract BED annotations from an annotated BAM file (reverses injection)]' \
'--extract[Extract BED annotations from an annotated BAM file (reverses injection)]' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'(-p --prefix)--strip[Strip panSN-spec information from contig names Mutually exclusive with --prefix]' \
'-h[Print help]' \
'--help[Print help]' \
'-V[Print version]' \
'--version[Print version]' \
':reference -- Reference FASTA file to create mock BAM from (supports .gz/.bgz compression):_default' \
&& ret=0
;;
(pg-lift)
_arguments "${_arguments_options[@]}" : \
'-i+[Input file with annotations to lift (defaults to BED format)]:INPUT:_default' \
'--input=[Input file with annotations to lift (defaults to BED format)]:INPUT:_default' \
'-g+[Pangenome graph file in GBZ format]:GRAPH:_default' \
'--graph=[Pangenome graph file in GBZ format]:GRAPH:_default' \
'-o+[Output BED file with lifted annotations]:OUT:_default' \
'--out=[Output BED file with lifted annotations]:OUT:_default' \
'-p+[panSN-spec prefix to add before graph injection (e.g., "HG002_2#0#")]:PREFIX:_default' \
'--prefix=[panSN-spec prefix to add before graph injection (e.g., "HG002_2#0#")]:PREFIX:_default' \
'--target=[Target sample/haplotype name for surjection (e.g., "HG002_1")]:TARGET:_default' \
'--split-size=[Split contigs into multiple BAM records every N base pairs for graph injection]:SPLIT_SIZE:_default' \
'--vg-threads=[Number of threads to use for vg operations]:VG_THREADS:_default' \
'--vg-binary=[Path to vg binary (default\: searches PATH)]:VG_BINARY:_default' \
'-d+[Delimiter character to use when stripping panSN-spec (default\: '\''#'\'')]:DELIMITER:_default' \
'--delimiter=[Delimiter character to use when stripping panSN-spec (default\: '\''#'\'')]:DELIMITER:_default' \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'--bam[Input file is in BAM format (default\: BED format)]' \
'--keep-intermediate[Keep intermediate files for debugging]' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'-h[Print help]' \
'--help[Print help]' \
'-V[Print version]' \
'--version[Print version]' \
':reference -- Reference FASTA file to use as source coordinates:_default' \
&& ret=0
;;
(pg-pansn)
_arguments "${_arguments_options[@]}" : \
'-F+[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'--filter=[BAM bit flags to filter on, equivalent to \`-F\` in samtools view]:BIT_FLAG:_default' \
'-x+[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ftx=[Filtering expression to use for filtering records Example\: filter to nucleosomes with lengths greater than 150 bp -x "len(nuc)>150" Example\: filter to msps with lengths between 30 and 49 bp -x "len(msp)=30\:50" Example\: combine 2+ filter expressions -x "len(nuc)<150,len(msp)=30\:50" Filtering expressions support len() and qual() functions over msp, nuc, m6a, cpg]:FILTER_EXPRESSION:_default' \
'--ml=[Minium score in the ML tag to use or include in the output]:MIN_ML_SCORE:_default' \
'--strip-starting-basemods=[strip basemods in the first or last X bp of the read]:STRIP_STARTING_BASEMODS:_default' \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'--header-out=[Additionally write the output BAM header to this file]:HEADER_OUT:_default' \
'(--strip)-p+[panSN-spec prefix to add to contig names (e.g., "HG002#1#") If provided, adds the prefix. Mutually exclusive with --strip]:PREFIX:_default' \
'(--strip)--prefix=[panSN-spec prefix to add to contig names (e.g., "HG002#1#") If provided, adds the prefix. Mutually exclusive with --strip]:PREFIX:_default' \
'-d+[Delimiter character to use when stripping panSN-spec (default\: '\''#'\'')]:DELIMITER:_default' \
'--delimiter=[Delimiter character to use when stripping panSN-spec (default\: '\''#'\'')]:DELIMITER:_default' \
'-1+[Tag to identify haplotype 1 contigs (e.g., "haplotype1") If both hap1-tag and hap2-tag are provided, reads will be tagged with HP based on contig names]:HAP1_TAG:_default' \
'--hap1-tag=[Tag to identify haplotype 1 contigs (e.g., "haplotype1") If both hap1-tag and hap2-tag are provided, reads will be tagged with HP based on contig names]:HAP1_TAG:_default' \
'-2+[Tag to identify haplotype 2 contigs (e.g., "haplotype2") If both hap1-tag and hap2-tag are provided, reads will be tagged with HP based on contig names]:HAP2_TAG:_default' \
'--hap2-tag=[Tag to identify haplotype 2 contigs (e.g., "haplotype2") If both hap1-tag and hap2-tag are provided, reads will be tagged with HP based on contig names]:HAP2_TAG:_default' \
'-q+[The mapping quality must be greater than or equal to this value for haplotag assignment]:MIN_MAPQ:_default' \
'--min-mapq=[The mapping quality must be greater than or equal to this value for haplotag assignment]:MIN_MAPQ:_default' \
'-c+[BAM file to copy header from (excluding SQ and HD tags)]:COPY_HEADER:_default' \
'--copy-header=[BAM file to copy header from (excluding SQ and HD tags)]:COPY_HEADER:_default' \
'-u[Output uncompressed BAM files]' \
'--uncompressed[Output uncompressed BAM files]' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'(-p --prefix)--strip[Strip panSN-spec information from contig names Mutually exclusive with --prefix]' \
'-h[Print help]' \
'--help[Print help]' \
'-V[Print version]' \
'--version[Print version]' \
'::bam -- Input BAM file. If no path is provided stdin is used. For m6A prediction, this should be a HiFi bam file with kinetics data. For other commands, this should be a bam file with m6A calls:_files' \
'::out -- Output BAM file:_default' \
&& ret=0
;;
(completions)
_arguments "${_arguments_options[@]}" : \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'-h[Print help]' \
'--help[Print help]' \
'-V[Print version]' \
'--version[Print version]' \
':shell -- If provided, outputs the completion file for given shell:(bash elvish fish powershell zsh)' \
&& ret=0
;;
(man)
_arguments "${_arguments_options[@]}" : \
'-t+[Threads]:THREADS:_default' \
'--threads=[Threads]:THREADS:_default' \
'*-v[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'*--verbose[Logging level \[-v\: Info, -vv\: Debug, -vvv\: Trace\]]' \
'--quiet[Turn off all logging]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'-V[Print version]' \
'--version[Print version]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_fibertools-rs__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:fibertools-rs-help-command-$line[1]:"
        case $line[1] in
            (predict-m6a)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(add-nucleosomes)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(fire)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(extract)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(center)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(footprint)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(qc)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(track-decorators)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(pileup)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(clear-kinetics)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(strip-basemods)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(ddda-to-m6a)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(fiber-hmm)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(validate)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(pg-inject)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(pg-lift)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(pg-pansn)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(completions)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(man)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
}

(( $+functions[_fibertools-rs_commands] )) ||
_fibertools-rs_commands() {
    local commands; commands=(
'predict-m6a:Predict m6A positions using HiFi kinetics data and encode the results in the MM and ML bam tags. Also adds nucleosome (nl, ns) and MTase sensitive patches (al, as)' \
'm6A:Predict m6A positions using HiFi kinetics data and encode the results in the MM and ML bam tags. Also adds nucleosome (nl, ns) and MTase sensitive patches (al, as)' \
'm6a:Predict m6A positions using HiFi kinetics data and encode the results in the MM and ML bam tags. Also adds nucleosome (nl, ns) and MTase sensitive patches (al, as)' \
'add-nucleosomes:Add nucleosomes to a bam file with m6a predictions' \
'fire:Add FIREs (Fiber-seq Inferred Regulatory Elements) to a bam file with m6a predictions' \
'extract:Extract fiberseq data into plain text files' \
'ex:Extract fiberseq data into plain text files' \
'e:Extract fiberseq data into plain text files' \
'center:This command centers fiberseq data around given reference positions. This is useful for making aggregate m6A and CpG observations, as well as visualization of SVs' \
'c:This command centers fiberseq data around given reference positions. This is useful for making aggregate m6A and CpG observations, as well as visualization of SVs' \
'ct:This command centers fiberseq data around given reference positions. This is useful for making aggregate m6A and CpG observations, as well as visualization of SVs' \
'footprint:Infer footprints from fiberseq data' \
'qc:Collect QC metrics from a fiberseq bam file' \
'track-decorators:Make decorated bed files for fiberseq data' \
'pileup:Make a pileup track of Fiber-seq features from a FIRE bam' \
'clear-kinetics:Remove HiFi kinetics tags from the input bam file' \
'strip-basemods:Strip out select base modifications' \
'ddda-to-m6a:Convert a DddA BAM file to pseudo m6A BAM file' \
'fiber-hmm:Apply FiberHMM to a bam file' \
'validate:Validate a Fiber-seq BAM file for m6A, nucleosome, and optionally FIRE calls' \
'pg-inject:Create a mock BAM file from a reference FASTA with perfectly aligned sequences' \
'pg-lift:Lift annotations through a pangenome graph from source to target coordinates' \
'pg-pansn:Add or strip panSN-spec prefixes from BAM contig names' \
'completions:Make command line completions' \
'man:Make a man page for fibertools-rs' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'fibertools-rs commands' commands "$@"
}
(( $+functions[_fibertools-rs__add-nucleosomes_commands] )) ||
_fibertools-rs__add-nucleosomes_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs add-nucleosomes commands' commands "$@"
}
(( $+functions[_fibertools-rs__center_commands] )) ||
_fibertools-rs__center_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs center commands' commands "$@"
}
(( $+functions[_fibertools-rs__clear-kinetics_commands] )) ||
_fibertools-rs__clear-kinetics_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs clear-kinetics commands' commands "$@"
}
(( $+functions[_fibertools-rs__completions_commands] )) ||
_fibertools-rs__completions_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs completions commands' commands "$@"
}
(( $+functions[_fibertools-rs__ddda-to-m6a_commands] )) ||
_fibertools-rs__ddda-to-m6a_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs ddda-to-m6a commands' commands "$@"
}
(( $+functions[_fibertools-rs__extract_commands] )) ||
_fibertools-rs__extract_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs extract commands' commands "$@"
}
(( $+functions[_fibertools-rs__fiber-hmm_commands] )) ||
_fibertools-rs__fiber-hmm_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs fiber-hmm commands' commands "$@"
}
(( $+functions[_fibertools-rs__fire_commands] )) ||
_fibertools-rs__fire_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs fire commands' commands "$@"
}
(( $+functions[_fibertools-rs__footprint_commands] )) ||
_fibertools-rs__footprint_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs footprint commands' commands "$@"
}
(( $+functions[_fibertools-rs__help_commands] )) ||
_fibertools-rs__help_commands() {
    local commands; commands=(
'predict-m6a:Predict m6A positions using HiFi kinetics data and encode the results in the MM and ML bam tags. Also adds nucleosome (nl, ns) and MTase sensitive patches (al, as)' \
'add-nucleosomes:Add nucleosomes to a bam file with m6a predictions' \
'fire:Add FIREs (Fiber-seq Inferred Regulatory Elements) to a bam file with m6a predictions' \
'extract:Extract fiberseq data into plain text files' \
'center:This command centers fiberseq data around given reference positions. This is useful for making aggregate m6A and CpG observations, as well as visualization of SVs' \
'footprint:Infer footprints from fiberseq data' \
'qc:Collect QC metrics from a fiberseq bam file' \
'track-decorators:Make decorated bed files for fiberseq data' \
'pileup:Make a pileup track of Fiber-seq features from a FIRE bam' \
'clear-kinetics:Remove HiFi kinetics tags from the input bam file' \
'strip-basemods:Strip out select base modifications' \
'ddda-to-m6a:Convert a DddA BAM file to pseudo m6A BAM file' \
'fiber-hmm:Apply FiberHMM to a bam file' \
'validate:Validate a Fiber-seq BAM file for m6A, nucleosome, and optionally FIRE calls' \
'pg-inject:Create a mock BAM file from a reference FASTA with perfectly aligned sequences' \
'pg-lift:Lift annotations through a pangenome graph from source to target coordinates' \
'pg-pansn:Add or strip panSN-spec prefixes from BAM contig names' \
'completions:Make command line completions' \
'man:Make a man page for fibertools-rs' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'fibertools-rs help commands' commands "$@"
}
(( $+functions[_fibertools-rs__help__add-nucleosomes_commands] )) ||
_fibertools-rs__help__add-nucleosomes_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs help add-nucleosomes commands' commands "$@"
}
(( $+functions[_fibertools-rs__help__center_commands] )) ||
_fibertools-rs__help__center_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs help center commands' commands "$@"
}
(( $+functions[_fibertools-rs__help__clear-kinetics_commands] )) ||
_fibertools-rs__help__clear-kinetics_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs help clear-kinetics commands' commands "$@"
}
(( $+functions[_fibertools-rs__help__completions_commands] )) ||
_fibertools-rs__help__completions_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs help completions commands' commands "$@"
}
(( $+functions[_fibertools-rs__help__ddda-to-m6a_commands] )) ||
_fibertools-rs__help__ddda-to-m6a_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs help ddda-to-m6a commands' commands "$@"
}
(( $+functions[_fibertools-rs__help__extract_commands] )) ||
_fibertools-rs__help__extract_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs help extract commands' commands "$@"
}
(( $+functions[_fibertools-rs__help__fiber-hmm_commands] )) ||
_fibertools-rs__help__fiber-hmm_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs help fiber-hmm commands' commands "$@"
}
(( $+functions[_fibertools-rs__help__fire_commands] )) ||
_fibertools-rs__help__fire_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs help fire commands' commands "$@"
}
(( $+functions[_fibertools-rs__help__footprint_commands] )) ||
_fibertools-rs__help__footprint_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs help footprint commands' commands "$@"
}
(( $+functions[_fibertools-rs__help__help_commands] )) ||
_fibertools-rs__help__help_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs help help commands' commands "$@"
}
(( $+functions[_fibertools-rs__help__man_commands] )) ||
_fibertools-rs__help__man_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs help man commands' commands "$@"
}
(( $+functions[_fibertools-rs__help__pg-inject_commands] )) ||
_fibertools-rs__help__pg-inject_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs help pg-inject commands' commands "$@"
}
(( $+functions[_fibertools-rs__help__pg-lift_commands] )) ||
_fibertools-rs__help__pg-lift_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs help pg-lift commands' commands "$@"
}
(( $+functions[_fibertools-rs__help__pg-pansn_commands] )) ||
_fibertools-rs__help__pg-pansn_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs help pg-pansn commands' commands "$@"
}
(( $+functions[_fibertools-rs__help__pileup_commands] )) ||
_fibertools-rs__help__pileup_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs help pileup commands' commands "$@"
}
(( $+functions[_fibertools-rs__help__predict-m6a_commands] )) ||
_fibertools-rs__help__predict-m6a_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs help predict-m6a commands' commands "$@"
}
(( $+functions[_fibertools-rs__help__qc_commands] )) ||
_fibertools-rs__help__qc_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs help qc commands' commands "$@"
}
(( $+functions[_fibertools-rs__help__strip-basemods_commands] )) ||
_fibertools-rs__help__strip-basemods_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs help strip-basemods commands' commands "$@"
}
(( $+functions[_fibertools-rs__help__track-decorators_commands] )) ||
_fibertools-rs__help__track-decorators_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs help track-decorators commands' commands "$@"
}
(( $+functions[_fibertools-rs__help__validate_commands] )) ||
_fibertools-rs__help__validate_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs help validate commands' commands "$@"
}
(( $+functions[_fibertools-rs__man_commands] )) ||
_fibertools-rs__man_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs man commands' commands "$@"
}
(( $+functions[_fibertools-rs__pg-inject_commands] )) ||
_fibertools-rs__pg-inject_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs pg-inject commands' commands "$@"
}
(( $+functions[_fibertools-rs__pg-lift_commands] )) ||
_fibertools-rs__pg-lift_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs pg-lift commands' commands "$@"
}
(( $+functions[_fibertools-rs__pg-pansn_commands] )) ||
_fibertools-rs__pg-pansn_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs pg-pansn commands' commands "$@"
}
(( $+functions[_fibertools-rs__pileup_commands] )) ||
_fibertools-rs__pileup_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs pileup commands' commands "$@"
}
(( $+functions[_fibertools-rs__predict-m6a_commands] )) ||
_fibertools-rs__predict-m6a_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs predict-m6a commands' commands "$@"
}
(( $+functions[_fibertools-rs__qc_commands] )) ||
_fibertools-rs__qc_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs qc commands' commands "$@"
}
(( $+functions[_fibertools-rs__strip-basemods_commands] )) ||
_fibertools-rs__strip-basemods_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs strip-basemods commands' commands "$@"
}
(( $+functions[_fibertools-rs__track-decorators_commands] )) ||
_fibertools-rs__track-decorators_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs track-decorators commands' commands "$@"
}
(( $+functions[_fibertools-rs__validate_commands] )) ||
_fibertools-rs__validate_commands() {
    local commands; commands=()
    _describe -t commands 'fibertools-rs validate commands' commands "$@"
}

if [ "$funcstack[1]" = "_fibertools-rs" ]; then
    _fibertools-rs "$@"
else
    compdef _fibertools-rs fibertools-rs
fi
