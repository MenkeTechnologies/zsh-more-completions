#compdef nvidia-detect
# Documentation: (RPM Fusion package; no online manpage indexed)
# Reference accessed: 2026-05-16
# Verified: RPM Fusion `nvidia-detect` script. Primary mode is no-args
# PCI scan returning the package name. Conservative stub with
# documented minimal CLI.
# Removed from prior file: bogus man.archlinux.org search URL.

# nvidia-detect: RPM Fusion NVIDIA driver detector
local curcontext="$curcontext" ret=1
_arguments -C \
  '(-h --help)'{-h,--help}'[show help]' \
  '(-v --verbose)'{-v,--verbose}'[verbose output]' \
  '(-l --list)'{-l,--list}'[list supported devices]' \
  '--version[show version]' && ret=0
return ret
