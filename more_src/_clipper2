#compdef clipper2
# Documentation: https://github.com/AngusJohnson/Clipper2
# Reference accessed: 2026-05-16
# Verified: AngusJohnson/Clipper2 is a C++/C#/Delphi polygon clipping
# LIBRARY. Homebrew clipper2 formula installs the library. No CLI binary
# named `clipper2` exists upstream.
# Removed from prior file: bogus man.archlinux.org search URL;
# fabricated -h/--help/-v/--version flags (no CLI binary to flag).

# clipper2: polygon clipping library (no CLI binary)
local curcontext="$curcontext" ret=1
_arguments -C \
  ':no CLI binary (library only):' && ret=0
return ret
