#compdef create-atp-agent
# Documentation: https://www.npmjs.com/package/create-atp-agent
# Reference accessed: 2026-05-23
# Verified: npm view create-atp-agent bin; npm readme Flags section.

# Zsh completion for create-atp-agent - Agent Trust Protocol agent scaffolder

local curcontext="$curcontext" state line ret=1

_arguments -C   '--dashboard-only[serve onboarding UI without scaffolding a project]'   '--no-dashboard[scaffold without starting onboarding server]'   '--no-open[start server but do not launch browser]'   '--skip-install[skip npm install after scaffolding]'   ':project directory:_directories' && ret=0

return ret
