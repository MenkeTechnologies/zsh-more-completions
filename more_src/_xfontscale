#compdef xfontscale
# Documentation: (not a real X11 utility)
# Reference accessed: 2026-05-16
# Verified: extensive WebSearch finds no X11 binary named `xfontscale`
# in xorg, freedesktop, or Debian/Ubuntu indices. The user likely
# intended `mkfontscale` (creates fonts.scale index) or `xfontsel`
# (font selector). Conservative stub retained per "do not delete
# files" directive.
# Removed from prior file: fabricated -h/--help/-V/--version flags and
# bogus man.archlinux.org search URL.

# xfontscale: NOT A REAL BINARY (see mkfontscale or xfontsel)
local curcontext="$curcontext" ret=1
_arguments -C \
  '*:arg:_files' && ret=0
return ret
