#compdef chef-cli
# Documentation: https://man.archlinux.org/search?q=chef-cli
# Reference accessed: 2026-05-16
# Source: https://github.com/devrev/adaas-chef-cli/blob/217b5ac1fa2ce1afc048e57639efee84653fe49d/autocomplete/zsh_autocomplete
# Repository: devrev/adaas-chef-cli (autocomplete/zsh_autocomplete)
#!/bin/zsh


# Initialize completion system if needed
if (( ! $+functions[compinit] )); then
  autoload -U compinit
  compinit
fi

_cli_zsh_autocomplete() {
  local -a opts
  local cur
  cur=${words[-1]}
  if [[ "$cur" == "-"* ]]; then
    opts=("${(@f)$(${words[@]:0:#words[@]-1} ${cur} --generate-bash-completion)}")
  else
    opts=("${(@f)$(${words[@]:0:#words[@]-1} --generate-bash-completion)}")
  fi

  if [[ "${opts[1]}" != "" ]]; then
    _describe 'values' opts
  else
    _files
  fi
}

# Register the completion function
