#compdef fd2pascal

local curcontext="$curcontext" state line ret=1

_arguments -s \
  '-o+[output Pascal unit]:file:_files -g "*.pas"' \
  '-l[lowercase identifier names]' \
  '-c[generate constants]' \
  '*:input FD file:_files -g "*.fd"' && ret=0

return ret
