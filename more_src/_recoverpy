#compdef recoverpy

# Zsh completion for recoverpy — file recovery TUI

local curcontext="$curcontext" ret=1

_arguments -s -S \
  '(-h --help)'{-h,--help}'[help]' \
  '(-p --partition)'{-p,--partition}'[block device]:device:_files' && ret=0

return ret
