#compdef goat

# https://github.com/bluesky-social/goat — urfave/cli/v3 (main.go + */cmd vars)
_arguments -s -S \
  '--help[usage]' \
  '--version[version]' \
  '--log-level=-[slog level]:level:' \
  '--plc-host=-[PLC directory URL]:url:' \
  '1:command:(get ls firehose resolve xrpc repo blob lex account plc bsky record syntax key pds relay)' \
  '*::arguments:_default'
