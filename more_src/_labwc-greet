#compdef labwc-greet
# Documentation: (no upstream binary by this name; greeter pattern uses
# labwc -s <greetd-command>)
# Reference accessed: 2026-05-16
# Verified: labwc-greet is not a standalone binary in upstream labwc. Greeter
# integration is performed by invoking labwc with -s <greetd> session command.
# Tool kept per "do not delete files" directive with minimal stub.

# labwc-greet: hypothetical greeter wrapper (no upstream binary)
local curcontext="$curcontext" ret=1
_arguments -C \
  '*::extra args:_files' && ret=0
return ret
