#compdef impacket-net
# Documentation: https://github.com/fortra/impacket/blob/master/examples/net.py
# SAMR rpc client implementation

local ret=1

_arguments -s -S \
  '(-h --help)'{-h,--help}'[show help]' \
  '-debug[turn DEBUG output ON]' \
  '-ts[adds timestamp to every logging output]' \
  '-hashes=[NTLM hashes, format is LMHASH:NTHASH]:hashes' \
  '-no-pass[don'\''t ask for password (useful for -k)]' \
  '-k[use Kerberos authentication from ccache file]' \
  '-aesKey=[AES key to use for Kerberos authentication (128 or 256 bits)]:hexkey' \
  '-dc-ip=[IP address of the domain controller]:ip' \
  '-target-ip=[IP address of the target machine]:ip' \
  '-port=[destination port to connect to SMB server]:port:(139 445)' \
  '1:target (\[\[domain/\]username\[:password\]@\]<host>):' \
  '*::command:((user\:"enumerate all domain/local user accounts" computer\:"enumerate all computers in domain level" localgroup\:"enumerate local groups of local computer" group\:"enumerate domain groups in domain controller"))' && ret=0

return ret
