#compdef tzdata-update
# Documentation: (Debian tzdata postinst helper, no user CLI)
# Reference accessed: 2026-05-16
# Verified: tzdata-update is a Debian tzdata package internal helper
# run only from dpkg postinst with internal subcommands (configure,
# triggered, etc.). No user-facing CLI flags.
# Removed from prior file: fabricated -h/--help/-V/--version flags and
# bogus man.archlinux.org search URL.

# tzdata-update: Debian internal postinst helper (no user CLI)
local curcontext="$curcontext" ret=1
_arguments -C && ret=0
return ret
