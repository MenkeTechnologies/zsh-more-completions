#compdef zipkin
# Documentation: https://github.com/openzipkin/zipkin/blob/master/zipkin-server/README.md
# Reference accessed: 2026-05-21
# Verified: upstream docs at the URL above.
# STUB: zipkin server is configured via env vars (QUERY_PORT, STORAGE_TYPE,
# COLLECTOR_*_ENABLED, KAFKA_BOOTSTRAP_SERVERS, ES_HOSTS, MYSQL_*, etc.) not
# CLI flags. The bare `zipkin` invocation has no documented user-facing CLI
# surface beyond `java -jar zipkin.jar`.

local curcontext="$curcontext" state line ret=1
_arguments -s -S '*:file:_files' && ret=0
return ret
