#compdef below

# Zsh completion for below - system monitor from Meta

local curcontext="$curcontext" state line ret=1

_arguments -s -S -C \
  '(- *)'{-h,--help}'[show help]' \
  '(- *)'{-V,--version}'[show version]' \
  '1:command:->commands' \
  '*::arg:->args' && ret=0

case $state in
  commands)
    local -a commands=(
      'live:display live system data'
      'record:record system data'
      'replay:replay recorded data'
      'dump:dump recorded data'
      'snapshot:snapshot recorded data'
    )
    _describe -t commands 'below command' commands && ret=0
    ;;
  args)
    case $line[1] in
      live)
        _arguments -s -S \
          '(- *)'{-h,--help}'[show help]' \
          '--host=[remote host]:host' \
          '--port=[remote port]:port' \
          && ret=0
        ;;
      record)
        _arguments -s -S \
          '(- *)'{-h,--help}'[show help]' \
          '--retain-for-s=[retention time in seconds]:seconds' \
          '--store-dir=[data store directory]:directory:_directories' \
          '--compress[compress stored data]' \
          '--skew-detection-threshold=[clock skew threshold]:threshold' \
          '--collect-io-stat[collect I/O stats]' \
          '--debug[enable debug logging]' \
          && ret=0
        ;;
      replay)
        _arguments -s -S \
          '(- *)'{-h,--help}'[show help]' \
          '--host=[remote host]:host' \
          '--port=[remote port]:port' \
          '--time=[replay from this time]:time' \
          && ret=0
        ;;
      dump)
        _arguments -s -S \
          '(- *)'{-h,--help}'[show help]' \
          '--time=[time to dump]:time' \
          '--host=[remote host]:host' \
          '--port=[remote port]:port' \
          '--everything[dump all data]' \
          '--output=[output format]:format:(json csv)' \
          && ret=0
        ;;
      snapshot)
        _arguments -s -S \
          '(- *)'{-h,--help}'[show help]' \
          && ret=0
        ;;
    esac
    ;;
esac

return ret
