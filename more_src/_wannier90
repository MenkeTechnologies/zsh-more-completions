#compdef wannier90 wannier90.x
# wannier90 — maximally-localized Wannier functions from Bloch states

_arguments -s -S \
  '-pp[generate a list of required overlaps (preprocessing only)]' \
  '*:input seedname (without .win extension):_files -g "*.win(:r)"'
