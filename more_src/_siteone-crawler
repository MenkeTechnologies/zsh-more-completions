#compdef siteone-crawler

# https://github.com/janreges/siteone-crawler — recursive site / SEO / accessibility auditor

_arguments -s -S \
  '(-h --help)'{-h,--help}'[help]' \
  '--timezone=[set TZ before run]:timezone:' \
  '*::args:_default' && return 0
