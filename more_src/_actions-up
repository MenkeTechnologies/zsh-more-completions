#compdef actions-up

# actions-up — bumps pinned Actions SHAs; see `actions-up --help`

local curcontext="$curcontext" ret=1

_arguments -s -S \
  '--yes[non-interactive]' \
  '*:workflow files:_files' && ret=0

return ret
