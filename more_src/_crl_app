#compdef crl_app
# Documentation: stub — no canonical upstream `crl_app` CLI found
# Reference accessed: 2026-05-17
# Verified: no upstream docs locate this as a standalone binary.

# crl_app is a name used by the Erlang/OTP public_key application's internal
# CRL handling module — it is not a user-facing shell command and has no
# documented argv interface. WebSearch 2026-05-17 surfaced no other tool by
# that name. This stub returns file completion to avoid surprising the user.
_files
