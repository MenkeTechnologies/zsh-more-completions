#compdef impacket-ping

# Impacket ping.py: basic ICMP echo using raw socket library
local curcontext="$curcontext" ret=1
_arguments -C \
  '-h[show help]' \
  '*:source destination:' && ret=0
return ret
