#compdef xbeach
# Documentation: https://xbeach.readthedocs.io/en/latest/xbeach_manual.html
# XBeach storm-impact / coastal model; reads params.txt from the current directory (no CLI flags).

local ret=1

_arguments -s -S \
  '*:params file:_files -g "params.txt"' && ret=0

return ret
