#compdef pixforge
# Documentation: https://man.archlinux.org/search?q=pixforge
# Reference accessed: 2026-05-16
# Source: cargo binstall pixforge@0.3.1
# Probed via: completions zsh
# Repository: https://github.com/GitAashishG/pixforge

autoload -U is-at-least

_pixforge() {
    typeset -A opt_args
    typeset -a _arguments_options
    local ret=1

    if is-at-least 5.2; then
        _arguments_options=(-s -S -C)
    else
        _arguments_options=(-s -C)
    fi

    local context curcontext="$curcontext" state line
    _arguments "${_arguments_options[@]}" : \
'-p+[Prompt text. Use \`-\` to read the prompt from stdin (until EOF)]:PROMPT:_default' \
'--prompt=[Prompt text. Use \`-\` to read the prompt from stdin (until EOF)]:PROMPT:_default' \
'-o+[Output PNG path. Default\: ./pixforge-{YYYYMMDD-HHMMSS}-{hash6}.png]:OUTPUT:_files' \
'--output=[Output PNG path. Default\: ./pixforge-{YYYYMMDD-HHMMSS}-{hash6}.png]:OUTPUT:_files' \
'--profile=[Profile name from config.toml. Overrides PIXFORGE_PROFILE and default_profile]:PROFILE:_default' \
'-m+[Override the model / deployment name for this call]:MODEL:_default' \
'--model=[Override the model / deployment name for this call]:MODEL:_default' \
'-W+[Image width in pixels. Adapter validates against its provider'\''s allowed sizes]:WIDTH:_default' \
'--width=[Image width in pixels. Adapter validates against its provider'\''s allowed sizes]:WIDTH:_default' \
'-H+[Image height in pixels. Adapter validates against its provider'\''s allowed sizes]:HEIGHT:_default' \
'--height=[Image height in pixels. Adapter validates against its provider'\''s allowed sizes]:HEIGHT:_default' \
'--endpoint=[Override the provider endpoint URL]:ENDPOINT:_default' \
'--api-version=[Override the API version (where applicable)]:API_VERSION:_default' \
'--quality=[Quality hint passed to providers that support it (e.g. OpenAI gpt-image-*\: \`low\` (~15s), \`medium\`, \`high\` (~3min, default for the model)). Ignored by providers that don'\''t support it]:QUALITY:_default' \
'--timeout=[HTTP timeout in seconds for a single generation call. Default 300s (gpt-image-2 high quality can take several minutes)]:TIMEOUT:_default' \
'--max-attempts=[Max attempts (1 + retries) for transient failures]:MAX_ATTEMPTS:_default' \
'--no-open[Don'\''t open the resulting image in the default viewer]' \
'-q[Suppress progress on stderr (final path is still printed on stdout)]' \
'--quiet[Suppress progress on stderr (final path is still printed on stdout)]' \
'-h[Print help]' \
'--help[Print help]' \
'-V[Print version]' \
'--version[Print version]' \
":: :_pixforge_commands" \
"*::: :->pixforge" \
&& ret=0
    case $state in
    (pixforge)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:pixforge-command-$line[1]:"
        case $line[1] in
            (init)
_arguments "${_arguments_options[@]}" : \
'-p+[Prompt text. Use \`-\` to read the prompt from stdin (until EOF)]:PROMPT:_default' \
'--prompt=[Prompt text. Use \`-\` to read the prompt from stdin (until EOF)]:PROMPT:_default' \
'-o+[Output PNG path. Default\: ./pixforge-{YYYYMMDD-HHMMSS}-{hash6}.png]:OUTPUT:_files' \
'--output=[Output PNG path. Default\: ./pixforge-{YYYYMMDD-HHMMSS}-{hash6}.png]:OUTPUT:_files' \
'--profile=[Profile name from config.toml. Overrides PIXFORGE_PROFILE and default_profile]:PROFILE:_default' \
'-m+[Override the model / deployment name for this call]:MODEL:_default' \
'--model=[Override the model / deployment name for this call]:MODEL:_default' \
'-W+[Image width in pixels. Adapter validates against its provider'\''s allowed sizes]:WIDTH:_default' \
'--width=[Image width in pixels. Adapter validates against its provider'\''s allowed sizes]:WIDTH:_default' \
'-H+[Image height in pixels. Adapter validates against its provider'\''s allowed sizes]:HEIGHT:_default' \
'--height=[Image height in pixels. Adapter validates against its provider'\''s allowed sizes]:HEIGHT:_default' \
'--endpoint=[Override the provider endpoint URL]:ENDPOINT:_default' \
'--api-version=[Override the API version (where applicable)]:API_VERSION:_default' \
'--quality=[Quality hint passed to providers that support it (e.g. OpenAI gpt-image-*\: \`low\` (~15s), \`medium\`, \`high\` (~3min, default for the model)). Ignored by providers that don'\''t support it]:QUALITY:_default' \
'--timeout=[HTTP timeout in seconds for a single generation call. Default 300s (gpt-image-2 high quality can take several minutes)]:TIMEOUT:_default' \
'--max-attempts=[Max attempts (1 + retries) for transient failures]:MAX_ATTEMPTS:_default' \
'--force[Overwrite an existing config file]' \
'--no-open[Don'\''t open the resulting image in the default viewer]' \
'-q[Suppress progress on stderr (final path is still printed on stdout)]' \
'--quiet[Suppress progress on stderr (final path is still printed on stdout)]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(config-path)
_arguments "${_arguments_options[@]}" : \
'-p+[Prompt text. Use \`-\` to read the prompt from stdin (until EOF)]:PROMPT:_default' \
'--prompt=[Prompt text. Use \`-\` to read the prompt from stdin (until EOF)]:PROMPT:_default' \
'-o+[Output PNG path. Default\: ./pixforge-{YYYYMMDD-HHMMSS}-{hash6}.png]:OUTPUT:_files' \
'--output=[Output PNG path. Default\: ./pixforge-{YYYYMMDD-HHMMSS}-{hash6}.png]:OUTPUT:_files' \
'--profile=[Profile name from config.toml. Overrides PIXFORGE_PROFILE and default_profile]:PROFILE:_default' \
'-m+[Override the model / deployment name for this call]:MODEL:_default' \
'--model=[Override the model / deployment name for this call]:MODEL:_default' \
'-W+[Image width in pixels. Adapter validates against its provider'\''s allowed sizes]:WIDTH:_default' \
'--width=[Image width in pixels. Adapter validates against its provider'\''s allowed sizes]:WIDTH:_default' \
'-H+[Image height in pixels. Adapter validates against its provider'\''s allowed sizes]:HEIGHT:_default' \
'--height=[Image height in pixels. Adapter validates against its provider'\''s allowed sizes]:HEIGHT:_default' \
'--endpoint=[Override the provider endpoint URL]:ENDPOINT:_default' \
'--api-version=[Override the API version (where applicable)]:API_VERSION:_default' \
'--quality=[Quality hint passed to providers that support it (e.g. OpenAI gpt-image-*\: \`low\` (~15s), \`medium\`, \`high\` (~3min, default for the model)). Ignored by providers that don'\''t support it]:QUALITY:_default' \
'--timeout=[HTTP timeout in seconds for a single generation call. Default 300s (gpt-image-2 high quality can take several minutes)]:TIMEOUT:_default' \
'--max-attempts=[Max attempts (1 + retries) for transient failures]:MAX_ATTEMPTS:_default' \
'--no-open[Don'\''t open the resulting image in the default viewer]' \
'-q[Suppress progress on stderr (final path is still printed on stdout)]' \
'--quiet[Suppress progress on stderr (final path is still printed on stdout)]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(profiles)
_arguments "${_arguments_options[@]}" : \
'-p+[Prompt text. Use \`-\` to read the prompt from stdin (until EOF)]:PROMPT:_default' \
'--prompt=[Prompt text. Use \`-\` to read the prompt from stdin (until EOF)]:PROMPT:_default' \
'-o+[Output PNG path. Default\: ./pixforge-{YYYYMMDD-HHMMSS}-{hash6}.png]:OUTPUT:_files' \
'--output=[Output PNG path. Default\: ./pixforge-{YYYYMMDD-HHMMSS}-{hash6}.png]:OUTPUT:_files' \
'--profile=[Profile name from config.toml. Overrides PIXFORGE_PROFILE and default_profile]:PROFILE:_default' \
'-m+[Override the model / deployment name for this call]:MODEL:_default' \
'--model=[Override the model / deployment name for this call]:MODEL:_default' \
'-W+[Image width in pixels. Adapter validates against its provider'\''s allowed sizes]:WIDTH:_default' \
'--width=[Image width in pixels. Adapter validates against its provider'\''s allowed sizes]:WIDTH:_default' \
'-H+[Image height in pixels. Adapter validates against its provider'\''s allowed sizes]:HEIGHT:_default' \
'--height=[Image height in pixels. Adapter validates against its provider'\''s allowed sizes]:HEIGHT:_default' \
'--endpoint=[Override the provider endpoint URL]:ENDPOINT:_default' \
'--api-version=[Override the API version (where applicable)]:API_VERSION:_default' \
'--quality=[Quality hint passed to providers that support it (e.g. OpenAI gpt-image-*\: \`low\` (~15s), \`medium\`, \`high\` (~3min, default for the model)). Ignored by providers that don'\''t support it]:QUALITY:_default' \
'--timeout=[HTTP timeout in seconds for a single generation call. Default 300s (gpt-image-2 high quality can take several minutes)]:TIMEOUT:_default' \
'--max-attempts=[Max attempts (1 + retries) for transient failures]:MAX_ATTEMPTS:_default' \
'--no-open[Don'\''t open the resulting image in the default viewer]' \
'-q[Suppress progress on stderr (final path is still printed on stdout)]' \
'--quiet[Suppress progress on stderr (final path is still printed on stdout)]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(profile)
_arguments "${_arguments_options[@]}" : \
'-p+[Prompt text. Use \`-\` to read the prompt from stdin (until EOF)]:PROMPT:_default' \
'--prompt=[Prompt text. Use \`-\` to read the prompt from stdin (until EOF)]:PROMPT:_default' \
'-o+[Output PNG path. Default\: ./pixforge-{YYYYMMDD-HHMMSS}-{hash6}.png]:OUTPUT:_files' \
'--output=[Output PNG path. Default\: ./pixforge-{YYYYMMDD-HHMMSS}-{hash6}.png]:OUTPUT:_files' \
'--profile=[Profile name from config.toml. Overrides PIXFORGE_PROFILE and default_profile]:PROFILE:_default' \
'-m+[Override the model / deployment name for this call]:MODEL:_default' \
'--model=[Override the model / deployment name for this call]:MODEL:_default' \
'-W+[Image width in pixels. Adapter validates against its provider'\''s allowed sizes]:WIDTH:_default' \
'--width=[Image width in pixels. Adapter validates against its provider'\''s allowed sizes]:WIDTH:_default' \
'-H+[Image height in pixels. Adapter validates against its provider'\''s allowed sizes]:HEIGHT:_default' \
'--height=[Image height in pixels. Adapter validates against its provider'\''s allowed sizes]:HEIGHT:_default' \
'--endpoint=[Override the provider endpoint URL]:ENDPOINT:_default' \
'--api-version=[Override the API version (where applicable)]:API_VERSION:_default' \
'--quality=[Quality hint passed to providers that support it (e.g. OpenAI gpt-image-*\: \`low\` (~15s), \`medium\`, \`high\` (~3min, default for the model)). Ignored by providers that don'\''t support it]:QUALITY:_default' \
'--timeout=[HTTP timeout in seconds for a single generation call. Default 300s (gpt-image-2 high quality can take several minutes)]:TIMEOUT:_default' \
'--max-attempts=[Max attempts (1 + retries) for transient failures]:MAX_ATTEMPTS:_default' \
'--no-open[Don'\''t open the resulting image in the default viewer]' \
'-q[Suppress progress on stderr (final path is still printed on stdout)]' \
'--quiet[Suppress progress on stderr (final path is still printed on stdout)]' \
'-h[Print help]' \
'--help[Print help]' \
":: :_pixforge__subcmd__profile_commands" \
"*::: :->profile" \
&& ret=0

    case $state in
    (profile)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:pixforge-profile-command-$line[1]:"
        case $line[1] in
            (show)
_arguments "${_arguments_options[@]}" : \
'-p+[Prompt text. Use \`-\` to read the prompt from stdin (until EOF)]:PROMPT:_default' \
'--prompt=[Prompt text. Use \`-\` to read the prompt from stdin (until EOF)]:PROMPT:_default' \
'-o+[Output PNG path. Default\: ./pixforge-{YYYYMMDD-HHMMSS}-{hash6}.png]:OUTPUT:_files' \
'--output=[Output PNG path. Default\: ./pixforge-{YYYYMMDD-HHMMSS}-{hash6}.png]:OUTPUT:_files' \
'--profile=[Profile name from config.toml. Overrides PIXFORGE_PROFILE and default_profile]:PROFILE:_default' \
'-m+[Override the model / deployment name for this call]:MODEL:_default' \
'--model=[Override the model / deployment name for this call]:MODEL:_default' \
'-W+[Image width in pixels. Adapter validates against its provider'\''s allowed sizes]:WIDTH:_default' \
'--width=[Image width in pixels. Adapter validates against its provider'\''s allowed sizes]:WIDTH:_default' \
'-H+[Image height in pixels. Adapter validates against its provider'\''s allowed sizes]:HEIGHT:_default' \
'--height=[Image height in pixels. Adapter validates against its provider'\''s allowed sizes]:HEIGHT:_default' \
'--endpoint=[Override the provider endpoint URL]:ENDPOINT:_default' \
'--api-version=[Override the API version (where applicable)]:API_VERSION:_default' \
'--quality=[Quality hint passed to providers that support it (e.g. OpenAI gpt-image-*\: \`low\` (~15s), \`medium\`, \`high\` (~3min, default for the model)). Ignored by providers that don'\''t support it]:QUALITY:_default' \
'--timeout=[HTTP timeout in seconds for a single generation call. Default 300s (gpt-image-2 high quality can take several minutes)]:TIMEOUT:_default' \
'--max-attempts=[Max attempts (1 + retries) for transient failures]:MAX_ATTEMPTS:_default' \
'--no-open[Don'\''t open the resulting image in the default viewer]' \
'-q[Suppress progress on stderr (final path is still printed on stdout)]' \
'--quiet[Suppress progress on stderr (final path is still printed on stdout)]' \
'-h[Print help]' \
'--help[Print help]' \
':name:_default' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_pixforge__subcmd__profile__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:pixforge-profile-help-command-$line[1]:"
        case $line[1] in
            (show)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(completions)
_arguments "${_arguments_options[@]}" : \
'-p+[Prompt text. Use \`-\` to read the prompt from stdin (until EOF)]:PROMPT:_default' \
'--prompt=[Prompt text. Use \`-\` to read the prompt from stdin (until EOF)]:PROMPT:_default' \
'-o+[Output PNG path. Default\: ./pixforge-{YYYYMMDD-HHMMSS}-{hash6}.png]:OUTPUT:_files' \
'--output=[Output PNG path. Default\: ./pixforge-{YYYYMMDD-HHMMSS}-{hash6}.png]:OUTPUT:_files' \
'--profile=[Profile name from config.toml. Overrides PIXFORGE_PROFILE and default_profile]:PROFILE:_default' \
'-m+[Override the model / deployment name for this call]:MODEL:_default' \
'--model=[Override the model / deployment name for this call]:MODEL:_default' \
'-W+[Image width in pixels. Adapter validates against its provider'\''s allowed sizes]:WIDTH:_default' \
'--width=[Image width in pixels. Adapter validates against its provider'\''s allowed sizes]:WIDTH:_default' \
'-H+[Image height in pixels. Adapter validates against its provider'\''s allowed sizes]:HEIGHT:_default' \
'--height=[Image height in pixels. Adapter validates against its provider'\''s allowed sizes]:HEIGHT:_default' \
'--endpoint=[Override the provider endpoint URL]:ENDPOINT:_default' \
'--api-version=[Override the API version (where applicable)]:API_VERSION:_default' \
'--quality=[Quality hint passed to providers that support it (e.g. OpenAI gpt-image-*\: \`low\` (~15s), \`medium\`, \`high\` (~3min, default for the model)). Ignored by providers that don'\''t support it]:QUALITY:_default' \
'--timeout=[HTTP timeout in seconds for a single generation call. Default 300s (gpt-image-2 high quality can take several minutes)]:TIMEOUT:_default' \
'--max-attempts=[Max attempts (1 + retries) for transient failures]:MAX_ATTEMPTS:_default' \
'--no-open[Don'\''t open the resulting image in the default viewer]' \
'-q[Suppress progress on stderr (final path is still printed on stdout)]' \
'--quiet[Suppress progress on stderr (final path is still printed on stdout)]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':shell -- The shell to generate completions for:(bash elvish fish powershell zsh)' \
&& ret=0
;;
(man)
_arguments "${_arguments_options[@]}" : \
'-p+[Prompt text. Use \`-\` to read the prompt from stdin (until EOF)]:PROMPT:_default' \
'--prompt=[Prompt text. Use \`-\` to read the prompt from stdin (until EOF)]:PROMPT:_default' \
'-o+[Output PNG path. Default\: ./pixforge-{YYYYMMDD-HHMMSS}-{hash6}.png]:OUTPUT:_files' \
'--output=[Output PNG path. Default\: ./pixforge-{YYYYMMDD-HHMMSS}-{hash6}.png]:OUTPUT:_files' \
'--profile=[Profile name from config.toml. Overrides PIXFORGE_PROFILE and default_profile]:PROFILE:_default' \
'-m+[Override the model / deployment name for this call]:MODEL:_default' \
'--model=[Override the model / deployment name for this call]:MODEL:_default' \
'-W+[Image width in pixels. Adapter validates against its provider'\''s allowed sizes]:WIDTH:_default' \
'--width=[Image width in pixels. Adapter validates against its provider'\''s allowed sizes]:WIDTH:_default' \
'-H+[Image height in pixels. Adapter validates against its provider'\''s allowed sizes]:HEIGHT:_default' \
'--height=[Image height in pixels. Adapter validates against its provider'\''s allowed sizes]:HEIGHT:_default' \
'--endpoint=[Override the provider endpoint URL]:ENDPOINT:_default' \
'--api-version=[Override the API version (where applicable)]:API_VERSION:_default' \
'--quality=[Quality hint passed to providers that support it (e.g. OpenAI gpt-image-*\: \`low\` (~15s), \`medium\`, \`high\` (~3min, default for the model)). Ignored by providers that don'\''t support it]:QUALITY:_default' \
'--timeout=[HTTP timeout in seconds for a single generation call. Default 300s (gpt-image-2 high quality can take several minutes)]:TIMEOUT:_default' \
'--max-attempts=[Max attempts (1 + retries) for transient failures]:MAX_ATTEMPTS:_default' \
'--no-open[Don'\''t open the resulting image in the default viewer]' \
'-q[Suppress progress on stderr (final path is still printed on stdout)]' \
'--quiet[Suppress progress on stderr (final path is still printed on stdout)]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(setup)
_arguments "${_arguments_options[@]}" : \
'--provider=[(--non-interactive) Provider id\: azure-mai|azure-openai|openai-compat|gemini]:PROVIDER:_default' \
'--set-endpoint=[(--non-interactive) Endpoint URL (where applicable)]:ENDPOINT:_default' \
'--set-model=[(--non-interactive) Model / deployment name]:MODEL:_default' \
'--set-api-version=[(--non-interactive) API version (azure-mai / azure-openai deployment dialect)]:API_VERSION:_default' \
'--set-api-key-env=[(--non-interactive) Env var that holds the secret]:API_KEY_ENV:_default' \
'--set-auth-style=[(--non-interactive) Auth style for openai-compat\: bearer|api-key|none]:AUTH_STYLE:_default' \
'--set-dialect=[(--non-interactive) Azure OpenAI dialect\: deployment|v1]:DIALECT:_default' \
'--profile-name=[(--non-interactive) Profile name to write]:PROFILE_NAME:_default' \
'-p+[Prompt text. Use \`-\` to read the prompt from stdin (until EOF)]:PROMPT:_default' \
'--prompt=[Prompt text. Use \`-\` to read the prompt from stdin (until EOF)]:PROMPT:_default' \
'-o+[Output PNG path. Default\: ./pixforge-{YYYYMMDD-HHMMSS}-{hash6}.png]:OUTPUT:_files' \
'--output=[Output PNG path. Default\: ./pixforge-{YYYYMMDD-HHMMSS}-{hash6}.png]:OUTPUT:_files' \
'--profile=[Profile name from config.toml. Overrides PIXFORGE_PROFILE and default_profile]:PROFILE:_default' \
'-W+[Image width in pixels. Adapter validates against its provider'\''s allowed sizes]:WIDTH:_default' \
'--width=[Image width in pixels. Adapter validates against its provider'\''s allowed sizes]:WIDTH:_default' \
'-H+[Image height in pixels. Adapter validates against its provider'\''s allowed sizes]:HEIGHT:_default' \
'--height=[Image height in pixels. Adapter validates against its provider'\''s allowed sizes]:HEIGHT:_default' \
'--quality=[Quality hint passed to providers that support it (e.g. OpenAI gpt-image-*\: \`low\` (~15s), \`medium\`, \`high\` (~3min, default for the model)). Ignored by providers that don'\''t support it]:QUALITY:_default' \
'--timeout=[HTTP timeout in seconds for a single generation call. Default 300s (gpt-image-2 high quality can take several minutes)]:TIMEOUT:_default' \
'--max-attempts=[Max attempts (1 + retries) for transient failures]:MAX_ATTEMPTS:_default' \
'--non-interactive[Skip prompts; require all fields via flags below]' \
'--set-default[(--non-interactive) Set this profile as the new default]' \
'--no-open[Don'\''t open the resulting image in the default viewer]' \
'-q[Suppress progress on stderr (final path is still printed on stdout)]' \
'--quiet[Suppress progress on stderr (final path is still printed on stdout)]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(advanced-config)
_arguments "${_arguments_options[@]}" : \
'-p+[Prompt text. Use \`-\` to read the prompt from stdin (until EOF)]:PROMPT:_default' \
'--prompt=[Prompt text. Use \`-\` to read the prompt from stdin (until EOF)]:PROMPT:_default' \
'-o+[Output PNG path. Default\: ./pixforge-{YYYYMMDD-HHMMSS}-{hash6}.png]:OUTPUT:_files' \
'--output=[Output PNG path. Default\: ./pixforge-{YYYYMMDD-HHMMSS}-{hash6}.png]:OUTPUT:_files' \
'--profile=[Profile name from config.toml. Overrides PIXFORGE_PROFILE and default_profile]:PROFILE:_default' \
'-m+[Override the model / deployment name for this call]:MODEL:_default' \
'--model=[Override the model / deployment name for this call]:MODEL:_default' \
'-W+[Image width in pixels. Adapter validates against its provider'\''s allowed sizes]:WIDTH:_default' \
'--width=[Image width in pixels. Adapter validates against its provider'\''s allowed sizes]:WIDTH:_default' \
'-H+[Image height in pixels. Adapter validates against its provider'\''s allowed sizes]:HEIGHT:_default' \
'--height=[Image height in pixels. Adapter validates against its provider'\''s allowed sizes]:HEIGHT:_default' \
'--endpoint=[Override the provider endpoint URL]:ENDPOINT:_default' \
'--api-version=[Override the API version (where applicable)]:API_VERSION:_default' \
'--quality=[Quality hint passed to providers that support it (e.g. OpenAI gpt-image-*\: \`low\` (~15s), \`medium\`, \`high\` (~3min, default for the model)). Ignored by providers that don'\''t support it]:QUALITY:_default' \
'--timeout=[HTTP timeout in seconds for a single generation call. Default 300s (gpt-image-2 high quality can take several minutes)]:TIMEOUT:_default' \
'--max-attempts=[Max attempts (1 + retries) for transient failures]:MAX_ATTEMPTS:_default' \
'--no-open[Don'\''t open the resulting image in the default viewer]' \
'-q[Suppress progress on stderr (final path is still printed on stdout)]' \
'--quiet[Suppress progress on stderr (final path is still printed on stdout)]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_pixforge__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:pixforge-help-command-$line[1]:"
        case $line[1] in
            (init)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(config-path)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(profiles)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(profile)
_arguments "${_arguments_options[@]}" : \
":: :_pixforge__subcmd__help__subcmd__profile_commands" \
"*::: :->profile" \
&& ret=0

    case $state in
    (profile)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:pixforge-help-profile-command-$line[1]:"
        case $line[1] in
            (show)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(completions)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(man)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(setup)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(advanced-config)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
}

(( $+functions[_pixforge_commands] )) ||
_pixforge_commands() {
    local commands; commands=(
'init:Write a starter config file at \$XDG_CONFIG_HOME/pixforge/config.toml' \
'config-path:Print the resolved config file path and exit' \
'profiles:List all profiles in the current config file' \
'profile:Print the resolved settings for a profile (api_key shown only as env source + status)' \
'completions:Print shell completion script to stdout' \
'man:Print a man page (roff format) to stdout' \
'setup:Interactive setup wizard — recommended for first-time configuration. Walks you through picking a provider, validating each field as you type, and optionally testing the connection before saving' \
'advanced-config:Open the raw config file in \$EDITOR. If \$EDITOR is unset, prints the path. For power users who'\''d rather edit TOML by hand than use the wizard' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'pixforge commands' commands "$@"
}
(( $+functions[_pixforge__subcmd__advanced-config_commands] )) ||
_pixforge__subcmd__advanced-config_commands() {
    local commands; commands=()
    _describe -t commands 'pixforge advanced-config commands' commands "$@"
}
(( $+functions[_pixforge__subcmd__completions_commands] )) ||
_pixforge__subcmd__completions_commands() {
    local commands; commands=()
    _describe -t commands 'pixforge completions commands' commands "$@"
}
(( $+functions[_pixforge__subcmd__config-path_commands] )) ||
_pixforge__subcmd__config-path_commands() {
    local commands; commands=()
    _describe -t commands 'pixforge config-path commands' commands "$@"
}
(( $+functions[_pixforge__subcmd__help_commands] )) ||
_pixforge__subcmd__help_commands() {
    local commands; commands=(
'init:Write a starter config file at \$XDG_CONFIG_HOME/pixforge/config.toml' \
'config-path:Print the resolved config file path and exit' \
'profiles:List all profiles in the current config file' \
'profile:Print the resolved settings for a profile (api_key shown only as env source + status)' \
'completions:Print shell completion script to stdout' \
'man:Print a man page (roff format) to stdout' \
'setup:Interactive setup wizard — recommended for first-time configuration. Walks you through picking a provider, validating each field as you type, and optionally testing the connection before saving' \
'advanced-config:Open the raw config file in \$EDITOR. If \$EDITOR is unset, prints the path. For power users who'\''d rather edit TOML by hand than use the wizard' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'pixforge help commands' commands "$@"
}
(( $+functions[_pixforge__subcmd__help__subcmd__advanced-config_commands] )) ||
_pixforge__subcmd__help__subcmd__advanced-config_commands() {
    local commands; commands=()
    _describe -t commands 'pixforge help advanced-config commands' commands "$@"
}
(( $+functions[_pixforge__subcmd__help__subcmd__completions_commands] )) ||
_pixforge__subcmd__help__subcmd__completions_commands() {
    local commands; commands=()
    _describe -t commands 'pixforge help completions commands' commands "$@"
}
(( $+functions[_pixforge__subcmd__help__subcmd__config-path_commands] )) ||
_pixforge__subcmd__help__subcmd__config-path_commands() {
    local commands; commands=()
    _describe -t commands 'pixforge help config-path commands' commands "$@"
}
(( $+functions[_pixforge__subcmd__help__subcmd__help_commands] )) ||
_pixforge__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'pixforge help help commands' commands "$@"
}
(( $+functions[_pixforge__subcmd__help__subcmd__init_commands] )) ||
_pixforge__subcmd__help__subcmd__init_commands() {
    local commands; commands=()
    _describe -t commands 'pixforge help init commands' commands "$@"
}
(( $+functions[_pixforge__subcmd__help__subcmd__man_commands] )) ||
_pixforge__subcmd__help__subcmd__man_commands() {
    local commands; commands=()
    _describe -t commands 'pixforge help man commands' commands "$@"
}
(( $+functions[_pixforge__subcmd__help__subcmd__profile_commands] )) ||
_pixforge__subcmd__help__subcmd__profile_commands() {
    local commands; commands=(
'show:Print resolved profile fields. The api_key is never printed; only the env var name and a status (set | empty | unset)' \
    )
    _describe -t commands 'pixforge help profile commands' commands "$@"
}
(( $+functions[_pixforge__subcmd__help__subcmd__profile__subcmd__show_commands] )) ||
_pixforge__subcmd__help__subcmd__profile__subcmd__show_commands() {
    local commands; commands=()
    _describe -t commands 'pixforge help profile show commands' commands "$@"
}
(( $+functions[_pixforge__subcmd__help__subcmd__profiles_commands] )) ||
_pixforge__subcmd__help__subcmd__profiles_commands() {
    local commands; commands=()
    _describe -t commands 'pixforge help profiles commands' commands "$@"
}
(( $+functions[_pixforge__subcmd__help__subcmd__setup_commands] )) ||
_pixforge__subcmd__help__subcmd__setup_commands() {
    local commands; commands=()
    _describe -t commands 'pixforge help setup commands' commands "$@"
}
(( $+functions[_pixforge__subcmd__init_commands] )) ||
_pixforge__subcmd__init_commands() {
    local commands; commands=()
    _describe -t commands 'pixforge init commands' commands "$@"
}
(( $+functions[_pixforge__subcmd__man_commands] )) ||
_pixforge__subcmd__man_commands() {
    local commands; commands=()
    _describe -t commands 'pixforge man commands' commands "$@"
}
(( $+functions[_pixforge__subcmd__profile_commands] )) ||
_pixforge__subcmd__profile_commands() {
    local commands; commands=(
'show:Print resolved profile fields. The api_key is never printed; only the env var name and a status (set | empty | unset)' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'pixforge profile commands' commands "$@"
}
(( $+functions[_pixforge__subcmd__profile__subcmd__help_commands] )) ||
_pixforge__subcmd__profile__subcmd__help_commands() {
    local commands; commands=(
'show:Print resolved profile fields. The api_key is never printed; only the env var name and a status (set | empty | unset)' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'pixforge profile help commands' commands "$@"
}
(( $+functions[_pixforge__subcmd__profile__subcmd__help__subcmd__help_commands] )) ||
_pixforge__subcmd__profile__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'pixforge profile help help commands' commands "$@"
}
(( $+functions[_pixforge__subcmd__profile__subcmd__help__subcmd__show_commands] )) ||
_pixforge__subcmd__profile__subcmd__help__subcmd__show_commands() {
    local commands; commands=()
    _describe -t commands 'pixforge profile help show commands' commands "$@"
}
(( $+functions[_pixforge__subcmd__profile__subcmd__show_commands] )) ||
_pixforge__subcmd__profile__subcmd__show_commands() {
    local commands; commands=()
    _describe -t commands 'pixforge profile show commands' commands "$@"
}
(( $+functions[_pixforge__subcmd__profiles_commands] )) ||
_pixforge__subcmd__profiles_commands() {
    local commands; commands=()
    _describe -t commands 'pixforge profiles commands' commands "$@"
}
(( $+functions[_pixforge__subcmd__setup_commands] )) ||
_pixforge__subcmd__setup_commands() {
    local commands; commands=()
    _describe -t commands 'pixforge setup commands' commands "$@"
}

if [ "$funcstack[1]" = "_pixforge" ]; then
    _pixforge "$@"
else
    compdef _pixforge pixforge
fi
