#compdef komodo

# Komodo: commercial UCI chess engine; pure UCI on stdin, no CLI flags
local curcontext="$curcontext" ret=1

# Komodo accepts UCI on stdin. Some builds support a bench mode.
_arguments -C \
  '1:command:(bench)' \
  '*:UCI option / book / EGTB path:_files' && ret=0

return ret
