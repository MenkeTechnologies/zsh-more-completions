#compdef qodem

# Zsh completion for qodem — terminal BBS client

local curcontext="$curcontext" ret=1

_arguments -s -S \
  '(-h --help)'{-h,--help}'[help]' \
  '*:script:_files' && ret=0

return ret
