#compdef hopm
# _hopm — Hybrid Open Proxy Monitor IRC services daemon (ircd-hybrid/hopm)
# upstream: https://github.com/ircd-hybrid/hopm
# Flags from src/main.c (getopt "dc:").

_hopm() {
    _arguments \
        '-c+[config file]:file:_files -g "*.conf"' \
        '-d[run as daemon]'
}

_hopm "$@"
