#compdef mcp-server-puppeteer
# Documentation: https://github.com/modelcontextprotocol/servers-archived/tree/main/src/puppeteer
# Reference accessed: 2026-05-27
# Verified: upstream src/puppeteer/index.ts (no CLI args; reads optional PUPPETEER_LAUNCH_OPTIONS, ALLOW_DANGEROUS, DOCKER_CONTAINER env vars)

# Zsh completion for mcp-server-puppeteer - archived MCP server for headless browser automation
# Accepts no CLI flags. Configuration via PUPPETEER_LAUNCH_OPTIONS, ALLOW_DANGEROUS, DOCKER_CONTAINER env vars.

local ret=1

_message 'no arguments (set PUPPETEER_LAUNCH_OPTIONS, ALLOW_DANGEROUS, DOCKER_CONTAINER env vars)' && ret=0

return ret
