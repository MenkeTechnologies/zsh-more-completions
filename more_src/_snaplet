#compdef snaplet
# Documentation: https://github.com/snaplet
# Reference accessed: 2026-05-21
# Verified: upstream docs at the URL above.
# STUB: snaplet's standalone CLI was deprecated (project pivoted to
# @snaplet/seed via npx); upstream no longer publishes a stable flag set.
# Existing file was someone's leaked .zshrc, not a completion. Reset.

local curcontext="$curcontext" state line ret=1
_arguments -s -S '*:file:_files' && ret=0
return ret
