#compdef plymouth-list-fonts
# Documentation: (not a real upstream tool)
# Reference accessed: 2026-05-16
# Verified: searched freedesktop plymouth source and Debian/Fedora
# package contents - no `plymouth-list-fonts` binary exists. The
# plymouth project ships `plymouth-set-default-theme`, `plymouthd`,
# `plymouth`. Conservative stub retained per "do not delete files"
# directive.
# Removed from prior file: fabricated -h/--help/-V/--version flags and
# bogus man.archlinux.org search URL.

# plymouth-list-fonts: not a real upstream binary
local curcontext="$curcontext" ret=1
_arguments -C \
  '*:arg:_files' && ret=0
return ret
