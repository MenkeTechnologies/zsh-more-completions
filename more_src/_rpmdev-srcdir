#compdef rpmdev-srcdir
# Documentation: (not in upstream rpmdevtools)
# Reference accessed: 2026-05-16
# Verified: no `rpmdev-srcdir` binary in upstream rpmdevtools. The
# canonical way to print the source directory is
# `rpm --eval '%{_sourcedir}'`. Conservative stub retained per
# "do not delete files" directive.
# Removed from prior file: fabricated -h/--help/-V/--version flags and
# bogus man.archlinux.org search URL.

# rpmdev-srcdir: not a known upstream binary (use `rpm --eval`)
local curcontext="$curcontext" ret=1
_arguments -C && ret=0
return ret
