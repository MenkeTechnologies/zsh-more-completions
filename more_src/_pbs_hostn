#compdef pbs_hostn
# PBS: report the hostname and address information for a host

local ret=1

_arguments -s \
  '-v[turn on verbose mode]' \
  '--version[print PBS version information and exit]' \
  '*:host:_hosts' && ret=0

return ret
