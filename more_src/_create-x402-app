#compdef create-x402-app
# Documentation: https://www.npmjs.com/package/create-x402-app
# Reference accessed: 2026-05-23
# Verified: npm view create-x402-app bin; npm readme Command Line Arguments section.

# Zsh completion for create-x402-app - x402 Mantle payment-enabled app scaffolder

local curcontext="$curcontext" state line ret=1

_arguments -C \
  '--fullstack[scaffold fullstack Next.js + API template]' \
  '--backend[scaffold backend-only API template]' \
  '--express[use Express backend framework]' \
  '--hono[use Hono backend framework]' \
  '--bun[use Bun as package manager]' \
  '--pnpm[use pnpm as package manager]' \
  '--yarn[use yarn as package manager]' \
  '--skip-install[skip dependency installation]' \
  ':project name:' && ret=0

return ret
