#compdef pv_import
# Csound utility to convert a comma-separated text file to a .pvx file

local ret=1

_arguments -s -S \
  ':cstext file:_files' \
  ':pv file:_files -g "*.pvx"' && ret=0

return ret
