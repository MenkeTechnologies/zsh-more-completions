#compdef arq
# Documentation: https://arq-docs.helpmanual.io/
# Reference accessed: 2026-05-17
# Verified: arq-docs.helpmanual.io (python-arq/arq, async job queue).
# Removed from prior file: bogus man.archlinux.org search URL;
# fabricated -v/--verbose flag (not in upstream — arq has no verbose flag);
# missing --custom-log-dict.

# Completion for arq - Python async job queue CLI

local curcontext="$curcontext" state line ret=1

_arguments -s -S \
  '(- *)'{-h,--help}'[show help]' \
  '(- *)--version[show version]' \
  '--burst[burst mode, exit when queue is empty]' \
  '--check[health check]' \
  '--watch=[watch directory for changes and reload]:directory:_directories' \
  '--custom-log-dict=[custom logging config]:file:_files' \
  '1:worker settings module (e.g. app.WorkerSettings)' && ret=0

return ret
