#compdef cronboard

# antoniorodr/cronboard — Textual TUI; `main()` runs CronBoard().run() (no argparse CLI in 0.5.x).

local ret=1
_arguments -s -S \
  '(-h --help)'{-h,--help}'[help (stdlib/terminal; may exit immediately)]' \
  && ret=0
return ret
