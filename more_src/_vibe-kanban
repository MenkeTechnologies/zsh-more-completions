#compdef vibe-kanban vibe-kanban-cli
# Documentation: https://github.com/BloopAI/vibe-kanban
# Reference accessed: 2026-05-21
# Verified: upstream docs at the URL above.
# README documents the invocation `npx vibe-kanban` and the runtime
# environment variables PORT, BACKEND_PORT, FRONTEND_PORT, HOST,
# MCP_HOST, MCP_PORT, DISABLE_WORKTREE_CLEANUP, VK_ALLOWED_ORIGINS,
# VK_SHARED_API_BASE, VK_SHARED_RELAY_API_BASE, VK_TUNNEL. The README
# does not document any CLI subcommands or flags, so this is a thin
# completion that accepts files / passes through to the wrapper.
# STUB: upstream README documents env vars only; no CLI flag set.

local curcontext="$curcontext" state line ret=1
_arguments -s -S '*:file:_files' && ret=0
return ret
