#compdef codeium
# Documentation: https://github.com/Exafunction/codeium
# Reference accessed: 2026-05-17
# Verified: upstream docs at the URL above.

# Codeium (now Windsurf) ships several binaries with this name; the public
# repo does not document a stable user-facing CLI. Conservative stub —
# defer to `codeium --help` for the variant on the current PATH.

local ret=1

_arguments -s -S \
  '(- *)'{-h,--help}'[show help]' \
  '(- *)--version[show version]' \
  '*::arg:_files' && ret=0

return ret
