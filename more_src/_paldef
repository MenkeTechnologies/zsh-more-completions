#compdef paldef
# Documentation: https://starlink.eao.hawaii.edu/devdocs/sun95.htx/sun95ss133.html
# KAPPA: loads the default palette to a colour table

local ret=1

_arguments -s \
  'DEVICE=[graphics device to receive the palette]:device' && ret=0

return ret
