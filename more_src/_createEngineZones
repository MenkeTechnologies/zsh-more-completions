#compdef createEngineZones
# Documentation: https://cfd.direct/openfoam/user-guide/
# Source: OpenFOAM/OpenFOAM-12 applications/utilities/mesh/manipulation/createEngineZones — add cylinderHead/pistonBowl zones to engine mesh (C++).
# Reference accessed: 2026-06-10

local curcontext="$curcontext" state line ret=1
typeset -A opt_args

_arguments -s \
    '-help[show help]' \
    '-doc[show documentation]' \
    '-srcDoc[show source documentation]' \
    '-case+[OpenFOAM case directory]:dir:_files -/' \
    '-fileHandler+[file handler]:handler:(uncollated collated masterUncollated)' \
    '-parallel[run in parallel]' \
    '-noFunctionObjects[disable function objects]' \
    '-cylinderHead[create cylinderHead pointZone]' \
    '-pistonBowl[create piston pointZone]' \
    '-region+[region name]:region:_default' \
    && ret=0

return ret
