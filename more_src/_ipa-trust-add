#compdef ipa-trust-add
# Documentation: (subcommand of ipa CLI, not a standalone binary)
# Reference accessed: 2026-05-16
# Verified: no standalone manpage in freeipa upstream. `ipa-trust-add` is the
# subcommand `ipa trust-add` of the main IPA CLI tool.

# ipa-trust-add: subcommand of ipa CLI (use `ipa trust-add`)
local curcontext="$curcontext" ret=1
_arguments -C \
  '*::extra args:_files' && ret=0
return ret
