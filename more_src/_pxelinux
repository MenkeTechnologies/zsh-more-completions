#compdef pxelinux

# PXELINUX: network boot loader (uses tftp boot directory)
local curcontext="$curcontext" ret=1
_arguments -C \
  '*::PXE boot args (typically not invoked directly):_files' && ret=0
return ret
