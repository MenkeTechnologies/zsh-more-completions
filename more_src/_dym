#compdef dym
# Documentation: https://man.archlinux.org/search?q=dym
# Reference accessed: 2026-05-16
# Source: https://github.com/hisbaan/didyoumean
# Release asset: dym-1.1.4-x86_64-apple-darwin.tar.gz
# Type: maintainer-shipped (GitHub release)
# Harvested: 2026-05-15

autoload -U is-at-least

_dym() {
    typeset -A opt_args
    typeset -a _arguments_options
    local ret=1

    if is-at-least 5.2; then
        _arguments_options=(-s -S -C)
    else
        _arguments_options=(-s -C)
    fi

    local context curcontext="$curcontext" state line
    _arguments "${_arguments_options[@]}" \
'-n+[Change the number of matches printed]:NUMBER: ' \
'--number=[Change the number of matches printed]:NUMBER: ' \
'-l+[Select the desired language using the locale code (en, fr, sp, etc.)]:LANG: ' \
'--lang=[Select the desired language using the locale code (en, fr, sp, etc.)]:LANG: ' \
'-h[Print help information]' \
'--help[Print help information]' \
'-V[Print version information]' \
'--version[Print version information]' \
'-c[Print clean output]' \
'--clean-output[Print clean output]' \
'-v[Print verbose output]' \
'--verbose[Print verbose output]' \
'-y[Yank (copy) to the system cliboard]' \
'--yank[Yank (copy) to the system cliboard]' \
'--print-langs[Display a list of supported languages]' \
'--update-langs[Update all language files]' \
'::search-term:' \
&& ret=0
}

(( $+functions[_dym_commands] )) ||
_dym_commands() {
    local commands; commands=()
    _describe -t commands 'dym commands' commands "$@"
}

_dym "$@"
