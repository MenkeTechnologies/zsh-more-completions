#compdef lockbook
# Source: nixpkgs (https://github.com/NixOS/nixpkgs)
# Package: lockbook
# Type: maintainer-shipped (nixpkgs)
# Harvested: 2026-05-15
function _lockbook {
    comp_output=$( lockbook complete zsh "$(($CURRENT - 1))" "${words[*]}" )
    eval ${comp_output}
}

