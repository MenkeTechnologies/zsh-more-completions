#compdef create-next-auth-app
# Documentation: https://www.npmjs.com/package/create-next-auth-app
# Reference accessed: 2026-05-24
# Verified: npx create-next-auth-app --help.

# Zsh completion for create-next-auth-app - NextAuth.js app scaffolder

local curcontext="$curcontext" state line ret=1

_arguments -C \
  '(-V --version)'{-V,--version}'[show version]' \
  '--src-dir[initialize inside src/ directory]' \
  '--import-alias=[import alias]:alias:' \
  '--use-npm[use npm as package manager]' \
  '--use-pnpm[use pnpm as package manager]' \
  '--use-yarn[use Yarn as package manager]' \
  '--use-bun[use Bun as package manager]' \
  '(-h --help)'{-h,--help}'[show help]' \
  ':project directory:_directories' && ret=0

return ret
