#compdef nsssd-switch
# _nsssd-switch — skarnet nsssd-switch (round-robin NSS backend chooser: each `bitfield backend... ""` group adds backends for a subset of nss requests; backends are themselves nsssd-* binaries)
# upstream: https://github.com/skarnet/nsss
# Source: src/nsssd/nsssd-switch.c on tip; positional only.

_nsssd-switch() {
    _arguments '*::bitfield-and-backends:'
}

_nsssd-switch "$@"
