#compdef pg_visibility
# Documentation: https://www.postgresql.org/docs/current/pgvisibility.html
# Reference accessed: 2026-05-20
# Verified: upstream docs at the URL above.
# STUB: pg_visibility is a PostgreSQL contrib SQL extension (visibility-map
# inspection functions), not a CLI binary. No canonical command-line interface
# exists. Provided as a minimal placeholder so #compdef matching does not error.

local curcontext="$curcontext" ret=1
_arguments -C \
  '*:argument:' && ret=0
return ret
