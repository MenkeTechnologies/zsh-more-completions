#compdef idx
# Documentation: https://idx.dev/
# Reference accessed: 2026-05-18
# Verified: upstream docs at the URL above.

# idx: Google Project IDX CLI (cloud development environment).

_idx() {
    local line
    local -a subcmds

    subcmds=(
        'restart:Rebuilds and restarts your environment'
        'completion:Generate the autocompletion script for the specified shell'
        'help:Help about any command'
    )

    _arguments -C \
        '(-h --help)'{-h,--help}'[Display help for the command]' \
        '1: :->cmds' \
        '*::arg:->args'

    case $state in
        cmds)
            _describe -t commands 'idx commands' subcmds
            ;;
        args)
            case $line[1] in
                help)
                    _describe -t commands 'idx commands' subcmds
                    ;;
            esac
            ;;
    esac
}

_idx "$@"
