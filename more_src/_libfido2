#compdef libfido2

# libfido2 package: provides fido2-token, fido2-cred, fido2-assert
# (which are already covered by their own completions). Top-level
# package entry kept for consistency.
local curcontext="$curcontext" ret=1
_arguments -C \
  '*::libfido2 args:_files' && ret=0
return ret
