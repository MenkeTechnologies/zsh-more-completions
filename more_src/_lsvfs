#compdef lsvfs
# Documentation: man lsvfs (macOS / FreeBSD)
# Reference accessed: 2026-05-19
# Verified: local `man lsvfs` on macOS 26.4 (FreeBSD 2.0+).
# STUB — upstream manpage documents zero flags; lsvfs only takes
# optional vfsname arguments.

# lsvfs: list installed virtual file systems (optional vfsname args)
_arguments -s -S \
    '*:filesystem type:(apfs hfs msdos nfs devfs autofs)'
