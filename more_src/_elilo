#compdef elilo

# ELILO: EFI Linux boot loader (older systems, esp. IA64)
local curcontext="$curcontext" ret=1
_arguments -C \
  '*::elilo args (typically not invoked directly):_files' && ret=0
return ret
