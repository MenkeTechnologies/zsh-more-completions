#compdef het_import
# Csound utility to convert a comma-separated text file to a .het file

local ret=1

_arguments -s -S \
  ':cstext file:_files' \
  ':het file:_files -g "*.het"' && ret=0

return ret
