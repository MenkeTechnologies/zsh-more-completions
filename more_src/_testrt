#compdef testrt
# Documentation: https://gaussian.com/testrt/
# Validate a Gaussian route specification without running a calculation

local ret=1

_arguments -s -S \
  '-m[memory to allocate (must precede other arguments)]:amount' \
  '*:route specification (quoted, e.g. "# rhf/sto-3g")' && ret=0

return ret
