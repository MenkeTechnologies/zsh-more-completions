#compdef der2ascii

# Zsh completion for der2ascii (DER → PEM text)

local curcontext="$curcontext" ret=1

_arguments -s -S \
  '(-h --help)'{-h,--help}'[show help]' \
  '*:DER input:_files' && ret=0

return ret
