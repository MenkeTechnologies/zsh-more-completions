#compdef gauopt
# Documentation: https://gaussian.com/gauopt/
# Optimize arbitrary parameters by repeated Gaussian runs (reads template from stdin)

local ret=1

_arguments -s -S \
  '-m[memory to allocate (must precede other arguments)]:amount' && ret=0

return ret
