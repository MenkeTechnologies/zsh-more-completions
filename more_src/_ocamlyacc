#compdef ocamlyacc
# Documentation: ocamlyacc usage
# Source: OCaml ocamlyacc — LALR(1) parser generator.
# Reference accessed: 2026-06-10

local curcontext="$curcontext" state line ret=1
typeset -A opt_args

_arguments -s \
    '-v[generate description file]' \
    '--strict[treat conflicts as errors]' \
    '-q[no informational messages]' \
    '-b+[output filename prefix (default same as input)]:file_prefix:' \
    '1:OCaml parser source:_files -g "*.mly"' \
    && ret=0

return ret
