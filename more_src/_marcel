#compdef marcel
# Documentation: https://marceltheshell.org/
# Reference accessed: 2026-05-17
# Verified: geophile/marcel upstream — Python-object pipeline shell.
# main.py treats `sys.argv[1]` as a script path or workspace name;
# there is no documented top-level flag surface.
# Removed from prior file: bogus man.archlinux.org search URL;
# fabricated -h/--help, -v/--version, -c/--command, -e/--execute,
# --dill (none exist on the marcel REPL launcher).

local ret=1
_arguments -s -S \
  '1:script or workspace:_files -g "*.py(-.)"' && ret=0
return ret
