#compdef docker-credential-pass docker-credential-secretservice docker-credential-osxkeychain docker-credential-wincred
# _docker-credential-pass — docker credential helper protocol (docker/docker-credential-helpers)
# upstream: https://github.com/docker/docker-credential-helpers
# Actions from credentials/credentials.go.

_docker-credential-pass() {
    local -a args
    args=(
        '(- *)'{-h,--help}'[show help]'
        '(- *)'{-v,--version}'[show version]'
        '1:action:(store get erase list version)'
    )
    _arguments $args
}

_docker-credential-pass "$@"
