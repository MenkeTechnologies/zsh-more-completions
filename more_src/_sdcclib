#compdef sdcclib
# Documentation: https://sdcc.sourceforge.net/doc/sdccman.pdf
# Reference accessed: 2026-05-21
# Verified: sdccman.pdf references sdcclib historically; current SDCC tree
# (trunk/sdcc/support/) no longer contains a sdcclib subdirectory — sdar has
# replaced it for archive operations. Could not verify a stable upstream flag
# set for the legacy sdcclib binary.
# STUB: legacy SDCC library manager, modern replacement is sdar; specific
# flag set could not be verified against current upstream source.

local curcontext="$curcontext" state line ret=1
_arguments -s -S '*:file:_files -g "*.(lib|rel|o)"' && ret=0
return ret
