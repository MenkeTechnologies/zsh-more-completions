#compdef proguardgui
# Documentation: https://www.guardsquare.com/en/products/proguard/manual/gui
# Reference accessed: 2026-05-16
# Verified: Guardsquare ProGuard GUI docs. Invoked as
# `java -jar proguardgui.jar [config.pro]`. Source code shows extra
# arguments are ignored; pure GUI.
# Removed from prior file: fabricated -h/--help/-V/--version flags and
# bogus man.archlinux.org search URL.

# proguardgui: ProGuard configuration GUI (no CLI flags)
local curcontext="$curcontext" ret=1
_arguments -C \
  '*:config file:_files -g "*.pro(-.)"' && ret=0
return ret
