#compdef btrfs-snap

local curcontext="$curcontext" ret=1

_arguments -C \
  '(- *)-h[show help]' \
  '-r[read-only snapshot]' \
  '-b+[snapshot subdirectory]:dir:_files -/' \
  '-c[create snapshot]' \
  '-d[delete oldest]' \
  '*:path:_files -/' && ret=0

return ret
