#compdef wordle
# Documentation: none found
# Reference accessed: 2026-05-21
# Verified: upstream docs at the URL above.
# STUB: many `wordle` implementations exist (CLI clones of the NYT game) with
# differing flag sets; no canonical upstream binary identifiable.

local curcontext="$curcontext" state line ret=1
_arguments -s -S \
  '(- *)'{-h,--help}'[show help]' \
  '*:file:_files' && ret=0

return ret
