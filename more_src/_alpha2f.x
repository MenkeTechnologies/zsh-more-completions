#compdef alpha2f.x
# alpha2f.x — Quantum ESPRESSO Eliashberg spectral function alpha^2 F via tetrahedron method (reads input from standard input: alpha2f.x < input)

# alpha2f.x is a serial post-processing utility that reads its input from stdin
# (e.g. "alpha2f.x < input"); it takes no command-line flags.
_files
