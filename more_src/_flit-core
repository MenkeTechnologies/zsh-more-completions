#compdef flit-core
# Documentation: https://flit.pypa.io/en/stable/
# Reference accessed: 2026-05-17
# Verified: PyPA flit-core is a PEP 517/660 BUILD BACKEND LIBRARY
# (`flit_core.buildapi`), NOT a CLI binary. The user-facing CLI
# is `flit` (separate package). This completion exists as a stub
# only since some shell environments expect the file. For real
# use, install `flit` and invoke `flit init/build/install/publish`.
# Removed from prior file: bogus man.archlinux.org search URL;
# fabricated build-sdist/build-wheel/prepare-metadata subcommands
# (those are Python module hook function names, not CLI verbs).

local ret=1
_message 'flit-core is a build backend library, not a CLI; use `flit` instead' && ret=0
return ret
