#compdef typioca
# Documentation: https://github.com/bloznelis/typioca
# Reference accessed: 2026-05-16
# Verified: bloznelis/typioca README. typioca is a TUI typing test
# configured via TOML; only known subcommand is `serve` (SSH server).
# No documented --help/--version flags upstream.
# Removed from prior file: bogus man.archlinux.org search URL;
# fabricated -h/--help flag.

# typioca: TUI typing speed test (Charmbracelet bubble tea)
local curcontext="$curcontext" ret=1
_arguments -C \
  '1:command:(serve)' && ret=0
return ret
