#compdef kwatch
# Documentation: https://github.com/abahmed/kwatch
# Reference accessed: 2026-05-19
# Verified: upstream docs at the URL above.
# STUB: kwatch is a Kubernetes operator/daemon deployed via Helm or kubectl
# manifests. The binary is configured entirely via a config.yaml file and
# exposes no CLI flags. Removed from prior file: invented --help, -n
# --namespace, -k --kubeconfig, -A --all-namespaces, --context flags.

local curcontext="$curcontext" ret=1
_arguments -C \
  '*::arg:_default' && ret=0
return ret
