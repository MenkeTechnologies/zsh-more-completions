#compdef DelphesROOT
# Documentation: https://delphes.github.io/workbook/quick-tour/
# DelphesROOT - Delphes fast detector simulation reading ROOT input

local ret=1

_arguments -s -S \
  '1:config card (Tcl):_files -g "*.(tcl|dat)"' \
  '2:output file (ROOT):_files -g "*.root"' \
  '*:input file (ROOT):_files -g "*.root"' && ret=0

return ret
