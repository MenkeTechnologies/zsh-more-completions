#compdef yosay

local -a arguments

arguments=(
    '--maxLength[Max number of characters per line]:length'
    '--help[Show help]'
    '--version[Show version]'
    '*:message text:'
)

_arguments -s -S $arguments
