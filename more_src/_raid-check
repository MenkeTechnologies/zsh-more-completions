#compdef raid-check

# raid-check: mdadm scrub helper (Fedora/RHEL)
local curcontext="$curcontext" ret=1
_arguments -C \
  '*::raid-check (start|stop|status|enable|disable):_files' && ret=0
return ret
