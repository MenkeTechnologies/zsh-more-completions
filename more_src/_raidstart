#compdef raidstart

# raidstart: start RAID arrays defined in /etc/raidtab (deprecated)
local curcontext="$curcontext" ret=1
_arguments -C \
  '-a[start all arrays]' \
  '-c[config file]:file:_files' \
  '*:RAID device:_files' && ret=0
return ret
