#compdef ipa-nis-manage
# Documentation: (no upstream manpage)
# Reference accessed: 2026-05-16
# Verified: no .1 manpage exists in current freeipa upstream tree.
# Tool kept per "do not delete files" directive with minimal stub.

# ipa-nis-manage: not present in current upstream FreeIPA
local curcontext="$curcontext" ret=1
_arguments -C \
  '(- *)'{-h,--help}'[show help]' \
  '*::extra args:_files' && ret=0
return ret
