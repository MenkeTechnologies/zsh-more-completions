#compdef rpmdev-sourcesum
# Documentation: (not in upstream rpmdevtools)
# Reference accessed: 2026-05-16
# Verified: no `rpmdev-sourcesum` binary in upstream rpm-software-
# management/rpmdevtools or rsrchboy mirror. Source checksum support
# is provided by rpmdev-md5 / rpmdev-sha* (see _rpmdev-md5).
# Conservative stub retained per "do not delete files" directive.
# Removed from prior file: fabricated -h/--help/-V/--version flags and
# bogus man.archlinux.org search URL.

# rpmdev-sourcesum: not a known upstream binary (see rpmdev-md5/sha*)
local curcontext="$curcontext" ret=1
_arguments -C \
  '*:spec file:_files -g "*.spec(-.)"' && ret=0
return ret
