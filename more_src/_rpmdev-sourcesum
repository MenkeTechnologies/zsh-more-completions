#compdef rpmdev-sourcesum
# Documentation: none found
# Reference accessed: 2026-05-21
# Verified: no `rpmdev-sourcesum` in rsrchboy/rpmdevtools. Source checksum
# support is provided by rpmdev-md5 / rpmdev-sha* (see _rpmdev-md5).
# STUB: not a real upstream tool; keep file for compat.

# rpmdev-sourcesum: not a known upstream binary (see rpmdev-md5/sha*)
local curcontext="$curcontext" ret=1
_arguments -C \
  '*:spec file:_files -g "*.spec(-.)"' && ret=0
return ret
