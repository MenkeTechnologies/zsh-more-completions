#compdef storcli64

# storcli64: 64-bit StorCLI variant (same interface as storcli)
local curcontext="$curcontext" ret=1
_arguments -C \
  '-h[show help]' \
  '-v[show version]' \
  '*:storcli64 command (e.g. /cX show, /cX/eX/sX show all):_files' && ret=0
return ret
