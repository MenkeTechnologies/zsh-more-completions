#compdef dbus-daemon-launch-helper
# Documentation: source-direct extraction from upstream jdebp/nosh
# source/dbus-daemon-launch-helper - the nosh replacement is a
# `#!/bin/sh` script (NOT a binary - decoded from the file's first
# line) that dispatches on $DBUS_STARTER_BUS_TYPE between system
# and session bus startup with no CLI flags or positionals.
# Reference accessed: 2026-05-30 via gh api.
# jdebp/nosh dbus-helper R132c — single-binary harvest of the
# nosh dbus-daemon launch wrapper. Counterpart to D-Bus's own
# setuid `dbus-daemon-launch-helper`, but here implemented as a
# privilege-dropping shell script invoked from a nosh service
# definition (the nosh implementation is NOT set-uid - per the
# upstream source comment "this script does NOT need to be
# set-UID/set-GID to anything").

_arguments -s -S \
  ': :_default'
