#compdef crate-admin
# Documentation: https://github.com/crate/crate-admin
# Reference accessed: 2026-05-17
# Verified: upstream is the CrateDB admin UI (web), not a CLI binary.
# Stubbed: no documented command-line interface.
_crate-admin() {
  _files
}

if [ "$funcstack[1]" = "_crate-admin" ]; then
    _crate-admin "$@"
else
    compdef _crate-admin crate-admin
fi
