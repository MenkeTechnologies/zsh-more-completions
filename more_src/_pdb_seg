#compdef pdb_seg
# Documentation: http://www.bonvinlab.org/pdb-tools/
# Modifies the segment identifier column of a PDB file

local ret=1

_arguments -s -S \
  '(-h --help)'{-h,--help}'[show help]' \
  '-[new segment id, e.g. -A]:segment id' \
  '*:PDB file:_files' && ret=0

return ret
