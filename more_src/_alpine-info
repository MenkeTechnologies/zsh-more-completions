#compdef alpine-info
# Documentation: (no upstream binary by this name)
# Reference accessed: 2026-05-16
# Verified: no upstream docs accessible. Alpine Linux uses `apk info` (not
# `alpine-info`). Tool kept per "do not delete files" directive with minimal stub.

# alpine-info: unknown/unverifiable upstream
local curcontext="$curcontext" ret=1
_arguments -C \
  '*::extra args:_files' && ret=0
return ret
