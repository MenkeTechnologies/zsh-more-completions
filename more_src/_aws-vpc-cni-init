#compdef aws-vpc-cni-init
# _aws-vpc-cni-init — AWS VPC CNI init container (configures sysctls + drops CNI plugin into HOST_CNI_BIN_PATH)
# upstream: https://github.com/aws/amazon-vpc-cni-k8s
# Source: cmd/aws-vpc-cni-init/main.go on tip; no CLI flags — configuration via env vars.
# Env vars: HOST_CNI_BIN_PATH, AWS_VPC_K8S_CNI_CONFIGURE_RPFILTER, DISABLE_TCP_EARLY_DEMUX.

_aws-vpc-cni-init() {
    _arguments \
        '(- *)'{-h,--help}'[show help]'
}

_aws-vpc-cni-init "$@"
