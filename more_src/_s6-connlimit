#compdef s6-connlimit
# Documentation: source-direct extraction from upstream skarnet/s6
# src/conn-tools/s6-connlimit.c main() (no subgetopt; pure env +
# argv[1..] chain-loader).
# Reference accessed: 2026-05-30 via gh api.
# Skarnet s6 connlimit R122c — environment-driven connection-count
# guard, chain-loads prog if under limit. Reads $PROTO, then queries
# $PROTOCONNNUM and $PROTOMAX from the UCSPI server environment to
# decide whether the current peer has exceeded its concurrent
# connection allowance. No CLI options.

_arguments -s -S \
  '*:program and arguments:_command_names -e'
