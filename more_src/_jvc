#compdef jvc
# Documentation: (Microsoft J++ - Windows-only, discontinued ~2001)
# Reference accessed: 2026-05-16
# Verified: jvc is the Microsoft Visual J++ compiler for Windows,
# discontinued around 2001 after the Microsoft/Sun lawsuit. No
# upstream Unix CLI documentation. Conservative stub retained per
# "do not delete files" directive.
# Removed from prior file: fabricated -h/--help/-V/--version flags and
# bogus man.archlinux.org search URL.

# jvc: Microsoft Visual J++ compiler (Windows-only, abandoned)
local curcontext="$curcontext" ret=1
_arguments -C \
  '*:Java source:_files -g "*.java(-.)"' && ret=0
return ret
