#compdef wf-config
# Documentation: https://github.com/WayfireWM/wf-config (README)
# Reference accessed: 2026-05-16
# Verified: wf-config is a library (.so), not a standalone CLI binary.
# Tool kept per "do not delete files" directive with minimal stub.

# wf-config: Wayfire config library (not a CLI)
local curcontext="$curcontext" ret=1
_arguments -C \
  '*::extra args:_files' && ret=0
return ret
