#compdef plyr
# Documentation: (ambiguous - no known CLI under this name)
# Reference accessed: 2026-05-16
# Verified: there is no upstream CLI binary named `plyr`. Candidates:
# (a) libglyr's CLI is `glyrc` (not plyr); (b) PyPI `plyr` is a
# Python pandas-like library with no CLI; (c) `plyr` on JS is an
# HTML5 media player library with no CLI. Likely confusion with
# `glyrc`. Conservative stub retained per "do not delete files"
# directive.
# Removed from prior file: fabricated -h/--help/-V/--version flags and
# bogus man.archlinux.org search URL.

# plyr: not a known upstream binary (see glyrc)
local curcontext="$curcontext" ret=1
_arguments -C \
  '*:arg:_files' && ret=0
return ret
