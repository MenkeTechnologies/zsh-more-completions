#compdef hyprland-workspaces-tui
# Source: nixpkgs (https://github.com/NixOS/nixpkgs)
# Package: hyprland-workspaces-tui
# Type: maintainer-shipped (nixpkgs)
# Harvested: 2026-05-15

autoload -U is-at-least

_hyprland-workspaces-tui() {
    typeset -A opt_args
    typeset -a _arguments_options
    local ret=1

    if is-at-least 5.2; then
        _arguments_options=(-s -S -C)
    else
        _arguments_options=(-s -C)
    fi

    local context curcontext="$curcontext" state line
    _arguments "${_arguments_options[@]}" : \
'-m+[Monitor name passed to hyprland-workspaces. ALL by default]:MONITOR:_default' \
'--monitor=[Monitor name passed to hyprland-workspaces. ALL by default]:MONITOR:_default' \
'-c+[Path to config file. Default config path is \$XDG_CONFIG_HOME/hyprland-workspaces-tui/config.toml]:CONFIG_PATH:_default' \
'--config-path=[Path to config file. Default config path is \$XDG_CONFIG_HOME/hyprland-workspaces-tui/config.toml]:CONFIG_PATH:_default' \
'-s+[Show special workspaces]:SHOW_SPECIAL:(true false)' \
'--show-special=[Show special workspaces]:SHOW_SPECIAL:(true false)' \
'-b+[Use big text]:BIG_TEXT:(true false)' \
'--big-text=[Use big text]:BIG_TEXT:(true false)' \
'--completions=[Generates completion scripts for the specified shell]:SHELL:(bash elvish fish powershell zsh)' \
'-d[Prints debug information to app.log file]' \
'--debug[Prints debug information to app.log file]' \
'-h[Print help]' \
'--help[Print help]' \
'-V[Print version]' \
'--version[Print version]' \
":: :_hyprland-workspaces-tui_commands" \
"*::: :->hyprland-workspaces-tui" \
&& ret=0
    case $state in
    (hyprland-workspaces-tui)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:hyprland-workspaces-tui-command-$line[1]:"
        case $line[1] in
            (plain)
_arguments "${_arguments_options[@]}" : \
'-c+[Use carriage return to override update line]:CARRIAGE_RETURN:(true false)' \
'--carriage-return=[Use carriage return to override update line]:CARRIAGE_RETURN:(true false)' \
'-p+[]:PRINT_ONCE:(true false)' \
'--print-once=[]:PRINT_ONCE:(true false)' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_hyprland-workspaces-tui__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:hyprland-workspaces-tui-help-command-$line[1]:"
        case $line[1] in
            (plain)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
}

(( $+functions[_hyprland-workspaces-tui_commands] )) ||
_hyprland-workspaces-tui_commands() {
    local commands; commands=(
'plain:Just print workspaces to stdout' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'hyprland-workspaces-tui commands' commands "$@"
}
(( $+functions[_hyprland-workspaces-tui__help_commands] )) ||
_hyprland-workspaces-tui__help_commands() {
    local commands; commands=(
'plain:Just print workspaces to stdout' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'hyprland-workspaces-tui help commands' commands "$@"
}
(( $+functions[_hyprland-workspaces-tui__help__help_commands] )) ||
_hyprland-workspaces-tui__help__help_commands() {
    local commands; commands=()
    _describe -t commands 'hyprland-workspaces-tui help help commands' commands "$@"
}
(( $+functions[_hyprland-workspaces-tui__help__plain_commands] )) ||
_hyprland-workspaces-tui__help__plain_commands() {
    local commands; commands=()
    _describe -t commands 'hyprland-workspaces-tui help plain commands' commands "$@"
}
(( $+functions[_hyprland-workspaces-tui__plain_commands] )) ||
_hyprland-workspaces-tui__plain_commands() {
    local commands; commands=()
    _describe -t commands 'hyprland-workspaces-tui plain commands' commands "$@"
}

if [ "$funcstack[1]" = "_hyprland-workspaces-tui" ]; then
    _hyprland-workspaces-tui "$@"
else
    compdef _hyprland-workspaces-tui hyprland-workspaces-tui
fi
