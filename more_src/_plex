#compdef plex
# Documentation: (no canonical CLI named `plex`)
# Reference accessed: 2026-05-20
# Verified: no upstream binary literally named `plex` ships with Plex Media
# Server. The real binary is `Plex Media Scanner` (with spaces) and uses
# different flags (--scan, --refresh, --section, -d/--directory, -p, -v, -x).
# Conservative stub retained per do-not-delete-files directive.
# STUB
# Removed from prior file: fabricated start/stop/restart subcommands, --token/
# --server flags, and bogus man.archlinux.org search URL.

local curcontext="$curcontext" ret=1
_arguments -C \
  '*:arg:_default' && ret=0
return ret
