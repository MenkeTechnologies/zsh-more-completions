#compdef keda
# Documentation: https://keda.sh/docs/latest/deploy/
# Reference accessed: 2026-05-19
# Verified: upstream docs at the URL above.

# STUB: KEDA (Kubernetes Event-driven Autoscaling) has no canonical
# standalone `keda` CLI binary. It is deployed via Helm, YAML manifests
# or the Operator Hub (see keda.sh/docs/latest/deploy/). The prior file
# invented install/uninstall/upgrade subcommands and flags that are not
# part of any documented upstream `keda` command; removed them.
local curcontext="$curcontext" ret=1
_arguments -C \
  '*:argument:_files' && ret=0
return ret
