#compdef smbprint

# smbprint: print to a remote SMB printer (CUPS filter / standalone)
local curcontext="$curcontext" ret=1
_arguments -C \
  '*:CUPS filter args (jobid user title copies options [file]):_files' && ret=0
return ret
