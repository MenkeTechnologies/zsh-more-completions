#compdef nvidia-docker

# nvidia-docker: legacy CLI wrapper around docker exposing NVIDIA GPUs
local curcontext="$curcontext" ret=1
_arguments -C \
  '(- *)-h[show help]' \
  '*::docker args:_files' && ret=0
return ret
