#compdef zookeeper-server

# zookeeper-server: ZooKeeper server control script
local curcontext="$curcontext" ret=1
_arguments -C \
  '(- *)-h[show help]' \
  '1:action:(start stop restart status upgrade print-cmd help)' \
  '*::zookeeper-server args:_files' && ret=0
return ret
