#compdef xfs_check

local curcontext="$curcontext" state line ret=1

_arguments -C \
  '-f[image is regular file]' \
  '-l+[xfs log device]:dev:_files -W /dev' \
  '-s[print only filesystem status]' \
  '-v[verbose output]' \
  '*:device or image:_files' && ret=0

return ret
