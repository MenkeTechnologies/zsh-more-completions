#compdef knative-kafka-controller
# _knative-kafka-controller — Knative Eventing Kafka Broker control-plane (sharedmain)
# upstream: https://github.com/knative-extensions/eventing-kafka-broker
# Flags from control-plane/cmd/kafka-controller/main.go + knative.dev/pkg sharedmain on tip.

_knative-kafka-controller() {
    _arguments \
        '(- *)'{-h,--help}'[show help]' \
        '--disable-controllers=[csv of disabled controllers]:csv:' \
        '--disable-ha=[disable high-availability mode]:bool:(true false)' \
        '--kubeconfig=[kubeconfig file]:file:_files' \
        '-v[klog verbosity level]:n:'
}

_knative-kafka-controller "$@"
