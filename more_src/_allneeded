#compdef allneeded
# Documentation: https://man.archlinux.org/man/allneeded.1.en
# allneeded - force the calculation of all fonts needed by DVI files

local ret=1

_arguments -s \
  '-r[use dvired instead of dvips]' \
  '*:DVI file:_files -g "*.dvi(-.)"' && ret=0

return ret
