#compdef local-user-authenticator
# _local-user-authenticator — Pinniped local-user-authenticator webhook (in-cluster authenticator backed by Secret-stored bcrypt hashes; demo/test only, not for production)
# upstream: https://github.com/vmware-tanzu/pinniped
# Source: internal/localuserauthenticator/localuserauthenticator.go on tip; no CLI flags — runs as a TokenReview webhook in the `local-user-authenticator` namespace and reads user/password from a Secret.

_local-user-authenticator() {
    _arguments \
        '(- *)'{-h,--help}'[show help]'
}

_local-user-authenticator "$@"
