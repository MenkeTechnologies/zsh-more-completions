#compdef continue
# Documentation: https://github.com/continuedev/continue
# Reference accessed: 2026-05-17
# Verified: upstream docs at the URL above.
#
# Note: the Continue project ships a CLI named `cn`, not `continue`.
# No upstream binary named `continue` exists; this stub avoids shipping
# fabricated subcommands.  See `_cn` (continuedev/continue) for the real CLI.

_default
