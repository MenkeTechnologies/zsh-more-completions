#compdef virt-firmware
# Documentation: https://www.mankier.com/1/virt-fw-vars
# Reference accessed: 2026-05-21
# Verified: upstream docs at the URL above.
# STUB: "virt-firmware" is a project umbrella (python3-virt-firmware
# package), not a binary. Real binaries are virt-fw-dump(1),
# virt-fw-vars(1), virt-fw-sigdb(1). Fall back to file completion in case
# a wrapper script is installed locally.

local curcontext="$curcontext" ret=1
_arguments -s -S '*:file:_files' && ret=0
return ret
