#compdef rpmdev-sortpkgs
# Documentation: (not in upstream rpmdevtools)
# Reference accessed: 2026-05-16
# Verified: searched upstream rpm-software-management/rpmdevtools and
# rsrchboy mirror - no `rpmdev-sortpkgs` binary. `rpmdev-sort` exists
# (numeric package version sort). Conservative stub retained per
# "do not delete files" directive.
# Removed from prior file: fabricated -h/--help/-V/--version flags and
# bogus man.archlinux.org search URL.

# rpmdev-sortpkgs: not a known upstream binary (see rpmdev-sort)
local curcontext="$curcontext" ret=1
_arguments -C \
  '*:file:_files' && ret=0
return ret
