#compdef glaurung
# Documentation: https://www.glaurungchess.com/
# Reference accessed: 2026-05-16
# Verified: Glaurung 2.x is a UCI engine driven entirely by UCI commands
# on stdin (predecessor to Stockfish). No CLI flag table.
# Removed from prior file: fabricated -uci/-xboard/-version flags
# (Glaurung is auto-UCI on stdin) and bogus man.archlinux.org search URL.

# glaurung: UCI chess engine (stdin-driven)
local curcontext="$curcontext" ret=1
_arguments -C \
  ':no-flags (UCI stdin interactive engine):' && ret=0
return ret
