#compdef acpi-call

# acpi_call: send ACPI method calls (laptop hotkey/battery hacks)
local curcontext="$curcontext" ret=1
_arguments -C \
  '*:ACPI method path and args (passed via /proc/acpi/call):_files' && ret=0
return ret
