#compdef crashlog
# Documentation: not found as standalone binary; "crashlog" is an LLDB script command
# Reference accessed: 2026-05-17
# Verified: no standalone "crashlog" CLI located via WebSearch; the name is used
# by an LLDB script (lldb.macosx.crashlog) loaded inside lldb, not a CLI binary.
_crashlog() {
  _files
}

if [ "$funcstack[1]" = "_crashlog" ]; then
    _crashlog "$@"
else
    compdef _crashlog crashlog
fi
