#compdef mapdump
# CCP4: produce an ASCII representation of sections from a binary map file

local ret=1

_arguments -s -S \
  '(-h --help)'{-h,--help}'[show help]' \
  'MAPIN[input binary map file to dump]:map:_files -g "*.(map|ccp4|msk)"' \
  '*:keyword:(RUN GO END)' && ret=0

return ret
