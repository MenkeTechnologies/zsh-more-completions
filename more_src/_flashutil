#compdef flashutil

# flashutil: generic vendor-specific firmware flash utility
local curcontext="$curcontext" ret=1
_arguments -C \
  '-h[show help]' \
  '-v[verbose]' \
  '*:firmware image file:_files' && ret=0
return ret
