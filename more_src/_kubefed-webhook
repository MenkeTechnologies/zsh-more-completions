#compdef kubefed-webhook
# _kubefed-webhook — KubeFed v1beta1 admission webhook server (validates KubeFedConfig + FederatedTypeConfig + ServicePlacement resources)
# upstream: https://github.com/kubernetes-sigs/kubefed
# Flags from cmd/webhook/app/webhook.go on tip.

_kubefed-webhook() {
    _arguments \
        '(- *)'{-h,--help}'[show help]' \
        '--version[print webhook version]' \
        '--kubeconfig=[path to kubeconfig (out-of-cluster only)]:file:_files' \
        '--master=[Kubernetes API server URL (overrides kubeconfig)]:url:_urls' \
        '--cert-dir=[directory holding the TLS certs]:dir:_files -/' \
        '--secure-port=[HTTPS port]:port:' \
        '-v[klog verbosity level]:n:' \
        '--logtostderr=[log to stderr instead of files]:bool:(true false)' \
        '--stderrthreshold=[stderr threshold]:level:(INFO WARNING ERROR FATAL)' \
        '--vmodule=[csv of pattern=N for verbose logging]:csv:'
}

_kubefed-webhook "$@"
