#compdef uu-tac
# Documentation: https://uutils.github.io/coreutils/docs/utils/tac.html
# Reference accessed: 2026-05-21
# Verified: upstream docs at the URL above.
# Source: Homebrew bottle
# Formula: uutils-coreutils
# Type: maintainer-shipped (Homebrew)
# Harvested: 2026-05-15

autoload -U is-at-least

_uu-tac() {
    typeset -A opt_args
    typeset -a _arguments_options
    local ret=1

    if is-at-least 5.2; then
        _arguments_options=(-s -S -C)
    else
        _arguments_options=(-s -C)
    fi

    local context curcontext="$curcontext" state line
    _arguments "${_arguments_options[@]}" : \
'-s+[use STRING as the separator instead of newline]:STRING:_default' \
'--separator=[use STRING as the separator instead of newline]:STRING:_default' \
'-b[attach the separator before instead of after]' \
'--before[attach the separator before instead of after]' \
'-r[interpret the sequence as a regular expression]' \
'--regex[interpret the sequence as a regular expression]' \
'-h[Print help]' \
'--help[Print help]' \
'-V[Print version]' \
'--version[Print version]' \
'::file:_files' \
&& ret=0
}

(( $+functions[_uu-tac_commands] )) ||
_uu-tac_commands() {
    local commands; commands=()
    _describe -t commands 'uu-tac commands' commands "$@"
}

if [ "$funcstack[1]" = "_uu-tac" ]; then
    _uu-tac "$@"
else
    compdef _uu-tac uu-tac
fi
