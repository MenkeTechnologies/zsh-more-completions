#compdef accounts-daemon
# _accounts-daemon — D-Bus user-account management daemon (accountsservice)
# upstream: https://gitlab.freedesktop.org/accountsservice/accountsservice
# Flags from src/main.c (GOptionEntry entries[]).

_accounts-daemon() {
    _arguments \
        '--version[output version information and exit]' \
        '--replace[replace the existing instance]' \
        '--debug[enable debugging code]'
}

_accounts-daemon "$@"
