#compdef isolinux

# ISOLINUX: ISO 9660 boot loader (used inside boot CDs/DVDs)
local curcontext="$curcontext" ret=1
_arguments -C \
  '*::ISOLINUX args (typically not directly invoked):_files' && ret=0
return ret
