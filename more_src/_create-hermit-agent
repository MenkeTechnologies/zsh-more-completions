#compdef create-hermit-agent
# Documentation: https://www.npmjs.com/package/create-hermit-agent
# Reference accessed: 2026-05-23
# Verified: npm view create-hermit-agent bin; npm readme Host selection section.

# Zsh completion for create-hermit-agent - Telegram agent harness scaffolder

local curcontext="$curcontext" state line ret=1

_arguments -C   '--host=[AI host CLI to embed]:host:(claude codex)'   ':agent name:' && ret=0

return ret
