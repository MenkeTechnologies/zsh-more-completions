#compdef create-janus
# Documentation: https://www.npmjs.com/package/create-janus
# Reference accessed: 2026-05-24
# Verified: npm view create-janus bin readme; npx create-janus --help output.

# Zsh completion for create-janus - AWS CDK + React scaffold CLI

local curcontext="$curcontext" state line ret=1

_arguments -C   '--description=[project description]:text:'   '--region=[AWS region]:region:(eu-central-1 us-east-1 us-west-2)'   '--github[create GitHub repo and push]'   '--connection-arn=[CodeStar connection ARN for CI/CD]:arn:'   '--role-arn=[IAM role ARN to assume for deployment]:arn:'   '--external-id=[external ID for role assumption]:id:'   '--deploy[deploy to AWS after scaffolding]'   '(-y --yes)'{-y,--yes}'[skip all confirmation prompts]'   '(-V --version)'{-V,--version}'[output version]'   '(-h --help)'{-h,--help}'[display help]'   ':project name:' && ret=0

return ret
