#compdef clevis-encrypt-http

# clevis encrypt http: encrypt data using a remote HTTP key server
local curcontext="$curcontext" ret=1
_arguments -C \
  '*:JSON-formatted config (url=...) on stdin, plaintext on stdin:' && ret=0
return ret
