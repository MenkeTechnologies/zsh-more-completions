#compdef dejagnu

local curcontext="$curcontext" state line ret=1
local -a commands

commands=(
  'help:show help'
  'report-card:produce report card'
  'runtest:run dejagnu test suite'
)

_arguments -C \
  '(- *)'{-h,--help}'[show help]' \
  '(- *)'{-V,--version}'[show version]' \
  '1: :->cmd' \
  '*::arg:->args' && ret=0

case $state in
  (cmd) _describe -t cmds 'dejagnu command' commands && ret=0 ;;
esac

return ret
