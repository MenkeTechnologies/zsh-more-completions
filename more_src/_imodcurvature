#compdef imodcurvature
# Documentation: https://bio3d.colorado.edu/imod/doc/man/imodcurvature.html
# Measure radius of curvature of model objects

local ret=1

_arguments -s \
  '(-h -help)'{-h,-help}'[show help]' \
  '(-in -i -InputFile)'{-in,-i,-InputFile}'[Name of input model file. If it is not entered with this...]:file:_files' \
  '-OutputFile[Name of output model file. If it is not entered with this]:file:_files' \
  '(-wl -w -WindowLength)'{-wl,-w,-WindowLength}'[Length in pixels over which to fit a circle to the contour...]:value' \
  '(-min -mi -MinimumWindowLength)'{-min,-mi,-MinimumWindowLength}'[Minimum length in pixels over which to fit a circle to the...]:value' \
  '(-zr -z -ZRangeToFit)'{-zr,-z,-ZRangeToFit}'[Range of contours in Z to include for fitting to a sphere....]:value' \
  '(-cy -CylinderSearchAngle)'{-cy,-CylinderSearchAngle}'[If this option is entered, points will be fit to a cylinder]:value' \
  '(-rc -RadiusCriterion)'{-rc,-RadiusCriterion}'[Lower and upper criteria for radius of curvature, in pixels.]:value' \
  '(-fc -f -FitCriterion)'{-fc,-f,-FitCriterion}'[Criterion for root mean squared error of the local fit to a...]:value' \
  '(-ob -o -ObjectsToDo)'{-ob,-o,-ObjectsToDo}'[List of objects to encode]:value' \
  '(-ro -RotateToXYPlane)'{-ro,-RotateToXYPlane}'[Rotate contours that are not all in one X/Y plane into the...]' \
  '(-st -StoreValues)'{-st,-StoreValues}'[Store the radius of curvature values directly in the model.]' \
  '(-kappa -k -KappaValues)'{-kappa,-k,-KappaValues}'[Store curvature values, which are the inverse of the radius...]' \
  '(-ps -PointSize)'{-ps,-PointSize}'[Use spherical point size to encode radius of curvature. Each]' \
  '(-sy -SymbolZoom)'{-sy,-SymbolZoom}'[Use circular symbols to encode radius of curvature, where...]:value' \
  '(-co -Color)'{-co,-Color}'[Use this entry to specify one or more colors to change...]:value' \
  '(-di -d -DivideRange)'{-di,-d,-DivideRange}'[When multiple colors are entered, by default they will be]' \
  '(-pa -UsePalette)'{-pa,-UsePalette}'[Use a 256-color false color palette for the colors. Enter a]:file:_files' \
  '(-sa -SampleSpacing)'{-sa,-SampleSpacing}'[Spacing of sample points along contour for fitting (default...]:value' \
  '(-me -MeanStored)'{-me,-MeanStored}'[Use this option to get an output of the mean value stored...]:value' \
  '(-ve -v -Verbose)'{-ve,-v,-Verbose}'[The verbose output will show the number of points being...]' \
  '(-tc -TestCircleFits)'{-tc,-TestCircleFits}'[Test circle fitting by fitting a circle to each contour in]' \
  '(-ts -TestSphereFits)'{-ts,-TestSphereFits}'[Test sphere fitting by making a new object with the points...]:value' \
  '(-si -SignedValues)'{-si,-SignedValues}'[If &quot;+&quot;, radii and curvatures will be stored as...]:value' \
  '-StandardInput[read parameters from standard input]' \
  '-usage[print usage]' \
  '*:file:_files' && ret=0

return ret
