#compdef hpacucli

# HP Array Configuration Utility CLI (legacy P-series controllers)
local curcontext="$curcontext" ret=1
_arguments -C \
  '-h[show help]' \
  '*:hpacucli command (e.g. controller all show, ctrl slot=N show config):_files' && ret=0
return ret
