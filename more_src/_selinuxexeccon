#compdef selinuxexeccon

# selinuxexeccon: print the SELinux exec context for a command/role
local curcontext="$curcontext" ret=1
_arguments -C \
  '*:command [from-context]:_command_names -e' && ret=0
return ret
