#compdef impacket-sniff
# Documentation: https://github.com/fortra/impacket/blob/master/examples/sniff.py
# Simple packet sniffer that captures and decodes packets via pcap

local ret=1

_arguments -s -S \
  '(-h --help)'{-h,--help}'[show help]' \
  '*:BPF filter expression (e.g. "tcp port 80"):' && ret=0

return ret
