#compdef clean
# Documentation: no single canonical upstream — `clean` is a generic name.
# Reference accessed: 2026-05-17
# Verified: stub. Likely a user shell function or one of many tools named clean.
# The Clean (Concurrent Clean) compiler uses `clm`, not `clean`.

local ret=1

_arguments -s -S \
  '(-h --help)'{-h,--help}'[show help]' \
  '*::arg:_files' && ret=0

return ret
