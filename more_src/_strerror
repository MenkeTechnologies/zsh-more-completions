#compdef strerror
# Documentation: none found
# Reference accessed: 2026-05-21
# Verified: upstream docs at the URL above.
# STUB: strerror(3) is a C library function, not a CLI command.
# No authoritative upstream documentation for a `strerror(1)` binary
# could be located; prior -h/-l flags were unverified.

local curcontext="$curcontext" state line ret=1
_arguments -s -S '*:file:_files' && ret=0
return ret
