#compdef retty

local curcontext="$curcontext" ret=1

_arguments -C \
  '(- *)-h[show help]' \
  '-v[verbose]' \
  '-l[list ttys]' \
  '1:pid:_pids' \
  '*:tty:_files -g "/dev/{tty,pts/*}"' && ret=0

return ret
