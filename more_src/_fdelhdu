#compdef fdelhdu
# Delete an extension from a FITS file

local ret=1

_arguments -s \
  "infile=[input FITS file and extension]:file:_files" \
  "confirm=[prompt for confirmation]:bool:(yes no)" \
  "proceed=[proceed with deletion]:bool:(yes no)" \
  "*:FITS file:_files" && ret=0

return ret
