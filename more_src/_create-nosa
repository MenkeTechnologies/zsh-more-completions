#compdef create-nosa
# Documentation: https://www.npmjs.com/package/create-nosa
# Reference accessed: 2026-05-23
# Verified: npm view create-nosa bin; npm readme Non-interactive Usage table.

# Zsh completion for create-nosa - Bun-first TanStack Start project scaffolder

local curcontext="$curcontext" state line ret=1

_arguments -C   '(-n --name)'{-n,--name=}'[project folder name]:name:'   '(-t --template)'{-t,--template=}'[template]:template:(start)'   '(-s --structure)'{-s,--structure=}'[codebase structure]:structure:(simple vertical)'   '(-a --addons)'{-a,--addons=}'[comma-separated add-ons]:addons:(shadcn drizzle betterauth google-oauth)'   '--bun[use Bun as the package runner]'   '(-h --help)'{-h,--help}'[show help]'   ':project name:' && ret=0

return ret
