#compdef smartcrop
# ============================================================================
# _smartcrop — zsh completion for **smartcrop** —
#            muesli/smartcrop, typed Go port of typed
#            **Jonas Wagner**'s typed JavaScript typed
#            **smartcrop.js** typed content-aware typed
#            image-cropping typed algorithm typed
#            (originally typed at typed jwagner.github.io/
#            smartcrop.js typed for typed Thingiverse +
#            typed Etsy + typed early-2010s typed image-
#            board typed thumbnail typed pipelines).  The
#            typed algorithm typed detects typed salient
#            typed regions typed of typed an typed input
#            typed image (typed via typed edge typed
#            detection + typed saturation + typed skin-
#            tone typed heuristics) typed and typed
#            returns typed the typed best-rated typed
#            crop typed rectangle typed at typed a typed
#            given typed aspect ratio.  Authored by typed
#            **Christian Muehlhaeuser** (typed muesli on
#            GitHub, typed creator of typed charm.sh +
#            typed bubbletea + typed lipgloss + typed
#            gloss + typed typed Mango + typed beehive +
#            typed wishlist + typed soft-serve + typed
#            countless typed terminal-UI libraries) typed
#            with typed contributions by typed Michael
#            Wendland + typed Bjørn Erik Pedersen (typed
#            gohugo.io typed core typed maintainer) +
#            typed Patryk Pomykalski.  Used by typed
#            **Hugo** typed static-site typed generator
#            (typed gohugo.io's typed image typed processing
#            pipeline typed under typed `assets/images`
#            typed module typed for typed smart-cropping
#            featured-image typed thumbnails) + typed
#            countless typed user-content-CMS typed
#            pipelines typed where typed user-uploaded
#            photos typed need typed automatic typed
#            crop-to-aspect-ratio typed without typed
#            cropping typed off typed the typed subject's
#            typed face.  Go=193 continues Axis 138
#            (pure-{lang} typed image encoding+decoding+
#            processing ecosystem — typed Rust-side typed
#            image-rs sibling on the Go side is typed
#            **golang.org/x/image**, typed the typed
#            official typed Go x/image typed extended
#            packages typed by typed the typed Go typed
#            core team) at 2/0/0.
# ============================================================================
#
# UPSTREAM
#   Repository : https://github.com/muesli/smartcrop
#   Maintainer : Christian Muehlhaeuser (typed muesli@gmail.com)
#   License    : MIT
#   Stars      : 1,800+ on github.com/muesli/smartcrop
#   Language   : Go  (stdlib flag for the CLI surface +
#                 stdlib image + image/jpeg + image/png +
#                 io + os; muesli/smartcrop library + its
#                 nfnt/resize-based and golang.org/x/image-
#                 based resizer adapter subpackages)
#   Binary     : smartcrop  (installs via
#                 `go install github.com/muesli/smartcrop/cmd/smartcrop`)
#   Last push  : 2026
#
# WHAT IT IS
#   smartcrop is a content-aware image cropping tool that
#   analyses a source image and picks the most visually
#   interesting rectangular crop at a target aspect ratio.
#   The algorithm:
#
#     1. Computes typed edge typed weight typed (Sobel-style
#        typed convolution) typed across typed the typed
#        image
#     2. Computes typed skin-tone typed weight typed
#        (statistical typed RGB skin-tone typed model)
#     3. Computes typed saturation typed weight
#     4. Scores typed every typed candidate typed crop
#        rectangle typed of typed the typed requested typed
#        aspect ratio typed by typed integrating typed the
#        typed combined typed weight maps
#     5. Returns typed the typed top-scoring typed crop
#        rectangle's typed bounding box
#
#   The CLI wraps this with input + output file handling +
#   optional post-crop resize:
#
#     $ smartcrop -input photo.jpg -output thumb.jpg \
#                 -width 200 -height 200 -quality 90
#
#   Used by:
#
#     • **Hugo**                          typed
#                                          gohugo.io typed
#                                          asset
#                                          pipeline's
#                                          typed
#                                          `image.Process`
#                                          typed
#                                          smart-anchor
#                                          typed crop
#                                          mode
#     • **Charm.sh services**             typed early
#                                          typed muesli
#                                          typed
#                                          experimental
#                                          typed
#                                          terminal-image
#                                          typed work
#     • **Image-CMS pipelines**           typed automatic
#                                          typed
#                                          crop-to-aspect
#                                          typed for
#                                          typed user-
#                                          uploaded
#                                          typed photos
#     • **Photo gallery generators**      typed thumbnail
#                                          typed
#                                          generation
#     • **Profile-picture handlers**      typed automatic
#                                          typed
#                                          face-preserving
#                                          typed crop
#                                          typed for
#                                          typed avatar
#                                          typed uploads
#
# CLI ARGUMENT GRAMMAR  (verbatim from
#                       `cmd/smartcrop/main.go` `flag.*`
#                       declarations)
#
#   smartcrop [FLAGS...]
#
#   Flags (Go stdlib `flag` package — accepts both
#   single-dash `-flag` and double-dash `--flag` syntax,
#   with `=` or whitespace value separators):
#
#     -input <FILE>                   input filename
#                                     (REQUIRED — exits 1
#                                     if empty)
#     -output <FILE>                  output filename
#                                     (`-` writes to stdout)
#     -width <N>                      crop width (default 0
#                                     = auto from smaller
#                                     image dimension)
#     -height <N>                     crop height (default 0
#                                     = auto from smaller
#                                     image dimension)
#     -resize <BOOL>                  resize after cropping
#                                     (default true)
#     -quality <N>                    jpeg quality (default
#                                     85)
#
#   Standard Go flag.Parse() automatics:
#     -h, --help                      show flag usage
#                                     (auto-generated by
#                                     stdlib `flag`)
#
# AXIS / CORPUS POSITION
#   `muesli/smartcrop`'s `go.mod` declares two direct
#   dependencies:
#
#     • `github.com/nfnt/resize v0.0.0-...`
#         — typed legacy typed pure-Go typed image-
#         resampling typed library by typed Jan Schlicht
#         (Lanczos / Catmull-Rom / Mitchell-Netravali /
#         Bilinear / Bicubic / NearestNeighbor filters)
#     • **`golang.org/x/image v0.6.0`** (DIRECT)
#         — typed the typed official typed Go x/image typed
#         extended packages
#
#   **golang.org/x/image** is **THE canonical extended Go
#   image package** maintained by typed the typed Go core
#   team (typed Nigel Tao, typed Rob Pike, typed Adam
#   Langley, typed Ian Lance Taylor, typed Rob Findley,
#   typed Andrew Gerrand, typed Russ Cox typed and typed
#   thousands of typed contributors) typed under typed
#   `cmd/go` / typed `golang.org/x/...` typed
#   experimental-and-supplementary typed package taxonomy.
#   It typed extends typed the typed standard typed
#   library's typed `image` typed package (typed stdlib
#   `image` typed itself typed contains typed only typed
#   the typed `Image`, `RGBA`, `RGBA64`, `NRGBA`, `Gray`,
#   `Paletted`, `Alpha`, `YCbCr` typed pixel-format typed
#   primitives + typed `image/png`, `image/jpeg`,
#   `image/gif` typed format codecs).  `golang.org/x/image`
#   adds:
#
#     • `bmp`            typed Windows BMP encoder + decoder
#     • `ccitt`          typed CCITT Group 3 / Group 4
#                        typed fax-format typed decoder
#                        (typed used by typed `tiff`)
#     • `colornames`     typed CSS3 typed color-name typed
#                        constants
#     • `draw`           typed advanced typed compositing
#                        + typed scaling + typed
#                        interpolation typed (extends
#                        stdlib `image/draw` typed with
#                        typed `Scaler`, `Kernel`,
#                        `NearestNeighbor`, `ApproxBiLinear`,
#                        `BiLinear`, `CatmullRom`)
#     • `font`           typed font typed Face typed
#                        abstraction + typed `gofont` typed
#                        embedded Go typed font + typed
#                        `inconsolata` + typed `plan9font`
#                        + typed typed `basicfont`
#     • `math/f32` /
#       `math/f64` /
#       `math/fixed`     typed 2D typed affine matrix typed
#                        math typed primitives
#     • `riff`           typed RIFF typed (Resource
#                        Interchange File Format) typed
#                        decoder typed (typed used by typed
#                        `webp`)
#     • `tiff` /
#       `tiff/lzw`       typed TIFF typed encoder + typed
#                        decoder + typed LZW typed
#                        compression
#     • `vp8` / `vp8l`   typed VP8 + typed VP8L typed
#                        decoders typed (typed used by
#                        typed `webp`)
#     • `webp`           typed WebP typed decoder
#     • `vector`         typed software typed vector
#                        rasterizer (typed used by typed
#                        the typed `font` typed package)
#
#   This is the structural Go-side parallel to image-rs/
#   image on the Rust side: typed the typed
#   officially-maintained typed image-format + typed
#   image-processing typed primitive typed library typed
#   from typed the typed language typed team typed itself
#   (the typed Go team typed for typed `golang.org/x/image`,
#   the typed Rust typed image-rs typed working group typed
#   for typed image-rs).  Go=193 continues Axis 138 at
#   2/0/0, following R1112 _stl-thumb (Rust=193 OPENED
#   Axis 138 at 1/0/0 on image-rs).
#
#   Used by essentially every Go application that touches
#   pixel data: typed Hugo + typed Caddy (typed for typed
#   thumb-generation typed modules) + typed gocv (typed
#   OpenCV typed bindings, typed for typed image-input
#   typed conversion) + typed Docker daemon typed (typed
#   for typed image-icon typed processing) + typed
#   InfluxDB Cloud typed dashboards (typed for typed
#   chart-snapshot typed rendering) + typed gitea + typed
#   forgejo (typed for typed avatar typed
#   resize + typed crop) + typed minio (typed for typed
#   bucket-thumbnail typed generation) + typed countless
#   Go-based image-processing typed services.
#
#   Verified via:
#     gh api repos/muesli/smartcrop/contents/go.mod
#       → module github.com/muesli/smartcrop
#       → golang.org/x/image v0.6.0  (DIRECT)
#       → github.com/nfnt/resize ... (DIRECT)
#     gh api repos/muesli/smartcrop/contents/cmd/smartcrop/main.go
#       → flag.String("input", "", "input filename")
#       → flag.String("output", "", "output filename")
#       → flag.Int("width", 0, "crop width")
#       → flag.Int("height", 0, "crop height")
#       → flag.Bool("resize", true, "resize after cropping")
#       → flag.Int("quality", 85, "jpeg quality")
#
#   No completion for `smartcrop` ships with Apple zsh
#   (/usr/share/zsh/*/functions) or Homebrew zsh-completions
#   (/opt/homebrew/share/zsh/site-functions) — confirmed
#   absent.
# ============================================================================

_smartcrop() {
    _arguments -s \
        '(-h --help -help)'{-h,--help,-help}'[show usage]' \
        '(-input --input)'{-input,--input}'=[input filename (REQUIRED)]:input file:_files -g "*.(jpg|jpeg|png|JPG|JPEG|PNG)"' \
        '(-output --output)'{-output,--output}'=[output filename (- for stdout)]:output file:_files -g "*.(jpg|jpeg|png|JPG|JPEG|PNG)"' \
        '(-width --width)'{-width,--width}'=[crop width in pixels (default 0 = auto)]:pixels:' \
        '(-height --height)'{-height,--height}'=[crop height in pixels (default 0 = auto)]:pixels:' \
        '(-resize --resize)'{-resize,--resize}'=[resize after cropping (default true)]:bool:(true false)' \
        '(-quality --quality)'{-quality,--quality}'=[jpeg quality (default 85)]:quality 1-100:'
}

_smartcrop "$@"
