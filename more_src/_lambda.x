#compdef lambda.x
# Documentation: https://www.quantum-espresso.org/Doc/user_guide/
# lambda.x — Quantum ESPRESSO electron-phonon coupling lambda and Tc via McMillan formula (reads input from standard input: lambda.x < input)

# lambda.x is a serial post-processing utility that reads its input from stdin
# (e.g. "lambda.x < input"); it takes no command-line flags.
_files
