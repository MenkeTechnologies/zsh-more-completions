#compdef create-hasura-app
# Documentation: https://www.npmjs.com/package/create-hasura-app
# Reference accessed: 2026-05-24
# Verified: npm view create-hasura-app bin.

# Zsh completion for create-hasura-app - Hasura application scaffolder

local curcontext="$curcontext" state line ret=1

_arguments -C   ':project directory:_directories' && ret=0

return ret
