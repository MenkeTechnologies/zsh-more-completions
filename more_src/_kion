#compdef kion
# Documentation: https://github.com/kionsoftware/kion-cli
# Reference accessed: 2026-05-17
# Verified: upstream docs at the URL above.
# Source: https://github.com/kionsoftware/kion-cli/blob/c5281be16a57264eb8ef4ee3f53d8b06c00ad013/_kion
# Repository: kionsoftware/kion-cli (_kion)

_cli_zsh_autocomplete() {
  local -a opts
  local cur
  cur=${words[-1]}
  if [[ "$cur" == "-"* ]]; then
    opts=("${(@f)$(${words[@]:0:#words[@]-1} ${cur} --generate-bash-completion)}")
  else
    opts=("${(@f)$(${words[@]:0:#words[@]-1} --generate-bash-completion)}")
  fi

  if [[ "${opts[1]}" != "" ]]; then
    _describe 'values' opts
  else
    _files
  fi
}

