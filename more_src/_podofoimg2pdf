#compdef podofoimg2pdf
# convert images to PDF files

local ret=1

_arguments -s -S \
  '-useimgsize[use the image size as page size instead of A4]' \
  '*:image or output PDF:_files' && ret=0

return ret
