#compdef engine-pkcs11

# OpenSSL engine_pkcs11: bridge OpenSSL to PKCS#11 cryptoki tokens
local curcontext="$curcontext" ret=1
_arguments -C \
  '*::engine_pkcs11 args (typically loaded into openssl via -engine):_files' && ret=0
return ret
