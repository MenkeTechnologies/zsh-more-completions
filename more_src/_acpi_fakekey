#compdef acpi_fakekey

# acpi_fakekey: inject fake ACPI keypress event (acpid helper)
local curcontext="$curcontext" ret=1
_arguments -C \
  '(- *)-h[show help]' \
  '1:keycode (decimal):' && ret=0
return ret
