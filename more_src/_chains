#compdef chains
# Documentation: (no upstream binary by this name)
# Reference accessed: 2026-05-16
# Verified: Debian, Ubuntu and Arch package indexes return no package or
# binary named "chains". The intended target is unclear (the name
# collides with kernel `chains` from the legacy ipchains era, but that
# tool is `ipchains(8)`, not `chains`). Conservative stub retained per
# "do not delete files" directive.
# Removed from prior file: fabricated -h/--help/-V/--version flags and
# bogus man.archlinux.org search URL.

# chains: not a known upstream binary
local curcontext="$curcontext" ret=1
_arguments -C \
  '*:arg:_files' && ret=0
return ret
