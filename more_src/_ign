#compdef ign
# Documentation: https://gazebosim.org/docs
# Ignition command line tool dispatcher (deprecated alias for gz)

local ret=1

_arguments -s -S \
  '(-h --help)'{-h,--help}'[show help]' \
  '(-v --version)'{-v,--version}'[print version]' \
  '--force-version=[use a specific library version]:version' \
  '--commands[list all available commands]' \
  '--versions[show available versions for a command]' \
  '1:command:(gazebo sim topic service model log sdf fuel launch msg plugin)' \
  '*::args:_normal' && ret=0

return ret
