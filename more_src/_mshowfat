#compdef mshowfat

# mtools mshowfat: show MS-DOS FAT cluster chain of files
local curcontext="$curcontext" ret=1
_arguments -C \
  '-i[image file (instead of MS-DOS drive)]:file:_files' \
  '*:DOS file path (e.g. a:/file):' && ret=0
return ret
