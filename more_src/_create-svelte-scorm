#compdef create-svelte-scorm
# Documentation: https://www.npmjs.com/package/create-svelte-scorm
# Reference accessed: 2026-05-24
# Verified: npm view create-svelte-scorm bin; package dist source (--template/-t flags).

# Zsh completion for create-svelte-scorm - Svelte SCORM e-learning project scaffolder

local curcontext="$curcontext" state line ret=1

_arguments -C   '(-t --template)'{-t,--template}'[template to use]:template:'   '(-h --help)'{-h,--help}'[display help]'   ':project name:' && ret=0

return ret
