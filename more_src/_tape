#compdef tape

# Zsh completion for tape - TAP-producing test harness for Node.js

_arguments -s -S \
  '(- *)'{-h,--help}'[display help]' \
  '*'{-r,--require}'=[require module before tests]:module:' \
  '--ignore=[ignore files matching glob pattern]:glob pattern:' \
  '*:test files:_files -g "*.{js,mjs,cjs}"'
