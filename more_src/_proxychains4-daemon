#compdef proxychains4-daemon

local -a arguments

arguments=(
    '-a[Listen address]:address:'
    '-p[Listen port]:port:'
    '-d[Run as daemon]'
    '-h[Show help]'
    '*:config file:_files'
)

_arguments -s -S $arguments
