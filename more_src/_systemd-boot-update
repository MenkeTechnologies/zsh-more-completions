#compdef systemd-boot-update
# Update systemd-boot EFI boot manager

_arguments -s -S \
  '(-h --help)'{-h,--help}'[show help]' \
  '--version[show version]' \
  '--no-pager[do not pipe output into a pager]' \
  '--graceful[exit gracefully if not an EFI system]' \
  && return 0
