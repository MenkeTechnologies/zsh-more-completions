#compdef xdg-desktop-portal xdg-document-portal xdg-permission-store
# Documentation: https://flatpak.github.io/xdg-desktop-portal/docs/
# Reference accessed: 2026-05-17
# Verified: flatpak xdg-desktop-portal docs. D-Bus portal daemon with
# minimal CLI surface.
# Removed from prior file: bogus man.archlinux.org search URL.

_arguments -s \
  '(-v --verbose)'{-v,--verbose}'[verbose debug]' \
  '(-r --replace)'{-r,--replace}'[replace running instance]' \
  '(- *)--version[show version]'
