#compdef het_export
# Documentation: https://csound.com/docs/manual/
# Csound utility to convert a .het file to a comma-separated text file

local ret=1

_arguments -s -S \
  ':het file:_files -g "*.het"' \
  ':cstext file:_files' && ret=0

return ret
