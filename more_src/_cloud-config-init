#compdef cloud-config-init

# cloud-init helper: run config phase only (called by cloud-init.service)
local curcontext="$curcontext" ret=1
_arguments -C \
  '-h[show help]' \
  '--help[show help]' \
  '*::cloud-init args (typically run by systemd, not the user):_files' && ret=0
return ret
