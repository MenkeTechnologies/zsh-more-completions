#compdef iptcedit
# Documentation: (no upstream binary by this name)
# Reference accessed: 2026-05-16
# Verified: no `iptcedit` binary exists in any common distribution. The
# canonical libiptcdata CLI is `iptc(1)`
# (https://libiptcdata.sourceforge.net/docs/iptc-commandline.html), not
# `iptcedit`. This completion target appears to be a misnomer or vendored
# script. Conservative stub retained per "do not delete files" directive.
# Removed from prior file: fabricated -h/--help/-V/--version flags and
# bogus man.archlinux.org search URL.

# iptcedit: not a known upstream binary (see iptc(1) from libiptcdata)
local curcontext="$curcontext" ret=1
_arguments -C \
  '*:file:_files' && ret=0
return ret
