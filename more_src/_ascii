#compdef ascii
# Documentation: https://man.archlinux.org/man/ascii.1.en
# Reference accessed: 2026-05-17
# Verified: upstream docs at the URL above.

_arguments -s -S \
  -a'[vertical columns for table modes]' \
  -b'[emit binary-coded table]' \
  -d'[emit decimal-coded table]' \
  -o'[emit octal-coded table]' \
  -x'[emit hexadecimal-coded table]' \
  -h'[usage]' \
  -s'[script-friendly one-line names]' \
  -t'[terse output]' \
  -v'[version]' \
  '*::characters:'
