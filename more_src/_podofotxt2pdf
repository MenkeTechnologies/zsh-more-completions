#compdef podofotxt2pdf
# convert plain text files into PDF files

local ret=1

_arguments -s -S \
  '-fontname+[use the named font]:font name' \
  '-utf8[input text is UTF8 encoded instead of Windows ANSI]' \
  '*:file:_files' && ret=0

return ret
