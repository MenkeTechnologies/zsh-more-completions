#compdef clojurec
# Documentation: https://github.com/schani/clojurec
# Reference accessed: 2026-05-16
# Verified: name is ambiguous. `clojurec` historically refers to
# schani/clojurec (Clojure-to-C compiler, abandoned). Modern Clojure
# compilation uses `clojure -M -e "(compile 'ns)"` or tools.build.
# Conservative stub.
# Removed from prior file: fabricated -h/--help/-V/--version flags and
# bogus man.archlinux.org search URL.

# clojurec: abandoned Clojure-to-C compiler (see clojure -M)
local curcontext="$curcontext" ret=1
_arguments -C \
  '*:clojure file:_files -g "*.clj(-.)"' && ret=0
return ret
