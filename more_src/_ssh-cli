#compdef ssh-cli
# Documentation: https://github.com/daniloaguiarbr/ssh-cli
# Reference accessed: 2026-05-21
# Verified: upstream binary probed via "ssh-cli completions zsh"
# (clap-generated). Source: cargo binstall ssh-cli@0.3.2.

autoload -U is-at-least

_ssh-cli() {
    typeset -A opt_args
    typeset -a _arguments_options
    local ret=1

    if is-at-least 5.2; then
        _arguments_options=(-s -S -C)
    else
        _arguments_options=(-s -C)
    fi

    local context curcontext="$curcontext" state line
    _arguments "${_arguments_options[@]}" : \
'--lang=[Força o idioma da CLI (ex.\: \`pt-BR\`, \`en-US\`)]:LOCALE:_default' \
'--config-dir=[Override do diretório de configuração (útil para testes)]:DIR:_files' \
'--output-format=[Formato global de saída (text, json)]:OUTPUT_FORMAT:((text\:"Texto legível por humanos (padrão)"
json\:"JSON estruturado"))' \
'-v[Aumenta a verbosidade de logs em stderr]' \
'--verbose[Aumenta a verbosidade de logs em stderr]' \
'-q[Suprime output não-JSON (modo silencioso)]' \
'--quiet[Suprime output não-JSON (modo silencioso)]' \
'--no-color[Desativa cores no output]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'-V[Print version]' \
'--version[Print version]' \
":: :_ssh-cli_commands" \
"*::: :->ssh-cli" \
&& ret=0
    case $state in
    (ssh-cli)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:ssh-cli-command-$line[1]:"
        case $line[1] in
            (vps)
_arguments "${_arguments_options[@]}" : \
'--lang=[Força o idioma da CLI (ex.\: \`pt-BR\`, \`en-US\`)]:LOCALE:_default' \
'--config-dir=[Override do diretório de configuração (útil para testes)]:DIR:_files' \
'--output-format=[Formato global de saída (text, json)]:OUTPUT_FORMAT:((text\:"Texto legível por humanos (padrão)"
json\:"JSON estruturado"))' \
'-v[Aumenta a verbosidade de logs em stderr]' \
'--verbose[Aumenta a verbosidade de logs em stderr]' \
'-q[Suprime output não-JSON (modo silencioso)]' \
'--quiet[Suprime output não-JSON (modo silencioso)]' \
'--no-color[Desativa cores no output]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
":: :_ssh-cli__subcmd__vps_commands" \
"*::: :->vps" \
&& ret=0

    case $state in
    (vps)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:ssh-cli-vps-command-$line[1]:"
        case $line[1] in
            (add)
_arguments "${_arguments_options[@]}" : \
'--name=[Nome único da VPS]:NAME:_default' \
'--host=[Hostname ou IP]:HOST:_default' \
'--port=[Porta SSH]:PORT:_default' \
'--user=[Usuário SSH]:USER:_default' \
'--password=[Senha SSH]:PASSWORD:_default' \
'--timeout=[Timeout em milissegundos para comandos]:TIMEOUT:_default' \
'--max-chars=[Limite de caracteres por output (\`"none"\` ou \`"0"\` = ilimitado)]:MAX_CHARS:_default' \
'--sudo-password=[Senha para \`sudo\`]:SUDO_PASSWORD:_default' \
'--su-password=[Senha para \`su -\`]:SU_PASSWORD:_default' \
'--lang=[Força o idioma da CLI (ex.\: \`pt-BR\`, \`en-US\`)]:LOCALE:_default' \
'--config-dir=[Override do diretório de configuração (útil para testes)]:DIR:_files' \
'--output-format=[Formato global de saída (text, json)]:OUTPUT_FORMAT:((text\:"Texto legível por humanos (padrão)"
json\:"JSON estruturado"))' \
'-v[Aumenta a verbosidade de logs em stderr]' \
'--verbose[Aumenta a verbosidade de logs em stderr]' \
'-q[Suprime output não-JSON (modo silencioso)]' \
'--quiet[Suprime output não-JSON (modo silencioso)]' \
'--no-color[Desativa cores no output]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
'--lang=[Força o idioma da CLI (ex.\: \`pt-BR\`, \`en-US\`)]:LOCALE:_default' \
'--config-dir=[Override do diretório de configuração (útil para testes)]:DIR:_files' \
'--output-format=[Formato global de saída (text, json)]:OUTPUT_FORMAT:((text\:"Texto legível por humanos (padrão)"
json\:"JSON estruturado"))' \
'--json[Saída em JSON (útil para pipes)]' \
'-v[Aumenta a verbosidade de logs em stderr]' \
'--verbose[Aumenta a verbosidade de logs em stderr]' \
'-q[Suprime output não-JSON (modo silencioso)]' \
'--quiet[Suprime output não-JSON (modo silencioso)]' \
'--no-color[Desativa cores no output]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(remove)
_arguments "${_arguments_options[@]}" : \
'--lang=[Força o idioma da CLI (ex.\: \`pt-BR\`, \`en-US\`)]:LOCALE:_default' \
'--config-dir=[Override do diretório de configuração (útil para testes)]:DIR:_files' \
'--output-format=[Formato global de saída (text, json)]:OUTPUT_FORMAT:((text\:"Texto legível por humanos (padrão)"
json\:"JSON estruturado"))' \
'-y[Pula confirmação interativa (obrigatório em scripts e modo não-interativo)]' \
'--yes[Pula confirmação interativa (obrigatório em scripts e modo não-interativo)]' \
'-v[Aumenta a verbosidade de logs em stderr]' \
'--verbose[Aumenta a verbosidade de logs em stderr]' \
'-q[Suprime output não-JSON (modo silencioso)]' \
'--quiet[Suprime output não-JSON (modo silencioso)]' \
'--no-color[Desativa cores no output]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':nome -- Nome da VPS a remover:_default' \
&& ret=0
;;
(edit)
_arguments "${_arguments_options[@]}" : \
'--host=[Novo hostname/IP]:HOST:_default' \
'--port=[Nova porta SSH]:PORT:_default' \
'--user=[Novo usuário]:USER:_default' \
'--password=[Nova senha]:PASSWORD:_default' \
'--timeout=[Novo timeout]:TIMEOUT:_default' \
'--max-chars=[Novo limite de caracteres]:MAX_CHARS:_default' \
'--sudo-password=[Nova senha sudo]:SUDO_PASSWORD:_default' \
'--su-password=[Nova senha su]:SU_PASSWORD:_default' \
'--lang=[Força o idioma da CLI (ex.\: \`pt-BR\`, \`en-US\`)]:LOCALE:_default' \
'--config-dir=[Override do diretório de configuração (útil para testes)]:DIR:_files' \
'--output-format=[Formato global de saída (text, json)]:OUTPUT_FORMAT:((text\:"Texto legível por humanos (padrão)"
json\:"JSON estruturado"))' \
'-v[Aumenta a verbosidade de logs em stderr]' \
'--verbose[Aumenta a verbosidade de logs em stderr]' \
'-q[Suprime output não-JSON (modo silencioso)]' \
'--quiet[Suprime output não-JSON (modo silencioso)]' \
'--no-color[Desativa cores no output]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':nome -- Nome da VPS a editar:_default' \
&& ret=0
;;
(show)
_arguments "${_arguments_options[@]}" : \
'--lang=[Força o idioma da CLI (ex.\: \`pt-BR\`, \`en-US\`)]:LOCALE:_default' \
'--config-dir=[Override do diretório de configuração (útil para testes)]:DIR:_files' \
'--output-format=[Formato global de saída (text, json)]:OUTPUT_FORMAT:((text\:"Texto legível por humanos (padrão)"
json\:"JSON estruturado"))' \
'--json[Saída em JSON]' \
'-v[Aumenta a verbosidade de logs em stderr]' \
'--verbose[Aumenta a verbosidade de logs em stderr]' \
'-q[Suprime output não-JSON (modo silencioso)]' \
'--quiet[Suprime output não-JSON (modo silencioso)]' \
'--no-color[Desativa cores no output]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':nome -- Nome da VPS a exibir:_default' \
&& ret=0
;;
(path)
_arguments "${_arguments_options[@]}" : \
'--lang=[Força o idioma da CLI (ex.\: \`pt-BR\`, \`en-US\`)]:LOCALE:_default' \
'--config-dir=[Override do diretório de configuração (útil para testes)]:DIR:_files' \
'--output-format=[Formato global de saída (text, json)]:OUTPUT_FORMAT:((text\:"Texto legível por humanos (padrão)"
json\:"JSON estruturado"))' \
'-v[Aumenta a verbosidade de logs em stderr]' \
'--verbose[Aumenta a verbosidade de logs em stderr]' \
'-q[Suprime output não-JSON (modo silencioso)]' \
'--quiet[Suprime output não-JSON (modo silencioso)]' \
'--no-color[Desativa cores no output]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_ssh-cli__subcmd__vps__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:ssh-cli-vps-help-command-$line[1]:"
        case $line[1] in
            (add)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(remove)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(edit)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(show)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(path)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(connect)
_arguments "${_arguments_options[@]}" : \
'--lang=[Força o idioma da CLI (ex.\: \`pt-BR\`, \`en-US\`)]:LOCALE:_default' \
'--config-dir=[Override do diretório de configuração (útil para testes)]:DIR:_files' \
'--output-format=[Formato global de saída (text, json)]:OUTPUT_FORMAT:((text\:"Texto legível por humanos (padrão)"
json\:"JSON estruturado"))' \
'-v[Aumenta a verbosidade de logs em stderr]' \
'--verbose[Aumenta a verbosidade de logs em stderr]' \
'-q[Suprime output não-JSON (modo silencioso)]' \
'--quiet[Suprime output não-JSON (modo silencioso)]' \
'--no-color[Desativa cores no output]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':nome -- Nome da VPS previamente adicionada via `vps add`:_default' \
&& ret=0
;;
(exec)
_arguments "${_arguments_options[@]}" : \
'--password=[Override de senha SSH para esta execução]:PASSWORD:_default' \
'--timeout=[Override de timeout em milissegundos]:TIMEOUT:_default' \
'--lang=[Força o idioma da CLI (ex.\: \`pt-BR\`, \`en-US\`)]:LOCALE:_default' \
'--config-dir=[Override do diretório de configuração (útil para testes)]:DIR:_files' \
'--output-format=[Formato global de saída (text, json)]:OUTPUT_FORMAT:((text\:"Texto legível por humanos (padrão)"
json\:"JSON estruturado"))' \
'--json[Saída em JSON]' \
'-v[Aumenta a verbosidade de logs em stderr]' \
'--verbose[Aumenta a verbosidade de logs em stderr]' \
'-q[Suprime output não-JSON (modo silencioso)]' \
'--quiet[Suprime output não-JSON (modo silencioso)]' \
'--no-color[Desativa cores no output]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':vps_nome -- Nome da VPS previamente adicionada via `vps add`:_default' \
':comando -- Comando shell a executar:_default' \
&& ret=0
;;
(sudo-exec)
_arguments "${_arguments_options[@]}" : \
'--password=[Override de senha SSH para esta execução]:PASSWORD:_default' \
'--sudo-password=[Override de senha sudo para esta execução]:SUDO_PASSWORD:_default' \
'--timeout=[Override de timeout em milissegundos]:TIMEOUT:_default' \
'--lang=[Força o idioma da CLI (ex.\: \`pt-BR\`, \`en-US\`)]:LOCALE:_default' \
'--config-dir=[Override do diretório de configuração (útil para testes)]:DIR:_files' \
'--output-format=[Formato global de saída (text, json)]:OUTPUT_FORMAT:((text\:"Texto legível por humanos (padrão)"
json\:"JSON estruturado"))' \
'--json[Saída em JSON]' \
'-v[Aumenta a verbosidade de logs em stderr]' \
'--verbose[Aumenta a verbosidade de logs em stderr]' \
'-q[Suprime output não-JSON (modo silencioso)]' \
'--quiet[Suprime output não-JSON (modo silencioso)]' \
'--no-color[Desativa cores no output]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':vps_nome -- Nome da VPS previamente adicionada via `vps add`:_default' \
':comando -- Comando shell a executar com privilégios sudo:_default' \
&& ret=0
;;
(scp)
_arguments "${_arguments_options[@]}" : \
'--lang=[Força o idioma da CLI (ex.\: \`pt-BR\`, \`en-US\`)]:LOCALE:_default' \
'--config-dir=[Override do diretório de configuração (útil para testes)]:DIR:_files' \
'--output-format=[Formato global de saída (text, json)]:OUTPUT_FORMAT:((text\:"Texto legível por humanos (padrão)"
json\:"JSON estruturado"))' \
'-v[Aumenta a verbosidade de logs em stderr]' \
'--verbose[Aumenta a verbosidade de logs em stderr]' \
'-q[Suprime output não-JSON (modo silencioso)]' \
'--quiet[Suprime output não-JSON (modo silencioso)]' \
'--no-color[Desativa cores no output]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
":: :_ssh-cli__subcmd__scp_commands" \
"*::: :->scp" \
&& ret=0

    case $state in
    (scp)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:ssh-cli-scp-command-$line[1]:"
        case $line[1] in
            (upload)
_arguments "${_arguments_options[@]}" : \
'--password=[Override de senha SSH para esta transferência]:PASSWORD:_default' \
'--lang=[Força o idioma da CLI (ex.\: \`pt-BR\`, \`en-US\`)]:LOCALE:_default' \
'--config-dir=[Override do diretório de configuração (útil para testes)]:DIR:_files' \
'--output-format=[Formato global de saída (text, json)]:OUTPUT_FORMAT:((text\:"Texto legível por humanos (padrão)"
json\:"JSON estruturado"))' \
'-v[Aumenta a verbosidade de logs em stderr]' \
'--verbose[Aumenta a verbosidade de logs em stderr]' \
'-q[Suprime output não-JSON (modo silencioso)]' \
'--quiet[Suprime output não-JSON (modo silencioso)]' \
'--no-color[Desativa cores no output]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':vps_nome -- Nome da VPS previamente adicionada via `vps add`:_default' \
':local -- Caminho do arquivo local a enviar:_files' \
':remote -- Caminho destino no servidor remote:_files' \
&& ret=0
;;
(download)
_arguments "${_arguments_options[@]}" : \
'--password=[Override de senha SSH para esta transferência]:PASSWORD:_default' \
'--lang=[Força o idioma da CLI (ex.\: \`pt-BR\`, \`en-US\`)]:LOCALE:_default' \
'--config-dir=[Override do diretório de configuração (útil para testes)]:DIR:_files' \
'--output-format=[Formato global de saída (text, json)]:OUTPUT_FORMAT:((text\:"Texto legível por humanos (padrão)"
json\:"JSON estruturado"))' \
'-v[Aumenta a verbosidade de logs em stderr]' \
'--verbose[Aumenta a verbosidade de logs em stderr]' \
'-q[Suprime output não-JSON (modo silencioso)]' \
'--quiet[Suprime output não-JSON (modo silencioso)]' \
'--no-color[Desativa cores no output]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':vps_nome -- Nome da VPS previamente adicionada via `vps add`:_default' \
':remote -- Caminho do arquivo no servidor remote:_files' \
':local -- Caminho local de destino:_files' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_ssh-cli__subcmd__scp__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:ssh-cli-scp-help-command-$line[1]:"
        case $line[1] in
            (upload)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(download)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(tunnel)
_arguments "${_arguments_options[@]}" : \
'--password=[Override de senha SSH para este tunnel]:PASSWORD:_default' \
'--lang=[Força o idioma da CLI (ex.\: \`pt-BR\`, \`en-US\`)]:LOCALE:_default' \
'--config-dir=[Override do diretório de configuração (útil para testes)]:DIR:_files' \
'--output-format=[Formato global de saída (text, json)]:OUTPUT_FORMAT:((text\:"Texto legível por humanos (padrão)"
json\:"JSON estruturado"))' \
'-v[Aumenta a verbosidade de logs em stderr]' \
'--verbose[Aumenta a verbosidade de logs em stderr]' \
'-q[Suprime output não-JSON (modo silencioso)]' \
'--quiet[Suprime output não-JSON (modo silencioso)]' \
'--no-color[Desativa cores no output]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':vps_nome -- Nome da VPS previamente adicionada via `vps add`:_default' \
':porta_local -- Porta local para escuta (ex.\: 8080):_default' \
':host_remoto -- Host remoto accesible via SSH (ex.\: 127.0.0.1):_default' \
':porta_remota -- Porta remota (ex.\: 5432):_default' \
&& ret=0
;;
(health-check)
_arguments "${_arguments_options[@]}" : \
'--password=[Override de senha SSH para este health-check]:PASSWORD:_default' \
'--lang=[Força o idioma da CLI (ex.\: \`pt-BR\`, \`en-US\`)]:LOCALE:_default' \
'--config-dir=[Override do diretório de configuração (útil para testes)]:DIR:_files' \
'--output-format=[Formato global de saída (text, json)]:OUTPUT_FORMAT:((text\:"Texto legível por humanos (padrão)"
json\:"JSON estruturado"))' \
'-v[Aumenta a verbosidade de logs em stderr]' \
'--verbose[Aumenta a verbosidade de logs em stderr]' \
'-q[Suprime output não-JSON (modo silencioso)]' \
'--quiet[Suprime output não-JSON (modo silencioso)]' \
'--no-color[Desativa cores no output]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::vps_nome -- Nome da VPS a verificar (usa VPS ativa se omitido):_default' \
&& ret=0
;;
(completions)
_arguments "${_arguments_options[@]}" : \
'--lang=[Força o idioma da CLI (ex.\: \`pt-BR\`, \`en-US\`)]:LOCALE:_default' \
'--config-dir=[Override do diretório de configuração (útil para testes)]:DIR:_files' \
'--output-format=[Formato global de saída (text, json)]:OUTPUT_FORMAT:((text\:"Texto legível por humanos (padrão)"
json\:"JSON estruturado"))' \
'-v[Aumenta a verbosidade de logs em stderr]' \
'--verbose[Aumenta a verbosidade de logs em stderr]' \
'-q[Suprime output não-JSON (modo silencioso)]' \
'--quiet[Suprime output não-JSON (modo silencioso)]' \
'--no-color[Desativa cores no output]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':shell -- Shell para gerar completions:(bash elvish fish powershell zsh)' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_ssh-cli__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:ssh-cli-help-command-$line[1]:"
        case $line[1] in
            (vps)
_arguments "${_arguments_options[@]}" : \
":: :_ssh-cli__subcmd__help__subcmd__vps_commands" \
"*::: :->vps" \
&& ret=0

    case $state in
    (vps)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:ssh-cli-help-vps-command-$line[1]:"
        case $line[1] in
            (add)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(remove)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(edit)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(show)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(path)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(connect)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(exec)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(sudo-exec)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(scp)
_arguments "${_arguments_options[@]}" : \
":: :_ssh-cli__subcmd__help__subcmd__scp_commands" \
"*::: :->scp" \
&& ret=0

    case $state in
    (scp)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:ssh-cli-help-scp-command-$line[1]:"
        case $line[1] in
            (upload)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(download)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(tunnel)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(health-check)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(completions)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
}

(( $+functions[_ssh-cli_commands] )) ||
_ssh-cli_commands() {
    local commands; commands=(
'vps:Gerencia VPSs cadastradas (add, list, remove, edit, show, path)' \
'connect:Define a VPS ativa (grava \`active = "<NOME>"\` no \`config.toml\`)' \
'exec:Executa um comando na VPS via SSH (stdout/stderr capturados)' \
'sudo-exec:Executa um comando com \`sudo\` na VPS via SSH' \
'scp:Transferência de arquivos via SCP (upload/download)' \
'tunnel:Cria um tunnel SSH (port-forward local)' \
'health-check:Verifica conectividade SSH com uma VPS' \
'completions:Gera completions de shell (bash, zsh, fish, powershell, elvish)' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'ssh-cli commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__completions_commands] )) ||
_ssh-cli__subcmd__completions_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli completions commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__connect_commands] )) ||
_ssh-cli__subcmd__connect_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli connect commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__exec_commands] )) ||
_ssh-cli__subcmd__exec_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli exec commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__health-check_commands] )) ||
_ssh-cli__subcmd__health-check_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli health-check commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__help_commands] )) ||
_ssh-cli__subcmd__help_commands() {
    local commands; commands=(
'vps:Gerencia VPSs cadastradas (add, list, remove, edit, show, path)' \
'connect:Define a VPS ativa (grava \`active = "<NOME>"\` no \`config.toml\`)' \
'exec:Executa um comando na VPS via SSH (stdout/stderr capturados)' \
'sudo-exec:Executa um comando com \`sudo\` na VPS via SSH' \
'scp:Transferência de arquivos via SCP (upload/download)' \
'tunnel:Cria um tunnel SSH (port-forward local)' \
'health-check:Verifica conectividade SSH com uma VPS' \
'completions:Gera completions de shell (bash, zsh, fish, powershell, elvish)' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'ssh-cli help commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__help__subcmd__completions_commands] )) ||
_ssh-cli__subcmd__help__subcmd__completions_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli help completions commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__help__subcmd__connect_commands] )) ||
_ssh-cli__subcmd__help__subcmd__connect_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli help connect commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__help__subcmd__exec_commands] )) ||
_ssh-cli__subcmd__help__subcmd__exec_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli help exec commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__help__subcmd__health-check_commands] )) ||
_ssh-cli__subcmd__help__subcmd__health-check_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli help health-check commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__help__subcmd__help_commands] )) ||
_ssh-cli__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli help help commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__help__subcmd__scp_commands] )) ||
_ssh-cli__subcmd__help__subcmd__scp_commands() {
    local commands; commands=(
'upload:Upload de arquivo local para remote' \
'download:Download de arquivo remote para local' \
    )
    _describe -t commands 'ssh-cli help scp commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__help__subcmd__scp__subcmd__download_commands] )) ||
_ssh-cli__subcmd__help__subcmd__scp__subcmd__download_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli help scp download commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__help__subcmd__scp__subcmd__upload_commands] )) ||
_ssh-cli__subcmd__help__subcmd__scp__subcmd__upload_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli help scp upload commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__help__subcmd__sudo-exec_commands] )) ||
_ssh-cli__subcmd__help__subcmd__sudo-exec_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli help sudo-exec commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__help__subcmd__tunnel_commands] )) ||
_ssh-cli__subcmd__help__subcmd__tunnel_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli help tunnel commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__help__subcmd__vps_commands] )) ||
_ssh-cli__subcmd__help__subcmd__vps_commands() {
    local commands; commands=(
'add:Adiciona uma nova VPS ao registro' \
'list:Lista todas as VPSs (senhas mascaradas)' \
'remove:Remove uma VPS do registro' \
'edit:Edita campos de uma VPS existente' \
'show:Exibe detalhes de uma VPS (senhas mascaradas)' \
'path:Exibe o caminho do arquivo de configuração' \
    )
    _describe -t commands 'ssh-cli help vps commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__help__subcmd__vps__subcmd__add_commands] )) ||
_ssh-cli__subcmd__help__subcmd__vps__subcmd__add_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli help vps add commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__help__subcmd__vps__subcmd__edit_commands] )) ||
_ssh-cli__subcmd__help__subcmd__vps__subcmd__edit_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli help vps edit commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__help__subcmd__vps__subcmd__list_commands] )) ||
_ssh-cli__subcmd__help__subcmd__vps__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli help vps list commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__help__subcmd__vps__subcmd__path_commands] )) ||
_ssh-cli__subcmd__help__subcmd__vps__subcmd__path_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli help vps path commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__help__subcmd__vps__subcmd__remove_commands] )) ||
_ssh-cli__subcmd__help__subcmd__vps__subcmd__remove_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli help vps remove commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__help__subcmd__vps__subcmd__show_commands] )) ||
_ssh-cli__subcmd__help__subcmd__vps__subcmd__show_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli help vps show commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__scp_commands] )) ||
_ssh-cli__subcmd__scp_commands() {
    local commands; commands=(
'upload:Upload de arquivo local para remote' \
'download:Download de arquivo remote para local' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'ssh-cli scp commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__scp__subcmd__download_commands] )) ||
_ssh-cli__subcmd__scp__subcmd__download_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli scp download commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__scp__subcmd__help_commands] )) ||
_ssh-cli__subcmd__scp__subcmd__help_commands() {
    local commands; commands=(
'upload:Upload de arquivo local para remote' \
'download:Download de arquivo remote para local' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'ssh-cli scp help commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__scp__subcmd__help__subcmd__download_commands] )) ||
_ssh-cli__subcmd__scp__subcmd__help__subcmd__download_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli scp help download commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__scp__subcmd__help__subcmd__help_commands] )) ||
_ssh-cli__subcmd__scp__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli scp help help commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__scp__subcmd__help__subcmd__upload_commands] )) ||
_ssh-cli__subcmd__scp__subcmd__help__subcmd__upload_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli scp help upload commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__scp__subcmd__upload_commands] )) ||
_ssh-cli__subcmd__scp__subcmd__upload_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli scp upload commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__sudo-exec_commands] )) ||
_ssh-cli__subcmd__sudo-exec_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli sudo-exec commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__tunnel_commands] )) ||
_ssh-cli__subcmd__tunnel_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli tunnel commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__vps_commands] )) ||
_ssh-cli__subcmd__vps_commands() {
    local commands; commands=(
'add:Adiciona uma nova VPS ao registro' \
'list:Lista todas as VPSs (senhas mascaradas)' \
'remove:Remove uma VPS do registro' \
'edit:Edita campos de uma VPS existente' \
'show:Exibe detalhes de uma VPS (senhas mascaradas)' \
'path:Exibe o caminho do arquivo de configuração' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'ssh-cli vps commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__vps__subcmd__add_commands] )) ||
_ssh-cli__subcmd__vps__subcmd__add_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli vps add commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__vps__subcmd__edit_commands] )) ||
_ssh-cli__subcmd__vps__subcmd__edit_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli vps edit commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__vps__subcmd__help_commands] )) ||
_ssh-cli__subcmd__vps__subcmd__help_commands() {
    local commands; commands=(
'add:Adiciona uma nova VPS ao registro' \
'list:Lista todas as VPSs (senhas mascaradas)' \
'remove:Remove uma VPS do registro' \
'edit:Edita campos de uma VPS existente' \
'show:Exibe detalhes de uma VPS (senhas mascaradas)' \
'path:Exibe o caminho do arquivo de configuração' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'ssh-cli vps help commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__vps__subcmd__help__subcmd__add_commands] )) ||
_ssh-cli__subcmd__vps__subcmd__help__subcmd__add_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli vps help add commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__vps__subcmd__help__subcmd__edit_commands] )) ||
_ssh-cli__subcmd__vps__subcmd__help__subcmd__edit_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli vps help edit commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__vps__subcmd__help__subcmd__help_commands] )) ||
_ssh-cli__subcmd__vps__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli vps help help commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__vps__subcmd__help__subcmd__list_commands] )) ||
_ssh-cli__subcmd__vps__subcmd__help__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli vps help list commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__vps__subcmd__help__subcmd__path_commands] )) ||
_ssh-cli__subcmd__vps__subcmd__help__subcmd__path_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli vps help path commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__vps__subcmd__help__subcmd__remove_commands] )) ||
_ssh-cli__subcmd__vps__subcmd__help__subcmd__remove_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli vps help remove commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__vps__subcmd__help__subcmd__show_commands] )) ||
_ssh-cli__subcmd__vps__subcmd__help__subcmd__show_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli vps help show commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__vps__subcmd__list_commands] )) ||
_ssh-cli__subcmd__vps__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli vps list commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__vps__subcmd__path_commands] )) ||
_ssh-cli__subcmd__vps__subcmd__path_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli vps path commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__vps__subcmd__remove_commands] )) ||
_ssh-cli__subcmd__vps__subcmd__remove_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli vps remove commands' commands "$@"
}
(( $+functions[_ssh-cli__subcmd__vps__subcmd__show_commands] )) ||
_ssh-cli__subcmd__vps__subcmd__show_commands() {
    local commands; commands=()
    _describe -t commands 'ssh-cli vps show commands' commands "$@"
}

if [ "$funcstack[1]" = "_ssh-cli" ]; then
    _ssh-cli "$@"
else
    compdef _ssh-cli ssh-cli
fi
