#compdef create-turbo-eth
# Documentation: https://www.npmjs.com/package/create-turbo-eth
# Reference accessed: 2026-05-23
# Verified: npm view create-turbo-eth bin; npm readme Flags and Templates sections.

# Zsh completion for create-turbo-eth - full-stack web3 app template CLI

local curcontext="$curcontext" state line ret=1

_arguments -C   '--skip-git[skip git repository initialization]'   '--skip-install[skip package installation]'   '--pnpm[use pnpm as the package manager]'   '--npm[use npm as the package manager]'   '--yarn[use yarn as the package manager]'   '--template=[custom bootstrap template]:template:(template-disco-app template-tally-app template-bank-app places turbo-slides)'   ':project name:' && ret=0

return ret
