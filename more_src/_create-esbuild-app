#compdef create-esbuild-app
# Documentation: https://github.com/sonofmagic/create-esbuild-app#readme
# Reference accessed: 2026-05-24
# Verified: npm view create-esbuild-app bin; npm readme at the URL above.

# Zsh completion for create-esbuild-app - npm package scaffolder with esbuild and eslint

local curcontext="$curcontext" state line ret=1

_arguments -C \
  ':project directory:_directories' && ret=0

return ret
