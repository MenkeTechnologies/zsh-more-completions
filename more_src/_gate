#compdef gate
# distribute a wordlist across multiple processing instances

local ret=1

_arguments -s -S \
  '1:mod (number of sections):' \
  '2:offset (which section, 0-indexed):' && ret=0

return ret
