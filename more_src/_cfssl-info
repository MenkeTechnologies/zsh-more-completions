#compdef cfssl-info

local curcontext="$curcontext" state line ret=1

_arguments -s -S \
  '-label[CA label to query]:label:' \
  '-profile[signing profile to query]:profile:' \
  '-remote[remote CFSSL server]:url:' \
  && ret=0

return ret
