#compdef dracut-pre-pivot

# dracut-pre-pivot: initramfs hook called just before pivot to real root
local curcontext="$curcontext" ret=1
_arguments -C \
  '*::dracut hook args (not user-invoked):_files' && ret=0
return ret
