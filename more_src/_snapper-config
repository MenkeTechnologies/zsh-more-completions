#compdef snapper-config

# snapper-config: helper for creating snapper configurations during install
local curcontext="$curcontext" ret=1
_arguments -C \
  '*::snapper-config args (subvolume name template):_files' && ret=0
return ret
