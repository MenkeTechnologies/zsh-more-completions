#compdef bedGraphPack
# pack together adjacent records representing same value

local ret=1

_arguments -s -S \
  '1:in bedGraph:_files -g "*.bedGraph"' \
  '2:out bedGraph:_files' && ret=0

return ret
