#compdef namaka
# Documentation: https://man.archlinux.org/search?q=namaka
# Reference accessed: 2026-05-16
# Source: nixpkgs (https://github.com/NixOS/nixpkgs)
# Package: namaka
# Type: maintainer-shipped (nixpkgs)
# Harvested: 2026-05-15

autoload -U is-at-least

_namaka() {
    typeset -A opt_args
    typeset -a _arguments_options
    local ret=1

    if is-at-least 5.2; then
        _arguments_options=(-s -S -C)
    else
        _arguments_options=(-s -C)
    fi

    local context curcontext="$curcontext" state line
    _arguments "${_arguments_options[@]}" : \
'*-c+[Command to run instead of \`nix flake check\`]:CMD:_default' \
'*--cmd=[Command to run instead of \`nix flake check\`]:CMD:_default' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'-V[Print version]' \
'--version[Print version]' \
'::dir -- Change to this working directory:_files' \
":: :_namaka_commands" \
"*::: :->namaka" \
&& ret=0
    case $state in
    (namaka)
        words=($line[2] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:namaka-command-$line[2]:"
        case $line[2] in
            (check)
_arguments "${_arguments_options[@]}" : \
'*-c+[Command to run instead of \`nix flake check\`]:CMD:_default' \
'*--cmd=[Command to run instead of \`nix flake check\`]:CMD:_default' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::dir -- Change to this working directory:_files' \
&& ret=0
;;
(c)
_arguments "${_arguments_options[@]}" : \
'*-c+[Command to run instead of \`nix flake check\`]:CMD:_default' \
'*--cmd=[Command to run instead of \`nix flake check\`]:CMD:_default' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::dir -- Change to this working directory:_files' \
&& ret=0
;;
(clean)
_arguments "${_arguments_options[@]}" : \
'*-c+[Command to run instead of \`nix flake check\`]:CMD:_default' \
'*--cmd=[Command to run instead of \`nix flake check\`]:CMD:_default' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::dir -- Change to this working directory:_files' \
&& ret=0
;;
(cl)
_arguments "${_arguments_options[@]}" : \
'*-c+[Command to run instead of \`nix flake check\`]:CMD:_default' \
'*--cmd=[Command to run instead of \`nix flake check\`]:CMD:_default' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::dir -- Change to this working directory:_files' \
&& ret=0
;;
(review)
_arguments "${_arguments_options[@]}" : \
'*-c+[Command to run instead of \`nix flake check\`]:CMD:_default' \
'*--cmd=[Command to run instead of \`nix flake check\`]:CMD:_default' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::dir -- Change to this working directory:_files' \
&& ret=0
;;
(r)
_arguments "${_arguments_options[@]}" : \
'*-c+[Command to run instead of \`nix flake check\`]:CMD:_default' \
'*--cmd=[Command to run instead of \`nix flake check\`]:CMD:_default' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::dir -- Change to this working directory:_files' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_namaka__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:namaka-help-command-$line[1]:"
        case $line[1] in
            (check)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(clean)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(review)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
}

(( $+functions[_namaka_commands] )) ||
_namaka_commands() {
    local commands; commands=(
'check:Wrapper around \`nix flake check\` to prepare snapshots for failed tests' \
'c:Wrapper around \`nix flake check\` to prepare snapshots for failed tests' \
'clean:Remove unused and pending snapshots' \
'cl:Remove unused and pending snapshots' \
'review:Review pending snapshots and selectively accept or reject them' \
'r:Review pending snapshots and selectively accept or reject them' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'namaka commands' commands "$@"
}
(( $+functions[_namaka__check_commands] )) ||
_namaka__check_commands() {
    local commands; commands=()
    _describe -t commands 'namaka check commands' commands "$@"
}
(( $+functions[_namaka__clean_commands] )) ||
_namaka__clean_commands() {
    local commands; commands=()
    _describe -t commands 'namaka clean commands' commands "$@"
}
(( $+functions[_namaka__help_commands] )) ||
_namaka__help_commands() {
    local commands; commands=(
'check:Wrapper around \`nix flake check\` to prepare snapshots for failed tests' \
'clean:Remove unused and pending snapshots' \
'review:Review pending snapshots and selectively accept or reject them' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'namaka help commands' commands "$@"
}
(( $+functions[_namaka__help__check_commands] )) ||
_namaka__help__check_commands() {
    local commands; commands=()
    _describe -t commands 'namaka help check commands' commands "$@"
}
(( $+functions[_namaka__help__clean_commands] )) ||
_namaka__help__clean_commands() {
    local commands; commands=()
    _describe -t commands 'namaka help clean commands' commands "$@"
}
(( $+functions[_namaka__help__help_commands] )) ||
_namaka__help__help_commands() {
    local commands; commands=()
    _describe -t commands 'namaka help help commands' commands "$@"
}
(( $+functions[_namaka__help__review_commands] )) ||
_namaka__help__review_commands() {
    local commands; commands=()
    _describe -t commands 'namaka help review commands' commands "$@"
}
(( $+functions[_namaka__review_commands] )) ||
_namaka__review_commands() {
    local commands; commands=()
    _describe -t commands 'namaka review commands' commands "$@"
}

if [ "$funcstack[1]" = "_namaka" ]; then
    _namaka "$@"
else
    compdef _namaka namaka
fi
