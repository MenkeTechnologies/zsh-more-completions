#compdef imodmesh
# Documentation: https://bio3d.colorado.edu/imod/doc/man/imodmesh.html
# Create a triangle mesh from IMOD contour data

local ret=1

_arguments -s \
  '-u[Use parameters stored in each object to set options for...]' \
  '-c[This option will cap off the ends of objects at their...]' \
  '-C[This option will cap off all unconnected contours.]' \
  '-D[This option can be used with the -C option to keep caps from]:value' \
  '-p[Dont make connections unless a given percentage of the...]:value' \
  '-s[Normally connections are only made to adjacent sections in...]' \
  '-P[Do the given number of passes through the contours,...]:value' \
  '-S[This will forbid contours with different Surface numbers...]' \
  '-I[Ignore time values and connect contours at different times....]' \
  '-f[Force more connections. Do a final pass through the contour]' \
  '-t[Open contours are by default connected together in a mesh if]:value' \
  '-d[Override the default diameter used for making tubes with...]:value' \
  '-E[Cap the ends of tubes made with the -t option.]' \
  '-H[Cap the ends of tubes with hemispheres (domes) instead of...]' \
  '-T[Do more time consuming calculations by searching...]' \
  '-o[Do operations only on objects in the list of numbers. The...]:value' \
  '-R[Tolerance value for point reduction, given in pixels. With...]:value' \
  '-i[Form a mesh between contours at z-values that are a...]:value' \
  '-z[minimum,maximum,(increment)]' \
  '-x[minimum,maximum]' \
  '-y[minimum,maximum Exclude triangles from the mesh if they are]' \
  '-l[Form a low resolution mesh. Any new mesh data created by...]' \
  '-F[This option sets the criterion Z difference for deciding...]:value' \
  '-a[Append the mesh data to objects, replacing only the existing]' \
  '-e[Erase all mesh data instead of creating new data. Other...]' \
  '-n[Recompute the normals in the existing mesh, without...]' \
  '-Z[A scaling multiplier applied to the z values of normals. The]:value' \
  '-noc[noc]' \
  '-noC[noC]' \
  '-nos[nos]' \
  '-noS[noS]' \
  '-noI[noI]' \
  '-nof[nof]' \
  '-noE[parameters.]' \
  '-noT[Turn off the corresponding option when using stored...]' \
  '-noD[Remove any restrictions on Z values to cap to when using...]' \
  '-not[Do not mesh open objects as tubes when using stored...]' \
  '-nox[parameters.]' \
  '-noy[parameters.]' \
  '-noz[Remove any limits on X, Y, or Z coordinates when using...]' \
  '-B[Make mesh output backward-compatible to IMOD before 3.6.14...]' \
  '*:file:_files' && ret=0

return ret
