#compdef uu-fold
# Documentation: https://man.archlinux.org/search?q=uu-fold
# Reference accessed: 2026-05-16
# Source: Homebrew bottle
# Formula: uutils-coreutils
# Type: maintainer-shipped (Homebrew)
# Harvested: 2026-05-15

autoload -U is-at-least

_uu-fold() {
    typeset -A opt_args
    typeset -a _arguments_options
    local ret=1

    if is-at-least 5.2; then
        _arguments_options=(-s -S -C)
    else
        _arguments_options=(-s -C)
    fi

    local context curcontext="$curcontext" state line
    _arguments "${_arguments_options[@]}" : \
'-w+[set WIDTH as the maximum line width rather than 80]:WIDTH:_default' \
'--width=[set WIDTH as the maximum line width rather than 80]:WIDTH:_default' \
'-b[count using bytes rather than columns (meaning control characters such as newline are not treated specially)]' \
'--bytes[count using bytes rather than columns (meaning control characters such as newline are not treated specially)]' \
'(-b --bytes)-c[count using character positions rather than display columns]' \
'(-b --bytes)--characters[count using character positions rather than display columns]' \
'-s[break lines at word boundaries rather than a hard cut-off]' \
'--spaces[break lines at word boundaries rather than a hard cut-off]' \
'-h[Print help]' \
'--help[Print help]' \
'-V[Print version]' \
'--version[Print version]' \
'::file:_files' \
&& ret=0
}

(( $+functions[_uu-fold_commands] )) ||
_uu-fold_commands() {
    local commands; commands=()
    _describe -t commands 'uu-fold commands' commands "$@"
}

if [ "$funcstack[1]" = "_uu-fold" ]; then
    _uu-fold "$@"
else
    compdef _uu-fold uu-fold
fi
