#compdef bigWigCat
# merge non-overlapping bigWig files directly into bigWig format

local ret=1

_arguments -s -S \
  '-itemsPerSlot=[number of data points bundled at lowest level (default 1024)]:n' \
  '1:out bw:_files' \
  '*:in bw:_files -g "*.(bw|bigWig)"' && ret=0

return ret
