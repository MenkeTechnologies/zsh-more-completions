#compdef mlxqos
# Documentation: https://github.com/Mellanox/mstflint
# Reference accessed: 2026-05-19
# Verified: upstream docs at the URL above.
# STUB: "mlxqos" is not a documented NVIDIA MFT or mstflint binary. The
# Mellanox QoS host tool is named mlnx_qos (covered separately if/when added).
# Keeping a minimal fallback so #compdef matches if a vendor ships this name.

_arguments -s \
  '(- *)'{-h,--help}'[show help]' \
  '*:: :_default'
