#compdef ai-claude
# Documentation: https://github.com/patrickespake/ai_cli_management
# Reference accessed: 2026-05-23
# Verified: ai_global_installer.sh ai-claude parallel task runner (tasks.json).

# Zsh completion for ai-claude - Claude parallel worktree task runner

local curcontext="$curcontext" state line ret=1

_arguments -C \
  ':tasks file (default tasks.json):file:_files -g "*.json"' && ret=0

return ret
