#compdef ibus-anthy
# Documentation: (IBus engine; no upstream CLI flags)
# Reference accessed: 2026-05-16
# Verified: ibus-anthy ships ibus-setup-anthy (Python GTK setup GUI)
# and ibus-engine-anthy (daemon-launched). No documented CLI flags
# for either binary.
# Removed from prior file: fabricated -h/--help/-V/--version flags and
# bogus man.archlinux.org search URL.

# ibus-anthy: IBus Anthy Japanese input method (GUI setup + daemon)
local curcontext="$curcontext" ret=1
_arguments -C \
  '*:arg:_files' && ret=0
return ret
