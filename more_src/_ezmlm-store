#compdef ezmlm-store

local curcontext="$curcontext" ret=1

_arguments -C \
  '(- *)-h[show help]' \
  '-r[reject if no message-id]' \
  '-R[accept always]' \
  '1:dir:_files -/' && ret=0

return ret
