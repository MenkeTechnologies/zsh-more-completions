#compdef mrc2tif
# Documentation: https://bio3d.colorado.edu/imod/doc/man/mrc2tif.html
# Convert MRC file into TIFF files

local ret=1

_arguments -s \
  '-s[Create a single output file, a TIFF stack with all of t]' \
  '-j[Produce JPEG files instead of TIFF files. See above for]' \
  '-p[Produce PNG files instead of TIFF files. See above for ]' \
  '-c[Compress the data in TIFF files with the type of compre]:value:' \
  '-q[Set the quality for JPEG or ZIP compression (ZIP compre]:value:' \
  '-S[Specify an initial intensity scaling that will scale "m]:value:' \
  '-C[Specify intensity scaling to match the scaling achieved]:value:' \
  '-a[Scale data for conversion to bytes so that it has the g]:value:' \
  '-z[Starting and ending Z slices in file to output, numbere]:value:' \
  '-i[Initial file number. The default is the number of the f]:value:' \
  '-r[Set the resolution setting of the output file in dots p]:value:' \
  '-P[Use the pixel spacing in the input file header to set t]' \
  '-m[Use the pixel spacings in an ".mdoc" file associated wi]' \
  '-T[Organize output data in tiles instead of strips, which ]:value:' \
  '-t[Set the criterion for processing each image in strips t]:value:' \
  '-O[Override the default behavior on whether to compress da]:value:' \
  '-o[Use old IMOD code instead of libtiff for writing the fi]' \
  '*:file:_files' && ret=0

return ret
