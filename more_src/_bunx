#compdef bunx

# Zsh completion for bunx - execute npm package binaries (bun equivalent of npx)

_arguments -s -S \
  '--bun[force use of bun runtime]' \
  '(-h --help)'{-h,--help}'[show help]' \
  '1:package name:' \
  '*::args:'
