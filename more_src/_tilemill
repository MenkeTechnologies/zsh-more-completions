#compdef tilemill
# Documentation: https://github.com/tilemill-project/tilemill
# Reference accessed: 2026-05-16
# Verified: upstream tilemill-project/tilemill. Project abandoned ~2017.
# Electron app launched via `tilemill` with internal Node config
# (`--config.<key>=<val>`); no formal CLI flag specification exists.
# Removed from prior file: fabricated -h/--help/-V/--version flags and
# bogus man.archlinux.org search URL.

# tilemill: Mapbox Studio predecessor (abandoned Electron app)
local curcontext="$curcontext" ret=1
_arguments -C \
  '*:arg:_files' && ret=0
return ret
