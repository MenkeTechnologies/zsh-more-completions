#compdef mini_client
# Documentation: https://github.com/Mbed-TLS/mbedtls/blob/development/programs/ssl/mini_client.c
# Reference accessed: 2026-05-19
# Verified: upstream source at the URL above.
# STUB: mini_client is an Mbed TLS sample program; main(void) takes no
# command-line arguments (server, port, cipher, etc. are hardcoded).
# Prior file's param=value list (server_name, server_addr, server_port,
# debug_level) was invented.

local curcontext="$curcontext" ret=1
_arguments -C \
  '*:argument: ' && ret=0
return ret
