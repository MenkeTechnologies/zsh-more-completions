#compdef kpasswd

local -a arguments

arguments=(
    '-?[Display help]'
    '--help[Display help]'
    '--usage[Display usage]'
    '-c[Cache name]:cache name:_files'
    '--cache=[Cache name]:cache name:_files'
    '--admin-server=[Admin server to contact]:server'
    '*:principal'
)

_arguments -s -S $arguments
