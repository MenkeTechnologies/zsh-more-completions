#compdef debug-cql
# Documentation: https://github.com/apache/cassandra/blob/trunk/bin/debug-cql
# Reference accessed: 2026-05-17
# Verified: upstream docs at the URL above.
# debug-cql is a thin wrapper that exec's org.apache.cassandra.transport.Client
# via $JAVA $JVM_OPTS $cassandra_parms; it has no fixed CLI flags of its own
# and passes "$@" through to the Java Client main class.

_message 'debug-cql passes arguments to org.apache.cassandra.transport.Client'
