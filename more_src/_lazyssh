#compdef lazyssh

# Zsh completion for lazyssh — SSH manager TUI

local curcontext="$curcontext" ret=1

_arguments -s -S \
  '(-h --help)'{-h,--help}'[help]' \
  '(-V --version)'{-V,--version}'[version]' \
  '--config[ssh config]:file:_files' && ret=0

return ret
