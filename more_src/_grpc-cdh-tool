#compdef grpc-cdh-tool
# _grpc-cdh-tool — Confidential Data Hub gRPC test client (talks to a running grpc-cdh; unseal-secret/unwrap-key/get-resource/secure-mount)
# upstream: https://github.com/confidential-containers/guest-components
# Subcommands + flags from confidential-data-hub/hub/src/bin/grpc-cdh-tool.rs Cli on tip (clap).

_grpc-cdh-tool() {
    local context state line
    typeset -A opt_args
    _arguments -C \
        '(- *)'{-h,--help}'[show help]' \
        '(- *)'{-V,--version}'[print version and exit]' \
        '(-s --socket)'{-s,--socket}'=[gRPC socket (http://127.0.0.1:50000)]:url:_urls' \
        '1: :->cmd' \
        '*::arg:->args'
    case $state in
        cmd)
            local -a subs=(
                unseal-secret:'unseal a sealed secret'
                unwrap-key:'unwrap an OCI image encryption key'
                get-resource:'fetch a confidential resource from the KBS'
                secure-mount:'mount a Storage object via the CDH secure-mount provider'
            )
            _describe -t commands 'grpc-cdh-tool subcommand' subs
            ;;
        args)
            case $line[1] in
                unseal-secret)
                    _arguments \
                        '(-s --secret-path)'{-s,--secret-path}'=[file containing the sealed secret]:file:_files'
                    ;;
                unwrap-key)
                    _arguments \
                        '(-a --annotation-path)'{-a,--annotation-path}'=[file containing the AnnotationPacket]:file:_files'
                    ;;
                get-resource)
                    _arguments \
                        '(-r --resource-uri)'{-r,--resource-uri}'=[KBS Resource URI]:uri:'
                    ;;
                secure-mount)
                    _arguments \
                        '(-s --storage-path)'{-s,--storage-path}'=[file containing the Storage object]:file:_files'
                    ;;
            esac
            ;;
    esac
}

_grpc-cdh-tool "$@"
