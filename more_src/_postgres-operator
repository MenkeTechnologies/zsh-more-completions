#compdef postgres-operator
# _postgres-operator — Crunchy Data PGO 5 (reconciles PostgresCluster + PGUpgrade + PGAdmin CRs into StatefulSets + pgBackRest backups)
# upstream: https://github.com/crunchydata/postgres-operator
# Source: cmd/postgres-operator/main.go on tip; no CLI flags — runtime config is env-driven (CRUNCHY_DEBUG, PGO_CONTROLLER_LEASE_NAME, PGO_NAMESPACE, PGO_TARGET_NAMESPACE, PGO_TARGET_NAMESPACES, PGO_WORKERS, PGO_FEATURE_GATES, OTEL_*).

_postgres-operator() {
    _arguments \
        '(- *)'{-h,--help}'[show help]'
}

_postgres-operator "$@"
