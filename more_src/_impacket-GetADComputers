#compdef impacket-GetADComputers
# Documentation: https://github.com/fortra/impacket/blob/master/examples/GetADComputers.py
# Queries target domain for computer data

local ret=1

_arguments -s -S \
  '(-h --help)'{-h,--help}'[show help]' \
  '-user=[requests data for specific user]:user' \
  '-ts[adds timestamp to every logging output]' \
  '-debug[turn DEBUG output ON]' \
  '-resolveIP[tries to resolve the IP address of computer objects via nslookup on the DC]' \
  '-hashes=[NTLM hashes, format is LMHASH:NTHASH]:hashes' \
  '-no-pass[don'\''t ask for password (useful for -k)]' \
  '-k[use Kerberos authentication from ccache file]' \
  '-aesKey=[AES key to use for Kerberos authentication (128 or 256 bits)]:hexkey' \
  '-dc-ip=[IP address of the domain controller]:ip' \
  '-dc-host=[hostname of the domain controller to use]:hostname' \
  '1:target (domain\[/username\[:password\]\]):' && ret=0

return ret
