#compdef monero-blockchain-stats
# Source: https://github.com/BibXMR/monero-zsh-completions/blob/6540df4783f354863955175790dae12735e30b2d/_monero-blockchain-stats
# Repository: BibXMR/monero-zsh-completions (_monero-blockchain-stats)

_monero-blockchain-stats() {
  _arguments \
    '(- *)--help[Produce help message]' \
    '--block-start=[start at block number]:block height' \
    '--block-stop=[Stop at block number]:block height' \
    '--data-dir=[Specify data directory]:data directory:_directories' \
    '--log-level=[0-4 or categories]:log level' \
    '(--testnet)--stagenet[Run on stagenet.]' \
    '(--stagenet)--testnet[Run on testnet.]' \
    '--with-diff[with difficulty]' \
    '--with-emission[with coin emission]' \
    '--with-fees[with txn fees]' \
    '--with-hours[with txns per hour]' \
    '--with-inputs[with input stats]' \
    '--with-outputs[with output stats]' \
    '--with-ringsize[with ringsize stats]'
}

_monero-blockchain-stats "$@"
