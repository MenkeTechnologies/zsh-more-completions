#compdef mcp-server-everything
# Documentation: https://raw.githubusercontent.com/modelcontextprotocol/servers/main/src/everything/index.ts
# Reference accessed: 2026-05-27
# Verified: upstream src/everything/index.ts (process.argv.slice(2); scriptName = args[0] || "stdio")

# Zsh completion for mcp-server-everything - reference MCP server demonstrating all capabilities

local curcontext="$curcontext" state line ret=1

_arguments -C \
  '1:transport:(stdio sse streamableHttp)' && ret=0

return ret
