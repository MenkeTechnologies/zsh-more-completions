#compdef twinwave
# Run two synchronized GTKWave viewers (markers, scroll, zoom)

local ret=1

_arguments -s -S \
  '+[open two synchronized viewers in one window]' \
  '++[open two synchronized viewers in two windows]' \
  '*:gtkwave arg:_files' && ret=0

return ret
