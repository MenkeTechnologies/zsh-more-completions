#compdef cabal-store-gc
# _cabal-store-gc — garbage-collect ~/.cabal/store (phadej/cabal-extras)
# upstream: https://github.com/phadej/cabal-extras
# Flags from cabal-store-gc/src/CabalStoreGC/Main.hs (optparse-applicative).

_cabal-store-gc() {
    _arguments \
        '(- *)'{-h,--help}'[show help]' \
        '(- *)--version[show version]' \
        '(-w --with-compiler)'{-w+,--with-compiler=}'[compiler to use]:ghc:_command_names -e' \
        '(--default --count --collect --add-project-root --add-root --cleanup-roots)--default[default action: add-project-root + count]' \
        '(--default --count --collect --add-project-root --add-root --cleanup-roots)--count[count unreferenced components]' \
        '(--default --count --collect --add-project-root --add-root --cleanup-roots)--collect[collect unreferenced components]' \
        '(--default --count --collect --add-project-root --add-root --cleanup-roots)--add-project-root[add current plan.json as indirect root]' \
        '(--default --count --collect --add-project-root --add-root --cleanup-roots)--add-root=[plan.json file as indirect root]:file:_files -g "plan.json"' \
        '(--default --count --collect --add-project-root --add-root --cleanup-roots)--cleanup-roots[collect dangling indirect roots]'
}

_cabal-store-gc "$@"
