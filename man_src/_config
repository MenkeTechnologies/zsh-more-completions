#compdef config

# Autogenerated from man page /usr/share/man/en.UTF-8/man8/config.8.gz

local arguments

arguments=(
    '-V[Print the config version number.]'
    '-C[If the INCLUDE_CONFIG_FILE is present in a configuration file, kernel image w…]'
    '-I[Search in path for any file included by the include directive.]'
    '-d[Use destdir as the output directory, instead of the default one.]'
    '-s[Use srcdir as the source directory, instead of the default one.]'
    '-m[Print the MACHINE and MACHINE_ARCH values for this kernel and exit.]'
    '-g[Configure a system for debugging.]'
    '-x[Print kernel configuration file embedded into a kernel file.]'
    '-p[Configure a system for profiling; for example, kgmon 8 and gprof 1.]'
    '*:filename:_files'
)
_arguments -s $arguments
