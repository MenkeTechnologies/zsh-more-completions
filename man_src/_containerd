#compdef containerd

# Autogenerated from man page /usr/share/man/man1/containerd.1.gz

local arguments

arguments=(
    {--config,-c}'[: Specify the default path to the configuration file (default: "/etc/containe…]'
    {--log-level,-l}'[: Set the logging level.]'
    {--address,-a}'[: UNIX socket address for containerd\s GRPC server to listen on (default: "/r…]'
    '--root[The containerd root directory (default: "/var/lib/containerd").]:value'
    '--state[The containerd state directory (default: "/run/containerd").]:value'
    {--help,-h}'[: Show containerd command help text.]'
    {--version,-v}'[: Print the containerd server version.]'
    '*:filename:_files'
)
_arguments -s $arguments
