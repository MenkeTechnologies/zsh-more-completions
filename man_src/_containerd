#compdef containerd

# Autogenerated from man page
# Updated with latest containerd options

local arguments

arguments=(
    {--config,-c}'[Path to the configuration file (default: /etc/containerd/config.toml)]:file:_files'
    {--log-level,-l}'[Set the logging level]:level:(trace debug info warn error fatal panic)'
    {--address,-a}'[UNIX socket address for containerd GRPC server]:address'
    '--root[The containerd root directory (default: /var/lib/containerd)]:directory:_directories'
    '--state[The containerd state directory (default: /run/containerd)]:directory:_directories'
    {--help,-h}'[Show help text]'
    {--version,-v}'[Print the containerd server version]'
    '*:filename:_files'
)
_arguments -s $arguments
