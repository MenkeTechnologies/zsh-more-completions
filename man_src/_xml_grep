#compdef xml_grep

# Autogenerated from man page /usr/share/man/man1/xml_grep.1p.gz

local arguments

arguments=(
    '--help[brief help message.]'
    '--man[full documentation.]'
    '--Version[display the tool version.]'
    '--root[look for and return xml chunks matching <cond> .]:cond'
    '--cond[return the chunks (or file names) only if they contain elements matching <con…]:cond'
    '--files[return only file names (do not generate an \\s-1XML\\s0 output) .]'
    '--count[return only the number of matches in each file .]'
    '--strict[without this option parsing errors are reported to \\s-1STDOUT\\s0 and the file…]'
    '--date[when on (by default) the wrapping element get a \\f(CW\\*(C`date\\*(C\ attribute…]'
    '--encoding[encoding of the xml output (utf-8 by default).]:enc'
    '--nb_results[output only <nb> results.]:nb'
    '--by_file[output only <nb> results by file.]'
    '--wrap[wrap the xml result in the provided tag (defaults to \xml_grep\) .]:tag'
    '--nowrap[same as using \\f(CW\\*(C`--wrap \\*(Aq\\*(Aq\\*(C\: the xml result is not wrapped.]'
    '--descr[attributes of the wrap tag (defaults to \\f(CW\\*(C`version="<VERSION>" date="<…]:string'
    '--group_by_file[wrap results for each files into a separate element.]:optional_tag'
    '--exclude[same as using \\f(CW\\*(C`-v\\*(C\ in grep: the elements that match the conditio…]:condition'
    '--pretty_print[pretty print the output using XML::Twig styles (\\\f(CW\\*(C`indented\\*(C\\, \\\…]:optional_style'
    '--text_only[Displays the text of the results, one by line.]'
    '--html[Allow \\s-1HTML\\s0 input, files are converted using HTML::TreeBuilder.]'
    '*:filename:_files'
)
_arguments -s $arguments
