#compdef bsnmpget

# Autogenerated from man page
# Updated with latest FreeBSD bsnmpget options

local arguments

arguments=(
    '-A[Authentication options to use with SNMPv3 PDUs]:auth_options'
    '-a[Skip sanity checks when adding OIDs to a PDU]'
    '-b[Tune the size of buffers used to send and receive packets]:bufsize'
    '-C[The context to query with SNMPv3 PDUs]:context'
    '-D[Perform SNMP USM Engine Discovery]'
    '-d[Turn on debugging]'
    '-e[Retry on error]'
    '-h[Print a short help text]'
    '-I[Load MIB description files to translate symbolic OIDs]:mib_list'
    '-i[List of MIB description files]:mib_files:_files'
    '-K[Calculate and display localized authentication and privacy keys]:keys'
    '-l[Path of the local Unix domain socket]:path:_files'
    '-M[Value for the max-repetitions field in a GetBulk PDU]:max_reps'
    '-N[Value for the non-repeaters field in a GetBulk PDU]:non_reps'
    '-n[Only use numerical representations for OIDs]'
    '-o[Format used to print the received response]:format'
    '-P[Privacy options to use with SNMPv3 PDUs]:priv_options'
    '-p[PDU type to send]:pdu_type'
    '-r[Number of resends before giving up]:retries'
    '-s[Server specification]:server:_hosts'
    '-t[Seconds before resending a request]:timeout'
    '-U[User credentials for SNMPv3 PDUs]:user'
    '-v[SNMP protocol version to use]:version:(1 2c 3)'
    '*:oid:'
)
_arguments -s $arguments
