#compdef e2undo

# Autogenerated from man page /usr/share/man/man8/e2undo.8.gz

local arguments

arguments=(
    '-f[Normally, e2undo will check the filesystem superblock to make sure the undo …]'
    '-h[Display a usage message.]'
    '-n[Dry-run; do not actually write blocks back to the filesystem.]'
    '-o[Specify the filesystem\s offset (in bytes) from the beginning of the device …]:offset"'
    '-v[Report which block we\re currently replaying.]'
    '-z[Before overwriting a file system block, write the old contents of the block t…]:undo_file'
    '*:filename:_files'
)
_arguments -s $arguments
