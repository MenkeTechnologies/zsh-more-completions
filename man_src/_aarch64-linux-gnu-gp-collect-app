#compdef gp-collect-app

# Autogenerated from man page /usr/share/man/man1/gp-collect-app.1.gz

local arguments

arguments=(
    '--version[print the version number and exit.]'
    '--help[print usage information and exit.]'
    '--verbose[enable (on) or disable (off) verbose mode; the default is "off".]:{on|off}'
    '-p[disable (off) or enable (on) clock-profiling using a default sampling granula…]:{off|on|lo|hi|<value>}'
    '-h[enable hardware event counter profiling and select the counter(s); to see the…]:{<ctr_def>...<ctr_n_def>}'
    '-o[specify the name for (and path to) the experiment directory; the the default …]:exp_name'
    '-O[the same as -o, but unlike the -o option, silently overwrite an existing expe…]:exp_name'
    '-C[add up to 10 comment labels to the experiment; comments appear in the notes s…]:label'
    '-j[program is a JVM; optionally set the <path> to a non-default JVM; the default…]'
    '-J[specify arguments to the JVM.]:java-args'
    '-t[specify the duration over which to record data; the default unit is seconds (…]:duration>m|s'
    '-n[dry run; display several run-time settings, but do not run the target, or col…]'
    '-y[specify delayed initialization and a pause/resume signal; by default the targ…]:signal>r'
    '-F[control to follow descendant processes; disable (off), enable (on), or collec…]:regex>}'
    '-a[specify archiving of binaries and other files; in addition to disable this fe…]:{off|on|ldobjects|src|usedldobjects|usedsrc}'
    '-S[disable (off) or enable (on) periodic sampling of process-wide resource utili…]:{off|on|<seconds>}'
    '-l[specify a signal that will trigger a sample of process-wide resource utilizat…]:signal'
    '-s[enable synchronization wait tracing; <option> is used to define the specifics…]:option><API'
    '-H[disable (off), or enable (on) heap tracing; the default is "-H off".]:{off|on}'
    '-i[disable (off), or enable (on) I/O tracing; the default is "-i off".]:{off|on}'
    '*:filename:_files'
)
_arguments -s $arguments
