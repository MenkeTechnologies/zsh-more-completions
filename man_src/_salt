#compdef salt

# Autogenerated from man page /usr/local/man/man1/salt.1
# Updated with fixes for troff artifacts and truncated descriptions

local arguments

arguments=(
    '--version[Print the version of Salt that is running]'
    '--versions-report[Show program dependencies and version number, then exit]'
    '(- * :)'{-h,--help}'[Show the help message and exit]'
    {-c,--config-dir}'[The location of the Salt configuration directory]:directory:_directories'
    {-t,--timeout}'[The timeout in seconds to wait for replies from minions]:seconds'
    {-s,--static}'[Return data from minions as a group after they all return]'
    '--async[Only print the job id instead of waiting for the job to run]'
    '--subset[Execute the routine on a random subset of targeted minions]:subset'
    {-v,--verbose}'[Turn on verbosity for the salt call]'
    '--hide-timeout[Hide timed out minions from the return data]'
    {-b,--batch-size}'[Execute on a progressive set of minions]:batch_size'
    {-a,--auth}'[Pass in an external authentication medium to validate against]:auth'
    {-T,--make-token}'[Used in conjunction with the -a option to create authentication tokens]'
    '--return[Choose an alternative returner to call on the minion]:returner'
    {-d,--doc,--documentation}'[Return the documentation for module functions available on minions]'
    '--args-separator[Set the special argument delimiter between command arguments]:separator'
    {-l,--log-level}'[Console logging log level]:level'
    '--log-file[Log file path]:log_file:_files'
    '--log-file-level[Logfile logging log level]:level'
    {-E,--pcre}'[Interpret target expression as a PCRE regular expression]'
    {-L,--list}'[Interpret target expression as a comma-delimited list]'
    {-G,--grain}'[Match target using Salt grains system values]'
    '--grain-pcre[Match target using Salt grains with PCRE regular expression]'
    {-N,--nodegroup}'[Use a predefined compound target from Salt master configuration]'
    {-R,--range}'[Use a range expression to evaluate the target]'
    {-C,--compound}'[Utilize many target definitions for granular targeting]'
    {-I,--pillar}'[Use a pillar value to identify targets]'
    {-S,--ipcidr}'[Match based on Subnet (CIDR notation) or IPv4 address]'
    '--out[Pass in an alternative outputter to display return data]'
    {--out-indent,--output-indent}'[Print the output indented by the provided value in spaces]:spaces'
    {--out-file,--output-file}'[Write the output to the specified file]:file:_files'
    {--out-file-append,--output-file-append}'[Append the output to the specified file]:file:_files'
    '--no-color[Disable all colored output]'
    '--force-color[Force colored output]'
    {--state-output,--state_output}'[Override the configured state_output value for minion output]:state_output'
    {--state-verbose,--state_verbose}'[Override the configured state_verbose value for minion output]:state_verbose'
    '*:filename:_files'
)
_arguments -s $arguments
