#compdef resize2fs

# Autogenerated from man page
# Updated with latest resize2fs options

local arguments

arguments=(
    '-b[turn on the 64bit feature and resize the group descriptors as necessary]'
    '-d[turn on various resize2fs debugging features]:debug-flags'
    '-f[force resize2fs to proceed with the filesystem resize operation]'
    '-F[flush the filesystem device buffer caches before beginning]'
    '-M[shrink the file system to minimize its size as much as possible]'
    '-p[print percentage completion bars for each resize2fs operation]'
    '-P[print an estimate of the number of file system blocks if shrunk with -M]'
    '-s[turn off the 64bit feature and free blocks that are no longer in use]'
    '-S[override the RAID stride that was specified when the filesystem was created]:RAID-stride'
    '-z[before overwriting a file system block, write the old contents to an undo file]:undo_file:_files'
    '*:filename:_files'
)
_arguments -s $arguments
