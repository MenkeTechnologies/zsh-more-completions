#compdef rdist

# Autogenerated from man page
# Updated with latest rdist options

local arguments

arguments=(
    '-A[set the minimum number of free files (inodes) on a filesystem]:num'
    '-a[set the minimum amount of free space (in bytes) on a filesystem]:num'
    '-D[enable copious debugging messages]'
    '-d[define var to have value]:value'
    '-F[do not fork any child rdist processes]'
    '-f[set the name of the distfile to use]:distfile:_files'
    '-l[set local logging options]:logopts'
    '-L[set remote logging options]:logopts'
    '-M[set the maximum number of simultaneously running child rdist processes]:num'
    '-m[limit which machines are to be updated]:machine:_hosts'
    '-n[print the commands without executing them]'
    '-o[specify the dist options to enable]:distopts'
    '-p[set the path where the rdistd server is searched for on the target host]:rdistd-path:_files'
    '-P[set the path to the transport command to be used]:transport-path:_files'
    '-t[set the timeout period in seconds for waiting for responses]:timeout'
    '-c[interpret the remaining arguments as a small distfile]'
    '-V[print version information and exit]'
    '*:filename:_files'
)
_arguments -s $arguments
