#compdef pmdumptext

# Autogenerated from man page /usr/share/man/man1/pmdumptext.1.gz

local arguments

arguments=(
    '-h[option, or to one or more archives by the.]'
    '-a[option, pmdumptext will contact pmcd (1) on the local host to obtain the req…]'
    '-i[option which displays the values in equal width columns.]'
    '-P[the width of the columns with.]'
    '-w[and the format of the values with the .]'
    '-G[and.]'
    '-F[options for the shortest of scientific or fixed digits, and a fixed width for…]'
    '-A[.]'
    '-O[.]'
    '-S[and.]'
    '-T[control the alignment, offset, start and end time when visualizing metrics fr…]'
    '-C[Exit before dumping any values, but after parsing the metrics.]'
    '-m[.]'
    '-l[.]'
    '-N[and/or.]'
    '-u[are specified.]'
    '-c[If no metrics are listed on the command line, a config file can be used to s…]'
    '-d[Specify the delimiter that separates each column of output.]'
    '-f[Use the format string for formatting the timestamp with each set of values.]'
    '-g[Run in graphical user interface (GUI) mode, with pmtime being used for VCR-al…]'
    '-H[Show all headers before dumping any metric values. This is equivalent to.]'
    '--lmNu[.]:.'
    '-M[Output the column number and complete metric names before dumping any values.]'
    '-n[Load an alternative local PMNS from the file pmnsfile.]'
    '-o[When a timestamp is being reported (ie.]'
    '-p[Connect to pmtime (1) on the specified port .]'
    '-r[Output the raw metric values, do not convert counters to rates.]'
    '-R[Repeat the header every lines of output.]'
    '-s[pmdumptext will terminate after this many samples.]'
    '-t[The interval argument follows the syntax described in PCPIntro (1), and in th…]'
    '-U[Change the output when values are unavailable to string .]'
    '-X[Output the column number and complete metric names, one-per-line, both before…]'
    '-z[Use the local timezone of the host that is the source of the performance met…]'
    '-Z[Use timezone when displaying the date and time.]'
    '*:filename:_files'
)
_arguments -s $arguments
