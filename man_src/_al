#compdef al

# Autogenerated from man page /usr/local/share/man/man1/al.1

local arguments

arguments=(
    '--algid[Algorithm used to hash files. The <id> must be specified in hexadecimal.]:id'
    {--base,--baseaddress}'[Base address for the library. THIS FEATURE ISN\T IMPLEMENTED.]:addr'
    '--bugreport[Create a \Bug Report\ file. THIS FEATURE ISN\T IMPLEMENTED.]:file'
    {--comp,--company}'[This inserts the company name into the assembly metadata.]:text'
    {--config,--configuration}'[This inserts the configuration string into the assembly metadata.]:text'
    {--copy,--copyright}'[This inserts the copyright message into the assembly metadata.]:text'
    {--c,--culture}'[This inserts the supported culture into the assembly metadata.]:text'
    {--delay,--delay+,--delaysign,--delaysign+}'[The generated assembly will be delay signed.]'
    {--delay-,--delaysign-}'[The generated assembly will be fully signed (i. e. not delay signed).]'
    {--descr,--description}'[This inserts a description of the assembly into the assembly metadata.]:text'
    {--e,--evidence}'[This embed into the assembly the specified file as assembly security evidence…]:filename'
    '--fileversion[Optional Win32 version. This overrides the normal assembly version.]:version'
    '--flags[Assembly flags. The <flags> must be specified in hexadecimal.]'
    '--fullpaths[Display files using fully-qualified filenames.]:flags'
    {--keyf,--keyfile}'[Strongname (sign) the output assembly using the key pair present in the spec…]:filename'
    {--keyn,--keyname}'[Strongname (sign) the output assembly using the key pair present in the spec…]:text'
    '--main[Specifies the method name of the assembly entry point.]:method'
    '--nologo[Suppress the startup banner and copyright message.]'
    '--out[Output file name for the assembly manifest.]:filename'
    '--prod[This inserts the product name into the assembly metadata.]:product'
    '--productv[This inserts the product version into the assembly metadata.]'
    {--t,--target}'[Valid <target>s are: lib | library to create a library (.]:target'
    '--template[Specifies an assembly to get default options from.]:filename'
    '--title[This inserts an assembly title into the assembly metadata.]:text'
    {--trade,--trademark}'[This inserts a trademark message into the assembly metadata.]:text'
    {--v,--version}'[This inserts the assembly version into the assembly metadata.]:version'
    '--win32icon[Use this icon for the output.]:filename'
    '--win32res[Specifies the Win32 resource file.]:filename'
    {-\?,--help}'[Display information about AL.]'
    '*:filename:_files'
)
_arguments -s $arguments
