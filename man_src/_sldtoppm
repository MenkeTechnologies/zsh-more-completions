#compdef sldtoppm

# Autogenerated from man page /usr/share/man/man1/sldtoppm.1.gz

local arguments

arguments=(
    '--adjust[If the display on which the slide file was created had non-square pixels, whe…]'
    '--dir[The input is assumed to be an AutoCAD slide library file.]'
    '--height[Scales the image in the vector domain so it is size pixels in height.]:size"'
    '--info[Dump the slide file header on standard error, displaying the original screen …]'
    '--lib[Extracts the slide with the given name from the slide library given as input.]:name"'
    '--Lib[Extracts the slide with the given name from the slide library given as input.]:name"'
    '--scale[Scales the image by factor R s , which may be any floating point value greate…]:s"'
    '--verbose[Dumps the slide file header and lists every vector and polygon in the file on…]'
    '--width[Scales the image in the vector domain so it is size pixels wide.]:size"'
    '--xsize[Scales the image in the vector domain so it is size pixels wide.]:size"'
    '--ysize[option is specified, the height will be adjusted to preserve the pixel aspect…]'
    '*:filename:_files'
)
_arguments -s $arguments
