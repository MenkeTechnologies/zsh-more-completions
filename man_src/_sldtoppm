#compdef sldtoppm

# Autogenerated from man page /usr/share/man/man1/sldtoppm.1.gz
# Updated with fixes for truncated descriptions

local arguments

arguments=(
    '--adjust[Adjust for non-square pixels on the original display]'
    '--dir[Treat input as an AutoCAD slide library file]'
    '--height[Scale the image to the specified height in pixels]:size'
    '--info[Dump the slide file header on standard error]'
    '--lib[Extract the slide with the given name from a slide library]:name'
    '--Lib[Extract the slide with the given name (case-sensitive) from a slide library]:name'
    '--scale[Scale the image by factor]:factor'
    '--verbose[Dump the slide file header and list all vectors and polygons]'
    '--width[Scale the image to the specified width in pixels]:size'
    '--xsize[Scale the image to the specified width in pixels]:size'
    '--ysize[Scale the image to the specified height in pixels]:size'
    '*:filename:_files'
)
_arguments -s $arguments
