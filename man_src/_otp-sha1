#compdef otp-sha1

# Autogenerated from man page /usr/share/man/en.UTF-8/man1/otp-sha1.1.gz

local arguments

arguments=(
    '-v[Display the version number and compile-time options, then exit.]'
    '-h[Display a brief help message and exit.]'
    {-4,-5}'[Selects MD4 or MD5, respectively, as the response generation algorithm.]'
    '-f[Force opiekey to continue, even where it normally shouldn\t.]'
    '-a[Allows you to input an arbitrary secret pass phrase, instead of running check…]'
    '-n[the number of one time access passwords to print. The default is one.]:<count>'
    '-x[Output the OTPs as hexadecimal numbers instead of six words.]'
    '-t[Generate an extended response of the specified type. Supported types are: .]:<type>'
    '*:filename:_files'
)
_arguments -s $arguments
