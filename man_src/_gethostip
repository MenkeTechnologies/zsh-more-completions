#compdef gethostip

local arguments

arguments=(
    '-d[Output the IP address in decimal format]'
    '-x[Output the IP address in hexadecimal format]'
    '-n[Output the host canonical name]'
    '*:filename:_files'
)
_arguments -s $arguments
