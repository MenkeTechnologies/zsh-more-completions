#compdef slapcat

# Autogenerated from man page /usr/share/man/man8/slapcat.8
# Updated with fixes for truncated descriptions and trailing periods

local arguments

arguments=(
    '-a[Only dump entries matching the asserted filter]:filter'
    '-b[Use the specified suffix to determine which database to generate output for]:suffix'
    '-c[Enable continue (ignore errors) mode]'
    '-d[Enable debugging messages at the specified level]:debug-level'
    '-f[Specify an alternative slapd.conf file]:slapd.conf:_files'
    '-F[Specify a config directory]:confdir:_directories'
    '-g[Disable subordinate gluing]'
    '-H[Use dn, scope and filter from URI to only handle matching entries]:URI'
    '-l[Write LDIF to specified file instead of standard output]:ldif-file:_files'
    '-n[Generate output for the nth database listed in the configuration file]:dbnum'
    '-o[Specify an option with an optional value]:option'
    '-s[Only dump entries in the subtree specified by this DN]:subtree-dn'
    '-v[Enable verbose mode]'
    '*:filename:_files'
)
_arguments -s $arguments
