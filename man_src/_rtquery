#compdef rtquery

# Autogenerated from man page
# Updated with latest rtquery options

local arguments

arguments=(
    '-n[display only the numeric network and host numbers instead of symbolic names]'
    '-p[use the poll command to request full routing information]'
    '-1[query using RIP version 1 instead of RIP version 2]'
    '-w[change the delay for an answer from each host]:seconds'
    '-r[ask about the route to destination address]:addr'
    '-a[display the authentication present in each response]'
    '-t[change tracing, where op is one of the following]:op'
    '*:filename:_files'
)
_arguments -s $arguments
