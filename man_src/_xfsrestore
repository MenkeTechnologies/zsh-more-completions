#compdef xfsrestore

# Autogenerated from man page /usr/share/man/man8/xfsrestore.8.gz

local arguments

arguments=(
    '-r[option specifies the cumulative mode.]'
    '-l[option with level > 0) or a resumed dump (xfsdump.]'
    '-R[option). The deltas must be applied in the order they were produced.]'
    '-a[Each invocation of xfsrestore creates a directory called xfsrestorehousekeepi…]:housekeeping'
    '-b[Specifies the blocksize, in bytes, to be used for the restore.]:blocksize'
    '-c[Use the specified program to alert the operator when a media change is requir…]:progname'
    '-e[Prevents xfsrestore from overwriting existing files in the dest directory.]'
    {-f,-f}'[Specifies a source of the dump to be restored.]'
    '-i[Selects interactive operation.]'
    '-m[Use the minimal tape protocol.]'
    '-n[Allows xfsrestore to restore only files newer than file.]:file'
    '-o[Restore file and directory owner/group even if not root.]'
    '-p[Causes progress reports to be printed at intervals of interval seconds.]:interval'
    '-q[Source tape drive is a QIC tape.]'
    '-s[Specifies a subtree to restore. Any number of.]:subtree'
    '-t[Displays the contents of the dump, but does not create or modify any files or…]'
    '-v[.]:verbosity'
    '-A[Do not restore extended file attributes.]'
    '-B[Change the ownership and permissions of the destination directory to match th…]'
    '-D[Restore DMAPI (Data Management Application Programming Interface) event setti…]'
    '-E[Prevents xfsrestore from overwriting newer versions of files.]'
    '-F[Inhibit interactive operator prompts.]'
    '-I[Causes the xfsdump inventory to be displayed (no restore is performed).]'
    '-L[and.]'
    '-S[options). Suboptions to filter the inventory display are described later.]'
    '-J[Inhibits inventory update when on-media session inventory encountered during …]'
    '-K[Force xfsrestore to use dump format 2 generation numbers.]'
    '-O[Insert the options contained in options_file into the beginning of the comman…]:options_file'
    '-Q[Force completion of an interrupted restore session.]'
    '-T[Inhibits interactive dialogue timeouts.]'
    '-X[Specifies a subtree to exclude. This is the converse of the.]:subtree'
    '-Y[Specify I/O buffer ring length.]:io_ring_length'
    '*:filename:_files'
)
_arguments -s $arguments
