#compdef escript

local arguments

arguments=(
    '--include_lib[Include record definitions from a library module]:module'
    '-c[Compile the escript regardless of the mode attribute]'
    '-d[Debug the escript]'
    '-i[Interpret the escript regardless of the mode attribute]'
    '-s[Perform a syntactic and semantic check of the script file]'
    '-n[Compile the escript using +native flag]'
    '*:filename:_files'
)
_arguments -s $arguments
