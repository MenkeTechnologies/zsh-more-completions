#compdef mpathconf

# Autogenerated from man page /usr/share/man/man8/mpathconf.8.gz

local arguments

arguments=(
    '--with_module[If set to y, this runs modprobe dm_multipath to install the multipath modules.]'
    '--with_multipathd[Option is not set by default.]'
    '--user_friendly_names[Set user_friendly_names option.]:n'
    '--find_multipaths[Set find_multipaths option.]:n'
    '--enable[Removes any line that blacklists all device nodes from the /etc/multipath.conf file.]'
    '--disable[Adds a line that blacklists all device nodes to the /etc/multipath.conf file.]'
    '--allow[Modifies the /etc/multipath.conf blacklist to blacklist all wwids and the blacklist exceptions to allow the specified device.]:device'
    '--user_friendly_name[If set to y, this adds the line user_friendly_names yes to the /etc/multipath.conf file.]'
    '--outfile[Write the resulting multipath configuration to <filename> instead of /etc/multipath.conf.]:filename'
    '*:filename:_files'
)
_arguments -s $arguments
