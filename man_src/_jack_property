#compdef jack_property

# Autogenerated from man page /usr/share/man/man1/jack_property.1.gz

local arguments

arguments=(
    '-l[list all metadata currently defined.]'
    '-d[deletes all metadata for identifier.]:identifier'
    '-D[delete all currently defined metadata.]'
    '-s[sets the metadata associated with key to value for identifer, with its type s…]'
    '-c[interpret a given identifier as a client name rather than a UUID.]'
    '-p[interpret a given identifier as a port name rather than a UUID.]'
    '*:filename:_files'
)
_arguments -s $arguments
