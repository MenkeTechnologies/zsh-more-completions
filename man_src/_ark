#compdef ark

# Autogenerated from man page /usr/share/man/man1/ark.1.gz

local arguments

arguments=(
    {-d,--dialog}'[Show a dialog for specifying the options for a batch or add operation.]'
    {-m,--mimetypes}'[List supported MIME types.]'
    {-o,--destination}'[Default the extraction directory to directory.]':directory
    {-c,--add}'[Query the user for an archive filename and add specified files to it.]'
    {-t,--add-to}'[Add the specified files to filename. Create archive if it does not exist.]':filename
    {-p,--changetofirstpath}'[Change the current directory to the first entry and add all other entries rel…]'
    {-f,--autofilename}'[Automatically choose a filename, with the selected suffix (for example rar, t…]':suffix
    {-b,--batch}'[Use the batch interface instead of the usual dialog.]'
    {-e,--autodestination}'[The destination argument will be set to the path of the first file supplied.]'
    {-a,--autosubfolder}'[Archive contents will be read, and if detected to not be a single folder arch…]'
    {-O,--opendestination}'[Open the destination folder when the extraction is complete.]'
    '*:filename:_files'
)
_arguments -s $arguments
