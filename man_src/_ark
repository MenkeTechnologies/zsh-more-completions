#compdef ark

# Autogenerated from man page /usr/share/man/man1/ark.1.gz
# Updated with latest KDE Ark options

local arguments

arguments=(
    {-d,--dialog}'[Show a dialog for specifying options for a batch or add operation]'
    {-m,--mimetypes}'[List supported MIME types]'
    {-o,--destination}'[Default the extraction directory to directory]:directory:_directories'
    {-c,--add}'[Query the user for an archive filename and add specified files to it]'
    {-t,--add-to}'[Add the specified files to filename. Create archive if it does not exist]:filename:_files'
    {-p,--changetofirstpath}'[Change to the directory of the first entry and add all other entries relative to it]'
    {-f,--autofilename}'[Automatically choose a filename with the selected suffix]:suffix'
    {-b,--batch}'[Use the batch interface instead of the usual dialog]'
    {-e,--autodestination}'[Set the destination argument to the path of the first file supplied]'
    {-a,--autosubfolder}'[If archive is not a single folder archive, create a subfolder for extraction]'
    {-O,--opendestination}'[Open the destination folder when the extraction is complete]'
    '*:filename:_files'
)
_arguments -s $arguments
