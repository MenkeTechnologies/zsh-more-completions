#compdef nginx

# Autogenerated from man page /usr/share/man/man8/nginx.8.gz
# Updated with latest nginx options

local arguments

arguments=(
    '-\?[Print help]'
    '-h[Print help]'
    '-v[Print nginx version]'
    '-V[Print nginx version, compiler version, and configure script parameters]'
    '-t[Test the configuration file and exit]'
    '-T[Same as -t, but additionally dump configuration files to standard output]'
    '-q[Suppress non-error messages during configuration testing]'
    '-s[Send a signal to the master process]:signal:(stop quit reopen reload)'
    '-p[Set the prefix path (default: /usr/share/nginx)]:prefix:_directories'
    '-e[Set the error log file path (default: logs/error.log)]:file:_files'
    '-c[Use an alternative configuration file]:file:_files'
    '-g[Set global configuration directives]:directives'
    '*:filename:_files'
)
_arguments -s $arguments
