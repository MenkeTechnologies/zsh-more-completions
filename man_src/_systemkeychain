#compdef systemkeychain

# Autogenerated from man page /usr/share/man/man8/systemkeychain.8
# Updated with fixes for truncated descriptions and trailing periods

local arguments

arguments=(
    '-C[Create a keychain and establish it as the primary system keychain]'
    '-s[Extract the master secret from source keychains and install them in a destination]'
    '-t[Test unlocking the system keychain]'
    '-k[Use a keychain other than the default as the destination system keychain]:keychain:_files'
    '-c[Create the target keychain if necessary]'
    '-f[Force creation of a system keychain or overwriting of an existing key]'
    '-v[Turn on verbose mode]'
    '*:filename:_files'
)
_arguments -s $arguments
