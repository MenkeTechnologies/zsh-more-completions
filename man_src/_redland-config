#compdef redland-config

# Autogenerated from man page
# Updated with latest redland-config options

local arguments

arguments=(
    '--version[print the currently installed version of redland]'
    '--version-decimal[print the currently installed version of redland as a decimal integer]'
    '--libs[print the linker flags necessary to link a redland program]'
    '--libtool-libs[print the flags necessary to build with libtool]'
    '--private-libs[print the linker flags necessary to build the librdf shared library]'
    '--cflags[print the compiler flags necessary to compile a redland program]'
    '--prefix[use PREFIX instead of the installation prefix]:PREFIX'
    '*:filename:_files'
)
_arguments -s $arguments
