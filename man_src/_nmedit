#compdef nmedit

# Autogenerated from man page /Library/Developer/CommandLineTools/usr/share/man/man1/nmedit.1

local arguments

arguments=(
    '-s[Leave the symbol table entries for the global symbols listed in filename glo…]:filename"'
    '-R[Change the symbol table entries for the global symbols listed in filename in…]:filename"'
    '-p[Change symbols to private externs instead of static.]'
    '-A[Leave all global absolute symbols except those with a value of zero, and save…]'
    '--arch[Specifies the architecture, arch_type, of the file for R nmedit (1) to proce…]:arch_type"'
    '-g[option to cc (1)) for the global symbols it changes to static symbols so that…]'
    '-o[Write the result into the file output. SEE ALSO strip(1), ld(1), arch(3).]:output'
    '*:filename:_files'
)
_arguments -s $arguments
