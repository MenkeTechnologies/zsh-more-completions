#compdef nmedit

# Autogenerated from man page /Library/Developer/CommandLineTools/usr/share/man/man1/nmedit.1

local arguments

arguments=(
    '-s[Leave the symbol table entries for the global symbols listed in filename as global symbols.]:filename'
    '-R[Change the symbol table entries for the global symbols listed in filename into private extern symbols.]:filename'
    '-p[Change symbols to private externs instead of static.]'
    '-A[Leave all global absolute symbols except those with a value of zero, and save the relocation entries.]'
    '--arch[Specifies the architecture, arch_type, of the file for nmedit to process.]:arch_type'
    '-g[Keep global symbols unchanged for the symbols it changes to static so that the resulting object can still be used with the debugger.]'
    '-o[Write the result into the file output.]:output'
    '*:filename:_files'
)
_arguments -s $arguments
