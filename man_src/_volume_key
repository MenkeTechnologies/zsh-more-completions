#compdef volume_key

# Autogenerated from man page /usr/share/man/man8/volume_key.8.gz

local arguments

arguments=(
    '--save[Expects operands VOLUME \[PACKET\]. Open VOLUME.]'
    '--restore[Expects operands VOLUME PACKET.]'
    '--setup-volume[Expects operands VOLUME PACKET NAME.]'
    '--reencrypt[Expects operand PACKET.]'
    '--dump[Expects operand PACKET.]'
    '--secrets[Expects operand PACKET.]'
    '--help[Show usage information.]'
    '--version[Show version of volume_key.]'
    {-b,--batch}'[Run in batch mode.]'
    {-d,--nss-dir}'[Use private keys in NSS database in DIR to decrypt public key-encrypted pack…]':DIR
    {-o,--output}'[Write the default secret to PACKET.]':PACKET
    '--output-data-encryption-key[Write the data encryption key (the key directly used to encrypt the actual vo…]:PACKET'
    '--output-passphrase[Write a passphrase that can be used to access the volume to PACKET.]:PACKET'
    '--create-random-passphrase[Generate a random alphanumeric passphrase, add it to VOLUME (without affecti…]:PACKET'
    {-c,--certificate}'[Load a certificate from the file specified by CERT and encrypt all output pa…]':CERT
    '--output-format[Use FORMAT for all output packets.]:FORMAT'
    '--unencrypted[Only dump the unencrypted parts of the packet, if any, with --dump.]'
    '--with-secrets[Include secrets in the output of --dump.]'
    '*:filename:_files'
)
_arguments -s $arguments
