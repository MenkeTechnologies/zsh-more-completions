#compdef pollinate

# Autogenerated from man page /usr/share/man/man1/pollinate.1.gz

local arguments

arguments=(
    {-b,--binary}'[rather than producing hex text sha512sum(1) output, convert each two hex digi…]'
    {-c,--curl-opts}'[options to pass through to curl(1).]':CURL_OPTS
    {-d,--device}'[device to seed (default is /dev/urandom); note, you can use - to print to std…]':DEVICE
    {-n,--no-challenge}'[disable the challenge/response portion of the protocol; this enables a compat…]'
    {-i,--insecure}'[ignore SSL/TLS certificate errors (use of this option is highly discouraged).]'
    {-p,--pool}'[remote pollen(8) server pool to interact with; this option can be specified m…]':SERVER
    {-r,--reseed}'[by default, pollinate technically only needs to run successfully once ever; t…]'
    {-s,--server}'[remote pollen(8) server to interact with; if this option is used, the pool is…]':SERVER
    '--strict[handle network errors strictly and exit non-zero for any network error encoun…]'
    {-t,--testing}'[test communications with the pollen(8) server; note that this will NOT actual…]'
    {-q,--quiet}'[silence any log messages.]'
    {-w,--wait}'[time in seconds you\re willing to wait for a response from the server, uses t…]':WAIT
    '*:filename:_files'
)
_arguments -s $arguments
