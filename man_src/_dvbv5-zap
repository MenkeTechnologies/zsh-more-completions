#compdef dvbv5-zap

# Autogenerated from man page /usr/share/man/man1/dvbv5-zap.1.gz

local arguments

arguments=(
    {-c,--channels}'[Read channels list from \file\. Defaults to ~/. tzap/channels. conf.]'
    {-3,--dvbv3}'[Force dvbv5-zap to use DVBv3 only.]'
    {-a,--adapter}'[Use the given adapter. Default value: 0.]':adapter#
    {-A,--audio_pid}'[Select a different audio Packet ID (PID).]':audio_pid#
    {-C,--cc}'[Set the default country to be used by the MPEG-TS parsers, in ISO 3166-1 two …]':country_code
    {-d,--demux}'[Use the given demux. Default value: 0.]':demux#
    {-f,--frontend}'[Use the given frontend. Default value: 0.]':frontend#
    {-I,--input-format}'[Format of the input file. Please notice that caps is ignored.]':format
    {-l,--lnbf}'[Type of LNBf to use \help\ lists the available ones.]':LNBf_type
    {-L,--search}'[Search/look for a string inside the traffic. Used only on monitor mode.]':string
    {-m,--monitor}'[Enable monitor mode.]'
    {-o,--output}'[Output filename.]':file
    {-p,--pat}'[Add PAT and PMT MPEG-TS tables to TS recording (implies -r).]'
    {-P,--all-pids}'[Don\t filter any pids. Instead, outputs all of them.]'
    {-r,--record}'[Sets up the /dev/dvb/adapteradapter#/dvr0 for MPEG-TS record.]'
    {-s,--silence}'[Increases silence (can be used more than once).]'
    {-S,--sat_number}'[Satellite number. Used only on satellite delivery systems.]':satellite_number
    {-t,--timeout}'[Amount of seconds to keep the tool running for zapping and for recording.]':seconds
    {-U,--freq_bpf}'[SCR/Unicable band-pass filter frequency to use, in kHz.]':frequency
    {-v,--verbose}'[Be verbose. Useful to check if the MPEG-TS is happenning fine.]'
    {-V,--video_pid}'[video pid program to use (default 0).]':video_pid#
    {-w,--lna}'[Enable, disable or put LNA power in auto mode. Not all frontends support it.]':LNA
    '-1[.]:auto'
    {-W,--wait}'[Adds additional wait time for DISEqC command completion.]':time
    {-x,--exit}'[Exit after tuning.]'
    {-X,--low_traffic}'[Also shows DVB traffic with less than 1 packet per second.]'
    {-\?,--help}'[Outputs the usage help.]'
    '--usage[Gives a short usage message.]'
    '--version[Prints program version. EXIT STATUS On success, it returns 0. EXAMPLES.]'
    '*:filename:_files'
)
_arguments -s $arguments
