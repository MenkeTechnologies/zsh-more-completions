#compdef swipl-ld

# Autogenerated from man page /opt/homebrew/share/man/man1/swipl-ld.1

local arguments

arguments=(
    '--pl[Specifies the prolog version to use. The default is swipl.]:prolog"'
    '--help[Shows options briefly.]'
    '--nostate[Just relinks the kernel.]'
    '-c[Compile C or C++ source-files into object files.]'
    '-E[Invoke the C preprocessor.]'
    '--shared[Link C, C++ or object files into a shared object (DLL) that can be loaded by …]'
    '--embed-shared[Embed SWI-Prolog into a DLL/Shared object rather than an executable.]'
    '--dll["Windows only" Same as -embed-shared Backward compatibility.]'
    '-F[By default, none is passed to the swipl command to create the saved state.]:base"'
    '--goal[The goal that is initially executed when the toplevel is started using "PL_t…]:goal"'
    '--toplevel[The goal that is executed as the main toplevel goal. E. i. the default.]:goal"'
    '--initfile[The file that is loaded by the toplevel. E. i. the default.]:file"'
    '--class[Set the save-class of the Prolog saved-state that is created.]:{runtimekerneldevelopment}"'
    '-v[Verbose operation. This echos the commands before executing them.]'
    '-f[Fake operation.]'
    '-o[Specifies the name of the final executable. The default is a. out.]:file"'
    '--cc[Specify the compiler to use for *. c files.]:C-compiler"'
    '--c++[Specify the compiler to use for C++ input files.]:C++-compiler"'
    '--ld[Specifies the linker to use. Default is the C-compiler.]:linker"'
    '-l[Specifies a C-library for linking the application.]:"library"'
    '-L[Specifies an additional library directory for the C-compiler.]:"library-directory"'
    {-g,--I*,--D*,--U*,--O*,--W*}'[Passed to the compiler as c-flags.]'
    '--pl-options*[Specify additional options for swipl.]'
    '--ld-options*[Additional options passed to the linker. For example: -ld-options,-Bstatic.]'
    '--cc-options*[Additional options passed to both C- and C++-compiler.]'
    '--dump-runtime-variables[.]'
    '-t[.]:toplevel.'
    '--lswipl[as well as the libraries required by the SWI-Prolog kernel will be passed to …]'
    '--pl-options[determines the option-separator.]'
    {--cc-options,--ld-options}'[.]'
    '--Bstatic[.]'
    '*:filename:_files'
)
_arguments -s $arguments
