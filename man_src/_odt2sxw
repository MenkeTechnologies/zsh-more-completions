#compdef unoconv

# Autogenerated from man page /usr/share/man/man1/unoconv.1.gz

local arguments

arguments=(
    {-c,--connection}'[UNO connection string to be used by the client to connect to an LibreOffice i…]'
    {-d,--doctype}'[Specify the LibreOffice document type of the backend format.]'
    {-e,--export}'[Set specific export filter options (related to the used LibreOffice filter).]'
    {-f,--format}'[Specify the output format for the document.]'
    {-F,--field}'[Replace user-defined text field with value . sp . if n \\{\\.]'
    {-i,--import}'[Set specific import filters options (related to the used LibreOffice import f…]'
    {-l,--listener}'[Start unoconv as listener for unoconv clients to connect to.]'
    {-n,--no-launch}'[By default if no listener is running, unoconv will launch its own (temporary)…]'
    {-o,--output}'[If the argument is a directory, put the converted documents in this directory.]'
    '--password[Provide a password to decrypt the document.]'
    '--pipe[Use a pipe as an alternative connection mechanism to talk to LibreOffice.]'
    {-p,--port}'[Port to listen on (as listener) or to connect to (as client). sp . if n \\{\\.]'
    '--preserve[Keep timestamp and permissions of the original document.]'
    {-s,--server}'[Server (address) to listen on (as listener) or to connect to (as client). sp .]'
    '--show[List the possible output formats to be used with -f.]'
    '--stdin[Read input file from stdin (filenames are ignored if provided).]'
    '--stdout[Print converted output file to stdout.]'
    {-t,--template}'[Specify the template to use for importing styles from.]'
    {-T,--timeout}'[When unoconv starts its own listener, try to connect to it for an amount of s…]'
    {-v,--verbose}'[Be more and more and more verbose.]'
    '*:filename:_files'
)
_arguments -s $arguments
