#compdef pmdumplog

# Autogenerated from man page /usr/share/man/man1/pmdumplog.1.gz

local arguments

arguments=(
    '-d[Display the metadata and descriptions for those performance metrics that]'
    '-i[Display the instance domains, and any variations in their instance members]'
    '-l[Dump the archive label, showing the log format version, the time and date]'
    '-L[Like -l , just a little more verbose]'
    '-m[Print the values for the performance metrics from the archive]'
    '-r[Process the archive in reverse order, from most recent to oldest recorded]'
    '-S[When using the -m option, the report will be restricted to those records]'
    '-s[Report the size in bytes of each physical record in the archive]'
    '-T[When using the -m option, the report will be restricted to those records]'
    '-t[Dump the temporal index that is used to provide accelerated access to large]'
    '-v[Verbose mode]'
    '-n[option is specified an alternative local PMNS is loaded from the file]'
    '-a[Report everything, i. e. the flags]'
    '-x[Extended timestamp reporting format that includes the day of the week, day]'
    '-Z[option changes the timezone to timezone in the format of the environment]'
    '-z[option changes the timezone to the local timezone at the host that is the]'
    '*:filename:_files'
)
_arguments -s $arguments
