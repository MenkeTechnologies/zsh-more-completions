#compdef db_hotbackup

# Autogenerated from man page /usr/share/man/man1/db_hotbackup.1.gz

local arguments

arguments=(
    '-b[Specify the target directory for the backup.]:backup_dir'
    '-c[Before performing the snapshot, checkpoint the source database environment an…]'
    '-D[Use the data and log directories listed in a DB_CONFIG configuration file in …]'
    '-d[Specify one or more source directories that contain databases; if none is spe…]:data_dir'
    '-F[Directly copy from the filesystem.]'
    '-h[Specify the source directory for the backup, that is, the database environmen…]:home'
    '-l[Specify a source directory that contains log files; if none is specified, the…]:log_dir'
    '-P[Specify an environment password.]:password'
    '-u[Update a pre-existing hot backup snapshot by copying in new log files.]'
    '-V[Write the library version number to the standard output, and exit.]'
    '-v[Run in verbose mode, listing operations as they are done.]'
    '*:filename:_files'
)
_arguments -s $arguments
