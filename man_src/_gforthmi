#compdef gforth

# Autogenerated from man page /usr/share/man/man1/gforth.1.gz

local arguments

arguments=(
    '--image-file[TQ "-i " "file" Loads the Forth image file instead of the default \\fCgforth.]:"file'
    '--path[TQ "-p " "path" Uses path for searching the image file and Forth source cod…]:"path'
    '--dictionary-size[TQ "-m " "size" Allocate size space for the Forth dictionary space instead …]:"size'
    '--data-stack-size[TQ "-d " "size" Allocate size space for the data stack instead of using the…]:"size'
    '--return-stack-size[TQ "-r " "size" Allocate size space for the return stack instead of using t…]:"size'
    '--fp-stack-size[TQ "-f " "size" Allocate size space for the floating point stack instead of…]:"size'
    '--locals-stack-size[TQ "-l " "size" Allocate size space for the locals stack instead of using t…]:"size'
    '--help[.]'
    '-h[Lists the available options, including some not described here (see also the …]'
    '-i[Loads the Forth image file instead of the default gforth. fi.]:file'
    '-p[Uses path for searching the image file and Forth source code files instead of…]:path'
    '-m[Allocate size space for the Forth dictionary space instead of using the defau…]:size'
    '-d[Allocate size space for the data stack instead of using the default specified…]:size'
    '-r[Allocate size space for the return stack instead of using the default specifi…]:size'
    '-f[Allocate size space for the floating point stack instead of using the default…]:size'
    '-l[Allocate size space for the locals stack instead of using the default specifi…]:size'
    '--evaluate[.]:forth'
    '-e[Evaluates the forth code.]:forth'
    '*:filename:_files'
)
_arguments -s $arguments
