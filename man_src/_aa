#compdef aa

# Autogenerated from man page /usr/share/man/man1/aa.1
# Updated with latest Apple Archive options

local arguments

arguments=(
    '-v[Increase verbosity. Default is silent operation]'
    '-h[Print usage and exit]'
    '-d[Target directory for archive/extract. Default is current directory]:dir:_directories'
    '-i[Input file. Default is stdin]:input_file:_files'
    '-o[Output file. Default is stdout]:output_file:_files'
    '-D[Set both dir and subdir from a single path]:dir_and_subdir:_directories'
    '-a[Compression algorithm used when creating archives]:algorithm:(lzfse lzma lz4 zlib raw)'
    '-b[Block size used when compressing archives]:block_size'
    '-t[Number of worker threads compressing/decompressing data]:worker_threads'
    '--subdir[Path to archive under dir]:subdir'
    '--wt[Number of writer threads extracting archive content]:writer_threads'
    {--enable-dedup,--no-enable-dedup}'[If set, files with same data will be stored as clone copies]'
    {--enable-holes,--no-enable-holes}'[If set, detect and create holes in files to store them more efficiently]'
    {--ignore-eperm,--no-ignore-eperm}'[If set, ignore EPERM errors when setting file attributes]'
    '--manifest[Alias for -exclude-field dat -include-field sh1,ckssiz]'
    '--include-path[Include entry paths exactly matching path]:path'
    '--exclude-path[Exclude entry paths exactly matching path]:path'
    '--include-path-list[File containing a list of paths to include, one entry per line]:path_list_file:_files'
    '--exclude-path-list[File containing a list of paths to exclude, one entry per line]:path_list_file:_files'
    '--include-regex[Include entry paths matching regular expression]:expr'
    '--exclude-regex[Exclude entry paths matching regular expression]:expr'
    '--exclude-name[Exclude entry paths where a single component matches exactly]:name'
    '--include-type[Include only entries matching the given types]:type_spec'
    '--exclude-type[Include only entries not matching the given types]:type_spec'
    '--include-field[Add the given fields to the set of field keys]:field_spec'
    '--exclude-field[Remove the given fields from the set of field keys]:field_spec'
    '*:filename:_files'
)
_arguments -s $arguments
