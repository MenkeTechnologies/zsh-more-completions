#compdef rasterizer

# Autogenerated from man page
# Updated with latest rasterizer options

local arguments

arguments=(
    '--scriptSecurityOff[remove any security check on the scripts running as a result of dispatching SVG events]'
    '--scriptSecurityOn[turn security back on if this is the first argument]'
    '--anyScriptOrigin[control whether scripts can be loaded from any location]'
    '-d[output directory or file]:<dir|file>:_files'
    '-m[output mime type]:<mimeType>'
    '-w[output width (floating point value)]:<width>'
    '-h[output height (floating point value)]:<height>'
    '--maxw[maximum output width (floating point value)]:<width>'
    '--maxh[maximum output height (floating point value)]:<height>'
    '-a[output area]:<area>'
    '--bg[output background color]:<color>'
    '--cssMedia[CSS media type for source SVG file conversion]:<media>'
    '--cssAlternate[CSS alternate stylesheet to use when converting]:<alternate>'
    '--cssUser[CSS user stylesheet URI to apply to converted SVG documents]:<userStylesheet>'
    '--font-family[default font-family value]:<defaultFontFamily>'
    '--lang[user language for SVG document conversion]:<userLanguage>'
    '-q[quality for the output image]:<quality>'
    '--indexed[reduce image to given number of bits per pixel using adaptive palette]:(1 2 4 8)'
    '--dpi[resolution for the output image]:<resolution>'
    '--validate[control whether source SVG files should be validated]'
    '--onload[rasterize after dispatching the onload event]'
    '--scripts[values for the type attribute in script tags which should be loaded]'
    '*:filename:_files'
)
_arguments -s $arguments
