#compdef rasterizer

# Autogenerated from man page /usr/share/man/man1/rasterizer.1.gz

local arguments

arguments=(
    '--scriptSecurityOff[removes any security check on the scripts running as a result of dispatching …]'
    '--scriptSecurityOn[If this is the first argument on the command-line, turn security back on.]'
    '--anyScriptOrigin[controls whether scripts can be loaded from any location.]'
    '-d[output directory. If there is a single input file, this can be a file.]:<dir|file>'
    '-m[output mime type.]:<mimeType>'
    '-w[output width. This is a floating point value.]:<width>'
    '-h[output height. This is a floating point value.]:<height>'
    '--maxw[Maximum output width. This is a floating point value.]:<width>'
    '--maxh[Maximum output height. This is a floating point value.]:<height>'
    '-a[output area.]:<area>'
    '--bg[output color. The format for <color> is a. r. g.]:<color>'
    '--cssMedia[CSS media type for which the source SVG files should be converted.]:<media>'
    '--cssAlternate[CSS alternate stylesheet to use when converting the source SVG files.]:<alternate>'
    '--cssUser[CSS user stylesheet URI to apply to converted SVG documents in addition to an…]:<userStylesheet>'
    '--font-family[Value used as a default when no font-family value is specified.]:<defaultFontFamily>'
    '--lang[User language to use when converting SVG documents.]:<userLanguage>'
    '-q[Quality for the output image.]:<quality>'
    '--indexed[Reduces the image to given number of bits per pixel using an adaptive pallete…]:(1|2|4|8)'
    '--dpi[Resolution for the ouptut image.]:<resolution>'
    '--validate[Controls whether the source SVG files should be validated.]'
    '--onload[Controls if the source SVG files must be rasterize after dispatching the \onl…]'
    '--scripts[values for the type attribute in the <script> tag) which should be loaded.]'
    '*:filename:_files'
)
_arguments -s $arguments
