#compdef blacklistd

# Autogenerated from man page
# Updated with latest FreeBSD blacklistd options

local arguments

arguments=(
    '-C[Use the specified program to communicate with the packet filter]:controlprog:_files'
    '-c[Specify the configuration file to read]:config_file:_files'
    '-D[Specify the Berkeley DB state file]:db_file:_files'
    '-d[Do not daemonize, run in the foreground]'
    '-f[Truncate the state database and flush all rules with specified name]:rulename'
    '-P[Specify a file containing a list of pathnames to listen on]:path_file:_files'
    '-R[Specify the default rule name for packet filter rules]:rulename'
    '-r[Re-read firewall rules from the internal database and re-add them]'
    '-s[Add a Unix socket path to the list blacklistd listens to]:sockpath'
    '-t[Interval in seconds to poll the state file for rule updates]:seconds'
    '-v[Print diagnostic messages to stdout instead of syslog]'
    '*:filename:_files'
)
_arguments -s $arguments
