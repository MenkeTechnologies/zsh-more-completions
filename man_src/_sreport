#compdef sreport

# Autogenerated from man page /usr/share/man/man1/sreport.1.gz
# Updated with fixes for truncated descriptions and trailing periods

local arguments

arguments=(
    {-a,--all_clusters}'[Use all clusters instead of only the local cluster]'
    '--federation[Generate a report for the federation if a member of one]'
    '(- * :)'{-h,--help}'[Print a help message describing the usage of sreport]'
    '--local[Generate a report for the local cluster even if part of a federation]'
    {-M,--cluster}'[The cluster(s) to generate reports for]:clusters'
    {-n,--noheader}'[Do not display header when listing results]'
    {-p,--parsable}'[Output will be pipe delimited with a trailing pipe]'
    {-P,--parsable2}'[Output will be pipe delimited without a trailing pipe]'
    {-Q,--quiet}'[Print no warning or informational messages, only error messages]'
    '-t[Specify the output time format]:format'
    {-T,--tres}'[Trackable resource (TRES) to report values for]:tres'
    {-v,--verbose}'[Print detailed event logging]'
    '*:filename:_files'
)
_arguments -s $arguments
