#compdef net

# Autogenerated from man page /usr/share/man/man8/net.8.gz

local arguments

arguments=(
    {-\?,--help}'[Print a summary of command line options.]'
    {-k,--kerberos}'[Try to authenticate with kerberos.]'
    {-w,--workgroup}'[Sets target workgroup or domain.]':target-workgroup
    {-W,--myworkgroup}'[Sets client workgroup or domain.]':workgroup
    {-U,--user}'[User name to use.]':user
    {-I,--ipaddress}'[IP address of target server to use.]':ip-address
    {-p,--port}'[Port on the target server to connect to (usually 139 or 445).]':port
    {-n,--netbiosname}'[This option allows you to override the NetBIOS name that Samba uses for itsel…]'
    {-S,--server}'[Name of target server.]':server
    {-l,--long}'[When listing data, give more information on each item.]'
    {-v,--verbose}'[When listing data, give more verbose information on each item.]'
    {-f,--force}'[Enforcing a net command.]'
    {-P,--machine-pass}'[Make queries to the external server using the machine account of the local se…]'
    '--request-timeout[Let client requests timeout after 30 seconds the default is 10 seconds.]:30'
    {-t,--timeout}'[Set timeout for client operations to 30 seconds.]':30
    '--use-ccache[Try to use the credentials cached by winbind.]'
    {-i,--stdin}'[Take input for net commands from standard input.]'
    '--tallocreport[Generate a talloc report while processing a net command.]'
    {-T,--test}'[Only test command sequence, dry-run.]'
    {-F,--flags}'[Pass down integer flags to a net subcommand.]':FLAGS
    {-C,--comment}'[Pass down a comment string to a net subcommand.]':COMMENT
    '--myname[Use MYNAME as a requester name for a net subcommand.]:MYNAME'
    {-c,--container}'[Use a specific AD container for net ads operations.]':CONTAINER
    {-M,--maxusers}'[Fill in the maxusers field in net rpc share operations.]':MAXUSERS
    {-r,--reboot}'[Reboot a remote machine after a command has been successfully executed (e. g.]'
    '--force-full-repl[When calling "net rpc vampire keytab" this option enforces a full re-creation…]'
    '--single-obj-repl[When calling "net rpc vampire keytab" this option allows one to replicate jus…]'
    '--clean-old-entries[When calling "net rpc vampire keytab" this option allows one to cleanup old e…]'
    '--db[Define dbfile for "net idmap" commands.]'
    '--lock[Activates locking of the dbfile for "net idmap check" command.]'
    {-a,--auto}'[Activates noninteractive mode in "net idmap check".]'
    '--repair[Activates repair mode in "net idmap check".]'
    '--acls[Includes ACLs to be copied in "net rpc share migrate".]'
    '--attrs[Includes file attributes to be copied in "net rpc share migrate".]'
    '--timestamps[Includes timestamps to be copied in "net rpc share migrate".]'
    {-X,--exclude}'[Allows one to exclude directories when copying with "net rpc share migrate".]':DIRECTORY
    '--destination[Defines the target servername of migration process (defaults to localhost).]:SERVERNAME'
    {-L,--local}'[Sets the type of group mapping to local (used in "net groupmap set").]'
    {-D,--domain}'[Sets the type of group mapping to domain (used in "net groupmap set").]'
    {-N,--ntname}'[Sets the ntname of a group mapping (used in "net groupmap set").]':NTNAME
    {-R,--rid}'[Sets the rid of a group mapping (used in "net groupmap set").]':RID
    '--reg-version[Assume database version {n|1,2,3} (used in "net registry check").]:REG_VERSION'
    {-o,--output}'[Output database file (used in "net registry check").]':FILENAME
    '--wipe[Create a new database from scratch (used in "net registry check").]'
    '--precheck[Defines filename for database prechecking (used in "net registry import").]:PRECHECK_DB_FILENAME'
    '--no-dns-updates[Do not perform DNS updates as part of "net ads join".]'
    '--keep-account[Prevent the machine account removal as part of "net ads leave".]'
    {-e,--encrypt}'[This command line parameter requires the remote server support the UNIX exten…]'
    {-d,--debuglevel}'[level is an integer from 0 to 10.]':level
    {-V,--version}'[Prints the program version number.]'
    {-s,--configfile}'[The file specified contains the configuration details required by the server.]':file
    '--log-basename[Base directory name for log/debug files. The extension ".]:logdirectory'
    '--option[Set the smb. conf(5) option "<name>" to value "<value>" from the command line.]:<value>'
    '*:filename:_files'
)
_arguments -s $arguments
