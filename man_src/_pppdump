#compdef pppdump

# Autogenerated from man page /usr/share/man/man8/pppdump.8.gz

local arguments

arguments=(
    '-h[Prints the bytes sent and received in hexadecimal]'
    '-p[Collects the bytes sent and received into PPP packets, interpreting the]'
    '-d[With the -p option, this option causes pppdump to decompress packets which]'
    '-r[Reverses the direction indicators, so that `sent\ is printed for bytes or]'
    '-m[Use mru as the MRU (maximum receive unit) for both directions of the link]:mru'
    '*:filename:_files'
)
_arguments -s $arguments
