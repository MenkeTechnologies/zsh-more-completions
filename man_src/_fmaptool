#compdef fmaptool

local arguments

arguments=(
  '-h[also produce a C header defining FMAP_OFFSET to the FMAP sections flash offset.]'
  '-R[also produce a text file listing the CBFS regions, comma separated.]'
  '*:filename:_files'
)

_arguments -s $arguments
