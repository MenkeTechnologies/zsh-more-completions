#compdef fitstopnm

# Autogenerated from man page /usr/share/man/man1/fitstopnm.1.gz

local arguments

arguments=(
    '--image[flag is specified.]'
    '--min[and .]'
    '--max[can be used to override the min and max values as read from the FITS header o…]'
    '--scanmax[can be used to force the program to scan the data even when DATAMIN and DATAM…]'
    '--printmax[is specified, the program will just print the min and max values and quit.]'
    '--noraw[can be used to force the program to produce an ASCII portable anymap.]'
    '*:filename:_files'
)
_arguments -s $arguments
