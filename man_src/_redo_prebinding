#compdef redo_prebinding

# Autogenerated from man page
# Updated with latest redo_prebinding options

local arguments

arguments=(
    '-c[only check if the file needs to have its prebinding redone and return status]'
    '-p[check only for prebound files and return status]'
    '-d[check only for dynamic shared library files and return status]'
    '-i[ignore non-prebound files (useful when running on all types of files)]'
    '-z[zero out the prebind check sum in the output if it has one]'
    '-u[unprebind, rather than reprebind (-c, -p, -d, -e ignored)]'
    '-e[replace any dependent library "@executable_path" prefix with the executable path]:executable_path:_files'
    '--seg_addr_table[use the segment address table file]:table_file_name:_files'
    '--seg_addr_table_filename[use pathname instead of the install name for matching]:pathname:_files'
    '--seg1addr[move the input library to base address]:address'
    '-r[prepend the rootdir argument to all dependent libraries]:rootdir:_directories'
    '-o[write the updated file into output_file rather than back into the input file]:output_file:_files'
    '-s[write the updated file to standard output]'
    '*:filename:_files'
)
_arguments -s $arguments
