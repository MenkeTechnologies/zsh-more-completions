#compdef dsync

# Autogenerated from man page /usr/share/man/man1/dsync.1.gz

local arguments

arguments=(
    '-c[read configuration from the given R config-file .]:config-file'
    '-D[Activates debug messages and makes dsync more verbose.]'
    '-f[Makes dsync run in \\(dqfull sync\\(dq mode rather than \\(dqfast sync\\(dq mode.]'
    '-m[Specifies the mailbox that should be synchronized or from which mails should…]:mailbox'
    '-o[Overrides the configuration setting from /etc/dovecot/dovecot.]'
    '-R[Reverse backup direction, so mails in location2 are backed up to default mai…]'
    '-u[Specifies that the userdb lookup for the given user should be done and used …]:user'
    '-C[Specifies an alternative mailbox name character.]:alt_char'
    '-v[Makes dsync more verbose.]'
    '*:filename:_files'
)
_arguments -s $arguments
