#compdef qterminal

# Autogenerated from man page
# Updated with latest qterminal options

local arguments

arguments=(
    {-d,--drop}'[start in "dropdown mode" (like Yakuake or Tilda)]'
    {-e,--execute}'[execute command instead of shell]:command'
    {-h,--help}'[print this help]'
    {-p,--profile}'[load qterminal with specific options]:profile'
    {-v,--version}'[print application version and exit]'
    {-w,--workdir}'[start session with specified work directory]:directory:_directories'
    '*:filename:_files'
)
_arguments -s $arguments
