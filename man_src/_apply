#compdef apply

# Autogenerated from man page /usr/share/man/man1/apply.1
# Updated with latest apply options

local arguments

arguments=(
    '-a[Use specified character as magic character instead of default %]:char'
    '-d[Display the commands that would have been executed, but do not actually execute them]'
    '-#[Take # arguments at a time from the argument list]'
    '*:argument:'
)
_arguments -s $arguments
