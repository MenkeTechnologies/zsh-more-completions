#compdef rrsync

# Autogenerated from man page
# Updated with latest rrsync options

local arguments

arguments=(
    '--ro[allow only reading from the DIR (implies --no-del and --no-lock)]'
    '--wo[allow only writing to the DIR]'
    '--munge[enable rsync --munge-links on the server side]'
    '--no-del[disable rsync --delete* and --remove* options]'
    '--no-lock[avoid the single-run (per-user) lock check (useful with --munge)]'
    '*:directory:_directories'
)
_arguments -s $arguments
