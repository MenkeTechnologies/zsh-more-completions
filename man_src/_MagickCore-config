#compdef MagickCore-config

# Autogenerated from man page /usr/local/share/man/man1/MagickCore-config.1

local arguments

arguments=(
    '--cppflags[Print the compiler flags that are needed to find the ImageMagick 1 C include …]'
    '--prefix[Print the directory under which target specific binaries and executables are …]'
    '--ldflags[Print the linker flags that are needed to link with the ImageMagick 1 library.]'
    '--version[Print the version of the ImageMagick 1 distribution to standard output.]'
    '--coder-path[Print the path where the ImageMagick 1 coder modules are installed.]'
    '--filter-path[Print the path where the ImageMagick 1 filter modules are installed.]'
    '*:filename:_files'
)
_arguments -s $arguments
