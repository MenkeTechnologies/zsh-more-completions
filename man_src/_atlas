#compdef atlas

# Autogenerated from man page
# Updated with latest Nim Atlas package manager options

local arguments

arguments=(
    '--keepCommits[Do not perform any git checkouts]'
    '--cfgHere[Also create/maintain a nim.cfg in the current working directory]'
    '--workspace[Use DIR as workspace]:DIR:_directories'
    '--project[Use DIR as the current project]:DIR:_directories'
    '--noexec[Do not perform any action that may run arbitrary code]'
    '--autoenv[Detect the minimal Nim version and setup a corresponding virtual environment]'
    '--autoinit[Auto initialize a workspace]'
    '--colors[Turn on or off colored output]:toggle:(on off)'
    '--resolver[Which resolution algorithm to use]:algorithm:(minver semver maxver)'
    '--showGraph[Show the dependency graph]'
    '--list[List all available and installed versions]'
    '--version[Show the version]'
    '--deps[Use DIR as the directory for dependencies]:DIR:_directories'
    '--keep[Keep the c_code subdirectory]'
    '--help[Show help]'
    '*:filename:_files'
)
_arguments -s $arguments
