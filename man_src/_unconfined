#compdef aa-unconfined

local arguments

arguments=(
    '--paranoid[Display all processes from /proc with tcp or udp ports that do not have AppArmor profiles]'
    '--with-ss[Use the ss command to find processes listening on network sockets]'
    '*:filename:_files'
)
_arguments -s $arguments
