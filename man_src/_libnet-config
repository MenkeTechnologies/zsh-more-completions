#compdef libnet-config

# Autogenerated from man page /usr/share/man/man1/libnet-config.1.gz

local arguments

arguments=(
    '--cflags[Set of compiler options (CFLAGS) to use when compiling files that use libnet.]'
    '--libs[Shows the complete set of libs and other linker options you will need in orde…]'
    '--defines[Set of compiler defines used to compile libnet.]'
    '*:filename:_files'
)
_arguments -s $arguments
