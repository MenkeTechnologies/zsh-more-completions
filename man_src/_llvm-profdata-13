#compdef llvm-profdata-13

# Autogenerated from man page /usr/share/man/man1/llvm-profdata-13.1.gz

local arguments

arguments=(
    '--help[Print a summary of command line options.]'
    {--output,-o}'[Specify the output file name.]'
    '--weighted-input[Specify an input file name along with a weight.]:weightfilename'
    {--input-files,-f}'[Specify a file which contains a list of files to merge.]'
    {--remapping-file,-r}'[Specify a file which contains a remapping from symbol names in the input prof…]'
    '--instr[Specify that the input profile is an instrumentation-based profile.]:(default)'
    '--sample[Specify that the input profile is a sample-based profile.]'
    '--binary[Emit the profile using a binary encoding.]:(default)'
    '--extbinary[Emit the profile using an extensible binary encoding.]'
    '--text[Emit the profile in text mode.]'
    '--gcc[Emit the profile using GCCâs gcov format (Not yet supported).]'
    '--sparse[Do not emit function records with 0 execution count.]:true|false'
    {--num-threads,-j}'[Use N threads to perform profile merging.]'
    '--failure-mode[Set the failure mode.]:any|all'
    '--prof-sym-list[Specify a file which contains a list of symbols to generate profile symbol li…]:path'
    '--compress-all-sections[Compress all sections when writing the profile.]:true|false'
    '--use-md5[Use MD5 to represent string in name table when writing the profile.]:true|false'
    '--gen-partial-profile[Mark the profile to be a partial profile which only provides partial profile …]:true|false'
    '--supplement-instr-with-sample[Supplement an instrumentation profile with sample profile.]:path_to_sample_profile'
    '--zero-counter-threshold[For the function which is cold in instr profile but hot in sample profile, if…]:threshold_float_number'
    '--instr-prof-cold-threshold[User specified cold threshold for instr profile which will override the cold …]:threshold_int_number'
    '--suppl-min-size-threshold[If the size of a function is smaller than the threshold, assume it can be inl…]:threshold_int_number'
    '--all-functions[Print details for every function.]'
    '--counts[Print the counter values for the displayed functions.]'
    '--function[Print details for a function if the functionâs name contains the given stri…]:string'
    '--topn[Instruct the profile dumper to show the top n functions with the hottest basi…]:n'
    '--memop-sizes[Show the profiled sizes of the memory intrinsic calls for shown functions.]'
    '--value-cutoff[Show only those functions whose max count values are greater or equal to n.]:n'
    '--list-below-cutoff[Only output names of functions whose max count value are below the cutoff val…]'
    '--showcs[Only show context sensitive profile counts.]'
    '--show-prof-sym-list[Show profile symbol list if it exists in the profile.]:true|false'
    '--show-sec-info-only[Show basic information about each section in the profile.]:true|false'
    '--cs[Only show overlap for the context sensitive profile counts.]'
    '*:filename:_files'
)
_arguments -s $arguments
