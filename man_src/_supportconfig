#compdef supportconfig

# Autogenerated from man page /usr/share/man/man8/supportconfig.8.gz
# Updated with fixes for truncated descriptions and trailing periods

local arguments

arguments=(
    '-h[Display this screen]'
    '-A[Activate all supportconfig functions with additional logging]'
    '-B[Include string in the tar ball filename]:string'
    '-C[Create a new default /etc/supportconfig.conf]'
    '-D[Use defaults, ignore /etc/supportconfig.conf]'
    '-E[Contact email address]:email'
    '-F[Display available supportconfig feature keywords]'
    '-G[GPG recipient user ID used to encrypt the tar ball]:gpg_uid'
    '-H[Limit number of included HA Policy engine files]:number'
    '-I[Default log file line count]:number'
    '-L[Create a full file listing from /]'
    '-M[Contact company terminal ID]:string'
    '-N[Contact name]:string'
    '-O[Contact company name]:string'
    '-P[Contact phone number]:string'
    '-Q[Run in quiet mode, no output]'
    '-R[Directory path for supportconfig output files]:directory:_directories'
    '-S[Limit number of included SAR files]:number'
    '-T[Binary execution timeout]:seconds'
    '-U[Set the upload target URI and initiate an upload]:URI'
    '-W[Contact company store ID]:string'
    '-X[Max system logs line count]:number'
    '-a[Upload the tar ball to the alternate target]'
    '-b[Switch to screen buffer mode]'
    '-c[Disable curl checks to SUSE update servers]'
    '-d[Exclude detailed disk information and scans]'
    '-f[Use files in the given directory instead of collecting]:directory:_directories'
    '-g[Use gzip instead of the default bzip2 compression]'
    '-i[Include keywords]:keywords'
    '-k[Disable automatic kernel module loading]'
    '-l[Include all log file lines and gather additional rotated logs]'
    '-m[Only gather a minimum amount of info]'
    '-o[Toggle keywords on or off]:keywords'
    '-p[Disable all plugin execution]'
    '-q[Add a unique identifier to the supportconfig tar ball filename]'
    '-r[Include the service request number in basic-environment]:srnum'
    '-s[Include full SLP service lists]'
    '-t[Just save log files here, do not create tarball]:directory:_directories'
    '-u[Upload the supportconfig tar ball to the specified target]'
    '-v[Perform an rpm -V for each installed rpm]'
    '-w[Enable verbose wait trace logging]'
    '-x[Only exclude keywords]:keywords'
    '-y[Gather full YaST log files]'
    '*:filename:_files'
)
_arguments -s $arguments
