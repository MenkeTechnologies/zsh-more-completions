#compdef vgrind

# Autogenerated from man page /usr/share/man/en.UTF-8/man1/vgrind.1.gz

local arguments

arguments=(
    '-W[forces output to the (wide) Versatec printer rather than the (narrow) Varian.]'
    '-d[specifies an alternate language definitions file (default is /usr/share/misc/…]'
    '-f[forces filter mode.]'
    '-h[specifies a particular header to put on every output page (default is the fil…]'
    '-l[specifies the language to use.]'
    '-n[forces no keyword bolding.]'
    '-p[use postproc to post-process the output, psroff 1 by default.]'
    '-s[specifies a point size to use on output (exactly the same as the argument of …]'
    '-t[similar to the same option in troff 1 causing formatted text to go to the sta…]'
    '-x[outputs the index file in a ``pretty\\ format.]'
    '*:filename:_files'
)
_arguments -s $arguments
