#compdef airmon-ng

# Autogenerated from man page /usr/share/man/man8/airmon-ng.8.gz
# Updated with latest aircrack-ng airmon-ng options

local arguments

arguments=(
    '--verbose[Verbose output, can be combined with other parameters]'
    '--debug[Debug output, can be combined with other parameters]'
    '--elite[Do not clean up interfaces on start/stop (DANGEROUS)]'
    '*:: :->subcmd'
)

_arguments -s $arguments

case "$state" in
    subcmd)
        local -a commands
        commands=(
            'start:Start monitor mode on interface'
            'stop:Stop monitor mode on interface'
            'check:Check for processes that might interfere'
        )
        _describe 'command' commands
        ;;
esac
