#compdef bind9-config

# Autogenerated from man page
# Updated with latest ISC BIND bind9-config options

local arguments

arguments=(
    '--cflags[Print compiler command line options required to compile files that use ISC BIND]'
    '--exec-prefix[Print the directory prefix used for architecture-dependent files]'
    '--libs[Print linker command line options used to link with ISC BIND libraries]'
    '--prefix[Print the directory prefix used in the ISC BIND installation]'
    '--version[Print the version of the installed ISC BIND suite]'
    '*:filename:_files'
)
_arguments -s $arguments
