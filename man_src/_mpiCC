#compdef mpic++

# Autogenerated from man page /usr/share/man/man1/mpic++.1.gz

local arguments

arguments=(
    '--showme[This option comes in several different variants (see below).]'
    '--showme:compile[Output the compiler flags that would have been supplied to the C++ compiler.]'
    '--showme:link[Output the linker flags that would have been supplied to the C++ compiler.]'
    '--showme:command[Outputs the underlying C++ compiler command (which may be one or more tokens).]'
    '--showme:incdirs[Outputs a space-delimited (but otherwise undecorated) list of directories that would have been in the include path.]'
    '--showme:libdirs[Outputs a space-delimited (but otherwise undecorated) list of directories that would have been in the library search path.]'
    '--showme:libs[Outputs a space-delimited (but otherwise undecorated) list of library names that would have been linked.]'
    '--showme:version[Outputs the version number of Open MPI.]'
    '--showme:help[Output a brief usage help message.]'
    '--qfixed[May be necessary to compile fixed-format Fortran source files.]'
    '--m64[Build 64-bit binaries, can be activated in a binary package providing both 32 and 64 bit support.]'
    '*:filename:_files'
)
_arguments -s $arguments
