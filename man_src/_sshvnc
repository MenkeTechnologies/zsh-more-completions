#compdef ssvnc

# Autogenerated from man page /usr/share/man/man1/ssvnc.1.gz

local arguments

arguments=(
    {--help,-h}'[Prints out to the terminal a brief description and the options.]'
    '--help[Starts up the GUI as though the \Help\ button was pressed to show the main He…]'
    '--cmd[Launch the ssvnc_cmd utility command directly (no GUI) with the given argumen…]:ssvnc_cmd-args'
    '--viewer[Shorthand for ssvnc -cmd -viewer \[viewer-args\].]:viewer-args'
    '--profiles[List the saved SSVNC profiles you have created.]'
    '--list[Same as -profiles.]'
    '--ssh[Start in "SSH Only Mode". No SSL aspects are shown.]'
    '--ts[Start in "Terminal Services Mode".]'
    '--tso[Same as -ts "Terminal Services Mode", however never let the user leave this m…]'
    '--ssl[Force the full GUI Mode: both SSL and SSH. This is the default.]'
    '--nv[Toggle the "Verify All Certs" button to be off at startup.]'
    '--nvb[Never show the "Verify All Certs" button.]'
    '--bigger[Make the Profile Selection Dialog window bigger.]'
    '--noenc[Start off in a mode where a \No Encryption\ check button is present.]'
    '--killstunnel[On Windows, automatically terminate the STUNNEL process when the viewer exits…]'
    '--nokillstunnel[On Windows, disable -killstunnel mode.]'
    '--mycert[Set the default "MyCert" to be /path/to/mycert. pem. Same as -cert.]:pathtomycert.pem'
    '--cacert[Set the default "ServerCert" to be /path/to/cacert. crt. Same as -ca.]:pathtocacert.crt'
    '--crl[Set the default Certificate Revocation List to be /path/to/mycrl. pem.]:pathtomycrl.pem'
    '*:filename:_files'
)
_arguments -s $arguments
