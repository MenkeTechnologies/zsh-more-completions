#compdef pstopnm

# Autogenerated from man page /usr/share/man/man1/pstopnm.1.gz

local arguments

arguments=(
    '--forceplain[forces the output file to be in plain (text) format.]'
    '--llx[selects bx as the lower left corner x coordinate (in inches).]:bx'
    '--lly[selects by as the lower left corner y coordinate (in inches).]:by'
    '--landscape[renders the image in landscape mode.]'
    '--portrait[renders the image in portrait mode.]'
    '--nocrop[does not crop the output image dimensions to match the PostScript image area …]'
    {--pbm,--pgm,--ppm}'[selects the format of the output file.]'
    '--stdout[causes output to go to Standard Output instead of to regular files, one per p…]'
    '--urx[selects tx as the upper right corner x coordinate (in inches).]:tx'
    '--ury[selects ty as the upper right corner y coordinate (in inches).]:ty'
    '--verbose[prints processing information to stdout.]'
    '--xborder[specifies that the border width along the Y axis should be frac times the d…]:frac'
    '--xmax[specifies that the maximum output image width should have a size less or equa…]:xs'
    '--xsize[specifies that the output image width must be exactly xs pixels.]:xsize'
    '--yborder[specifies that the border width along the X axis should be frac times the d…]:frac'
    '--ymax[specifies that the maximum output image height should have a size less or equ…]:ys'
    '--ysize[options, pstopnm maps the document into the output image by preserving its …]'
    '*:filename:_files'
)
_arguments -s $arguments
