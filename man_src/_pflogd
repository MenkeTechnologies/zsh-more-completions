#compdef pflogd

# Autogenerated from man page /usr/share/man/en.UTF-8/man8/pflogd.8.gz

local arguments

arguments=(
    '-D[Debugging mode. pflogd does not disassociate from the controlling terminal.]'
    '-d[Time in seconds to delay between automatic flushes of the file.]'
    '-f[Log output filename. Default is /var/log/pflog.]'
    '-i[Specifies the pflog 4 interface to use. By default, pflogd will use pflog0.]'
    '-p[Writes a file containing the process ID of the program to /var/run.]'
    '-s[Analyze at most the first snaplen bytes of data from each packet rather than …]'
    '-x[Check the integrity of an existing log file, and return.]'
    '*:filename:_files'
)
_arguments -s $arguments
