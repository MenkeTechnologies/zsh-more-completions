#compdef qshape

# Autogenerated from man page
# Updated with latest qshape options

local arguments

arguments=(
    '-s[display the sender domain distribution instead of the recipient domain distribution]'
    '-p[generate aggregate statistics for parent domains]'
    '-m[set the minimum subdomain count needed to show a separate line (with -p)]:min_subdomains'
    '-b[set the number of age distribution time buckets]:bucket_count'
    '-t[set the age limit in minutes for the first time bucket]:bucket_time'
    '-l[use a linear age sequence instead of geometric]'
    '-w[set the terminal width for output]:terminal_width'
    '-N[show intermediate results each batch_msg_count messages]:batch_msg_count'
    '-n[report only the top N domains in intermediate or final results]:batch_top_domains'
    '-c[set the main.cf configuration directory]:config_directory:_directories'
    '*:filename:_files'
)
_arguments -s $arguments
