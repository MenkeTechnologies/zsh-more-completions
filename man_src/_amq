#compdef amq

# Autogenerated from man page /usr/share/man/en.UTF-8/man8/amq.8.gz
# Updated with latest am-utils amq options

local arguments

arguments=(
    '-f[Ask the automounter to flush internal caches and reload all maps]'
    '-h[Specify an alternate host to query. Default is local host]:hostname:_hosts'
    '-i[Print information about the mount maps]'
    '-l[Tell amd to use log_file as the log file name]:log_file:_files'
    '-m[Ask the automounter to provide a list of mounted file systems]'
    '-p[Return the process ID of the remote or locally running amd]'
    '-q[Suppress error messages produced when attempting synchronous unmounts]'
    '-s[Ask the automounter to provide system-wide mount statistics]'
    '-u[Ask the automounter to unmount the file systems named in directory]:directory:_directories'
    '-v[Ask the automounter for its version information]'
    '-w[Translate a full pathname into a short amd pathname]'
    '-x[Ask the automounter to use the specified logging options]:log_options'
    '-D[Ask the automounter to use the specified debugging options]:debug_options'
    '-H[Display short usage message]'
    '-P[Contact an alternate running amd on a different RPC program number]:program_number'
    '-T[Contact amd using TCP transport only]'
    '-U[Contact amd using UDP transport only]'
    '*:directory:_directories'
)
_arguments -s $arguments
