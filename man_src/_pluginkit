#compdef pluginkit

# Autogenerated from man page /usr/share/man/man8/pluginkit.8

local arguments

arguments=(
    '-A[Matches find all versions of a given plug-in known to the system]'
    '-a[Explicitly adds plugins at the file location(s) given, even if they are not]'
    '-e[Perform a matching operation (see -m) and apply the given user election]'
    '-D[Matches find all physical instances of a given plug-in known to the system,]'
    '-i[Specifies a plug-in identifier to match, a short-hand for]'
    '-m[Requests pluginkit to scan all registered plug-ins for those matching the]'
    '-p[Specifies a plug-in protocol to match, a short-hand for]'
    '-r[Explicitly removes plugins at the file location(s) given]'
    '--raw[Present replies from the management daemon (pkd) in raw XML form]'
    '-v[Asks for more verbose operation]'
    '*:filename:_files'
)
_arguments -s $arguments
