#compdef wf-config
# Documentation: https://github.com/WayfireWM/wf-config (README)
# Reference accessed: 2026-05-21
# Verified: upstream docs at the URL above. wf-config is a library (.so),
# not a standalone CLI binary.
# STUB: no CLI documented upstream.

# wf-config: Wayfire config library (not a CLI)
local curcontext="$curcontext" ret=1
_arguments -C \
  '*::extra args:_files' && ret=0
return ret
