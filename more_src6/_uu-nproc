#compdef uu-nproc
# Documentation: https://uutils.github.io/coreutils/docs/utils/nproc.html
# Reference accessed: 2026-05-21
# Verified: upstream docs at the URL above.
# Source: Homebrew bottle
# Formula: uutils-coreutils
# Type: maintainer-shipped (Homebrew)
# Harvested: 2026-05-15

autoload -U is-at-least

_uu-nproc() {
    typeset -A opt_args
    typeset -a _arguments_options
    local ret=1

    if is-at-least 5.2; then
        _arguments_options=(-s -S -C)
    else
        _arguments_options=(-s -C)
    fi

    local context curcontext="$curcontext" state line
    _arguments "${_arguments_options[@]}" : \
'--ignore=[ignore up to N cores]:N:_default' \
'--all[print the number of cores available to the system]' \
'-h[Print help]' \
'--help[Print help]' \
'-V[Print version]' \
'--version[Print version]' \
&& ret=0
}

(( $+functions[_uu-nproc_commands] )) ||
_uu-nproc_commands() {
    local commands; commands=()
    _describe -t commands 'uu-nproc commands' commands "$@"
}

if [ "$funcstack[1]" = "_uu-nproc" ]; then
    _uu-nproc "$@"
else
    compdef _uu-nproc uu-nproc
fi
