#compdef waypoint-entrypoint
# _waypoint-entrypoint — Waypoint custom entrypoint that wraps a deployed process (hashicorp/waypoint)
# upstream: https://github.com/hashicorp/waypoint
# Flags from cmd/waypoint-entrypoint/main.go (flag).

_waypoint-entrypoint() {
    _arguments \
        '-h[show help]' \
        '-help[show help]' \
        '*::command:_command_names -e'
}

_waypoint-entrypoint "$@"
