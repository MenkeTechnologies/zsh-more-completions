#compdef zeek-pkg zeek-package
# _zeek-pkg — Zeek zeek-pkg modern package manager CLI for installing pinning upgrading removing Zeek scripts plugins protocol analyzers policy modules from Zeek package sources (zeek team, Python); argparse
# upstream: https://github.com/zeek/package-manager

_zeek-pkg() {
    local -a sub=(
        'install:install package'
        'upgrade:upgrade installed packages'
        'remove:remove package'
        'list:list packages'
        'search:search package database'
        'info:show package info'
        'config:show config'
        'env:print env vars'
        'autoconfig:create autoconfig'
        'refresh:refresh package sources'
        'pin:pin package version'
        'unpin:unpin package'
        'load:load package'
        'unload:unload package'
        'check:check package'
        'create:create new package'
        'test:run package tests'
        'bundle:bundle packages'
        'unbundle:unbundle packages'
        'purge:purge cached packages'
        'help:show help'
        'version:show version'
    )
    _arguments -C \
        '-h[show help]' \
        '--help[show help]' \
        '-V[show version]' \
        '--version[show version]' \
        '-v[verbose]' \
        '--verbose[verbose]' \
        '-q[quiet]' \
        '--quiet[quiet]' \
        '--configfile=+[file]:file:_files -g "*.cfg"' \
        '-C+[file]:file:_files -g "*.cfg"' \
        '--user-var=+[KEY=VALUE]:str:' \
        '--extra-source=+[name=URL]:str:' \
        '1:subcommand:->sub' \
        '*::args:->args'

    case $state in
        sub) _describe -t commands 'zeek-pkg subcommand' sub ;;
        args)
            case $words[1] in
                install|upgrade|remove|info|pin|unpin|load|unload) _arguments \
                    '--version=+[version]:str:' \
                    '--force[force]' \
                    '*:package:' ;;
                search) _arguments '*:keyword:' ;;
                create) _arguments \
                    '--features=+[csv]:str:' \
                    '--no-features[no features]' \
                    '*:dir name:_files -/' ;;
                bundle|unbundle) _arguments '*:bundle file:_files -g "*.bundle *.tar *.tar.gz *.tgz"' ;;
            esac
            ;;
    esac
}

_zeek-pkg "$@"
