#compdef watchwoman
# Documentation: https://github.com/radiosilence/watchwoman
# Reference accessed: 2026-05-21
# Verified: upstream docs at the URL above.
# Source: cargo binstall watchwoman@0.6.0
# Probed via: completion zsh

autoload -U is-at-least

_watchwoman() {
    typeset -A opt_args
    typeset -a _arguments_options
    local ret=1

    if is-at-least 5.2; then
        _arguments_options=(-s -S -C)
    else
        _arguments_options=(-s -C)
    fi

    local context curcontext="$curcontext" state line
    _arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'-V[Print version]' \
'--version[Print version]' \
":: :_watchwoman_commands" \
"*::: :->watchwoman" \
&& ret=0
    case $state in
    (watchwoman)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:watchwoman-command-$line[1]:"
        case $line[1] in
            (--foreground-daemon)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(foreground)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(completion)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':shell:(bash elvish fish powershell zsh)' \
&& ret=0
;;
(get-sockname)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(get-pid)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(version)
_arguments "${_arguments_options[@]}" : \
'*--required=[Required capabilities (comma-separated)]:REQUIRED:_default' \
'*--optional=[Optional capabilities (comma-separated)]:OPTIONAL:_default' \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(list-capabilities)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(watch-project)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
&& ret=0
;;
(watch)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
&& ret=0
;;
(watch-list)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(watch-del)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
&& ret=0
;;
(watch-del-all)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(clock)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
&& ret=0
;;
(query)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
':query:_default' \
&& ret=0
;;
(subscribe)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
':name:_default' \
':query:_default' \
&& ret=0
;;
(unsubscribe)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
':name:_default' \
&& ret=0
;;
(flush-subscriptions)
_arguments "${_arguments_options[@]}" : \
'--timeout-ms=[]:TIMEOUT_MS:_default' \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
&& ret=0
;;
(state-enter)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
':name:_default' \
&& ret=0
;;
(state-leave)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
':name:_default' \
&& ret=0
;;
(get-config)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
&& ret=0
;;
(log-level)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
'::level:_default' \
&& ret=0
;;
(log)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':level:_default' \
':message:_default' \
&& ret=0
;;
(status)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--json[Emit the raw JSON response instead of the formatted report]' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(shutdown-server)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(raw)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':pdu -- `\["command", "arg1", {"key"\:"val"}\]` shape:_default' \
&& ret=0
;;
(debug-recrawl)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
&& ret=0
;;
(debug-ageout)
_arguments "${_arguments_options[@]}" : \
'--age=[]:AGE:_default' \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
&& ret=0
;;
(debug-show-cursors)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
&& ret=0
;;
(debug-poll-for-settle)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
&& ret=0
;;
(find)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
'*::patterns:_default' \
&& ret=0
;;
(since)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
':clock:_default' \
&& ret=0
;;
(trigger)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
':spec:_default' \
&& ret=0
;;
(trigger-list)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
&& ret=0
;;
(trigger-del)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
':name:_default' \
&& ret=0
;;
(get-log)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(global-log-level)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
'::level:_default' \
&& ret=0
;;
(debug-contenthash)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
&& ret=0
;;
(debug-status)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(debug-root-status)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
&& ret=0
;;
(debug-watcher-info)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
&& ret=0
;;
(debug-watcher-info-clear)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(debug-get-asserted-states)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
&& ret=0
;;
(debug-get-subscriptions)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
&& ret=0
;;
(debug-kqueue-and-fsevents-recrawl)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
&& ret=0
;;
(debug-fsevents-inject-drop)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
':path:_default' \
&& ret=0
;;
(debug-set-parallel-crawl)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(debug-set-subscriptions-paused)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(debug-symlink-target-cache)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(install-agent)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(uninstall-agent)
_arguments "${_arguments_options[@]}" : \
'-U+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'-u+[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--sockname=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--unix-listener-path=[Path to the unix socket.  Falls back to \$WATCHMAN_SOCK, then a platform default under \$XDG_STATE_HOME (zeroconf).  Upstream watchman accepts this as \`-U/--sockname\` (deprecated) and \`-u/--unix-listener-path\`; all three forms are accepted]:SOCKNAME:_default' \
'--output-encoding=[Select wire encoding for socket output. Defaults to JSON for CLI use]:OUTPUT_ENCODING:(json bser bser2)' \
'--server-encoding=[Select wire encoding expected from the server. Defaults to JSON]:SERVER_ENCODING:(json bser bser2)' \
'-o+[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--logfile=[Path to a log file.  Tracing emits to stderr by default; this redirects it to the given path]:PATH:_default' \
'--log-level=[Numeric log level passed to tracing.  0 = off, 1 = warn (default), 2 = debug, 3+ = trace]:LEVEL:_default' \
'--pidfile=[Path to a pidfile.  Watchwoman'\''s daemon uses the socket path as its liveness signal, but writing a pidfile keeps scripts and monitors that grep for it happy]:PATH:_default' \
'--no-pretty[Compact JSON output instead of pretty-printed]' \
'--no-spawn[Don'\''t auto-spawn the daemon if the socket is missing]' \
'-j[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'--json-command[Read a JSON PDU from stdin and send it to the daemon directly. Used by \`git fsmonitor\`, Sapling, Metro, and every other tool that speaks watchman'\''s PDU protocol without the subcommand CLI. Pairs with \`--persistent\` for subscribe streams]' \
'-p[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'--persistent[Stay connected after the first response and stream unilateral PDUs (subscription updates, state-broadcasts) until EOF or the daemon closes the connection.  Matches watchman'\''s \`-p\`]' \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_watchwoman__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:watchwoman-help-command-$line[1]:"
        case $line[1] in
            (--foreground-daemon)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(completion)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(get-sockname)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(get-pid)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(version)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(list-capabilities)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(watch-project)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(watch)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(watch-list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(watch-del)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(watch-del-all)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(clock)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(query)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(subscribe)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(unsubscribe)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(flush-subscriptions)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(state-enter)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(state-leave)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(get-config)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(log-level)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(log)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(status)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(shutdown-server)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(raw)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(debug-recrawl)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(debug-ageout)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(debug-show-cursors)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(debug-poll-for-settle)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(find)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(since)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(trigger)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(trigger-list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(trigger-del)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(get-log)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(global-log-level)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(debug-contenthash)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(debug-status)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(debug-root-status)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(debug-watcher-info)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(debug-watcher-info-clear)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(debug-get-asserted-states)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(debug-get-subscriptions)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(debug-kqueue-and-fsevents-recrawl)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(debug-fsevents-inject-drop)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(debug-set-parallel-crawl)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(debug-set-subscriptions-paused)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(debug-symlink-target-cache)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(install-agent)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(uninstall-agent)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
}

(( $+functions[_watchwoman_commands] )) ||
_watchwoman_commands() {
    local commands; commands=(
'--foreground-daemon:Run the daemon in the foreground (matches watchman'\''s \`-f/--foreground\`)' \
'foreground:Run the daemon in the foreground (matches watchman'\''s \`-f/--foreground\`)' \
'completion:Print shell completion script for the given shell' \
'get-sockname:Print the path to the unix socket' \
'get-pid:Print the daemon'\''s PID' \
'version:Print the watchman-compatible version and capability probe result' \
'list-capabilities:List every capability the daemon advertises' \
'watch-project:Watch a path and return the enclosing project root' \
'watch:Watch a raw path without project-root resolution' \
'watch-list:Enumerate every currently watched root' \
'watch-del:Stop watching a root' \
'watch-del-all:Stop watching every root' \
'clock:Return the clock value for a root' \
'query:Run a structured query.  Pass the query spec as a JSON blob' \
'subscribe:Subscribe to a root; prints an initial response and exits' \
'unsubscribe:Unsubscribe from a named subscription' \
'flush-subscriptions:Wait for pending subscriptions to flush' \
'state-enter:Enter a named state on a root' \
'state-leave:Leave a named state on a root' \
'get-config:Fetch the watchmanconfig for a root' \
'log-level:Set or read the server log level' \
'log:Write a message to the server log' \
'status:Print a human-readable daemon status report\: uptime, RSS, per-root file counts, idle time, health, and the last 64 GC reaps' \
'shutdown-server:Tear the daemon down' \
'raw:Send an arbitrary JSON PDU and print the response' \
'debug-recrawl:Force a full rescan of a root from disk' \
'debug-ageout:Ageout sweep (no-op in watchwoman; matches wire shape)' \
'debug-show-cursors:Dump named cursors on a root' \
'debug-poll-for-settle:Block until the daemon settles pending events' \
'find:Legacy glob-style finder (\`since\`-less subset of \`query\`)' \
'since:Legacy since-delta (subset of \`query\`)' \
'trigger:Install a trigger that runs a command when files match' \
'trigger-list:List triggers installed on a root' \
'trigger-del:Delete a named trigger' \
'get-log:Dump the daemon'\''s in-memory log ring' \
'global-log-level:Same as \`log-level\`, for parity with upstream naming' \
'debug-contenthash:Return the SHA-1 content hash for a file in the watched tree' \
'debug-status:High-level daemon status dump' \
'debug-root-status:Per-root status + file count' \
'debug-watcher-info:Report the watcher backend (\`fsevents\`/\`inotify\`/\`kqueue\`)' \
'debug-watcher-info-clear:Clear watcher backend diagnostic caches' \
'debug-get-asserted-states:List states currently asserted on a root (via \`state-enter\`)' \
'debug-get-subscriptions:Dump registered subscriptions on a root' \
'debug-kqueue-and-fsevents-recrawl:Force a recrawl on the kqueue + FSEvents pair' \
'debug-fsevents-inject-drop:Inject a synthetic dropped-event signal for FSEvents testing' \
'debug-set-parallel-crawl:Toggle the debug parallel-crawl flag (accepted, no-op)' \
'debug-set-subscriptions-paused:Pause/unpause subscriptions globally (accepted, no-op)' \
'debug-symlink-target-cache:Return the symlink-target cache contents (always empty)' \
'install-agent:Install a macOS LaunchAgent so the daemon auto-starts at login and gets \`launchctl\` lifecycle management' \
'uninstall-agent:Remove the LaunchAgent installed by \`install-agent\`' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'watchwoman commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__--foreground-daemon_commands] )) ||
_watchwoman__subcmd__--foreground-daemon_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman --foreground-daemon commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__clock_commands] )) ||
_watchwoman__subcmd__clock_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman clock commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__completion_commands] )) ||
_watchwoman__subcmd__completion_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman completion commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__debug-ageout_commands] )) ||
_watchwoman__subcmd__debug-ageout_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman debug-ageout commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__debug-contenthash_commands] )) ||
_watchwoman__subcmd__debug-contenthash_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman debug-contenthash commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__debug-fsevents-inject-drop_commands] )) ||
_watchwoman__subcmd__debug-fsevents-inject-drop_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman debug-fsevents-inject-drop commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__debug-get-asserted-states_commands] )) ||
_watchwoman__subcmd__debug-get-asserted-states_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman debug-get-asserted-states commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__debug-get-subscriptions_commands] )) ||
_watchwoman__subcmd__debug-get-subscriptions_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman debug-get-subscriptions commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__debug-kqueue-and-fsevents-recrawl_commands] )) ||
_watchwoman__subcmd__debug-kqueue-and-fsevents-recrawl_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman debug-kqueue-and-fsevents-recrawl commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__debug-poll-for-settle_commands] )) ||
_watchwoman__subcmd__debug-poll-for-settle_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman debug-poll-for-settle commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__debug-recrawl_commands] )) ||
_watchwoman__subcmd__debug-recrawl_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman debug-recrawl commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__debug-root-status_commands] )) ||
_watchwoman__subcmd__debug-root-status_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman debug-root-status commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__debug-set-parallel-crawl_commands] )) ||
_watchwoman__subcmd__debug-set-parallel-crawl_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman debug-set-parallel-crawl commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__debug-set-subscriptions-paused_commands] )) ||
_watchwoman__subcmd__debug-set-subscriptions-paused_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman debug-set-subscriptions-paused commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__debug-show-cursors_commands] )) ||
_watchwoman__subcmd__debug-show-cursors_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman debug-show-cursors commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__debug-status_commands] )) ||
_watchwoman__subcmd__debug-status_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman debug-status commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__debug-symlink-target-cache_commands] )) ||
_watchwoman__subcmd__debug-symlink-target-cache_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman debug-symlink-target-cache commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__debug-watcher-info_commands] )) ||
_watchwoman__subcmd__debug-watcher-info_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman debug-watcher-info commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__debug-watcher-info-clear_commands] )) ||
_watchwoman__subcmd__debug-watcher-info-clear_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman debug-watcher-info-clear commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__find_commands] )) ||
_watchwoman__subcmd__find_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman find commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__flush-subscriptions_commands] )) ||
_watchwoman__subcmd__flush-subscriptions_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman flush-subscriptions commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__get-config_commands] )) ||
_watchwoman__subcmd__get-config_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman get-config commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__get-log_commands] )) ||
_watchwoman__subcmd__get-log_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman get-log commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__get-pid_commands] )) ||
_watchwoman__subcmd__get-pid_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman get-pid commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__get-sockname_commands] )) ||
_watchwoman__subcmd__get-sockname_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman get-sockname commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__global-log-level_commands] )) ||
_watchwoman__subcmd__global-log-level_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman global-log-level commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help_commands] )) ||
_watchwoman__subcmd__help_commands() {
    local commands; commands=(
'--foreground-daemon:Run the daemon in the foreground (matches watchman'\''s \`-f/--foreground\`)' \
'completion:Print shell completion script for the given shell' \
'get-sockname:Print the path to the unix socket' \
'get-pid:Print the daemon'\''s PID' \
'version:Print the watchman-compatible version and capability probe result' \
'list-capabilities:List every capability the daemon advertises' \
'watch-project:Watch a path and return the enclosing project root' \
'watch:Watch a raw path without project-root resolution' \
'watch-list:Enumerate every currently watched root' \
'watch-del:Stop watching a root' \
'watch-del-all:Stop watching every root' \
'clock:Return the clock value for a root' \
'query:Run a structured query.  Pass the query spec as a JSON blob' \
'subscribe:Subscribe to a root; prints an initial response and exits' \
'unsubscribe:Unsubscribe from a named subscription' \
'flush-subscriptions:Wait for pending subscriptions to flush' \
'state-enter:Enter a named state on a root' \
'state-leave:Leave a named state on a root' \
'get-config:Fetch the watchmanconfig for a root' \
'log-level:Set or read the server log level' \
'log:Write a message to the server log' \
'status:Print a human-readable daemon status report\: uptime, RSS, per-root file counts, idle time, health, and the last 64 GC reaps' \
'shutdown-server:Tear the daemon down' \
'raw:Send an arbitrary JSON PDU and print the response' \
'debug-recrawl:Force a full rescan of a root from disk' \
'debug-ageout:Ageout sweep (no-op in watchwoman; matches wire shape)' \
'debug-show-cursors:Dump named cursors on a root' \
'debug-poll-for-settle:Block until the daemon settles pending events' \
'find:Legacy glob-style finder (\`since\`-less subset of \`query\`)' \
'since:Legacy since-delta (subset of \`query\`)' \
'trigger:Install a trigger that runs a command when files match' \
'trigger-list:List triggers installed on a root' \
'trigger-del:Delete a named trigger' \
'get-log:Dump the daemon'\''s in-memory log ring' \
'global-log-level:Same as \`log-level\`, for parity with upstream naming' \
'debug-contenthash:Return the SHA-1 content hash for a file in the watched tree' \
'debug-status:High-level daemon status dump' \
'debug-root-status:Per-root status + file count' \
'debug-watcher-info:Report the watcher backend (\`fsevents\`/\`inotify\`/\`kqueue\`)' \
'debug-watcher-info-clear:Clear watcher backend diagnostic caches' \
'debug-get-asserted-states:List states currently asserted on a root (via \`state-enter\`)' \
'debug-get-subscriptions:Dump registered subscriptions on a root' \
'debug-kqueue-and-fsevents-recrawl:Force a recrawl on the kqueue + FSEvents pair' \
'debug-fsevents-inject-drop:Inject a synthetic dropped-event signal for FSEvents testing' \
'debug-set-parallel-crawl:Toggle the debug parallel-crawl flag (accepted, no-op)' \
'debug-set-subscriptions-paused:Pause/unpause subscriptions globally (accepted, no-op)' \
'debug-symlink-target-cache:Return the symlink-target cache contents (always empty)' \
'install-agent:Install a macOS LaunchAgent so the daemon auto-starts at login and gets \`launchctl\` lifecycle management' \
'uninstall-agent:Remove the LaunchAgent installed by \`install-agent\`' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'watchwoman help commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__--foreground-daemon_commands] )) ||
_watchwoman__subcmd__help__subcmd__--foreground-daemon_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help --foreground-daemon commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__clock_commands] )) ||
_watchwoman__subcmd__help__subcmd__clock_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help clock commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__completion_commands] )) ||
_watchwoman__subcmd__help__subcmd__completion_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help completion commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__debug-ageout_commands] )) ||
_watchwoman__subcmd__help__subcmd__debug-ageout_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help debug-ageout commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__debug-contenthash_commands] )) ||
_watchwoman__subcmd__help__subcmd__debug-contenthash_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help debug-contenthash commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__debug-fsevents-inject-drop_commands] )) ||
_watchwoman__subcmd__help__subcmd__debug-fsevents-inject-drop_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help debug-fsevents-inject-drop commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__debug-get-asserted-states_commands] )) ||
_watchwoman__subcmd__help__subcmd__debug-get-asserted-states_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help debug-get-asserted-states commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__debug-get-subscriptions_commands] )) ||
_watchwoman__subcmd__help__subcmd__debug-get-subscriptions_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help debug-get-subscriptions commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__debug-kqueue-and-fsevents-recrawl_commands] )) ||
_watchwoman__subcmd__help__subcmd__debug-kqueue-and-fsevents-recrawl_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help debug-kqueue-and-fsevents-recrawl commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__debug-poll-for-settle_commands] )) ||
_watchwoman__subcmd__help__subcmd__debug-poll-for-settle_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help debug-poll-for-settle commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__debug-recrawl_commands] )) ||
_watchwoman__subcmd__help__subcmd__debug-recrawl_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help debug-recrawl commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__debug-root-status_commands] )) ||
_watchwoman__subcmd__help__subcmd__debug-root-status_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help debug-root-status commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__debug-set-parallel-crawl_commands] )) ||
_watchwoman__subcmd__help__subcmd__debug-set-parallel-crawl_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help debug-set-parallel-crawl commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__debug-set-subscriptions-paused_commands] )) ||
_watchwoman__subcmd__help__subcmd__debug-set-subscriptions-paused_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help debug-set-subscriptions-paused commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__debug-show-cursors_commands] )) ||
_watchwoman__subcmd__help__subcmd__debug-show-cursors_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help debug-show-cursors commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__debug-status_commands] )) ||
_watchwoman__subcmd__help__subcmd__debug-status_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help debug-status commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__debug-symlink-target-cache_commands] )) ||
_watchwoman__subcmd__help__subcmd__debug-symlink-target-cache_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help debug-symlink-target-cache commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__debug-watcher-info_commands] )) ||
_watchwoman__subcmd__help__subcmd__debug-watcher-info_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help debug-watcher-info commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__debug-watcher-info-clear_commands] )) ||
_watchwoman__subcmd__help__subcmd__debug-watcher-info-clear_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help debug-watcher-info-clear commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__find_commands] )) ||
_watchwoman__subcmd__help__subcmd__find_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help find commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__flush-subscriptions_commands] )) ||
_watchwoman__subcmd__help__subcmd__flush-subscriptions_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help flush-subscriptions commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__get-config_commands] )) ||
_watchwoman__subcmd__help__subcmd__get-config_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help get-config commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__get-log_commands] )) ||
_watchwoman__subcmd__help__subcmd__get-log_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help get-log commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__get-pid_commands] )) ||
_watchwoman__subcmd__help__subcmd__get-pid_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help get-pid commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__get-sockname_commands] )) ||
_watchwoman__subcmd__help__subcmd__get-sockname_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help get-sockname commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__global-log-level_commands] )) ||
_watchwoman__subcmd__help__subcmd__global-log-level_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help global-log-level commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__help_commands] )) ||
_watchwoman__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help help commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__install-agent_commands] )) ||
_watchwoman__subcmd__help__subcmd__install-agent_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help install-agent commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__list-capabilities_commands] )) ||
_watchwoman__subcmd__help__subcmd__list-capabilities_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help list-capabilities commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__log_commands] )) ||
_watchwoman__subcmd__help__subcmd__log_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help log commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__log-level_commands] )) ||
_watchwoman__subcmd__help__subcmd__log-level_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help log-level commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__query_commands] )) ||
_watchwoman__subcmd__help__subcmd__query_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help query commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__raw_commands] )) ||
_watchwoman__subcmd__help__subcmd__raw_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help raw commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__shutdown-server_commands] )) ||
_watchwoman__subcmd__help__subcmd__shutdown-server_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help shutdown-server commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__since_commands] )) ||
_watchwoman__subcmd__help__subcmd__since_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help since commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__state-enter_commands] )) ||
_watchwoman__subcmd__help__subcmd__state-enter_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help state-enter commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__state-leave_commands] )) ||
_watchwoman__subcmd__help__subcmd__state-leave_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help state-leave commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__status_commands] )) ||
_watchwoman__subcmd__help__subcmd__status_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help status commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__subscribe_commands] )) ||
_watchwoman__subcmd__help__subcmd__subscribe_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help subscribe commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__trigger_commands] )) ||
_watchwoman__subcmd__help__subcmd__trigger_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help trigger commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__trigger-del_commands] )) ||
_watchwoman__subcmd__help__subcmd__trigger-del_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help trigger-del commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__trigger-list_commands] )) ||
_watchwoman__subcmd__help__subcmd__trigger-list_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help trigger-list commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__uninstall-agent_commands] )) ||
_watchwoman__subcmd__help__subcmd__uninstall-agent_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help uninstall-agent commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__unsubscribe_commands] )) ||
_watchwoman__subcmd__help__subcmd__unsubscribe_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help unsubscribe commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__version_commands] )) ||
_watchwoman__subcmd__help__subcmd__version_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help version commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__watch_commands] )) ||
_watchwoman__subcmd__help__subcmd__watch_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help watch commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__watch-del_commands] )) ||
_watchwoman__subcmd__help__subcmd__watch-del_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help watch-del commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__watch-del-all_commands] )) ||
_watchwoman__subcmd__help__subcmd__watch-del-all_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help watch-del-all commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__watch-list_commands] )) ||
_watchwoman__subcmd__help__subcmd__watch-list_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help watch-list commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__help__subcmd__watch-project_commands] )) ||
_watchwoman__subcmd__help__subcmd__watch-project_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman help watch-project commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__install-agent_commands] )) ||
_watchwoman__subcmd__install-agent_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman install-agent commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__list-capabilities_commands] )) ||
_watchwoman__subcmd__list-capabilities_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman list-capabilities commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__log_commands] )) ||
_watchwoman__subcmd__log_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman log commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__log-level_commands] )) ||
_watchwoman__subcmd__log-level_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman log-level commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__query_commands] )) ||
_watchwoman__subcmd__query_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman query commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__raw_commands] )) ||
_watchwoman__subcmd__raw_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman raw commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__shutdown-server_commands] )) ||
_watchwoman__subcmd__shutdown-server_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman shutdown-server commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__since_commands] )) ||
_watchwoman__subcmd__since_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman since commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__state-enter_commands] )) ||
_watchwoman__subcmd__state-enter_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman state-enter commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__state-leave_commands] )) ||
_watchwoman__subcmd__state-leave_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman state-leave commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__status_commands] )) ||
_watchwoman__subcmd__status_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman status commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__subscribe_commands] )) ||
_watchwoman__subcmd__subscribe_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman subscribe commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__trigger_commands] )) ||
_watchwoman__subcmd__trigger_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman trigger commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__trigger-del_commands] )) ||
_watchwoman__subcmd__trigger-del_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman trigger-del commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__trigger-list_commands] )) ||
_watchwoman__subcmd__trigger-list_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman trigger-list commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__uninstall-agent_commands] )) ||
_watchwoman__subcmd__uninstall-agent_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman uninstall-agent commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__unsubscribe_commands] )) ||
_watchwoman__subcmd__unsubscribe_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman unsubscribe commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__version_commands] )) ||
_watchwoman__subcmd__version_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman version commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__watch_commands] )) ||
_watchwoman__subcmd__watch_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman watch commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__watch-del_commands] )) ||
_watchwoman__subcmd__watch-del_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman watch-del commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__watch-del-all_commands] )) ||
_watchwoman__subcmd__watch-del-all_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman watch-del-all commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__watch-list_commands] )) ||
_watchwoman__subcmd__watch-list_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman watch-list commands' commands "$@"
}
(( $+functions[_watchwoman__subcmd__watch-project_commands] )) ||
_watchwoman__subcmd__watch-project_commands() {
    local commands; commands=()
    _describe -t commands 'watchwoman watch-project commands' commands "$@"
}

if [ "$funcstack[1]" = "_watchwoman" ]; then
    _watchwoman "$@"
else
    compdef _watchwoman watchwoman
fi
