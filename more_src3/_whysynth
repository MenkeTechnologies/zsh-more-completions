#compdef whysynth
# Documentation: http://smbolton.com/whysynth.html
# Reference accessed: 2026-05-21
# Verified: upstream docs at the URL above.
# STUB: WhySynth is a DSSI softsynth plugin (.so) invoked via a DSSI host
#       (jack-dssi-host); it has no documented standalone CLI flags.

local curcontext="$curcontext" state line ret=1
_arguments -s -S \
  '*:DSSI plugin (.so):_files -g "*.so"' && ret=0
return ret
