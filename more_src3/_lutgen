#compdef lutgen
# Documentation: https://github.com/ozwaldorf/lutgen-rs
# Reference accessed: 2026-05-19
# Verified: upstream completion dispatcher (bpaf ShellComp); the binary
# generates its own completions via --bpaf-complete-rev.
# Source: nixpkgs (https://github.com/NixOS/nixpkgs)
# Package: lutgen
# Type: maintainer-shipped (nixpkgs)
# Harvested: 2026-05-15
local line
line="${words[1]} --bpaf-complete-rev=7 ${words[@]:1}"
if [[ ${words[-1]} == "" ]]; then
    line="${line} \"\""
fi
source <(eval ${line})

