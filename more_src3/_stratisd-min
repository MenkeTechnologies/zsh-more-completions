#compdef stratisd-min
# _stratisd-min — minimal Stratis storage daemon (stratis-storage/stratisd)
# upstream: https://github.com/stratis-storage/stratisd
# Flags from src/bin/stratis-min/stratisd-min.rs (clap builder).

_stratisd-min() {
    _arguments \
        '--log-level=[set log verbosity]:level:(trace debug info warn error)' \
        '--sim[enable sim engine]'
}

_stratisd-min "$@"
