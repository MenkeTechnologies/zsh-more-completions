#compdef uu-md5sum
# Documentation: https://uutils.github.io/coreutils/docs/utils/md5sum.html
# Reference accessed: 2026-05-21
# Verified: upstream docs at the URL above.
# Source: Homebrew bottle
# Formula: uutils-coreutils
# Type: maintainer-shipped (Homebrew)
# Harvested: 2026-05-15

autoload -U is-at-least

_uu-md5sum() {
    typeset -A opt_args
    typeset -a _arguments_options
    local ret=1

    if is-at-least 5.2; then
        _arguments_options=(-s -S -C)
    else
        _arguments_options=(-s -C)
    fi

    local context curcontext="$curcontext" state line
    _arguments "${_arguments_options[@]}" : \
'-b[]' \
'--binary[]' \
'-c[read checksums from the FILEs and check them]' \
'--check[read checksums from the FILEs and check them]' \
'-w[warn about improperly formatted checksum lines]' \
'--warn[warn about improperly formatted checksum lines]' \
'--status[don'\''t output anything, status code shows success]' \
'--quiet[don'\''t print OK for each successfully verified file]' \
'--ignore-missing[don'\''t fail or report status for missing files]' \
'--strict[exit non-zero for improperly formatted checksum lines]' \
'--tag[create a BSD style checksum]' \
'-t[read in text mode (default)]' \
'--text[read in text mode (default)]' \
'-z[end each output line with NUL, not newline, and disable file name escaping]' \
'--zero[end each output line with NUL, not newline, and disable file name escaping]' \
'-h[Print help]' \
'--help[Print help]' \
'-V[Print version]' \
'--version[Print version]' \
'::file:_files' \
&& ret=0
}

(( $+functions[_uu-md5sum_commands] )) ||
_uu-md5sum_commands() {
    local commands; commands=()
    _describe -t commands 'uu-md5sum commands' commands "$@"
}

if [ "$funcstack[1]" = "_uu-md5sum" ]; then
    _uu-md5sum "$@"
else
    compdef _uu-md5sum uu-md5sum
fi
