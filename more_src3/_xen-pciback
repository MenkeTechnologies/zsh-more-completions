#compdef xen-pciback
# Documentation: none found
# Reference accessed: 2026-05-21
# Verified: xen-pciback is a Linux kernel module (drivers/xen/xen-pciback/),
# not a userspace command. It is configured via /sys/bus/pci/drivers/pciback,
# kernel boot params (xen-pciback.hide=...), or modprobe options. No CLI binary
# ships with this name upstream.
# STUB: no userspace CLI; left as files-only completion.

local curcontext="$curcontext" state line ret=1
_arguments -s -S '*:file:_files' && ret=0
return ret
