#compdef hev-socks5-tunnel
# _hev-socks5-tunnel — tun2socks tunneling daemon (heiher/hev-socks5-tunnel)
# upstream: https://github.com/heiher/hev-socks5-tunnel
# Usage from src/hev-main.c.

_hev-socks5-tunnel() {
    _arguments \
        '(- *)--version[show version]' \
        '1:config file:_files -g "*.(yml|yaml)"'
}

_hev-socks5-tunnel "$@"
