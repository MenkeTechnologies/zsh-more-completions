#compdef kbt
# Documentation: https://github.com/bloznelis/kbt
# Reference accessed: 2026-05-19
# Verified: bloznelis/kbt src/main.rs — clap Args struct is empty
# with #[command(version)]; only --help/--version exist. Layout is
# chosen via an interactive menu, NOT a flag. STUB: removed the
# invented --layout option and bogus man.archlinux search URL.

# kbt: keyboard tester TUI

local ret=1
_arguments -s -S \
  '(- *)'{-h,--help}'[print help]' \
  '(- *)'{-V,--version}'[print version]' && ret=0
return ret
