#compdef whowatch

# Zsh completion for whowatch - interactive real-time user monitoring tool

_whowatch() {
  local curcontext="$curcontext" state line ret=1

  _arguments -s -S \
    '-H[do not resolve hostnames]' \
    && ret=0

  return ret
}

_whowatch "$@"
