#compdef primes
# Documentation: man primes(6)
# Source: openbsd/src games/primes/primes.c — generate primes in range (C).
# Reference accessed: 2026-06-10

local curcontext="$curcontext" state line ret=1
typeset -A opt_args

_arguments -s \
    '(- *)-h[show usage]' \
    '1::start:_default' \
    '2::stop:_default' \
    && ret=0

return ret
