#compdef openl2tp
# Documentation: (no upstream binary by this exact name)
# Reference accessed: 2026-05-16
# Verified: openl2tp is the package; real binaries are openl2tpd (daemon) and
# l2tpconfig (control utility). Tool kept per "do not delete files" directive
# with minimal stub.

# openl2tp: package name (use openl2tpd or l2tpconfig)
local curcontext="$curcontext" ret=1
_arguments -C \
  '(- *)'{-h,--help}'[show help]' \
  '*::extra args:_files' && ret=0
return ret
