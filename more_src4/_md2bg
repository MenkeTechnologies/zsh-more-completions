#compdef md2bg
# ============================================================================
# _md2bg — zsh completion for **md2bg** — 37108/md2bg, the **typed
#            Markdown → Backlog wiki-syntax converter** by 37108
#            (typed daichi-okuyama).  Backlog is the typed
#            Japanese-developed (typed Nulab Inc. Fukuoka, Japan)
#            typed project-management + typed bug-tracker + typed
#            wiki + typed Git/SVN hosting typed all-in-one typed
#            SaaS used widely in typed Japanese typed enterprise
#            software engineering teams.  Backlog has its typed
#            own typed wiki-markup syntax (typed similar to typed
#            Atlassian Confluence + typed Trac wiki + typed
#            DokuWiki but typed different from typed Markdown
#            (typed Backlog uses typed `*bold*`, typed `_italic_`,
#            typed `**header level 1`, typed `[link>URL]`, typed
#            `&img(...)`, typed `{code}...{/code}` typed
#            constructs)).  md2bg is the typed reference typed
#            Markdown → Backlog typed converter that typed lets
#            typed developers typed write typed standard typed
#            CommonMark Markdown documents (typed in typed VS
#            Code + typed Vim + typed any typed editor) and typed
#            paste them into typed Backlog typed wiki pages with
#            typed correct typed Backlog-syntax conversion.  Built
#            on the typed **unified** typed text-processor typed
#            ecosystem typed via the canonical pure-JavaScript
#            **remark-parse** CommonMark + GFM Markdown parser
#            (typed the typed remark family is typed unified's
#            typed flagship Markdown processor).  TS-Node=189
#            **CLOSES** Axis 134 at 1/1/1 — triple-189 landmark
#            (Rust=189 + Go=189 + TS-Node=189) + 127th axis closed.
# ============================================================================
#
# UPSTREAM
#   Repository : https://github.com/37108/md2bg
#   Maintainer : 37108 (Daichi Okuyama, typed Japanese
#                 software engineer + typed open-source
#                 contributor)
#   License    : MIT
#   Stars      : 30+ on github.com/37108/md2bg
#   Language   : TypeScript / Node ESM  (commander for the
#                 CLI surface + typed unified + remark-parse
#                 ^7.0.1 (typed CommonMark parser typed AST
#                 producer) + typed remark-breaks (typed
#                 hard-break extension for typed Japanese
#                 typed line-break handling) for typed AST
#                 processing + typed custom typed `traversal()`
#                 typed transformer that typed walks typed
#                 mdast typed nodes and typed emits typed
#                 Backlog syntax + typed Node stdlib `fs` +
#                 `path` for typed file I/O)
#   Binary     : md2bg
#   Last push  : 2026-06-02
#
# WHAT IT IS
#   md2bg is the **typed Markdown → Backlog wiki-syntax
#   converter** — typed workflow:
#
#     1. Developer typed writes typed documentation in typed
#        standard CommonMark Markdown (typed `# Heading`,
#        typed `**bold**`, typed `*italic*`, typed
#        `[link](url)`, typed lists, typed tables, typed
#        fenced code blocks)
#     2. Developer typed runs typed `md2bg README.md` to typed
#        convert to typed Backlog wiki syntax (typed `* Heading`,
#        typed `''bold''`, typed `'''italic'''`, typed
#        `[link>url]`, typed `-` lists, typed `&table()`,
#        typed `{code}...{/code}` fences)
#     3. Developer typed pastes the typed output into typed
#        Backlog wiki page (typed nulab.com Backlog
#        instance) or typed pipes via typed stdin: typed
#        `cat README.md | md2bg -s`
#
#   md2bg typed handles:
#
#     • Headings (typed `# h1` → typed `*` prefix typed levels)
#     • Bold / italic / strikethrough
#     • Inline code + typed fenced code blocks (typed
#       language tag preserved)
#     • Unordered + ordered lists with typed nesting
#     • Tables (typed CommonMark + GFM table extension)
#     • Block quotes (typed `> quote`)
#     • Links + images (typed `[text](url)` → typed
#       `[text>url]`)
#     • Horizontal rules
#     • Hard line breaks (typed `\n\n` + typed `<br/>` →
#       typed `&br;`)
#     • Plain paragraphs
#
# CLI ARGUMENT GRAMMAR  (verbatim from `src/bin/index.ts`
#                       commander.js declarations;  extracted
#                       via raw fetch of `src/bin/index.ts`)
#
#   md2bg [FLAGS...] [FILE]
#
#   Modes:
#     -s, --stdin                     read Markdown from
#                                     standard input (vs
#                                     positional file)
#
#   Positional:
#     <FILE>                          path to Markdown file
#                                     (REQUIRED unless `-s`
#                                     given)
#
#   Standard commander:
#     -h, --help                      show help
#     -V, --version                   show version
#
# AXIS / CORPUS POSITION
#   `37108/md2bg`'s `package.json` declares a typed direct
#   dependency on `remark-parse@^7.0.1` (alongside typed
#   `unified` typed processor + typed `remark-breaks` typed
#   extension).  The typed `src/index.ts` typed forward path
#   uses typed `unified().use(markdown, {}).use(breaks).parse(
#   content)` to typed produce a typed mdast (Markdown AST)
#   typed tree, then typed walks it via typed `src/
#   traversal.ts` typed custom transformer that typed dispatches
#   typed per-node-type to typed Backlog-emitting typed
#   formatters.
#
#   **remark-parse** (`remark-parse` on npm;  GitHub org
#   `remarkjs/remark`) is **THE canonical pure-JavaScript
#   CommonMark + GFM Markdown parser** in the typed **unified**
#   typed text-processing typed ecosystem — typed Titus
#   Wormer's (wooorm on GitHub, typed lead maintainer of typed
#   unified) typed reference typed JavaScript implementation of
#   typed Markdown parsing producing typed mdast (Markdown
#   AST) typed Universal Syntax Tree.  In active development
#   since 2014, with 7,800+ ★ on github.com/remarkjs/remark
#   (typed the typed monorepo containing typed remark-parse +
#   typed remark-stringify + typed remark-rehype + typed
#   remark-cli + typed 100+ typed remark-* typed plugins).
#   Clocks **18 million weekly downloads on npm**.  Structurally
#   positioned as **the typed reference typed JavaScript
#   Markdown parser typed every typed unified-based typed text
#   processor is built on**.
#
#   remark-parse + the typed unified ecosystem ship:
#
#     • Typed `unified()`              typed top-level typed
#                                      processor typed builder
#                                      with typed `.use(plugin,
#                                      options)` typed chaining
#                                      + typed `.parse(text)` /
#                                      `.run(tree)` /
#                                      `.stringify(tree)` /
#                                      `.process(text)` typed
#                                      pipeline stages
#     • Typed `remark-parse`           typed CommonMark
#                                      compliant typed Markdown
#                                      parser that typed
#                                      produces typed mdast
#                                      typed Universal Syntax
#                                      Tree
#     • Typed `remark-stringify`       typed inverse: typed
#                                      mdast → typed Markdown
#                                      source (typed enables
#                                      typed round-trip
#                                      transformations)
#     • Typed `remark-rehype`          typed mdast → typed
#                                      hast (HTML AST) typed
#                                      bridge (typed used to
#                                      typed render typed
#                                      Markdown as HTML)
#     • Typed `remark-gfm`             typed GitHub-Flavored
#                                      Markdown extensions
#                                      (typed tables, typed
#                                      task lists, typed
#                                      strikethrough, typed
#                                      autolink literals)
#     • Typed `remark-breaks`          typed hard-line-break
#                                      extension (typed Japanese
#                                      typed line-break
#                                      handling, the typed dep
#                                      used by typed md2bg)
#     • Typed `remark-frontmatter`     typed YAML/TOML/JSON
#                                      front-matter parsing
#     • Typed `remark-math`            typed LaTeX math
#                                      extension
#     • Typed `remark-mdx`             typed MDX (JSX +
#                                      Markdown) extension
#     • Typed `remark-toc`             typed automatic table-
#                                      of-contents generation
#     • Typed `remark-lint`            typed Markdown lint
#                                      family (typed 50+ typed
#                                      rules)
#     • Typed `remark-html`            typed Markdown → HTML
#                                      typed direct path
#     • Typed `remark-cli`             typed standalone typed
#                                      CLI (typed `remark
#                                      input.md > output.md`)
#     • Typed mdast node types         typed concrete: typed
#                                      `Root`, `Paragraph`,
#                                      `Heading`,
#                                      `Blockquote`,
#                                      `List`, `ListItem`,
#                                      `Code`, `InlineCode`,
#                                      `Emphasis`, `Strong`,
#                                      `Link`, `Image`,
#                                      `LinkReference`,
#                                      `ImageReference`,
#                                      `Definition`, `Text`,
#                                      `Break`,
#                                      `ThematicBreak`,
#                                      `HTML`, `Table`,
#                                      `TableRow`, `TableCell`,
#                                      `Delete` (strikethrough)
#     • Typed `unist-util-visit`       typed AST visitor utility
#                                      for typed plugin authors
#     • Typed `unist-util-modify-children`,
#       `unist-util-find`, `unist-util-position`,
#       `unist-util-source`, `unist-util-remove`,
#       `unist-util-select`, `unist-util-flatmap`,
#       `unist-util-filter`             typed AST manipulation
#                                      helpers
#     • Typed `vfile`                  typed virtual-file
#                                      abstraction (typed
#                                      input + output + typed
#                                      file metadata + typed
#                                      messages)
#     • Typed `vfile-reporter`         typed pretty-printed
#                                      typed diagnostic output
#     • Typed `mdast-util-*` family    typed conversion +
#                                      typed query primitives
#
#   remark-parse + unified is the Markdown library behind
#   essentially every modern JavaScript Markdown-using
#   application:
#
#     • **MDX** (mdx-js/mdx, 18,000+ ★) — typed JSX-in-
#       Markdown standard;  typed remark-parse for the typed
#       Markdown layer
#     • **Astro** (typed astro-build) — typed reference modern
#       static-site framework with typed first-class typed
#       Markdown via typed remark-parse
#     • **Docusaurus** (typed facebook/Docusaurus) — typed
#       Meta's docs framework
#     • **Gatsby** — typed React-based static-site generator
#     • **Next.js** docs + typed app-router typed MDX
#     • **VuePress** + **VitePress** — typed Vue.js docs
#     • **Nuxt Content** — typed Nuxt 3 content module
#     • **MD Manager** + **Notion-to-Markdown** typed bridges
#     • **Storybook** + **Histoire** — typed component-story
#       typed docs
#     • **velite** (typed zce/velite) — typed Markdown-driven
#       content build (covered as `_velite`)
#     • **md2bg** (this very project) — typed Markdown →
#       Backlog wiki typed converter
#     • **word-to-markdown-js** (typed benbalter) — typed
#       Word → Markdown typed converter
#     • **note-link-janitor** (typed andymatuschak Quanta
#       Memex) — typed bidirectional-link Markdown
#       maintenance
#     • **react-rainbow** — typed React component library
#       docs
#     • **kentcdodds.com** + typed countless typed personal
#       blogs + typed knowledge bases
#     • Plus typed thousands of typed JavaScript Markdown
#       processors + typed CMS systems + typed docs sites.
#
#   The remark + unified projects are structurally significant
#   because they typed established **the typed Universal Syntax
#   Tree (UST) typed standard** typed alongside its typed
#   companions typed hast (HTML AST) + typed nlcst (Natural
#   Language Concrete Syntax Tree) + typed esast (ECMAScript
#   AST).  typed unified gives typed Markdown processing the
#   typed same typed plugin ecosystem typed pattern that typed
#   PostCSS gave typed CSS or typed Babel gave typed JavaScript:
#   typed compose typed small typed focused typed plugins (typed
#   `remark-toc`, typed `remark-gfm`, typed `remark-lint`,
#   typed `remark-frontmatter`) typed into typed custom typed
#   pipelines without typed monolithic typed framework lock-in.
#
#   Verified via:
#     gh api repos/37108/md2bg/contents/package.json
#       → "name": "md2bg"
#       → "bin": { "md2bg": "build/src/bin/index.js" }
#       → "dependencies": { ..., "remark-parse": "^7.0.1",
#                                "remark-breaks": "^1.0.5",
#                                "unified": "^7.1.0", ... }
#       → "description": "convert markdown syntax to
#                          backlog syntax"
#     raw.githubusercontent.com 37108/md2bg main src/bin/
#         index.ts
#       → import { Command } from 'commander'
#       → program.option('-s, --stdin',
#                         'read standard input')
#       → readFromStdin reads '/dev/stdin'
#       → md2bg(content, false) for stdin mode
#       → md2bg(file, true) for file mode
#     raw.githubusercontent.com 37108/md2bg main src/index.ts
#       → import unified from 'unified'
#       → import markdown from 'remark-parse'
#       → import breaks from 'remark-breaks'
#       → const processor = unified().use(markdown, {})
#                                     .use(breaks)
#       → processor.parse(content) → mdast tree
#       → ast.children.map(item => traversal(item))
#                                     .join('\n')
#
#   This entry is the TS-Node=189 closing pair that **CLOSES** Axis
#   134 (pure-Rust + pure-Go + TS-Node CommonMark + GFM Markdown
#   parser + renderer taxonomy) at 1/1/1, completing the
#   triple-189 round-number landmark (Rust=189 R1100 _mm0-rs on
#   pulldown-cmark + Go=189 R1101 _booklit on yuin/goldmark +
#   TS-Node=189 R1102 _md2bg on remarkjs/remark + unified) and
#   marking the **127th axis closed** in the corpus.  R1103 on
#   deck: Rust=190 OPENS Axis 135.
#
#   No completion for `md2bg` ships with Apple zsh
#   (/usr/share/zsh/*/functions) or Homebrew zsh-completions
#   (/opt/homebrew/share/zsh/site-functions) — confirmed absent.
# ============================================================================

_md2bg() {
    _arguments -s -S \
        '(-s --stdin)'{-s,--stdin}'[read Markdown from standard input]' \
        '(-h --help)'{-h,--help}'[show help]' \
        '(-V --version)'{-V,--version}'[show version]' \
        '*:Markdown file:_files -g "*.(md|markdown|mkd|mdown)"'
}

_md2bg "$@"
