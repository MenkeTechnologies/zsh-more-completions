#compdef conan_server

local arguments

arguments=(
  {-h,--help}'[show this help message and exit]'
  '--migrate[run the pending migrations]'
  '--server[_dir SERVER_DIR, -d SERVER_DIR]'
  '*:filename:_files'
)

_arguments -s $arguments
