#compdef kafka-metadata-shell

local -a arguments

arguments=(
    '--snapshot[Path to the metadata snapshot file]:file:_files'
    '--directory[Path to the metadata log directory]:directory:_directories'
)

_arguments -s -S $arguments
