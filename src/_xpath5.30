#compdef xpath5.30

local arguments

arguments=(
  '-q[only output the resulting PATH.]'
  '-s[use suffix instead of linefeed.]'
  '-p[use prefix instead of nothing.]'
  '-n[t use an external DTD.]'
  '*:filename:_files'
)

_arguments -s $arguments
