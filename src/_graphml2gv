#compdef graphml2gv

local arguments

arguments=(
  '-g[<name>  : use <name> as template for graph names]'
  '-o[<file>  : output to <file> (stdout)]'
  '-v[: verbose mode]'
  '*:filename:_files'
)

_arguments -s $arguments
