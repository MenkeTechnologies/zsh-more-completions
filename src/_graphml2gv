#compdef graphml2gv

local -a arguments

arguments=(
  '-g[use <name> as template for graph names]:name:'
  '-o[output to <file> (stdout)]:file:'
  '-v[verbose mode]'
  '*:filename:_files'
)

_arguments -s -S $arguments
