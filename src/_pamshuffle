#compdef pamshuffle


local -a arguments

arguments=(
    '--column[Shuffle vertical columns, i. e. perform the same movement on all rows.]'
    '--randomseed[This is the seed for the random number generator that generates the pixels.]:integer'
    '*:filename:_files'
)
_arguments -s -S $arguments
