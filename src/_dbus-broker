#compdef dbus-broker

# Autogenerated from man page /usr/share/man/man1/dbus-broker.1.gz

local arguments

arguments=(
    {-h,--help}'[print usage information and exit immediately.]'
    '--version[print build-version and exit immediately.]'
    '--audit[enable logging to the linux audit subsystem (no-op if audit support was not c…]'
    '--controller[use the inherited file-descriptor with the given number as the controlling so…]:FD'
    '--log[use the inherited file-descriptor with the given number to access the system …]:FD'
    '--machine-id[set the machine-id to be advertised by the broker via the org. freedesktop.]:ID'
    '--max-bytes[maximum number of bytes each user may allocate in the broker (Default: 16 MiB…]:BYTES'
    '--max-fds[maximum number of file descriptors each user may allocate in the broker (Defa…]:FDS'
    '--max-matches[maximum number of match rules each user may allocate in the broker (Default: …]:MATCHES'
    '--max-objects[maximum total number of names, peers, pending replies, etc each user may allo…]:OBJECTS'
    '*:filename:_files'
)
_arguments -s $arguments
