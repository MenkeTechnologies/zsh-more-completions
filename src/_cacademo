#compdef cacademo

local -a arguments

arguments=(
    '-d[Driver]:driver:(x11 gl caca slang ncurses raw null)'
    '-h[Show help]'
    '-l[List available drivers]'
    '-v[Show version]'
)

_arguments -s -S $arguments
