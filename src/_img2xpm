#compdef img2xpm

local arguments

arguments=(
  '-o[the directory to place the .xpm file(s), defaults to]'
  '-m[if the original image has a mask or transparency defined]'
  '-n[A filename to write the .xpm data to. Defaults to the]'
  '*:filename:_files'
)

_arguments -s $arguments
