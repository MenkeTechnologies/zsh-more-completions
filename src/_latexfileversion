#compdef latexfileversion

local -a arguments

arguments=(
    '-l[Show only the file date]'
    '-d[Show only the file date]'
    '-v[Show only the file version]'
    '-h[Show help message]'
    '*:LaTeX file:_files -g "*.{sty,cls,dtx,tex}"'
)

_arguments -s -S $arguments
