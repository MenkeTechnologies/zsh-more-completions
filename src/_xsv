#compdef xsv

# Zsh completion for xsv - a fast CSV command line toolkit

local curcontext="$curcontext" state line ret=1

local -a subcommands=(
  'cat:Concatenate CSV files by row or column'
  'count:Count records'
  'fixlengths:Make all records have same length'
  'flatten:Show one field per line'
  'fmt:Format CSV output (change delimiter, etc.)'
  'frequency:Show frequency tables for each column'
  'headers:Show column headers'
  'index:Create an index of the given CSV'
  'input:Deal with common variations in CSV format'
  'join:Join CSV files'
  'partition:Partition CSV data based on a column value'
  'pseudo:Replace values in a column with incrementing integers'
  'rename:Rename the columns of CSV data'
  'reverse:Reverse rows of CSV data'
  'sample:Randomly sample CSV data'
  'search:Search CSV data with a regular expression'
  'select:Select, re-order, duplicate or drop columns'
  'slice:Slice records from CSV'
  'sort:Sort CSV data'
  'split:Split CSV data into many files'
  'stats:Compute descriptive statistics'
  'table:Align CSV data into columns'
  'help:Print help'
)

_arguments -s -S \
  '(-h --help)'{-h,--help}'[show help]' \
  '--list[list all commands available]' \
  '(-v --version)'{-v,--version}'[show version]' \
  ':subcommand:->subcommand' \
  '*:: :->args' && ret=0

case $state in
  subcommand)
    _describe -t subcommands 'subcommand' subcommands && ret=0
    ;;
  args)
    _arguments \
      '(-d --delimiter)'{-d,--delimiter}'[field delimiter]:delimiter:' \
      '(-n --no-headers)'{-n,--no-headers}'[treat first row as data (not headers)]' \
      '(-o --output)'{-o,--output}'[write output to file]:file:_files' \
      '*:CSV file:_files -g "*.csv"' && ret=0
    ;;
esac

return ret
