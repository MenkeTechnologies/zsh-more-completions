#compdef ppdi

local -a arguments

arguments=(
  '-I[add include directory to search path.]'
  '-o[.drv Set driver information file (otherwise ppdi.drv).]'
  '*:filename:_files'
)

_arguments -s -S $arguments
