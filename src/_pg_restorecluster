#compdef pg_restorecluster

# Autogenerated from man page /usr/share/man/man1/pg_restorecluster.1.gz

local -a arguments

arguments=(
    '--datadirR[X Item "-d --datadir DIR" Use IDIRR as data directory for the restored cluste…]'
    '--portR[X Item "-p --port N" Use port INR for restored cluster (default is next free …]'
    '--startR[X Item "-s --start" Start cluster after restoring (default for restore from d…]:4'
    {-d,--datadir}'[.]'
    {-p,--port}'[.]'
    {-s,--start}'[.]':4
    '--archive[.]:4'
    '--pitr[.]:4'
    '--recovery-target-time[Additionally to setting restore_command, set recovery_target_time to TIMESTAM…]:4'
    '--wal-archive[.]:4'
    '*:filename:_files'
)
_arguments -s -S $arguments
