#compdef xscertadmin

local arguments

arguments=(
  {-x,--xml}'[output sent to stdout is in xml format]'
  {-v,--version}'[print the version of the tool]'
  {-d,--debug}'[sets the level of diagnostic message detail, 1 for on, 0 for off]'
  '*:filename:_files'
)

_arguments -s $arguments
