#compdef dpkg-distaddfile

local -a arguments

arguments=(
  '-f[write files here instead of debian/files.]:files-list-file:'
  '--version[show the version.]'
  '*:filename:_files'
)

_arguments -s -S $arguments
