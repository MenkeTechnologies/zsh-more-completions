#compdef react-native

local -a arguments



local -a subcmds
subcmds=('init:init <ProjectName> generates a new project and installs its dependencies')
_describe 'command' subcmds

arguments=(
  {-h,--help}'[output usage information]'
  {-v,--version}'[the version number]'
)

_arguments -s -S $arguments


