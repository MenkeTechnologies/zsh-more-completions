#compdef odfmeta

# Autogenerated from man page /usr/share/man/man1/odfmeta.1.gz

local arguments

arguments=(
    '-l[List (extract) all known metadata fields.]'
    {-v,-V}'[Print the version number of the ODF document IformatR.]'
    '-c[Make field values continuous by normalizing white space.]'
    '-d[Update the modification date to the current date and time.]'
    '-x[Extract the contents of this metafield from the file.]:ImetafieldR'
    '-X[Same as -x, but also preserves/includes the field name.]:ImetafieldR'
    '-a[Append a custom metafield to the metadata; but only if a similar field does n…]:ImetafieldR'
    '-A[Append a custom metafield to the metadata in any case.]:ImetafieldR'
    '-I[Append a custom metafield to the metadata and remove any existing similar fie…]:ImetafieldR'
    '-o[Filename to write modified ODT file to.]:IpathR'
    '*:filename:_files'
)
_arguments -s $arguments
