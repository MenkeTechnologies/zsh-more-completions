#compdef runsvdir

local -a arguments

arguments=(
  '-P[run each runsv in a new session]'
  '-s[script to run on SIGTERM/SIGHUP]:script:_files'
  '1:directory:_directories'
)

_arguments -s -S $arguments
