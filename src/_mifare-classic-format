#compdef mifare-classic-format

local -a arguments

arguments=(
  '-f[fast format (only erase MAD)]'
  '-y[do not ask for confirmation (dangerous)]'
  '*:filename:_files'
)

_arguments -s -S $arguments
