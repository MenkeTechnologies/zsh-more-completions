#compdef markdown2tex

local -a arguments

arguments=(
    '-h[Show help]'
    '-o[Output file]:output file:_files -g "*.tex"'
    '-s[Standalone document]'
    '*:Markdown file:_files -g "*.{md,markdown,txt}"'
)

_arguments -s -S $arguments
