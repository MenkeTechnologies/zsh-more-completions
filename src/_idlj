#compdef idlj

local -a arguments

arguments=(
  '--help[1): Expected one of `typedef `struct `union `enum `const `exception `interface `valuetype `module; encountered `<identifier>.]'
  '*:filename:_files'
)

_arguments -s -S $arguments
