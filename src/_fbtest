#compdef fbtest

local arguments

arguments=(
  '-f[the frame buffer device (default /dev/fb0)]'
  '-C[the virtual console device (default /dev/tty1)]'
  '*:filename:_files'
)

_arguments -s $arguments
