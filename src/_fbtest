#compdef fbtest

local -a arguments

arguments=(
  '-f[the frame buffer device (default /dev/fb0)]:device:_files'
  '-C[the virtual console device (default /dev/tty1)]:device:_files'
  '*:filename:_files'
)

_arguments -s -S $arguments
