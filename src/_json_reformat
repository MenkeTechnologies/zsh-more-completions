#compdef json_reformat

local arguments

arguments=(
  '-m[json rather than beautify (default)]'
  '-u[invalid UTF8 inside strings during parsing]'
  '-e[any forward slashes (for embedding in HTML)]'
  '*:filename:_files'
)

_arguments -s $arguments
