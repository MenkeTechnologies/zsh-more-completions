#compdef fourcc2pixfmt

local arguments

arguments=(
  '-l[list the pixel format for each fourcc]'
  '-L[list the fourccs for each pixel format]'
  '-p[given a pixel format, print the list of associated fourccs (one per line)]'
  '-h[print this help]'
  '*:filename:_files'
)

_arguments -s $arguments
