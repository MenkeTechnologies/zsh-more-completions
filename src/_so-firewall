#compdef so-firewall

local arguments

arguments=(
  '--apply[- After updating the firewall configuration files, apply the new firewall state]'
  '--defaultports[- Read port groups from default configuration files instead of local configuration.]'
  '*:filename:_files'
)

_arguments -s $arguments
