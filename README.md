```
 ███▄ ▄███▓ ▒█████   ██▀███  ▓█████
▓██▒▀█▀ ██▒▒██▒  ██▒▓██ ▒ ██▒▓█   ▀
▓██    ▓██░▒██░  ██▒▓██ ░▄█ ▒▒███
▒██    ▒██ ▒██   ██░▒██▀▀█▄  ▒▓█  ▄
▒██▒   ░██▒░ ████▓▒░░██▓ ▒██▒░▒████▒
░ ▒░   ░  ░░ ▒░▒░▒░ ░ ▒▓ ░▒▓░░░ ▒░ ░
░  ░      ░  ░ ▒ ▒░   ░▒ ░ ▒░ ░ ░  ░
░      ░   ░ ░ ░ ▒    ░░   ░    ░
       ░       ░ ░     ░        ░  ░
  ▄████▄   ▒█████   ███▄ ▄███▓ ██▓███   ██▓    ▓█████▄▄▄█████▓ ██▓ ▒█████   ███▄    █▓  ██████
▒██▀ ▀█  ▒██▒  ██▒▓██▒▀█▀ ██▒▓██░  ██▒▓██▒    ▓█   ▀▓  ██▒ ▓▒▓██▒▒██▒  ██▒▓██▒█   █▒▒██    ▒
▒▓█    ▄ ▒██░  ██▒▓██    ▓██░▓██░ ██▓▒▒██░    ▒███  ▒ ▓██░ ▒░▒██▒▒██░  ██▒▒██░ █  █░░ ▓██▄
▒▓▓▄ ▄██▒▒██   ██░▒██    ▒██ ▒██▄█▓▒ ▒▒██░    ▒▓█  ▄░ ▓██▓ ░ ░██░▒██   ██░░██  █  █  ▒   ██▒
▒ ▓███▀ ░░ ████▓▒░▒██▒   ░██▒▒██▒ ░  ░░██████▒░▒████▒ ▒██▒ ░ ░██░░ ████▓▒░░▒██▒ █ ██▒██████▒▒
░ ░▒ ▒  ░░ ▒░▒░▒░ ░ ▒░   ░  ░▒▓▒░ ░  ░░ ▒░▓  ░░░ ▒░ ░ ▒ ░░   ░▓  ░ ▒░▒░▒░ ░▒▓░▒▓▒ ▒ ▒▓▒ ▒ ░
  ░  ▒    ░ ▒ ▒░ ░  ░      ░░▒ ░     ░ ░ ▒  ░ ░ ░  ░   ░      ▒ ░  ░ ▒ ▒░  ░▒ ░ ░  ░ ░▒  ░ ░
░       ░ ░ ░ ▒  ░      ░   ░░         ░ ░      ░   ░        ▒ ░░ ░ ░ ▒   ░    ░  ░  ░  ░
░ ░         ░ ░         ░                 ░  ░   ░  ░         ░      ░ ░   ░       ░     ░
░                                                                                         ░
```

[![CI](https://github.com/MenkeTechnologies/zsh-more-completions/actions/workflows/ci.yml/badge.svg)](https://github.com/MenkeTechnologies/zsh-more-completions/actions/workflows/ci.yml)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![zsh](https://img.shields.io/badge/zsh-plugin-cyan.svg)](https://github.com/MenkeTechnologies/zpwr)

### `[THE LARGEST CURATED ZSH COMPLETION CORPUS IN EXISTENCE // 37,161 FILES]`

> *"Every CLI you've heard of, completed. Plus several you haven't."*

> _"The shell is the street. The completions are the chrome."_

### [`strykelang`](https://github.com/MenkeTechnologies/strykelang) &middot; [`zshrs`](https://github.com/MenkeTechnologies/zshrs) · [`MenkeTechnologiesMeta`](https://github.com/MenkeTechnologies/MenkeTechnologiesMeta) · [`zpwr`](https://github.com/MenkeTechnologies/zpwr) · [`zshrs`](https://github.com/MenkeTechnologies/zshrs) · [`zsh-cargo-completion`](https://github.com/MenkeTechnologies/zsh-cargo-completion) · [`zsh-expand`](https://github.com/MenkeTechnologies/zsh-expand)

---

## Table of Contents

- [\[0x00\] // WHAT IS THIS](#0x00-what-is-this)
- [\[0x01\] // JACK IN](#0x01-jack-in)
- [\[0x02\] // BOOT SEQUENCE](#0x02-boot-sequence)
- [\[0x03\] // STATS](#0x03-stats)
- [\[0x04\] // TESTING](#0x04-testing)

---

## [0x00] // WHAT IS THIS

A **massive** zsh completions plugin — over 30k command completions wired into compsys. Most were auto-generated by Python scripts that jacked into `--help` output, man pages, and web research, ripping completion data straight from the source. But then all completions have been cleaned, verified and updated with latest command options. Many have been handwritten as well.  Architecture-prefixed completions live in the `architecture_src` directory. A large ZUnit suite (see **// STATS** and **// TESTING**) validates structure, syntax, and coverage.

### // HARVESTED ECOSYSTEMS

Tool names for the completion corpus were harvested from:

- **Nix / nixpkgs** — every `pkgs.<attr>` shipping a binary
- **Homebrew** — every formula in `homebrew/core` (and the `cask` bottles that install CLIs)
- **Debian / Ubuntu APT** — every `bin/*` from `apt-cache pkgnames`, `/usr/share/man/man1`, `/usr/share/man/man8`, `/usr/share/man/man5`, `/usr/share/man/man6`, etc.
- **Fedora** — every binary from Fedora package repos
- **macOS system binaries** — `/usr/bin`, `/usr/sbin`, `/usr/libexec`, etc.
- **Kali Linux** — every binary shipped in the Kali package set
- **Alpine Linux** — `apk` package set
- **FreeBSD** — ports binaries and man pages
- **Various other Linux distros**
- **GitHub Releases** + **GitHub tree / code search** (multiple harvest rounds)
- **npm / npx / web research** — `create-*` scaffolders, scoped `@org/*` CLIs, and other Node tools verified against the npm registry (`npm view` bin/readme), live `npx … --help` output, package dist/tarballs, and upstream readmes (multi-round manual harvest; trivial or interactive-only scaffolders skipped)
- **Local zinit plugin checkouts** — completions vendored from installed plugins
- **crates.io**
- **Exotic language ecosystems** — Hackage (Haskell: `hakyll`, `shake`, `taffybar`), OPAM (OCaml: `odig`, `irmin`, `js_of_ocaml`, `learn-ocaml`, `alcotest`), Hex.pm (Elixir: `phx_new`, `ex_doc`, `dialyxir`, `igniter`, `mneme`), CPAN (Perl: `perlimports`, `perlnavigator`), CRAN (R: `radian`, `littler`), and AUR helpers (`aurman`, `pakku`) — verified against each project's cmdliner/argparse/OptionParser/Mix.Task switches, GetOptions definitions, or Cmd modules at the upstream source URL embedded in every completion file
- **Termux:API + Termux:tools** (Android) — all 57 `termux-*` commands from `termux-api-package/scripts/` (toast, vibrate, notification, sms-send, location, sensor, camera-photo, tts-speak, dialog, share, …) plus 13 `termux-*` commands from `termux-tools/scripts/` (backup, change-repo, info, open-url, reload-settings, restore, setup-storage, wake-lock, …) — flags parsed directly from each upstream `show_usage()` echo block and `getopts` spec, generated by `scripts/` automation
- **ECMWF eccodes** (meteorology) — `grib_*`, `bufr_*`, `gts_*`, `metar_*`, `taf_*`, `codes_*` toolkit binaries from `ecmwf/eccodes/tools/`; option letters and help text resolved via `tools/grib_options.cc` `grib_options_help_list[]` and each tool's per-file `names[]` array at https://github.com/ecmwf/eccodes/tree/develop/tools
- **NCO** (netCDF Operators) — `ncatted`, `ncbo`, `ncra`, `ncrcat`, `ncrename`, etc.; canonical short/long option help text extracted from `nco/nco/src/nco/nco_ctl.c` `nco_usg_prn()` and per-operator man pages at https://github.com/nco/nco
- **GDAL / OGR utilities** — geospatial raster + vector toolkit binaries from `OSGeo/gdal/apps/` (`nearblack`, `gdal_footprint`, `sozip`, `gnmanalyse`, `gnmmanage`, …); option lists verified against the per-tool reference docs at https://gdal.org/en/stable/programs/ and the upstream `apps/` C++ argparse setup
- **CERN ROOT** (HEP data analysis) — `rootbrowse`, `rootcp`, `rootls`, `rootmkdir`, `rootmv`, `rootrm`, `rootnb`, `rootcling`, `rootreadspeed`, `rootdrawtree`, `rooteventselector`, `rootprint`, `rootslimtree`, `hadd`, `h2root` — argparse setups read from `root-project/root/main/python/*.py` and `main/src/*.cxx` at https://github.com/root-project/root
- **ISC BIND 9** (DNS) — `named*`, `rndc*`, `dnssec-*`, `nsec3hash`, `named-nzd2nzf`, `delv`, `mdig`, `arpaname`, `tsig-keygen`, `dnstap-read`, `ddns-confgen` — synopsis + flag tables harvested from the per-tool `.rst` manpage sources under `bin/dnssec/`, `bin/named/`, `bin/tools/` at https://github.com/isc-projects/bind9
- **EPICS Channel Access** (particle-accelerator control) — `caget`, `caput`, `cainfo`, `camonitor` — flag tables harvested from each tool's `usage()` printf block in `epics-base/epics-base/modules/ca/src/tools/*.c` at https://github.com/epics-base/epics-base
- **CFEngine** (config management) — every `cf-*` binary in upstream (`cf-agent`, `cf-promises`, `cf-execd`, `cf-monitord`, `cf-serverd`, `cf-runagent`, `cf-key`, `cf-net`, `cf-secret`, `cf-testd`, `cf-upgrade`, `cf-check`); short/long option pairs + HINTS strings read from each binary's `cfengine/core/cf-*/cf-*.c` source (cf-serverd from `cf-serverd-functions.c`, cf-upgrade from `cf-upgrade/command_line.c`) `OPTIONS[]`/`HINTS[]` getopt_long tables at https://github.com/cfengine/core
- **VoIP / SIP stacks** — `fs_cli` (FreeSWITCH; `signalwire/freeswitch libs/esl/fs_cli.c` `usage_str`), `kamcmd`+`kamctl` (Kamailio; `kamailio/kamailio utils/kamcmd/kamcmd.c` `help_msg` and `utils/kamctl/kamctl` dispatch table), `baresip` (`baresip/baresip src/main.c` `usage()`), `rtpengine` (`sipwise/rtpengine daemon/main.c` `GOptionEntry[]`)
- **Seismology / miniSEED** — IRIS/EarthScope CLI suite (`msi`, `dataselect`, `mseed2sac`, `slinktool`, `slarchive`); flag tables harvested from each tool's `usage()` function in `EarthScope/{msi,dataselect,slinktool,slarchive}/src/*.c` and `iris-edu/mseed2sac/src/mseed2sac.c`
- **ROS 1 (Noetic)** — robotics middleware CLIs (`roscore`, `rosrun`, `rosbag`, `rostopic`, `rosnode`, `roslaunch`, `rosparam`, `rosservice`, `rospack`, `rosmsg`/`rossrv`); subcommand tables read from `ros/ros_comm noetic-devel tools/*/src/**/__init__.py` (`_fullusage()`/`rosbag_main.RosbagCmds`), `ros/rospack noetic-devel src/rospack.cpp Rospack::usage()`, and `ros/ros noetic-devel tools/rosbash/scripts/rosrun`
- **MAVLink / drones** — `mavproxy.py` (ground station/proxy; `ArduPilot/MAVProxy MAVProxy/mavproxy.py` `OptionParser`) plus pymavlink analysis + code-gen tools (`mavlogdump.py`, `mavgraph.py`, `mavextract.py`, `mavflightmodes.py`, `mavgen.py`, `mavflighttime.py`, `mavfft.py`, `magfit.py`, `extract_parms.py`) at `ArduPilot/pymavlink tools/*.py` `ArgumentParser` setups
- **Quantum chemistry (Grimme group)** — `xtb` (GFN-xTB semiempirical QM at `grimme-lab/xtb`), `crest` (conformer ensemble search at `crest-lab/crest`), `censo` (ensemble sorting at `grimme-lab/CENSO`), `dftd4` (D4 dispersion at `dftd4/dftd4`), plus `packmol` (MD box packing at `m3g/packmol`); flag tables read from each project's CLI parser / readthedocs commandline page
- **Apache Jena (RDF / SPARQL)** — `riot` (RDF I/O Tool), `rsparql`/`sparql`/`arq`/`qparse` (SPARQL query), `shacl` (W3C SHACL validate + parse), full TDB2 toolkit (`tdb2.tdbloader`, `tdb2.tdbquery`, `tdb2.tdbstats`, `tdb2.tdbbackup`, `tdb2.tdbcompact`), `rdfcompare`/`rdfdiff` (graph isomorphism diff), `fuseki-server` (Fuseki2 SPARQL HTTP server); flag tables harvested from `jena.apache.org/documentation/{io,query,shacl,tdb2,tools,fuseki2}/` reference pages and `apache/jena/jena-cmds/src/main/java/{jena,tdb2,tdb2/cmdline}/*.java` ArgDecl + usage() sources
- **OpenFOAM (CFD)** — mesh + solver + post-processing utilities (`checkMesh`, `renumberMesh`, `mergeMeshes`, `transformPoints`, `surfaceCheck`, `topoSet`, `setFields`, `foamRun`, `foamToVTK`, `potentialFoam`, plus existing `blockMesh`/`decomposePar`/`reconstructPar`/`pimpleFoam`/`simpleFoam`/`snappyHexMesh`); per-tool `argList::addOption()`/`addBoolOption()` strings + the common argList option set (`-case`, `-parallel`, `-roots`, `-region`, …) read from `OpenFOAM/OpenFOAM-12/applications/**/*.C` and `src/OpenFOAM/global/argList/argList.C`
- **rt-tests** (Linux real-time benchmarks) — `hackbench`, `signaltest`, `pi_stress`, `pmqtest`, `sigwaittest` (alongside existing `cyclictest`); `getopt_long` long_options[] + `display_help()`/`print_usage_exit()` tables read from each tool's source under `rt-tests/src/*/*.c`
- **Madagascar (seismic / RSF)** — `sfin`, `sfwindow`, `sfmath`, `sfcat`, `sfattr`, `sfreverse`, `sftransp`, `sfdd`, `sfpad`, `sfdisfil`, `sfheaderwindow`, `sfheadersort`, `sfheadercut`, `sfget`, `sfput`, `sfspike`, `sfcmplx`, `sfreal`/`sfimag`, `sfinterleave`, `sfmask`, `sfclip`, `sfsmooth`, `sfbandpass`, `sfricker`, `sfagc`, `sfgraph`, `sfgrey`, `sfwiggle`, `sfcontour`, `sffft1` — key=value parameter style with names + types resolved from each tool's `sf_get*()` calls across `ahay/src/{system/main,system/generic,system/seismic,plot/main}/*.c` (e.g. `Mclip.c clip= value=`, `Mbandpass.c flo= fhi= phase=`, `grey.c gpow= pclip= polarity= color=`, `wiggle.c xpos= zplot= poly=`, `contour.c c= nc= dc= c0=`, `Mfft1.c inv= sym= opt= memsize=`)
- **Orfeo Toolbox** (Earth observation / remote sensing) — 20 OTB apps: `otbcli_BandMath`, `otbcli_ExtractROI`, `otbcli_ConcatenateImages`, `otbcli_Rasterization`, `otbcli_Segmentation`, `otbcli_OpticalCalibration`, `otbcli_OrthoRectification`, `otbcli_Smoothing`, `otbcli_HaralickTextureExtraction`, `otbcli_ImageClassifier`, `otbcli_TrainImagesClassifier`, `otbcli_KMeansClassification`, `otbcli_BinaryMorphologicalOperation`, `otbcli_EdgeExtraction`, `otbcli_Pansharpening`, `otbcli_RadiometricIndices`, `otbcli_ComputeImagesStatistics`, `otbcli_Mosaic`, `otbcli_ColorMapping`, `otbcli_DimensionalityReduction`; per-app parameter trees (including dotted sub-parameters like `-mode.extent.ulx`, `-filter.meanshift.spatialr`, `-acqui.sun.elev`, `-outputs.spacingx`, `-type.gaussian.stdev`, `-parameters.nbbin`, `-classifier.libsvm.k`, `-classifier.sharkrf.nbtrees`, `-method.bayes.lambda`, `-comp.feather.slim.exponent`, `-method.ica.g`, `-channels.nir`) harvested from the OTB CookBook reference at https://www.orfeo-toolbox.org/CookBook/Applications/
- **Astrometry.net** (astronomical plate-solving aux) — `image2xy` (FITS source extractor), `image2pnm` (raster→PNM), `an-fitstopnm` (FITS→PNM with stretching), `wcsinfo` (WCS header inspector), `wcs-xy2rd`/`wcs-rd2xy` (pixel↔RA/Dec), `build-index` (quad-index builder), `hpsplit` (split catalogue by healpix), `plotxy` (XY markers over image), `tabsort` (sort FITS table) — alongside existing `solve-field`/`plot-constellations`; flag tables read from each tool's `print_help()` / `printHelp()` / `getopt` source at `dstndstn/astrometry.net {util,solver,blind}/*.c` plus `manpages.debian.org` `image2xy.1`/`plotxy.1`
- **GMT** (Generic Mapping Tools, classic-mode) — 105 binaries: `psxy`, `pscoast`, `grdimage`, `grdmath`, `grdtrack`, `pstext`, `psbasemap`, `psconvert`, `grdfilter`, `grdsample`, `grdcontour`, `xyz2grd`, `grd2xyz`, `nearneighbor`, `blockmean` (shared compdef also serves `blockmedian`/`blockmode`), `mapproject`, `triangulate`, `makecpt`, `grdinfo`, `grdcut`, `grdedit`, `grdgradient`, `grdblend`, `gmtset`, `gmtwhich`, `grdvector`, `gmtselect`, `gmtmath`, `sample1d`, `fitcircle`, `grdpaste`, `grdvolume`, `gmtspatial`, `trend1d`, `gmt2kml`, `gmtinfo`, `gmtconvert`, `gmtconnect`, `gmtsimplify`, `kml2gmt`, `trend2d`, `sphinterpolate`, `gmtbinstats`, `gmtregress`, `filter1d`, `mgd77info`, `mgd77list`, `dimfilter`, `psmeca`, `psvelo`, `pscoupe`, `psrose`, `pswiggle`, `gpsgridder`, `grdrotater`, `gmtflexure`, `gravfft`, `talwani2d`, `earthtide`, `backtracker`, `hotspotter`, `rotconverter`, `x2sys_init`, `sac`, `grdseamount`, `mgd77track`, `mgd77magref`, `mgd77manage`, `mgd77path`, `mgd77sniffer`, `x2sys_binlist`, `x2sys_cross`, `x2sys_datalist`, `x2sys_get`, `x2sys_put`, `x2sys_list`, `x2sys_merge`, `x2sys_report`, `x2sys_solve`, `originater`, `grd2cpt`, `grdfft`, `grdmask`, `grdproject`, `grdhisteq`, `grdtrend`, `grdlandmask`, `grdfill`, `pshistogram`, `psscale`, `grdconvert`, `grdinterpolate`, `psimage`, `pslegend`, `psmask`, `psternary`, `pssolar`, `grdmix`, `grdgdal`, `grd2kml`, `polespotter`, `talwani3d`, `grdgravmag3d`, `gmtgravmag3d`, `gshhg` (alongside existing `gmt` modern-mode wrapper); single-letter option syntax (`-J` projection, `-R` region, `-B` basemap, `-W` pen, `-G` fill, …) + per-program options (e.g. `pscoast -D{full,high,intermediate,low,crude}`, `grdtrack -S{mean,median,mode}` stack reducers, `grdmath`/`gmtmath` 200+ RPN operators each, `grdfilter -F{b,c,g,f,o,m,p,h,l,L,u,U}` filter types, `psconvert -T{b,e,f,j,g,G,m,s,t,F}` output formats, `blockmean -S{m,n,s,w}` statistic, `makecpt -F{R,r,h,c,gray}` CPT format, `grdcut -E{x,y}` 3-D cube slice axis, `grdblend -C{f,l,o,u}` clobber mode, `gmtwhich -G{a,c,l,u}` download destination, `sample1d -F{l,a,c,n,L,A,C,N}` interpolation, `fitcircle -L{1,2}` L1/L2 norm, `grdvolume -S{e,f,k,M,n,u}` distance units, `gmt2kml -F{e,l,p,s,t,w}` feature types, `kml2gmt -F{s,l,p}` feature filter, `sphinterpolate -Q{g,l,p,s}` tension method, `gmtbinstats -T{h,r}` hex/rect tiling, `gmtregress -E{x,y,o,r}` regression direction × `-N{1,2,r,w}` norm) harvested from https://docs.generic-mapping-tools.org/latest/*.html
- **OSRM** (Open Source Routing Machine) — `osrm-extract`, `osrm-contract`, `osrm-customize`, `osrm-partition`, `osrm-routed`; full pipeline from `.osm.pbf` ingest → contraction-hierarchies (CH) or multi-level-Dijkstra (MLD) preprocessing → HTTP routing daemon. Boost program_options long-flag tables read straight from `Project-OSRM/osrm-backend src/tools/{extract,contract,customize,partition,routed}.cpp`. Notable enums: `osrm-routed --algorithm={CH,MLD}` + the dataset-name / mmap / shared-memory loading modes.
- **Valhalla** (Open Source Routing Machine sibling) — `valhalla_service` (HTTP / direct-request CLI with route/locate/optimized_route/isochrone/trace_route/trace_attributes/height/transit_available/expansion/centroid/status action enum), `valhalla_build_tiles` (graph-build pipeline with 12-stage `-s/-e` start/end selectors initialize→cleanup), `valhalla_build_admins` (country/state lookup DB), `valhalla_export_edges` (-x column / -r row separators + -f ferries / -u unnamed flags), `valhalla_expand_bounding_box` (-b min_x,min_y,max_x,max_y bbox expander), `valhalla_add_elevation` (SRTM elevation for selected tiles), `valhalla_add_landmarks`, `valhalla_build_landmarks` (landmark POI DB from PBF), `valhalla_build_statistics`, `valhalla_add_predicted_traffic` (-s traffic-coverage summary + -t traffic-tile-dir), `valhalla_assign_speeds`, `valhalla_build_connectivity`, `valhalla_benchmark_admins`, `valhalla_loki_worker`+`valhalla_thor_worker`+`valhalla_odin_worker` (Loki/Thor/Odin HTTP worker processes — minimal `config.json` positional, no cxxopts). cxxopts long-flag tables read from `valhalla/valhalla src/{,mjolnir/}valhalla_*.cc`.
- **Gentoo portage-utils** (q-tools) — `qlist`, `qsearch`, `qfile`, `qdepends`, `qsize`, `qatom`, `qcheck`, `qgrep`, `qkeyword`, `qlop`, `qmerge`, `qpkg`, `quse`, `qmanifest`, `qxpak` (alongside existing `emerge`, `equery`, `eix`, `eselect`, `layman`); per-tool `qX_long_opts[]`/`qX_opts_help[]` static arrays read from each binary's source at `gentoo/portage-utils q*.c`. Notable: `qdepends -d/-r/-p/-b/-i` exposes the full DEPEND/RDEPEND/PDEPEND/BDEPEND/IDEPEND dependency atoms; `qsize -m/-k/-b` is modelled as a mutually-exclusive unit group; `qkeyword` exposes the `imlate`/`dropped`/`needsstable`/`all`/`not`/`stable`/`testing` arch-keyword filters that drive Gentoo stable-team workflows; `qlop` parses `emerge.log` for merge-time stats (`--summary`/`--time`/`--average`/`--predict`/`--running` modes); `qmerge -f/-F/-s/-K/-U/-p/-k/-u/-y/-O` covers the binary-package install/uninstall path with `--keepwork` tempdir preservation and `--nodeps` skip; `qpkg -c/-E` clean modes plus `-g/-x` gpkg/tbz2 format override for the binhost; `quse -L/-D/-I/-R` queries USE flags or LICENSE with describe/installed/repo filters and `-F` format string; `qmanifest -g/-s/-p/-d/-o` generates thick Manifests with GPG signing and overlay scoping; `qxpak -l/-x/-c/-O` is the tar-style read/write driver for Gentoo's XPAK metadata archives.
- **The Sleuth Kit** (TSK / digital forensics) — `mmls` (volume-system / partition-table dumper with `-t {dos,mac,bsd,sun,gpt}` media-management types and `-a/-A/-m/-M` allocated/unallocated/metadata-volume filters), `fls` (file/directory name lister with `-d/-u/-D/-F/-l/-p/-r/-a` deleted-vs-undeleted × dir-vs-file × long/path/recursive modes plus `-m` mactime timeline format and `-z` timezone skew), `icat` (inode→content extractor with `-h/-r/-s` sparse-hole-skip/recover-deleted/include-slack), `fsstat` (filesystem statistics with `-t` type-only short-circuit), `blkls` (data-unit / unallocated-space extractor with `-e/-a/-A/-l/-s` every-block/allocated/unallocated/timemachine/slack-only modes), `ils` (inode lister with `-e/-O/-p/-r/-m` every/open-removed/orphan/removed/mactime × `-a/-A` allocated/unallocated × `-l/-L` linked/unlinked × `-z/-Z` likely-unused/likely-used 6-axis filter cube), `istat` (inode-detail dumper with `-B num` disk-unit address expansion and `-z` timezone), `blkcat` (data-unit content viewer with mutually-exclusive `-a/-h/-w` ASCII/hexdump/HTML modes + `-u` raw/blkls/swap unit-size override + `-s` unit-statistics), `mactime` (ASCII-timeline generator over `fls -m`/`ils -m` body files with `-b/-g/-p/-i` body/group/passwd/index file paths, `-d/-h/-m/-y` comma-delim/header/numeric-month/ISO-8601 formatting, and `-z list` zone enumeration), `img_stat` (image-file metadata dumper with `-t` type-only mode); shared `-f {ext2,ext3,ext4,fat,fat12,fat16,fat32,ntfs,ufs,ufs1,ufs2,iso9660,hfs,ext,yaffs2,list}` fstype × `-i {raw,aff,afd,afm,afflib,ewf,split}` imgtype × `-o`/`-b` sector-offset/sector-size disk-geometry quartet across every tool, `blkstat` (data-unit detail dumper), `blkcalc` (cross-image data-unit address converter with mutually-exclusive `-d/-s/-u` regular↔slack↔unallocated mapping for chaining `blkls` output back to original image offsets), `ifind` (data-unit/filename→inode reverse lookup with mutually-exclusive `-d data_unit`/`-n file`/`-p par_inode` source modes + `-a` all-occurrences + `-l` long-list), `sigfind` (binary-signature scanner over block-aligned offsets with `-t {dospart,ext2,ext3,ext4,fat,fat12,fat16,fat32,hfs+,hfsj,iso9660,ntfs,reiserfs,ufs1,ufs2,jffs2}` template presets and `-l` little-endian byte-swap), `jls` (filesystem journal block lister for ext3/ext4/NTFS/HFS+); `jcat` (specific-journal-block content dumper, pairs with `jls`), `hfind` (hash-database lookup with `-i {nsrl-md5,nsrl-sha1,md5sum,hk,encase,idxonly}` index-creation modes for NSRL/HashKeeper/EnCase databases, plus `-f lookup_file` batch mode and `-q` quick 0/1 boolean output), `img_cat` (raw image-content streamer with `-s/-e` sector range), `tsk_recover` (image→local-directory file exporter with mutually-exclusive `-a/-e` allocated-only/all-files modes plus `-d dir_inum` subtree-only recovery), `mmstat` (volume-system metadata dumper sharing `mmls`'s `-t {dos,mac,bsd,sun,gpt}` mmtype enum); `sorter` (file-type classification pipeline with `-h` HTML category index, `-e/-E` extension-mismatch flagging, `-md5/-sha1` per-file hashing, and a 4-database hash-lookup quartet `-a` known-bad / `-x` known-good / `-n` NSRL / `-c`/`-C` additive-vs-only-config), `tsk_loaddb` (Autopsy SQLite-database populator with `-a` append-to-existing, `-k` skip-block-data-table, `-h` per-file MD5 hashing), `tsk_gettimes` (mactime body-file producer with `-s`/`-z` skew+timezone normalization for direct piping into `mactime`), `tsk_comparedir` (image-vs-local-directory diff with `-n inum` subtree start), `usnjls` (NTFS USN `$UsnJrnl:$J` change-journal lister with mutually-exclusive `-l` long-format / `-m` mactime body output for timeline integration); harvested from `sleuthkit/sleuthkit man/{mmls,fls,icat,fsstat,blkls,ils,istat,blkcat,mactime,img_stat,blkstat,blkcalc,ifind,sigfind,jls,jcat,hfind,img_cat,tsk_recover,mmstat,sorter,tsk_loaddb,tsk_gettimes,tsk_comparedir,usnjls}.1` manpages.
- **libewf** (Expert Witness Format / EnCase E01/Ex01 image manipulation) — `ewfacquire` (interactive disk-to-EWF acquirer with `-f {ewf,smart,ftk,encase1..7,encase7-v2,linen5..7,ewfx}` 15-format target enum, `-A` 15-codepage header table for `windows-874..1258` East-Asian/Cyrillic/Greek/Turkish/Hebrew/Arabic/Baltic/Vietnamese case-file localization, `-b {16,32,…,32768}` 12-step chunk-sector size, `-d {sha1,sha256}` extra-digest, `-c level-or-method:level` deflate compression, `-m {fixed,removable,optical,memory}` media type × `-M {logical,physical}` flags, `-T` CUE-format optical TOC, `-r` retries + `-R` resume + `-w` zero-on-error EnCase-mimic, plus case/examiner/evidence/notes/description metadata fields and `-2` secondary target for redundant write), `ewfacquirestream` (stdin-pipe acquirer with same format/codepage/metadata surface minus `-R`/`-T`/`-r`/`-u`/`-w`/`-g`), `ewfexport` (EWF→raw/files/another-format converter with mutually-exclusive 17-target enum including `-t -` stdout sink), `ewfinfo` (image-metadata dumper with mutually-exclusive `-e/-i/-m` error/acquisition/media info filters, `-f {text,dfxml}` output format, `-d {ctime,dm,md,iso8601}` date format, `-B` bodyfile mode, `-H` logical hierarchy, `-F path` per-file lookup), `ewfverify` (stored-hash integrity verifier with `-d {md5,sha1,sha256}` digest selector); harvested from `libyal/libewf manuals/ewf*.1`. Round 67 adds `ewfmount` (FUSE-mount EWF as a raw block device, with `-f {raw,files}` exposure mode and `-X` extended FUSE pass-through) and `ewfrecover` (broken-image data recovery with same `-A` 15-codepage table plus `-l` log + `-p` buffer + `-t` target sink + `-x` chunk-IO toggle).
- **libyal libvshadow** (Windows Volume Shadow Copy / VSS forensics) — `vshadowinfo` (VSS-store metadata dumper with `-a` allocation-info expansion and `-o` byte offset into the host volume) and `vshadowmount` (FUSE-driver exposing each shadow copy as a separate raw block file under the mountpoint, with `-o` volume offset and `-X` extended FUSE pass-through); harvested from `libyal/libvshadow manuals/vshadow{info,mount}.1`.
- **TestDisk suite — fidentify** (cgsecurity, PhotoRec signature engine driver) — single-pass file-format identifier with `--check` PhotoRec-equivalent validation and `--version`; harvested from `cgsecurity/testdisk man/fidentify.8.in` (companion to existing `testdisk`/`photorec` completions).
- **libyal disk-encryption / filesystem-info family** (Joachim Metz forensic readers, shared `-o offset` × `-p password` × `-u unattended` × `-v/-V` skeleton) — `bdeinfo` (BitLocker Drive Encryption with `-k FVEK:TWEAK` base16 full-volume-encryption-key + tweak pair, `-r` recovery password, `-s startup.BEK` USB-startup-key file, `-u` unattended for forensics scripting), `fvdeinfo` (macOS FileVault Drive Encryption with `-e EncryptedRoot.plist.wipekey` per-machine plist key path, `-k` volume master key base16, `-r` recovery password), `luksdeinfo` (Linux Unified Key Setup with `-k` master-key base16 + `-p` password), `fsapfsinfo` (Apple File System reader with `-B` bodyfile sink + `-d` per-entry MD5 + `-E entry`/`-f fsid`/`-F path`/`-H` 4-axis hierarchy-traversal filter, plus `-p`/`-r` per-volume password+recovery for encrypted APFS), `vslvminfo` (Linux Logical Volume Manager VG/PV/LV info); harvested from `libyal/{libbde,libfvde,libluksde,libfsapfs,libvslvm} manuals/*.1`. Round 72 adds the matching FUSE-mount drivers `bdemount`/`fvdemount`/`luksdemount` (same key/password/recovery-password/offset surface as their `*info` siblings, with shared `-X` extended FUSE pass-through), plus `qcowinfo` (QEMU QCOW v1/v2 copy-on-write disk-image inspector) and `modiinfo` (Apple Mac OS Disk Image inspector covering sparseimage / sparsebundle / UDIF `.dmg`); harvested from `libyal/{libbde,libfvde,libluksde,libqcow,libmodi} manuals/*.1`.
- **libyal filesystem readers** (Joachim Metz read-only forensic FS parsers — shared `-B bodyfile` × `-d` per-entry-MD5 × `-E entry` × `-F path` × `-H` hierarchy × `-o offset` × `-v/-V` skeleton matching the TSK `mactime`-pipeline body-file convention) — `fsntfsinfo` (NTFS reader with the unique `-U` `$UsnJrnl` USN-change-journal dumper for timeline analysis), `fsextinfo` (ext2/ext3/ext4 with `-E` per-inode lookup), `fshfsinfo` (HFS/HFS+/HFSX with `-f` per-file-system selector for multi-volume images), `fsxfsinfo` (XFS), `fsfatinfo` (FAT12/FAT16/FAT32/exFAT — no `-o` since FAT volumes are typically full-image); harvested from `libyal/{libfsntfs,libfsext,libfshfs,libfsxfs,libfsfat} manuals/fs*info.1`. Round 73 supplements with `fsrefsinfo` (Windows ReFS — Resilient File System used on Storage Spaces and Hyper-V volumes — with `-a` allocation expansion).
- **libyal disk-image / virtualization-format inspectors** — `odrawinfo` (Optical Disc Raw inspector for CUE/TOC sheet + `.bin`/`.img` track pair, with `-i` ignore-data-files mode for sheet-only metadata listing) and `phdiinfo` (Parallels Hard Disk Image `.hdd`/`.hds` inspector — completes the VM-disk format quintet alongside libvmdk/libvhdi/libqcow/libmodi); harvested from `libyal/{libodraw,libphdi} manuals/{odraw,phdi}info.1`.
- **libyal Windows-internals inspectors** — `wrcinfo` (PE `.rsrc` Windows Resource Compiler section reader — extracts icons, cursors, message tables, version info, dialog templates — with `-H` resource-hierarchy view and the standard 15-codepage `-c` ring) and `agdbinfo` (Application Compatibility / Shim Database `.sdb` reader — exposes the AppCompat shim flags that NTUSER.DAT's `AppCompatFlags` key references); harvested from `libyal/{libwrc,libagdb} manuals/{wrc,agdb}info.1`.
- **libyal database / device / scanner family** — `esedbinfo` (Extensible Storage Engine `.edb` inspector for Windows Search `Windows.edb`, Exchange `Mailbox.edb`, AD `ntds.dit`, SRUM `SRUDB.dat`), `esedbexport` (ESE table exporter with `-m {all,tables}` mode + `-T table` single-table extraction + `-t basename.export` target-directory naming — flag table read from `esedbtools/esedbexport.c` getopt `c:hl:m:t:T:vV` since no manpage ships), `smdevinfo` (Storage Media Device inspector for raw `/dev/sd*`/`.img` block devices with `-i` ignore-data mode), `creginfo` (Windows 9x/Me Compressed Registry reader for `USER.DAT`/`SYSTEM.DAT` with the standard `-c` 15-codepage ring + `-H` hierarchy view — pre-NT registry sibling of `regfinfo`), `sigscan` (libsigscan binary-signature CLI scanner — driver for the pattern-matching engine that PhotoRec/fidentify share); harvested from `libyal/{libesedb,libsmdev,libcreg,libsigscan} manuals/*.1` plus `libyal/libesedb esedbtools/esedbexport.c`.
- **KLayout** (Matthias Köfferlein, IC physical layout editor + headless batch suite) — `klayout` main editor with `-z`/`-zz` non-GUI/headless mode for CI workflows, `-b` batch (`-zz -nc -rx`), `-r`/`-rr`/`-rm` Ruby/Python script entry points with `-rd var=value` variable injection, `-l layer.lyp` properties file + `-n tech`/`-nn .lyt` per-layout technology binding, `-m rdb`/`-mn l2n` report-database / LVS-database overlays, `-gr`/`-gp`/`-gb`/`-gx`/`-gi` GUI-action record/replay/stop/rate/incremental harness for regression tests, plus `-c`/`-nc`/`-t`/`-nt` config-file life-cycle modes; plus the strm* converter/comparator buddies built on the shared `bdConverterMain.cc` + `bdReaderOptions.cc` + `bdWriterOptions.cc` triplet — `strm2gds`/`strm2oas`/`strm2cif`/`strm2dxf`/`strm2txt`/`strm2gdstxt`/`strm2lstr`/`strm2mag` 8 format-output binaries share one compdef exposing the full reader-options surface (layer mapping, GDS2 multi-XY/big-records, OASIS strict-mode, CIF wire mode, DXF unit/text/polyline/circle/contour 6-axis tuning, Magic lambda/library-path) and writer-options surface (scale factor / DBU / GDS2 max-vertex-count + cellname-length + zero-length-path conversion + timestamps, OASIS compression-level + CBLOCK + strict-mode, DXF polygon mode, CIF dummy-calls); plus `strmcmp` (structural layout comparator with `-t` tolerance + `-m` max-diff-count + 4-axis `-np`/`-nl`/`--boxes-as-polygons`/`--paths-as-polygons` normalization toggles + `-c` property-based cell mapping), `strmxor` (XOR geometric difference with `-u` deep hierarchical mode + `-n` thread count + `-p` tile size + `-m` tile-edge healing + `-b` per-tolerance layer-number offset), `strmclip` (rectangle/layer-based layout clipping with `-r "l,b,r,t"` clip box and `-l` clip-layer source); harvested from `klayout.de/command_args.html` (main binary) and `KLayout/klayout src/buddies/src/bd/{strmcmp,strmxor,strmclip,bdConverterMain,bdReaderOptions,bdWriterOptions}.cc` (helpers).
- **IceStorm** (Clifford Wolf / YosysHQ — iCE40 FPGA reverse-engineered bitstream toolchain, alongside existing `icepack`/`icebram`/`iceprog`/`icetime`/`icemulti`/`iceunpack` and the nextpnr/yosys synthesis pair) — `icebox_explain` (config-bit explainer with `-b` bit-name printing × `-m` per-tile bitmap × `-A` include-uninteresting-tiles × `-t "x y"` single-tile drill-down), `icebox_html` (per-tile HTML render with `-x`/`-y` tile selector + `-d outdir` and mutually-exclusive `-8`/`-3` HX8K/LP384 chip-family mode), `icebox_vlog` (bitstream→synthesizable-Verilog reverse converter with strip-comments toggle pair `-s`/`-S`, mutually-exclusive `-l`/`-p pcf`/`-P pcf` IO-naming triplet — IO-tile-names / pcf-derived / iCEcube2-pcf-with-hacks — plus `-L` `.sym`-based symbol lookup, `-n module-name`, `-d package` chip-pin override, `-c` multi-bit-port collection, `-R` IeRen-DB checks, `-D` exactly-one-driver checks), `icebox_hlc2asc` and `icebox_asc2hlc` (the high-level-config ↔ bitstream round-trip pair, with the `asc2hlc` direction adding `-m`/`-A` for tile bitmap dumping like `icebox_explain`); harvested from `YosysHQ/icestorm icebox/{icebox_explain,icebox_html,icebox_vlog,icebox_hlc2asc,icebox_asc2hlc}.py` getopt loops.
- **Verible** (Google / CHIPS Alliance SystemVerilog parser & tool suite — Abseil-flags-based, alongside existing `verible-verilog-format`/`verible-verilog-lint`/`verible-verilog-syntax`) — `verible-verilog-preprocessor` (3-subcommand `preprocess`/`strip-comments`/`generate-variants` SV preprocessor with `--limit_variants N` for `ifdef enumeration), `verible-verilog-obfuscate` (identifier obfuscator with `--save_map`/`--load_map` round-trippable translation dictionary + `--decode` reverse-direction de-obfuscation + `--preserve_interface` module/port/parameter identity-map + `--preserve_builtin_functions` `sin()`/`ceil()` exemption), `verible-verilog-project` (file-dependency / symbol-reference analyzer with `--file_list_path`/`--file_list_root` build-order spec + comma-separated `--include_dir_paths` search-order ring + `--output_format {human,tsort,dot}` 3-way emission for tsort piping or Graphviz visualization), `verible-verilog-diff` (SV-aware diff with `--mode {format,obfuscate}` for verifying formatter or obfuscator output — `format` ignores whitespace, `obfuscate` preserves whitespace but compares token-text lengths only), `verible-patch-tool` (unified-diff helper with `changed-lines`/`apply-pick`/`stdin-test`/`cat-test` 4 subcommands — `changed-lines` extracts file+line-range pairs from a patch for piping into `verible-verilog-format --lines`); all share the standard Abseil `--flagfile`/`--fromenv`/`--tryfromenv`/`--undefok` flag-loading skeleton; harvested from `chipsalliance/verible verible/{verilog/tools/{preprocessor,obfuscator,project,diff},common/tools/patch-tool}.cc` ABSL_FLAG and SubcommandRegistry tables.
- **Computational-geometry mesh generators / remeshers** — `tetgen` (Hang Si / WIAS-Berlin — 3-D Delaunay tetrahedral mesh generator with `-p` PLC tetrahedralization + `-q` quality refine × `-a` volume cap × `-O` optimization level × `-S` Steiner cap, plus output emitter quintet `-f`/`-e`/`-n`/`-v`/`-g`/`-k` for `.face`/`.edge`/`.neigh`/Voronoi/`.mesh`(Medit)/`.vtk`(Paraview), and output-suppression sextet `-B`/`-N`/`-E`/`-F`/`-I`/`-J`; `-X` exact-arithmetic disable + `-M` no-merge for debugging Delaunay numerics), `triangle` (Jonathan Shewchuk — 2-D quality mesher with mutually-exclusive Delaunay-algorithm triplet `-i`/`-F`/`-l` incremental/sweepline/vertical-cuts-D&C, `-q` 20°-min quality + `-a` area cap, `-Y` boundary-Steiner prohibition, `-o2` second-order 6-node element generation, and a 7-flag output-suppression set), `mmg3d`/`mmg2d`/`mmgs` (MmgTools shared compdef family covering each `_O3`/`_O3d`/`_debug` build variant — anisotropic adaptive remesher with `-hausd` Hausdorff-distance × `-hgrad` gradation × `-hmin`/`-hmax`/`-hsiz` size-box, `-A` anisotropy without metric file, `-noinsert`/`-nomove`/`-noswap` operation-disable triplet, `-lag {0,1,2}` Lagrangian-displacement mode for FSI workflows, `-ls`/`-lssurf` isovalue mesh / boundary split, `-rmc val` small-volume component removal; mmg3d-only `-octree val` + `-optimLES` LES skewness; mmg2d-only `-3dMedit {1,2,3}` Gmsh visualization IO; mmgs-only `-keep-ref` level-set domain-reference preservation; SCOTCH-enabled builds add `-rn {0,1}` renumbering toggle on all three); harvested from `wias-berlin.de/software/tetgen/1.5/doc/manual/manual005.html`, `cs.cmu.edu/~quake/triangle.switch.html`, and `MmgTools/mmg src/{mmg2d,mmg3d,mmgs}/libmmg*_tools.c` + `src/common/libtools.c` shared `MMG5_mmgUsage`/`MMG5_paramUsage{1,2}`/`MMG5_2d3dUsage`/`MMG5_lagUsage`/`MMG5_advancedUsage` prints.
- **OpenFOAM helper scripts** (CFD case-management Bash harness, alongside existing `blockMesh`/`snappyHexMesh`/`icoFoam`/`simpleFoam`/`paraFoam`/`foamLog`/`foamJob`/`decomposePar`/`reconstructPar`/`foamCleanCase`) — `foamMonitor` (live gnuplot of an OpenFOAM data file with `-ascii` ASCII-in-terminal × `-logscale` log-y residual mode × `-flip` axis-swap × `-once` snapshot × `-refresh N` heartbeat × `-columns 2,4` per-column slicing + `-yrange "[0:1]"` + `-size 640,480` + `-title`), `foamSearch` (multi-dictionary grep with `-count` occurrence-prefix), `foamCloneCase` (case-copy with `-latestTime` / `-no-orig` / `-no-scripts` / `-processor` decomposed-case mode / `-startFrom time` time-window crop / `-template` template-search-path mode / `-add file...` extra-payload list), `foamCreateVideo` (PNG-sequence → MP4/WebM ffmpeg encoder with `-begin-pause`/`-end-pause` head/tail-padding × `-fps` × `-start` first-frame × `-image name` sequence-prefix × `-out` sink × `-webm` codec switch); harvested from `OpenFOAM/OpenFOAM-dev bin/{foamMonitor,foamSearch,foamCloneCase,foamCreateVideo}` `usage()` Bash functions. Round 80 extends with `foamEtcFile` (etc-file locator with `-all` enumerate-all-matches × `-list` searched-dirs dump × `-mode {u,g,o,ug,go,…}` user/group/other layer filter × `-prefix dir` alt-install-prefix × `-version Maj.Min.Rev` multi-version coexistence × `-quiet`/`-silent` stdout/stderr suppression pair), `foamExec` (environment-scoped command runner that delegates `-prefix`/`-version` to `foamEtcFile` then `exec`s under the resolved `FOAM_INST_DIR`, enabling per-version smoke-tests), `foamMergeCase` (3-way base+child case overlay for derived-case workflows), `foamRunTutorials` (recursive `Allrun` walker with `-skipFirst` to avoid double-running the root case and `-test` regression-loop mode), `foamSequenceVTKFiles` (ParaView-friendly VTK time-series symlink re-numberer with `-dir postProcessing` source × `-out sequencedVTK` sink × `-case` per-case override); harvested from `OpenFOAM/OpenFOAM-dev bin/{foamEtcFile,foamExec,foamMergeCase,foamRunTutorials,foamSequenceVTKFiles}` `usage()` Bash functions. Also `foamCleanPath` (PATH-style variable deduplicator / blacklist filter — used internally by `WM_PROJECT/etc/config.sh/*` to keep `PATH`/`LD_LIBRARY_PATH` clean across version swaps), `foamGet` (`$FOAM_ETC/caseDicts/`→case dictionary copier with `-ext`/`-no-ext` extension toggle, `-target system` destination override, `-region` multi-region mesh routing), `foamInfo` (class/model/BC tutorial-usage + C++ source-guide lookup with `-keyword` substring mode, `-all` lift the 10-tutorial/25-model caps, `-browser`/`-web` HTML doc opener), `foamNew` (boilerplate-generator dispatcher with `source`/`template` subcommands routing to `foamNewSource`/`foamNewTemplate` and `{C,H,I,IO}` C++ file-type axis), `foamTags` (etags TAGS builder across the OpenFOAM source tree); harvested from `OpenFOAM/OpenFOAM-dev bin/{foamCleanPath,foamGet,foamInfo,foamNew,foamTags}` `usage()` Bash functions. Plus `foamFind` (source-tree locator/grep with mutually-exclusive `-a`/`-m`/`-t`/`-d` `$FOAM_APP`/`$FOAM_MODULES`/`$FOAM_TUTORIALS`/explicit-dir search-root selector, `-s`/`-i` case-sensitive/insensitive content grep, `-l` `lnInclude` discovery emitting wmake `-I`/`-l` args, `-f`/`-o` find associated wmake `files`/`options`, `-c` `codeInclude`/`codeOptions`/`codeLibs` block-injection into a coded input, `-e` editor open, `-n`/`-p` line-numbers/print sinks), `foamNewApp` (application skeleton with `-plain` non-CFD-app variant), `foamNewBC` (custom boundary-condition generator with mutually-exclusive `-f`/`-m` `fixedValue`/`mixed` base × mutually-exclusive `-s`/`-v`/`-t`/`-symmTensor`/`-sphericalTensor`/`-a` 6-way field-type axis covering scalar/vector/tensor/symm-tensor/spherical-tensor/template), `foamNewFunctionObject` (runtime `functionObject` plugin skeleton), `foamUnMergeCase` (reverse of `foamMergeCase` separating child overlay from base case); harvested from `OpenFOAM/OpenFOAM-dev bin/{foamFind,foamNewApp,foamNewBC,foamNewFunctionObject,foamUnMergeCase}` `usage()` Bash functions. Plus `foamVTKSeries` (ParaView `.vtk.series` JSON-descriptor emitter for VTK time-series) and the `bin/tools/` build-system layer: `foamConfigurePaths` (relocatable-install patcher with `--foamInstall` prefix × `--projectName` × `--projectVersion` × `--thirdPartyName` × `--archOption {32,64}` × repeatable `--dependency name:type:ver` for ThirdParty pinning), `foamVersionCompare` (3-arg dotted-version comparator with `eq`/`lt`/`gt`/`le`/`ge` operator subcommand returning the relation via exit code — usable in `if foamVersionCompare 11.0.1 ge 11; then …`), `foamGenerateBashCompletion` (Bash-completion emitter for OpenFOAM utilities, with `-l` local-file mode for per-checkout regeneration), `foamListBinDirs`/`foamListThirdPartyBinDirs` (shared compdef — list per-`$WM_OPTIONS`-arch `bin`/`lib` directories under a pack root, used by the release-tarball builder); harvested from `OpenFOAM/OpenFOAM-dev bin/foamVTKSeries` and `bin/tools/{foamConfigurePaths,foamVersionCompare,foamGenerateBashCompletion,foamListBinDirs,foamListThirdPartyBinDirs}` `usage()` Bash functions.
- **JACK Audio Connection Kit** (jackaudio/jack2 low-latency audio server, alongside existing `jack_lsp`/`jack_metro`/`jack_transport`/`jack_rec`/`jack_session_notify`/`jack_bufsize`/`jack_evmon`/`jack_freewheel`/`jack_iodelay`/`jack_unload`/`jack_midiseq`/`jack_midisine`/`jack_monitor_client`/`jack_samplerate`/`jack_server_control`/`jack_showtime`/`jack_simple_client`/`jack_simple_session_client`/`jack_zombie`) — `jackd` (main server daemon with `-d {alsa,coreaudio,dummy,net,netone,oss,sun,portaudio,firewire}` 9-backend selector + mutually-exclusive `-R`/`-r` realtime-scheduling pair × `-P` priority × `-T` temporary-mode × `-Z` no-zombies safety × `-t` client-timeout × `-c {h,s}` HPET/system wall-clock × `-C internal-session-file` internal-client preload × `--replace-registry` shmem recovery), `jack_connect`/`jack_disconnect` (shared compdef — port connect/disconnect pair with `-s` server name), `jack_load` (in-process client loader with `-i` init-string + `-w` signal-wait-then-unload), `jack_property` (port/client metadata table editor with mutually-exclusive `-l`/`-d`/`-D`/`-s` list/delete-one/delete-all/set + mutually-exclusive `-c`/`-p` client-name/port-name identifier-interpretation override), `jack_wait` (server lifecycle blocker with mutually-exclusive `-w`/`-q`/`-c` wait-for-up/wait-for-down/check-once verbs and `-t` timeout for scripted startup); harvested from `manpages.debian.org/jackd.1` and `jackaudio/jack2 man/{jack_connect,jack_disconnect,jack_load,jack_property,jack_wait}.0`. Plus `jack_netsource` (NetJack1 audio-network source client with audio-in/out × MIDI-in/out 4-channel-count quartet `-i`/`-o`/`-I`/`-O`, `-p`/`-r`/`-B` UDP slave-listen / reply / NAT-bind port triplet, `-b {8,16}` bit-depth × `-c bytes` CELT lossy-encoder × `-m mtu` link tuning × `-R N` packet-redundancy multiplier × `-e` endianness-conversion skip), `jack_midi_dump` (human-readable MIDI-event dumper with mutually-exclusive `-a`/`-r` absolute-vs-relative-to-prev-event timestamp mode), `jack_alias` (port-alias editor with `-u`/`--unalias` removal mode), `jack_load_test` (client-load CPU stress harness with `-t usec` per-iteration timeout), `jack_tw` (thread-wait demo client testing the JACK `wait()`/`wake()` round-trip — positional client + server name only); harvested from `jackaudio/jack2 man/jack_netsource.0` and `jackaudio/jack-example-tools tools/{midi_dump,alias,load_test,tw}.c` `usage()` functions plus `tools/meson.build` binary-name mappings.
- **PipeWire** (Wim Taymans / RedHat — successor to PulseAudio/JACK for audio + video on Linux) — `pw-cli` (introspector + remote-instance manipulator with 21 subcommands `help`/`quit`/`list-vars` + module load/unload + `list-objects`/`info` + remote-instance lifecycle quartet `connect`/`disconnect`/`list-remotes`/`switch-remote` + factory-driven `create-node`/`create-device`/`create-link`/`export-node` + `destroy` global + `enum-params`/`set-param` + `permissions`/`get-permissions` + `send-command`), `pw-link` (port-graph editor with mutually-exclusive list-mode quartet `-o`/`-i`/`-l`/`-t` output/input/links/latency, `-m` monitor stream, `-I` ID-mode, `-v` verbose props, `-L` linger / `-P` passive link-policy pair, `-p JSON` extra properties, `-d` disconnect-pair mode), `pw-cat` (shared compdef for `pw-cat`/`pw-play`/`pw-record`/`pw-midiplay`/`pw-midirecord`/`pw-dsdplay` argv[0]-dispatched binary — file-IO with mutually-exclusive 6-way `-p`/`-r`/`-m`/`-c`/`-d`/`-s` playback/record/MIDI/MIDI2-clip/DSD/SysEx mode plus `--media-{type,category,role}` PulseAudio-compat property triplet, `--rate`/`--channels`/`--channel-map` PCM-spec triplet with introspection siblings `--list-{layouts,channel-names,formats,containers}`, `-q` resampler quality, `-M {midi,ump}` force-MIDI-format, `-n COUNT` sample-stop, `-a` raw mode, `--volume`), `pw-mon` (graph-event monitor with `-N {never,always,auto}` color-when ring), `pw-top` (per-node DSP-load top with `-b` batch mode + `-n` iteration cap); harvested from `docs.pipewire.org/page_man_pw-{cli,link,cat,mon,top}_1.html`. Plus `pw-dot` (Graphviz `.dot` graph emitter with `-a` all-object-types × `-s` linked-only × `-d` per-object-properties detail mode triplet, `-o file`/`-` sink/stdout, `-L` left-to-right vs default top-to-bottom + `-9` 90-degree-edge layout switches matching `dot`'s `rankdir`/`splines=ortho`), `pw-loopback` (virtual capture→playback mirror node with `-n` node-name × `-g` group-name × `-c`/`-m channel-map` channel topology × `-l ms`/`-d sec` latency+extra-delay × `-C`/`-P` capture/playback target selectors × `-i`/`-o` per-end JSON-properties pair — used for system-wide monitoring/recording loopbacks), `pw-mididump` (text-format MIDI-event dumper from any port), `pw-profiler` (daemon-side profiler-sample collector with `-o` log sink for offline `pw-profile-` analysis), `pw-dump` (full daemon-state JSON dumper with `-m` change-monitor stream mode and mutually-exclusive `-N` no-color / `-C {never,always,auto}` color-when terminal-output pair); harvested from `docs.pipewire.org/page_man_pw-{dot,loopback,mididump,profiler,dump}_1.html`. Plus `pw-metadata` (key-value metadata CLI with `-l` list × `-m` change-monitor × `-d` delete-all-or-by-key-of-id × `-n metadata-name` table selector — the default table stores `default.audio.sink`/`default.audio.source`/`default.configured.audio.sink` that `wpctl set-default` writes to), `pw-config` (config-fragment inspector with `paths`/`list [SECTION]`/`merge SECTION` 3 subcommands and `-n`/`-p` config-name+prefix selectors, `-L` no-newline + `-r` recursive reformat + `-N`/`-C {never,always,auto}` color-policy pair), `pw-reserve` (D-Bus `org.freedesktop.ReserveDevice1` device-reservation client with `-r` force-release + `-a app-name`/`-p priority` reservation metadata + `-m` monitor-vs-reserve toggle).
- **PulseAudio cross-server CLI** (and PipeWire-Pulse compatibility layer, alongside existing `wpctl`) — `pactl` (control daemon with `-s server`/`-f {text,json}`/`-n client-name` global header + 35 verb subcommands covering daemon `stat`/`info`/`exit`/`subscribe` + module load/unload + sample-cache upload/play/remove triplet + `move-{sink-input,source-output}` stream relocation + `suspend-{sink,source}` toggle + per-card-profile and per-port-latency-offset configuration + the full `get`/`set-{default-{sink,source},sink-port,source-port,sink-volume,source-volume,sink-input-volume,source-output-volume,sink-mute,source-mute,sink-input-mute,source-output-mute,sink-formats}` quartet/quintet of getters/setters with `1/0/toggle` boolean state, plus `send-message` D-Bus-style RPC), `pacat` (shared compdef for `pacat`/`paplay`/`parec`/`parecord`/`pamon` argv[0]-dispatched audio-IO with mutually-exclusive `-r`/`-p` record/playback pair, `--rate {8000..192000}`/`--format {u8,alaw,ulaw,s16le..float32be}`/`--channels {1,2,4,5,6,8}`/`--channel-map` 4-axis PCM-spec, sink-native-conversion triplet `--fix-{format,rate,channels}`, `--no-remix`/`--no-remap` channel-policy pair, latency/process-time bytes-vs-msec specifier pair, repeatable `--property=key=value`, `--raw` vs `--file-format` encoded-format switch); harvested from `manpages.debian.org/{pactl,pacat}.1`. Plus `pacmd` (interactive `default.pa`-syntax live shell whose grammar is documented at `pulse-cli-syntax(5)` — keeps a long-running command session against the daemon), `pasuspender` (run-and-suspend wrapper that pauses the PA daemon for the duration of a non-PA-aware program — typical use is `pasuspender -- jackd …` to release ALSA), `padsp` (`LD_PRELOAD` OSS-emulation shim with mutually-exclusive disable triplet `-M`/`-S`/`-D` for `/dev/mixer`, `/dev/sndstat`, `/dev/dsp` + `-s`/`-n`/`-m` server / client-app / stream-name passthrough + `-d` debug), `pulseaudio` (the daemon itself with `--start`/`--kill`/`--check` lifecycle triplet, `--dump-{conf,modules,resample-methods}` introspection triplet, `--system` system-wide-vs-per-user toggle, `--realtime`/`--high-priority`/`--no-cpu-limit` scheduling triplet, `--exit-idle-time`/`--scache-idle-time` idle-timeout pair, `--log-{level,target,meta,time,backtrace}` 5-axis logging surface with `--log-target={auto,syslog,journal,stderr,file:/path,newfile:/path}`, `--resample-method` covering the full SRC + speex-float-{0..10} + speex-fixed-{0..10} + ffmpeg + auto/copy/peaks 30-method ring, repeatable `-L "MODULE ARGS"` plugin-load, `-F` startup-script, `-C` interactive command interpreter, `-n` skip-default-script), `wpexec` (WirePlumber Lua-script runner with positional `script.lua` + optional JSON args for invoking session-manager APIs from shell); harvested from `manpages.debian.org/{pacmd,pasuspender,padsp,pulseaudio}.1` and `PipeWire/wireplumber src/tools/wpexec.c`.
- **alsa-utils sequencer + MIDI** (kernel ALSA seq/rawmidi userland, alongside existing `alsamixer`) — `aseqdump` (sequencer-event monitor with `-u {0,1,2}` legacy/UMP-MIDI-1.0/UMP-MIDI-2.0 client-version + `-r` UMP-legacy auto-conversion suppression + mutually-exclusive `-R`/`-N`/`-P` raw-0-based / MIDI-1.0-normalized-1-based / percentage value-display triplet + `-p client:port,...` source-port list), `amidi` (RawMIDI hardware-port I/O with mutually-exclusive `-l`/`-L` device-list-vs-rawmidi-definition-dump pair + send/receive duet `-s file`/`-r file`/`-S "hex bytes"` for SysEx + `-d` hex-dump receive + `-t` idle-timeout + `-a` keep-Active-Sensing-FEh + `-c` keep-Clock-F8h + `-i ms` per-SysEx inter-message delay), `aconnect` (sequencer wiring CLI with `-d` disconnect + `-e` exclusive-port-pair mode + mutually-exclusive `-r`/`-t` real-time-queue / tick-queue timestamp-conversion pair + 4-axis listing `-i`/`-o`/`-a`/`-l` input-readable / output-writable / include-inactive / current-connection-status + `-x` remove-all-connections), `arecordmidi` (`.mid` file recorder with `-b bpm`/`-f fps {24,25,29.97,30}`/`-t ticks` SMF tempo/resolution triplet + `-s` per-channel-to-track split + `-m client:port` metronome target + `-i num:denom` time-signature), `aplaymidi` (`.mid` player with `-u {0,1,2}` MIDI version + `-d seconds` inter-file silence-gap); harvested from `manpages.debian.org/{aseqdump,amidi,aconnect,arecordmidi,aplaymidi}.1`. Plus `alsactl` (mixer/control-state save/restore with 12 subcommands `store`/`restore`/`nrestore`/`init`/`daemon`/`rdaemon`/`kill`/`monitor`/`info`/`clean`/`dump-state`/`dump-cfg` + mutually-exclusive `-l`/`-L` lock-vs-no-lock pair + `-F` force-best-effort vs `-P` pedantic-no-mismatch restore-strictness toggle + `-I` no-init-fallback safety + `-D` UCM-defaults / `-U` skip-UCM toggle + `-r`/`-R` runstate-path-and-remove pair + `-b` background + `-s` syslog + `-p` daemon-store-period + `-n` nice + `-c` sched-idle + `-e` PID-file), `alsaucm` (Use-Case-Manager CLI with 10 subcommands `open`/`reset`/`reload`/`listcards`/`list`/`list1`/`get`/`geti`/`set`/`help`/`quit` + mutually-exclusive `-i`/`-b` interactive-vs-batch-file mode + `-c card` per-card scoping + `-n` no-auto-open), `aseqnet` (TCP-bridge for sequencer events between hosts with `-p` port + auto-subscribe `-s`/`-d` read/write pair + `-n` MIDI client name), `alsaloop` (userland PCM software loopback with `-P`/`-C` playback/capture PCM-device pair × `-X`/`-Y` per-direction CTL-device pair × `-l frames`/`-t usec` latency-units pair × `-f {S16_LE,…,FLOAT_BE}`/`-c chans`/`-r hz` PCM-spec triplet × `-n`/`-A` resampler-policy pair with `samplerate-{sincbest,sincmedium,sincfastest,zeroorderhold,linear}` libsamplerate converter ring × `-S {none,simple,captshift,playshift,samplerate,auto}` capture-to-playback sync mode × `-T` thread-control × `-m`/`-O` mixer-redirection pair × `-U` xrun profiling), `alsabat` (Basic Audio Tester — loopback-and-FFT QA harness with `-D card` × `-P`/`-C` playback/capture PCM-device pair + `-F` target signal frequency + `--snr-db`/`--snr-pc` mutually-exclusive noise-detection threshold pair + `--file` playback WAV + `--saveplay` capture-sink WAV + `--local` self-test mode + `--standalone` non-distributed mode + `--roundtriplatency` round-trip-latency measurement); harvested from `manpages.debian.org/{alsactl,alsaucm,aseqnet,alsaloop,alsabat}.1`. Plus `amixer` (CLI mixer with 14 subcommands `info`/`scontrols`/`scontents`/`set`(=`sset`)/`get`(=`sget`)/`controls`/`contents`/`cset`/`cget`/`sevents`/`events`/`help` covering both simple-mixer and card-control namespaces, `-s` stdin-script mode, mutually-exclusive `-R`/`-M` raw-vs-mapped percentage-evaluation toggle, `-c card`/`-D device` target pair), `aplay`/`arecord` (shared compdef — argv[0]-dispatched player/recorder with `-t {voc,wav,raw,au}` container × ~40-format `-f` ring covering S8/U8/S16/U16/S24/U24/S32/U32 LE+BE pairs + FLOAT/FLOAT64 LE+BE + IEC958 + MU_LAW/A_LAW/IMA_ADPCM/MPEG/GSM/SPECIAL + S20/S18 3-byte packed variants × `-r {8000..192000}`/`-c chans` PCM-spec, `-d`/`-s` duration-seconds-vs-frames stop pair, `-F`/`-B`/`--period-size`/`--buffer-size` period+buffer time-vs-frames quartet, `-A`/`-R`/`-T` avail-min/start-delay/stop-delay scheduling triplet, `-M` mmap × `-N` non-blocking × `-V {stereo,mono}` VU-meter × `-I` per-channel-file split, `--disable-{resample,channels,format,softvol}` plugin-bypass quartet, `--max-file-time`+`--use-strftime` filename-rotation pair, `--test-position`/`--test-coef`/`--test-nowait` ring-buffer-position diagnostic triplet, `--dump-hw-params` introspection, `--process-id-file` signal-control PID, `--fatal-errors` recovery-disable), `speaker-test` (multichannel positional test with `-t {pink,st2095,sine,wav}` 4-mode signal selector + `-c` channels + `-f`/`-X` sine-frequency + force-out-of-30-8kHz-range toggle + `-b`/`-p`/`-P` buffer/period/nperiods triplet + `-s` single-channel shot + `-w`/`-W` WAV-file/dir source + `-m` channel-map), `iecset` (IEC958 / S/PDIF status-bit setter with `-D`/`-c` device-or-card scoping + `-n` IEC958-element-index for multi-output cards + `-x` AESx-byte dump + `-i` stdin-script mode), `axfer` (modern xfer split with `transfer`/`list`/`version`/`help` subcommand + `capture`/`playback` direction-axis on `transfer` + 4-flag compat shim `-l`/`--list-devices`/`-L`/`--list-pcms` for aplay-script drop-in replacement); harvested from `manpages.debian.org/{amixer,aplay,speaker-test,iecset,axfer}.1`.
- **ipset** (Jozsef Kadlecsik / netfilter — Linux kernel IP-set table CLI, the hash/bitmap-set companion to nftables/iptables) — 14 verb subcommands `create`/`add`/`del`/`test`/`destroy`/`list`/`save`/`restore`/`flush`/`rename`/`swap`/`help`/`version`/`-` (stdin) covering the set lifecycle, with `create TYPE` set-type axis covering the full `bitmap:{ip,ip-mac,port}`, `hash:{ip,mac,ip-mac,net,net-net,ip-port,net-port,ip-port-ip,ip-port-net,net-port-net,net-iface}`, and `list:set` 15-type ring; global flags `-!`/`-exist` duplicate-create-tolerance, `-o {plain,save,xml,json}` output-format ring, `-r` name-resolution-on-list, `-s` sorted-output, `-n` names-only, `-t` terse-without-members, `-f` file-vs-stdin redirect, `-q` quiet; harvested from `manpages.debian.org/ipset.8`.
- **macOS sysadmin toolchain** (alongside existing `codesign`/`pkgbuild`/`productbuild`/`installer`/`security`/`iconutil`/`defaults`/`notarytool`/`xcode-select`/`xcrun`/`altool`/`stapler`/`spctl`/`dsymutil`) — `pkgutil` (receipt-database + flat-package CLI with `--packages`/`--pkgs-plist` enumeration, `--files`/`--pkg-info`/`--export-plist`/`--pkg-groups` per-pkg introspection sextet, `--verify`/`--repair` repair_packages(8) delegation pair, `--forget` receipt-discard, `--learn pkg --edit-pkg` ACL-update flow, `--expand`/`--flatten` directory↔flat-pkg pair, `--bom`/`--payload-files`/`--check-signature` flat-pkg unpack triplet, `--regexp` pattern matching), `plutil` (property-list utility with mutually-exclusive `-p`/`-lint`/`-convert` print/lint/convert action triplet + `-convert {xml1,binary1,json,swift,objc}` 5-format ring + `-r` JSON pretty-print + `-o`/`-e` alternate sink/extension pair + `-s` silent-success), `qlmanage` (Quick Look server + per-file test with mutually-exclusive `-r`/`-m`/`-h`/`-t`/`-p` reset / info / help / thumbnail / preview verb axis + thumbnail-tuning quartet `-s size`/`-f factor`/`-c uti`/`-g generator`/`-d debug-level`), `softwareupdate` (system-update CLI with mutually-exclusive `-l`/`-i`/`-d` list / install / download verb triplet + `--ignore`/`--reset-ignored` per-machine ignored-list pair + `--schedule` automatic-check toggle + `-a` apply-to-all + `-R` auto-restart + `--fetch-full-installer`/`--full-installer-version` macOS-installer download pair + `--no-scan` cache-only + `--background` headless + `--agree-to-license` unattended), `networksetup` (Network preference pane CLI with 20+ get/set pairs covering service-listing trio `-list{networkserviceorder,allnetworkservices,allhardwareports}`, per-service IP-config triplet `-setmanual`/`-setdhcp`/`-getinfo`, DNS+search-domain getter/setter quartet, Wi-Fi/AirPort 4-pair `getairportnetwork`/`setairportnetwork`/`getairportpower`/`setairportpower`, 3-way proxy-config family `web`/`securewebproxy`/`ftpproxy`, computer-name + MAC introspection pair); harvested from `unix.com/man-page/osx/{1/{pkgutil,plutil,qlmanage},8/{softwareupdate,networksetup}}/`. Plus `scutil` (System Configuration / dynamic-store CLI with `--prefs` raw-preference editor + mutually-exclusive `-r host`/`-w key` reachability/dynamic-store-watch pair with `-W` change-stream and `-t` timeout + `--get`/`--set` persistent-preference getter/setter for `ComputerName`/`HostName`/`LocalHostName` + `--dns`/`--proxy` system-state introspection pair + `--nc` VPN-connection subcommand family `list`/`status`/`statistics`/`show`/`start`/`stop`/`trigger`/`select`/`select-bytagid`), `mdfind` (Spotlight metadata query CLI with `-0` NUL-terminated paths for xargs piping + `-live` continuously-updating count + `-count` cardinality-only + `-onlyin dir` directory-scope limit + `-name` filename-only filter + mutually-exclusive `-literal`/`-interpret` Spotlight-syntax-vs-menu interpretation pair), `mdutil` (Spotlight index manager with mutually-exclusive `-i {on,off}`/`-d` per-volume enable-toggle + `-E` erase-and-rebuild + `-p` network-cache flush + `-s` status + `-a` all-volumes + `-v` verbose), `system_profiler` (System Profiler.app CLI backend with mutually-exclusive `-xml`/`-json` machine-readable output pair + `-detailLevel {mini,basic,full}` 3-level depth ring + `-timeout` deadline + `-listDataTypes` introspection + ~50-entry `SP{Software,Hardware,Applications,Audio,Bluetooth,…}DataType` data-type ring), `atos` (Mach-O address symbolicator with mutually-exclusive `-o binary`/`-p pid` static-file-vs-live-process target pair + `-arch {x86_64,arm64,arm64e,i386,ppc}` 5-arch ring + `-l load-addr`/`-s slide` ASLR-compensation pair + `-printHeader` process-info banner + `-f file` bulk-address-file batch mode); harvested from `unix.com/man-page/osx/{8/scutil,1/{mdfind,mdutil,atos},8/system_profiler}/`. Plus `lipo` (Mach-O universal-binary editor with mutually-exclusive 7-verb action axis `-info`/`-detailed_info`/`-create`/`-thin`/`-extract`/`-extract_family`/`-remove`/`-replace`/`-verify_arch` + `-arch {x86_64,x86_64h,arm64,arm64e,arm64_32,armv7,armv7s,armv7k,i386,ppc,ppc64}` 11-arch ring + `-arch_blank` MH_DYLIB_STUB output + `-segalign` per-arch segment-alignment + `-output` sink), `ditto` (HFS-aware copy/archive utility with 4-way archive-mode `-c`/`-z`/`-j`/`-k` cpio/gzip-cpio/bzip2-cpio/PKZip ring + `-x` extract toggle, 5-axis metadata-preservation pair family `--rsrc`/`--extattr`/`--qtn`/`--acl`/`--hfsCompression` + `--no…` opposites + `--preserveHFSCompression`/`--nopreserveHFSCompression`, `-X` cross-filesystem skip, `--keepParent` parent-dir-embed, `--arch` universal-thinning, `--bom` BOM-filter, `--sequesterRsrc` __MACOSX-shim, `--zlibCompressionLevel 0..9`, `--password` encrypted-ZIP), `hdiutil` (DiskImages.framework CLI with 19-subcommand surface covering attach/detach/eject lifecycle triplet + create/convert/burn/makehybrid format-conversion quartet + compact/resize/segment image-geometry triplet + verify/info/imageinfo/fsid/isencrypted/plist/pmap introspection septet + chpass passphrase + unmount), `diskutil` (Disk Utility.app backend with 21-subcommand `list`/`info`/`eject`/`mount`/`mountDisk`/`umount`/`umountDisk` mount-lifecycle septet + `rename`/`partitionDisk`/`eraseDisk`/`eraseVolume`/`secureErase`/`splitPartition`/`mergePartitions`/`resizeVolume` partition-modification octet + `repairDisk`/`verifyDisk`/`repairVolume`/`verifyVolume`/`repairPermissions` fsck-equivalent quintet + `appleRAID`/`coreStorage`/`apfs` (= `ap`) + `cs` storage-stack subverb trio), `mdls` (Spotlight metadata-attribute lister with repeatable `-name` per-attribute selector covering kMDItem* attribute namespace + `-raw` NUL-separated piping + `-nullMarker` null-value placeholder); harvested from `unix.com/man-page/osx/{1/{lipo,ditto,hdiutil,mdls},8/diskutil}/`. Plus `ibtool` (Interface Builder `.xib`/`.storyboard`/`.nib` compiler with `--compile` build mode + `--output-format {xml1,binary1,human-readable-text}` 3-format ring + `--import-strings-file` localization-merge + `--export-strings-file` strings-extract + `--upgrade` document-version migration + `--localizable-strings`/`--errors`/`--warnings`/`--notices` 4-axis report-inclusion family + `--target-device` 6-device ring + `--minimum-deployment-target` SDK floor + `--auto-activate-custom-fonts`), `actool` (Asset Catalog Tool `.xcassets`→`.car` compiler with `--app-icon`/`--launch-image`/`--accent-color`/`--widget-background-color` 4-asset-type primary selectors + `--include-all-app-icons` + repeatable `--alternate-app-icon` + `--platform {iphoneos,iphonesimulator,macosx,appletvos,…,xros,xrsimulator}` 10-platform ring + repeatable `--target-device` and `--include-language` + sticker-pack quintet `--include-sticker-content`/`--stickers-icon-role`/`--sticker-pack-identifier-prefix`/`--sticker-pack-strings-file` + `--standalone-icon-behavior {default,all,none}` PNG/ICNS-loose-file 3-mode ring + `--compress-pngs` pngcrush + `--filter-for-device-model`/`--filter-for-device-os-version` CAR-filter pair + on-demand-resource (`--enable-on-demand-resources`/`--asset-pack-output-specifications`) pair), `log` (Unified Logging 10-subcommand surface `show`/`stream`/`stats`/`collect`/`config`/`erase`/`emit`/`diagnose`/`repack`/`help` with per-subcommand args: `show`/`stream`/`stats`/`repack` accept `--predicate` NSPredicate filter × `--info`/`--debug`/`--signpost` level-include triplet × `--last`/`--start`/`--end` time-window triplet × `--style {default,compact,json,syslog,ndjson}` 5-format ring × `--color {always,auto,never}` × `--archive` `.logarchive` source × repeatable `--process` filter + `--source` source-loc inclusion; `collect` adds `--last`/`--start`/`--output`/`--size` archive-bound quartet), `powermetrics` (per-sampler power/freq tool with `-i ms`/`-n` interval+count pair + `-o {composite,cputime,power,gputime,energy_impact,wakeup_count,idle_wakeups}` 7-way process-ranking ring + `-f {text,plist}` machine-output pair + `-a` poweravg-period + `-t` wakeup-cost + 3-axis hide-data family `--hide-{platform-power,cpu-duty-cycle,gpu-duty-cycle}` + `--show-initial-usage`/`--show-usage-summary` boundary pair + repeatable `--samplers` covering ~15-sampler ring `cpu_power`/`gpu_power`/`thermal`/`smc`/`battery`/`network`/`disk`/`interrupts`/`tasks`/`ane_power`/`agpm`/`cpu_dvfm_stats`/`gpu_dvfm_stats`/`all`), `dscl` (Directory Service Command Line client with `-u`/`-P`/`-p`/`-f` 4-flag auth-axis + `-raw`/`-plist`/`-url`/`-q` 4-axis output-shape family + 8-verb command surface `-read`/`-create`/`-delete`/`-append`/`-merge`/`-change`/`-list`/`-search` + 6-subcommand interactive shell `-cd`/`-pushd`/`-popd`/`-auth`/`-authonly`/`-quit` + node-targets `.`/`/Search`/`/LDAPv3`); harvested from `unix.com/man-page/osx/1/{ibtool,powermetrics,dscl}/` and `keith.github.io/xcode-man-pages/{actool,log}.1.html`. Plus `launchctl` (launchd service-management CLI with 32-verb surface covering legacy `load`/`unload`/`start`/`stop` quartet + modern domain-aware `bootstrap`/`bootout`/`enable`/`disable`/`kickstart` quintet + introspection octet `list`/`print`/`print-cache`/`print-disabled`/`dumpstate`/`dumpjpcategory`/`blame`/`examine` + env quartet `getenv`/`setenv`/`unsetenv`/`export` + config triplet `config`/`limit`/`log` + domain-as-arg pattern `system`/`gui/UID`/`user/UID`/`pid/PID`), `tmutil` (Time Machine CLI with 24-verb surface covering enable/disable + local-snapshot pair + lifecycle quartet + destination quartet + exclusion triplet + restore/compare/uniquesize/calculatedrift introspection family; per-verb `compare` exposes 13-flag metadata-axis covering ACLs/xattrs/sizes/modes/owners/times/data-forks + max-depth + XML), `dscacheutil` (Directory Services cache CLI with mutually-exclusive verb-axis `-q`/`-cachedump`/`-configuration`/`-flushcache`/`-statistics` + `-q` 12-category lookup ring + cachedump-extender pair), `dseditgroup` (group editor with `-o {read,create,delete,edit,checkmember}` 5-verb action ring + auth quartet + record-mutation surface covering name/gid/GUID/SID/realname/keyword/comment/TTL + `-n` directory-node selector + `-t {user,group,computer}` record-type + `-f {n,l}` new-vs-legacy format), `mdimport` (Spotlight per-file importer with `-g plugin`/`-r plugin` override+reimport pair + mutually-exclusive `-A`/`-X`/`-L` attribute-table/schema/importer-list introspection ring + `-V`/`-p`/`-d {1,2,3,4}` instrumentation triplet + `-n` no-write + `-w ms` pacing); harvested from `unix.com/man-page/osx/{1/{launchctl,dscacheutil,mdimport},8/{tmutil,dseditgroup}}/`. Plus `pmset` (Power Management Set with mutually-exclusive `-a`/`-b`/`-c`/`-u` all/battery/charger/UPS power-source axis + mutually-exclusive `-g [domain]` getter with 17-domain ring `custom`/`live`/`disk`/`batt`/`ps`/`therm`/`thermlog`/`assertions`/`log`/`sched`/`cap`/`hid`/`bt`/`sleep`/`capabilities`/`accps`/`lastassertion`/`fanlevel` + 30+ setting keys covering `displaysleep`/`sleep`/`disksleep`/`womp`/`autorestart`/`ttyskeepawake`/`hibernatemode`/`lidwake`/`acwake`/`autopoweroff*`/`standby*`/`powernap`/`tcpkeepalive`/`disablesleep`/`restartafterkillpower`/`haltlevel`/`haltafter`/`haltremain`/`repeat`/`schedule`), `fdesetup` (FileVault 2 admin CLI with 19-verb surface covering enable/disable lifecycle + list/status/sync + add/remove user pair + changerecovery/removerecovery + authrestart + 6-bool predicate family `isactive`/`hasersonalrecoverykey`/`hasinstitutionalrecoverykey`/`usingrecoverykey`/`supportsauthrestart`/`validaterecovery` + `showdeferralinfo` + per-verb flags `-defer plist` + `-user`/`-uuid`/`-usertoadd` user-target triplet + `-inputplist`/`-outputplist` plist-I/O pair + `-prompt`/`-forcerestart`/`-authrestart` interactivity triplet + institutional triplet `-keychain`/`-certificate`/`-key` + `-norecoverykey` + `-verbose`), `nvram` (firmware-variable CLI with mutually-exclusive `-p` print-all / `-c` delete-all + per-variable `-d name` delete / `-f file` bulk-set / `-x` XML format), `ioreg` (IORegistry inspector with `-c IOService`/etc class-filter + `-d N` depth-cap + `-i` inheritance + `-k key` / `-n name` predicate pair + `-l` properties + `-p plane` 7-plane ring `IOACPIPlane`/`IOAudio`/`IODeviceTree`/`IOFireWire`/`IOPower`/`IOService`/`IOUSB` + `-r` rooted-subtrees + `-t` tree-location + `-x` hex + `-w` width-clip + `-a` XML + `-b` bold + `-f` smart-format), `sips` (Scriptable Image Processing System with repeatable `-g key`/`-s key value`/`-d key` per-property getter/setter/delete triplet + mutually-exclusive resize family `-z H W`/`--resampleWidth`/`--resampleHeight`/`-Z max` + `-c H W` crop + `-r degrees` rotate + `-f {horizontal,vertical}` flip + ColorSync triplet `-x` extract-profile / `-X` extract-tag / `-v` verify + `-i` Finder-icon + `-o`/`--out` sink pair); harvested from `unix.com/man-page/osx/{1/{pmset,sips},8/{fdesetup,nvram,ioreg}}/`. Plus `caffeinate` (power-assertion holder with `-d`/`-i`/`-m`/`-s` display/system-idle/disk-idle/system 4-axis assertion family + `-u` user-active wake + `-t seconds` lease + `-w PID` wait-for-pid mode), `sw_vers` (OS version printer with mutually-exclusive `-productName`/`-productVersion`/`-buildVersion` triplet for scriptable per-field output), `kextstat` (loaded-kext lister with `-b bundle-id` per-kext filter + `-k` no-kernel + `-l` list-only-no-header), `kmutil` (Catalina+ kext-management replacement for kextcache/kextload/kextstat/kextunload with 17-subcommand surface `create`/`inspect`/`load`/`unload`/`libraries`/`showloaded`/`dumpstate`/`find`/`check`/`log`/`print-diagnostics`/`clear-staging`/`migrate`/`install`/`rebuild`/`trigger-panic-medic`/`configure-boot` + 13-flag global axis covering `-a {x86_64,arm64,arm64e}` 3-arch ring × `-V {development,debug,kasan,release}` 4-variant ring × `-z` no-authentication × repeatable `-r` repository × `-R volume-root` × `-B`/`-S`/`-A` boot/system/aux collection-path triplet × `-M` allow-missing × `--kdk`/`--build` + per-verb `-b`/`-p` bundle-id+path target pair), `asr` (Apple Software Restore with 6-subcommand `restore`/`restoreexact`/`server`/`imagescan`/`help`/`version` + `--source`/`--target` IO pair + `--erase`/`--noprompt` confirmation pair + `--format {HFS+,HFSX,APFS}` 3-FS ring + 4-axis buffer-tuning `--buffers`/`--buffersize`/`--csumbuffers`/`--csumbuffersize` + `--noverify` + `--allowfragmentedcatalog` + `--corestorageconvert` + multicast quartet `--config`/`--timeout`/`--interface`/`--nostream` + `--puppetstrings` parseable progress + `--verbose`/`--debug` instrumentation pair); harvested from `unix.com/man-page/osx/{1/sw_vers,8/{caffeinate,kextstat,asr}}/` and `keith.github.io/xcode-man-pages/kmutil.8.html`. Plus `dtrace` (Sun-derived dynamic tracer with repeatable `-s` D-script + `-c command`/`-p pid` 2-target axis + repeatable `-n probe` × `-P provider` filter pair + `-l` list-only mode + `-q`/`-v`/`-w` quiet/verbose/destructive ladder + 5-axis cpp-control family `-C`/`-I`/`-D`/`-U`/`-H` + `-G` ELF/DOF object emitter + `-X {a,c,t,s}` C-standard mode + `-A`/`-Z`/`-F` anonymous-state/zero-match/coalesce trio + repeatable `-x option=value` runtime tuning + repeatable `-L` library-path), `dtruss` (DTrace-based strace clone with `-c` syscall-count + 3-axis time-display family `-d`/`-e`/`-o` relative/elapsed/on-cpu timestamp + mutually-exclusive `-l`/`-L` pid-print pair + `-a` all-details + `-s` stack-trace + `-f` follow-fork + repeatable `-t syscall` filter + mutually-exclusive `-p pid`/`-n name` target pair + `-b size` buffer-tune), `automount` (`automountd(8)` driver with `-v` verbose + `-c` cache-flush + `-u` unmount-non-busy + `-t seconds` unreferenced-timeout), `kextload`/`kextunload` (legacy kext load/unload pair superseded by `kmutil load`/`unload` on Catalina+ — `kextload` with repeatable `-b bundle-id`/`-d dependency`/`-r repository` + `-q`/`-v level` quiet/verbose pair; `kextunload` with `-c classname` IOService-instance-terminate + `-p` personalities-only-no-unload + same bundle-id/quiet/verbose surface + `-m` back-compat bundle-id alias); harvested from `unix.com/man-page/osx/{1/dtrace,1m/dtruss,8/{automount,kextload,kextunload}}/`.
- **BlueZ Bluetooth stack** (Linux kernel BT5/LE userland, alongside existing `hcitool`/`hciconfig`/`hciattach`/`btattach`/`btmon`/`btmgmt`-stub/`sdptool`/`l2ping`/`rfcomm`/`ciptool`/`bccmd`/`bdaddr`/`avinfo`/`bluemoon`/`integritysetup`/`meshctl`) — `bluetoothctl` (interactive controller with 10 menu subcommands `advertise`/`monitor`/`scan`/`gatt`/`admin`/`player`/`endpoint`/`transport`/`mgmt`/`assistant` + `-a {NoInputNoOutput,DisplayOnly,DisplayYesNo,KeyboardOnly,KeyboardDisplay}` SSP agent capability + `-e` Media endpoint registration + `-m` monitor mode + `-t seconds` non-interactive timeout), `btmgmt` (kernel `Bluetooth Management API` CLI with `--index` adapter selection + `--timeout` non-interactive deadline), `btproxy` (HCI relay between local controller and TCP/Unix peer with mutually-exclusive `-c`/`-l`/`-u` connect/TCP-listen/Unix-listen modes + `-p port` × `-i index` × `-a` AMP-emulation + `-e` ECC support emulation + `-r` local-redirect + `-z` zero-byte skip + `-d` debug), `btsnoop` (HCI `.btsnoop` trace file tool with mutually-exclusive `-m output` merge-files vs `-e input` extract-data modes + `-t {eir,ad,sdp}` extraction-type ring for EIR / advertising-data / SDP records); harvested from `manpages.debian.org/{bluetoothctl,btmgmt}.1` and `bluez/bluez tools/{btproxy,btsnoop}.c` `main_options[]` arrays. Plus `btinfo` (HCI raw-socket controller info dumper with `-i index` controller selection + `-r` reset + `-R` raw-device mode), `btconfig` (interactive controller-configuration shell), `btgatt-client` (LE GATT central with `-d bdaddr`/`-t {public,random}` destination + LE-address-type pair + `-m mtu` ATT-MTU + `-s {low,medium,high,fips}` security-level enum + `-i hciN` adapter selection), `btgatt-server` (LE GATT peripheral mirroring the client surface plus `-r` Heart-Rate-service preload + `-t` source-address-type), `3dsp` (Bluetooth 3D Synchronization Profile tester with mutually-exclusive `-D`/`-G` display-vs-glasses role-pair toggle + `-i index` controller selection + `-r` raw mode); harvested from `bluez/bluez tools/{btinfo,btconfig,btgatt-client,btgatt-server,3dsp}.c` `main_options[]` arrays. Plus `avtest` (AVDTP/AVCTP A2DP/AVRCP test harness with `--device hciN` controller + `--send`/`--reject command`/`--reject-code` command-injection triplet + `--invalid` invalid-PDU mode + `--preconf` codec-config preload + `--wait N` post-test hold + `--fragment` minimum-MTU-fragmented-transport mode + `--aac` MPEG2/4-AAC-LC codec + `--avctp` AVCTP-transport selection), `bcmfw` (Broadcom controller firmware upload tool), `eddystone` (Eddystone-URL / UID / TLM LE beacon transmitter with `-i index` controller selection), `mpris-proxy` (D-Bus MPRIS↔AVRCP media-player bridge with `-e` remote-player export and `-i index` controller pinning — installs as the BlueZ user-session AVRCP target so phone calls control desktop audio players), `isotest` (LE Isochronous Channels CIS/BIS test harness with mutually-exclusive 6-mode `-d`/`-r`/`-s`/`-n`/`-c`/`-m` dump-server / receive-server / connect-and-send / silent / reconnect / multiple-connects role triplet × 2, full ISO-link QoS axis covering `-G CIG`/`-T CIS`/`-N nbis` group/stream/count + `-S` SCA-or-adv-interval + `-P`/`-F` packing+framing pair + `-I usec`/`-L ms` interval+latency pair + `-Y` PHY + `-R` retransmission-count + `-B` QoS-preset shortcut + `-M` MTU + `-W` deferred-setup + `-j` jitter-buffer + `-V {help,…}` address-type-list, `-q` quiet packet-logging suppression); harvested from `bluez/bluez tools/{avtest,bcmfw,eddystone,mpris-proxy,isotest}.c` `main_options[]` + `usage()` printouts. Plus `scotest` (SCO audio test harness for HFP/HSP voice with mutually-exclusive 6-way `-d`/`-c`/`-m`/`-r`/`-s`/`-n` dump/reconnect/multi-connect/receive/send/silent role ring + `-b bytes` data buffer + `-W seconds` deferred-setup + `-V {0x0060,0x0003}` voice-setting CVSD-vs-Transparent), `oobtest` (Out-Of-Band pairing tester with mutually-exclusive `-B`/`-L` BR/EDR-vs-LE transport pair × mutually-exclusive `-S`/`-O`/`-P` Secure-Connections / SC-Only / Legacy-Pairing pairing-mode triplet × mutually-exclusive `-0`/`-1`/`-2` LE-legacy-TK / P-192 / P-256 OOB-data-type triplet × mutually-exclusive `-I`/`-A` initiator/acceptor role pair + `-R` static-random-address + `-Y` LE-privacy + `-D` debug-keys + `-C` cross-transport-pairing), `bneptest` (BNEP PAN-profile test harness with mutually-exclusive `-s`/`-c bdaddr` server/connect pair + 16-knob L3-layer surface covering source/dest MAC + control-message-type + frame-type + multicast-range + protocol-range + local/remote service-role + bridge/interface name + control + frame retransmission counters), `btiotest` (generic BlueZ BtIO transport-tester covering L2CAP/RFCOMM/SCO/ISO with `-c channel`/`-p psm`/`-j cid` per-transport endpoint triplet + mutually-exclusive `-s`/`-o` SCO/ISO mode toggle + `-t {0,1,2}` BR-EDR/LE-Public/LE-Random address-type ring + `-V {0x0060,0x0003}` voice + `-S`/`-U` static-vs-update security-level pair + `-r`/`-D`/`-a` time-triggered reject/disconnect/accept triplet + mutually-exclusive `-C`/`-m` central/master deprecated-alias pair + `-P 0-6` transmission priority with CAP_NET_ADMIN escalation), `seq2bseq` (Atheros/Qualcomm `.seq` → BlueZ binary `.bseq` firmware converter with `-o` sink); harvested from `bluez/bluez tools/{scotest,oobtest,bneptest,btiotest,seq2bseq}.c` `main_options[]` + `usage()` printouts. Plus `advtest` (LE Resolvable Set Identifier (RSI) ADV-data generator from a 128-bit SIRK), `hcieventmask` (HCI event-mask printer with `-i` device selection), `btmon-logger` (rotating HCI `.btsnoop` ring-file emitter with `-b basename` × `-p` mkdir-p × `-l limit` per-file size cap × `-c count` rotation depth), `create-image` (CPIO image emitter for kernel-self-test boot with `-o image` sink), and `hci-tester` (shared compdef covering `hci-tester`/`mgmt-tester`/`smp-tester`/`gap-tester`/`rfcomm-tester`/`bnep-tester`/`6lowpan-tester` — they all delegate `tester_init()`/`tester_run()` to `src/shared/tester.c` and share the 7-flag harness surface `-v` version + `-q` quiet + `-d` debug + `-m` monitor + `-l` list + `-p prefix`/`-s string` test-name filter pair); harvested from `bluez/bluez tools/{advtest,hcieventmask,btmon-logger,create-image}.c` `main_options[]` + `usage()` and `bluez/bluez src/shared/tester.c` GOptionContext array. Plus `bluetoothd` (the system-wide profile daemon with `-d categories` debug-filter + `-p`/`-P` plugin allow/deny pair + `-f path` explicit config-file + `-C` deprecated-CLI compat layer + `-E`/`-T` experimental/testing D-Bus interface toggle pair + `-K feature` kernel-experimental-feature opt-in + `-n` foreground), `bluetooth-meshd` (Mesh Profile daemon with `--io {auto,generic,ell-hci}` I/O backend selector + `--storage dir` node-config + `--config dir` daemon-config + `--dbus-debug` D-Bus-tap mode), `obex-server-tool` (OBEX server test harness with mutually-exclusive `-u`/`-b` UNIX-vs-Bluetooth transport pair + mutually-exclusive `-p`/`-s` packet/stream framing pair + `-c channel` RFCOMM channel + `-r root` filesystem-root scoping + `-i`/`-o` input/output MTU pair), `hex2hcd` (Broadcom Intel-HEX → `.hcd` controller-patch converter with `-T` version-table dump + `-o` output sink), `rctest` (RFCOMM transport test harness with mutually-exclusive 9-way role ring `-r`/`-w`/`-d`/`-s`/`-u`/`-n`/`-c`/`-m`/`-a` listen-receive / listen-send / listen-dump / connect-send / connect-receive / silent / reconnect / multi-connect / automated, `-P channel`/`-U uuid` RFCOMM endpoint pair + `-A`/`-E`/`-S` auth / encryption / secure-connection triplet + `-M` central + `-T` timestamps + `-B`/`-O` per-direction data-file source/sink + `-N`/`-C`/`-D` frames+delay-batch triplet); harvested from `bluez/bluez {src/main.c,mesh/main.c,tools/{obex-server-tool,hex2hcd,rctest}.c}` `options[]`/`main_options[]` + `usage()` arrays.
- **libxml2 + libxslt** (GNOME / xmlsoft.org XML toolchain) — `xmllint` (parser/validator/formatter with 4 schema-validation modes `--valid` DTD-from-doc / `--postvalid` after-parse / `--dtdvalid url` / `--dtdvalidfpi FPI` external-DTD / `--relaxng schema` / `--schema schema` W3C-XSD, parse-mode quartet `--memory` / `--push` / `--stream` / `--shell` interactive-navigation, output-mode set `--format` reindent / `--c14n` W3C-canonical / `--html` HTML-parser / `--htmlout` / `--xmlout` HTML-with-XML-serialization / `--compress` gzip / `--encode` charset, content-transform quartet `--noblanks` / `--nocdata` / `--noent` / `--nsclean` / `--dropdtd` / `--dtdattr` / `--xinclude` / `--noxincludenode`, network-control `--nonet` / `--catalogs` / `--nocatalogs` / `--path` / `--load-trace` / `--maxmem` / `--xpath` / `--pattern`, dev-test toggles `--auto` / `--debug` / `--debugent` / `--chkregister` / `--copy` / `--insert` / `--testIO` / `--repeat` / `--timing` / `--walker` / `--recover` / `--nowarning` / `--nowrap` / `--noout`), `xmlcatalog` (catalog editor with `--create` / `--add` `TYPE ORIG REPLACE` triplet form vs SGML-filename single form / `--del` / `--shell` interactive + mutually-exclusive `--sgml` super-catalog mode vs default XML + `--no-super-update` super-catalog skip), `xsltproc` (XSLT 1.0 processor with `--profile`/`--norman` profiling alias-pair + `--repeat`/`--timing` benchmarking pair + 4-axis tree-walk limits `--maxdepth` / `--maxvars` / `--maxparserdepth` / `--huge` + repeatable `--param NAME VALUE`/`--stringparam NAME VALUE` node-vs-string parameter-passing pair + sandboxing triplet `--nonet` / `--nowrite` / `--nomkdir` + `--writesubtree PATH` allow-list + `--dumpextensions` introspection); harvested from `manpages.debian.org/{xmllint,xmlcatalog,xsltproc}.1`.
- **WebAssembly Binary Toolkit** (WABT — `wat2wasm`/`wasm2wat`/`wasm-objdump`/`wasm-validate`/`wasm-decompile`/`wasm-interp`/`wasm-strip` already in repo) — `wast2json` (spec-test `.wast` to JSON+`.wasm` fixture generator with `-r` relocatable-for-lld + `--no-canonicalize-leb128s` 5-byte LEB128 + `--debug-names` + `--no-check` invalid-module-allow + the full proposal-enable matrix `--enable-{exceptions,mutable-globals,saturating-float-to-int,sign-extension,simd,threads,multi-value,bulk-memory,reference-types,tail-call,annotations,code-metadata,gc,memory64,multi-memory,extended-const,relaxed-simd,function-references}` × `--enable-all` master toggle), `wasm-stats` (per-opcode histogram + section-size emitter with `-c cutoff` minimum-count filter + `-s separator` element-vs-count formatter); harvested from `WebAssembly/wabt src/tools/{wast2json,wasm-stats}.cc` OptionParser arrays.
- **JS bundlers / test runners / stylesheet compilers** (alongside existing `bun`/`bunx`/`biome`/`esbuild`/`swc`/`oxlint`/`rspack`/`turbo`/`nx`/`vite`/`vitest`/`playwright`/`cypress`/`parcel`/`rolldown`/`tsx`/`tsdown`/`ava`/`tape`/`karma`/`lerna`/`postcss`/`tailwindcss`/`terser`/`verdaccio`) — `rollup` (ES-module bundler with `-f {amd,cjs,es,iife,umd,system}` 6-format ring + `-g id:Global,...` UMD/IIFE-globals injection + `-p plugin` repeatable + `--amd.{autoId,basePath,define,forceJsExtensionForImports,id}` AMD-quintet + 7-knob generated-code shape `--generatedCode.{arrowFunctions,constBindings,objectShorthand,symbols,reservedNamesAsProps}` + 9-knob source-map control `--sourcemap{BaseUrl,DebugIds,ExcludeSources,File,FileNames}`/`--embed-source-map`/`--no-source-map` + watch sextet `--watch.{onStart,onBundleStart,onBundleEnd,onEnd,onError,buildDelay,include,exclude,skipWrite,allowInputInsideOutputPath,clearScreen}` + 7-axis tree-shake disable family `--no-treeshake[.{annotations,moduleSideEffects,propertyReadSideEffects,tryCatchDeoptimization,unknownGlobalSideEffects}]`), `jest` (Facebook test runner with `--watch`/`--watchAll` watch pair + `-t name-regex`/`--testPathPatterns` filter pair + `-u` snapshot-update + `--coverage` + `-o` only-changed + `-i` serial + `--bail[=N]` early-exit + `-w` max-workers + `--detectOpenHandles`/`--forceExit` shutdown pair + `--clearCache`), `webpack` (webpack-cli with 9-subcommand surface `build`/`bundle`/`configtest`/`info`/`serve`/`version`/`watch`/`init`/`loader`/`plugin` + `--config-name` multi-config selector + `--mode {development,production,none}` + `--target {node,web,webworker,electron-*,nwjs,es5,es2015,es2017}` 10-target ring + `-d {eval*,inline-*,source-map,nosources-source-map,hidden-source-map}` 10-devtool ring + `--analyze` bundle-analyzer + `--fail-on-warnings` + `--merge` webpack-merge), `sass` (Dart Sass with `-s {expanded,compressed}` 2-style output + `--indented` .sass syntax + `-I path` repeatable load-path + `-p node` Node.js pkg: importer + `--source-map-urls {relative,absolute}` + 3-knob source-content `--embed-sources`/`--embed-source-map`/`--no-source-map` + watch pair `--watch`/`--poll` + 3-axis deprecation control `--fatal-deprecation`/`--future-deprecation`/`--silence-deprecation` + `--quiet-deps`/`--no-unicode`), `mocha` (BDD test runner with `-R` reporter (~13-reporter ring) × `-u {bdd,tdd,qunit,exports}` 4-UI ring × `-g/--fgrep`/`--invert` filter triplet × `-w`/`--watch-files`/`--watch-ignore` watch triplet × `--parallel`/`--jobs N` worker pair × `--forbid-only`/`--forbid-pending` CI-strict pair × `-r` pretest-require × `--timeout`/`--slow`/`--retries` timing triplet + `--check-leaks`/`--ignore-leaks` global-leak pair + `--dry-run`/`--fail-zero`/`--full-trace` debug triplet + `--recursive`/`--sort`/`--extension`); harvested from `rollupjs.org/command-line-interface`, `jestjs.io/docs/cli`, `webpack.js.org/api/cli`, `sass-lang.com/documentation/cli/dart-sass`, and `mochajs/mocha` `.mocharc.json` + CLI help. Plus `tsc` (TypeScript compiler with mutually-exclusive `--build`/`--init`/`--listFilesOnly`/`--showConfig`/`--clean`/`--version`/`--help`/`--all` 8-verb axis + `--watch` mode with 4-axis watcher-mode tuning `--watchFile {fixedPollingInterval,priorityPollingInterval,dynamicPriorityPolling,fixedChunkSizePolling,useFsEvents,useFsEventsOnParentDirectory}` × `--watchDirectory {useFsEvents,fixedPollingInterval,dynamicPriorityPolling,fixedChunkSizePolling}` × `--fallbackPolling {fixedInterval,priorityInterval,dynamicPriority,fixedChunkSize}` × `--synchronousWatchDirectory` + `--excludeDirectories`/`--excludeFiles` watch-skip pair + `-p project` + `--dry` plan-only + `--force` rebuild-all + `--verbose`), `uglifyjs` (JavaScript parser/mangler/compressor with full optimization-axis `-c`/`-m`/`-b`/`-O`/`-p` compress/mangle/beautify/output-opts/parse + `--mangle-props` property-mangle + mutually-exclusive `--ie`/`--v8`/`--webkit`/`--no-module` browser-quirks ring + `--module` ES-module mode + `--toplevel` + `--keep-fargs`/`--keep-fnames` symbol-preserve pair + `--source-map` opts + `--annotations`/`--no-annotations` comment-handling pair + `-d name=value` defines + `--name-cache` cross-bundle mangle stability + `--wrap name`/`-e enclose` namespace-wrap pair), `html-minifier` (shared compdef for `html-minifier`/`html-minifier-terser` with `--input-dir`/`--output-dir` directory-mode pair × `--file-ext` filter + 16-knob whitespace family `--collapse-{whitespace,boolean-attributes,inline-tag-whitespace}` / `--conservative-collapse` / `--preserve-line-breaks` / `--remove-{comments,empty-attributes,empty-elements,optional-tags,redundant-attributes,script-type-attributes,style-link-type-attributes,tag-whitespace,attribute-quotes}` + 4-engine content-minify quartet `--minify-{css,js,urls}` + `--process-conditional-comments` IE shim + 8-knob custom-regex extension family `--custom-attr-{assign,collapse,surround}`/`--custom-event-attributes`/`--ignore-custom-{comments,fragments}`/`--process-scripts`/`--trim-custom-fragments` + `--use-short-doctype`/`--html5` doctype pair + `--quote-character {',\"}` + `--sort-attributes`/`--sort-class-name` frequency-sort pair), `nodemon` (Node dev-restart watcher with repeatable `--watch path` + `-e ext,...` extension list + repeatable `--ignore glob` + `--delay duration` throttle + `--signal {SIGHUP,SIGINT,SIGUSR2,SIGTERM,SIGKILL}` 5-signal ring + `--exec` non-Node-script executor + `-L`/`--legacy-watch` NFS-friendly polling + `--exitcrash` no-restart-on-crash + `--inspect` Node debug-pass), `prettier` (opinionated formatter with mutually-exclusive `-w`/`-c`/`-l` write-in-place / check-mode / list-different action triplet + `--debug-check` correctness-changed guard + `--find-config-path` config-search introspection + `--config-precedence {cli-override,file-override,prefer-file}` 3-mode resolution ring + `--cache`/`--no-cache` + `--cache-strategy {metadata,content}` + `--cache-location` cache-tuning triplet + `--ignore-path`/`--no-editorconfig`/`--with-node-modules` 3-axis ignore-policy + `--stdin-filepath` + `-u` unknown-files-tolerant + `--parser` explicit-parser ring covering 22 parsers `babel`/`babel-ts`/`flow`/`typescript`/`css`/`less`/`scss`/`html`/`angular`/`vue`/`lwc`/`json`/`json5`/`jsonc`/`graphql`/`markdown`/`mdx`/`yaml`/`glimmer`); harvested from `typescriptlang.org/docs/handbook/compiler-options`, `github.com/mishoo/UglifyJS`, `github.com/kangax/html-minifier#options-quick-reference`, `github.com/remy/nodemon`, and `prettier.io/docs/cli`.
- **K8s / Go / proto / CLI-utility heavy hitters** — `helm` (Kubernetes package manager with 22-subcommand surface covering chart lifecycle quintet `install`/`upgrade`/`uninstall`/`rollback`/`history`, observability triplet `list`/`get`/`status`/`test`, chart-author trio `create`/`lint`/`package`, repository CRUD `repo` + `search`/`pull` + OCI `verify`, render `template`, `dependency` chart-deps manager + `show`/`completion`/`env`/`version` introspection quartet + 14-flag global axis covering kubeconfig+context+namespace + 4-impersonation flags `--kube-as-user`/`--kube-as-group`/`--kube-ca-file`/`--kube-token` + `--repository-{config,cache}` and `--registry-config` OCI-login pair + `--burst-limit` throttle), `jq` (JSON processor with 6-axis I/O-mode family `-n`/`-R`/`-s`/`-r`/`-c`/`-j`/`--raw-output0` covering null-input/raw-input/slurp/raw-output/compact/join/nul-sep + 6-axis formatting `-S`/`-a`/`-C`/`-M`/`--tab`/`--indent` covering sort-keys/ASCII-escape/color-pair/tab-vs-space-indent + 6-axis variable-passing `--arg`/`--argjson`/`--slurpfile`/`--rawfile`/`--args`/`--jsonargs` covering scalar/JSON/file-slurp/file-raw/positional-string/positional-JSON + `--stream`/`--unbuffered`/`-e`/`--seq` processing-quartet), `protoc` (Protocol Buffers compiler with 13-language `--LANG_out` ring covering C++/Java/Python/Go/C#/Objective-C/JS/PHP/Ruby/Dart/Kotlin/Rust + `--descriptor_set_out` FileDescriptorSet emitter + mutually-exclusive `--encode`/`--decode`/`--decode_raw` text↔binary action triplet + `--print_free_field_numbers` schema-inspection + `--include_imports`/`--include_source_info`/`--allow_proto3_optional` 3-axis descriptor-content family + `--error_format {gcc,msvs}` IDE-error-format pair + repeatable `-I`/`--proto_path` + `--plugin name=path` 3rd-party generator hook), `goimports` (Go formatter+import-fixer with mutually-exclusive `-l`/`-w`/`-d` list/write/diff action triplet + `-srcdir` virtual-source-root + `-local prefix` 3rd-party-import-group separator + `-format-only` gofmt-equivalent mode + `-e` all-errors + profiling triplet `-cpuprofile`/`-memprofile`/`-memrate`), `fzf-tmux` (`fzf` tmux wrapper with mutually-exclusive 5-axis layout-mode `-p`/`-u`/`-d`/`-l`/`-r` popup/up-split/down-split/left-split/right-split + popup-position quartet `-w`/`-h`/`-x`/`-y` for tmux 3.2+ + `--` fzf-options separator); harvested from `helm.sh/docs/helm/helm/`, `jqlang.org/manual/`, `manpages.debian.org/protoc.1`, `golang/tools cmd/goimports/goimports.go` flag definitions, and `junegunn/fzf bin/fzf-tmux` script.
- **File watchers / backup / cloud-sync / DB shells** — `entr` (file-change runner with `-a` all-events + `-c` clear-screen + `-d` directory-track-and-exit + `-n` non-interactive + `-p` postpone-first-run + `-r` reload-persistent-child + `-s` `$SHELL`-eval + `-x` custom-exit-status + `-z` exit-after-utility), `duplicity` (encrypted rdiff-based backups with 11-verb surface + short-alias pair for each `backup`/`bu`, `full`/`fb`, `incremental`/`ib`, `verify`/`vb`, `restore`/`rb`, `collection-status`/`st`, `list-current-files`/`ls`, `remove-older-than`/`ro`, `remove-all-but-n-full`/`ra`, `cleanup`/`cl`, `replicate` + repeatable `--encrypt-key`/`--sign-key` GPG-key pair + 4-axis include/exclude family `--include`/`--exclude` + `--include-filelist`/`--exclude-filelist` + `--exclude-globbing-filelist` + `--archive-dir`/`--volsize` cache-tuning pair + `--full-if-older-than` chain-policy + `-t`/`--time` point-in-time + `--no-encryption`/`--no-compression` skip-pair + `--dry-run`/`--force` confirmation pair), `rclone` (40+ subcommand universal-cloud CLI covering copy/sync/move IO triplet + 5-form list family `ls`/`lsd`/`lsl`/`lsf`/`lsjson` + mkdir/rmdir/rmdirs/delete/purge cleanup quintet + cat/mount/serve consumption triplet + check/dedupe/hashsum/sha1sum/md5sum integrity quintet + copyto/moveto single-file pair + link/about/ncdu/tree/size introspection quintet + config/listremotes/version/completion config-quartet + obscure/reveal password pair + `rcd` daemon + `selfupdate`/`settier` lifecycle pair + global axis covering `--config`/`--log-{file,level}` + verbose-pair `-v`/`-q` + `--dry-run`/`-i`/`-P` confirmation triplet + `--transfers`/`--checkers`/`--retries`/`--bwlimit`/`--max-depth` performance quintet + `--include`/`--exclude`/`--filter` filter-triplet + `--update`/`--checksum`/`--no-traverse` skip-policy triplet), `redis-cli` (Redis client with `-h`/`-p`/`-s`/`-u` connection-target quartet + `-a`/`--user`/`--pass`/`--askpass` auth-quartet + `-r`/`-i` repeat+interval pair + `-n` DB + `-x` stdin-arg + `-c` cluster-follow + machine-readable `--csv`/`--json`/`--quoted-json` output triplet + `--stat`/`--latency*`/`--lru-test`/`--intrinsic-latency`/`--slowlog` benchmarking sextet + `--rdb`/`--functions-rdb` snapshot pair + `--pipe`/`--pipe-timeout` mass-insert pair + key-introspection quartet `--bigkeys`/`--memkeys`/`--memkeys-samples`/`--hotkeys` + `--scan`/`--pattern`/`--quoted-pattern` scan-triplet + `--cluster` 11-subcommand sub-mode `create`/`check`/`call`/`info`/`reshard`/`rebalance`/`fix`/`add-node`/`del-node`/`import`/`backup`/`help` + TLS quintet `--tls`/`--sni`/`--cacert`/`--cacertdir`/`--insecure` + `--cert`/`--key`/`--tls-ciphers`/`--tls-ciphersuites` mTLS+cipher quartet), `mongo` (legacy MongoDB shell — deprecated 5.0+ in favor of `mongosh` — with `--eval` JS expr + `--shell`/`--nodb`/`--norc`/`--quiet` startup quartet + `--host`/`--port` connection pair + auth quintet `--username`/`--password`/`--authenticationDatabase`/`--authenticationMechanism {SCRAM-SHA-1,SCRAM-SHA-256,MONGODB-X509,PLAIN,GSSAPI,MONGODB-AWS}`/`--gssapiServiceName` + TLS sextet `--tls`/`--tlsCertificateKeyFile`/`--tlsCertificateKeyFilePassword`/`--tlsCAFile`/`--tlsAllowInvalidCertificates`/`--tlsAllowInvalidHostnames` + API-version triplet `--apiVersion`/`--apiStrict`/`--apiDeprecationErrors` + `--retryWrites`/`--disableImplicitSessions`/`--ipv6` connection-tuning triplet); harvested from `eradman.com/entrproject/entr.1.html`, `manpages.debian.org/duplicity.1`, `rclone.org/commands/rclone/`, `redis.io/docs/latest/develop/connect/cli/` + `redis-cli --help` reference, and `mongodb.com/docs/manual/reference/program/mongo/`.
- **Image / PDF / metadata toolchain** (alongside existing `magick`/`identify`/`mogrify`/`mediainfo`/`mupdf`/`pdftotext`/`pdfinfo`/`cpdf`/`xpdf`/`img2pdf`) — `convert` (ImageMagick legacy CLI with 8-axis geometry family `-resize`/`-crop`/`-rotate`/`-flip`/`-flop`/`-extent`/`-trim`/`-thumbnail` + 4-axis content-adjust quartet `-gamma`/`-level`/`-modulate`/`-monochrome` + filter+colorspace pair `-filter` 14-filter ring × `-colorspace` 9-space ring + 9-gravity `-gravity` placement ring + alpha-mode 10-state ring + format/quality/compression triplet + auto-orient + strip-metadata + per-channel 7-channel ring + append-vertical-horizontal pair `-append`/`+append`), `exiftool` (Phil Harvey metadata Swiss-knife with 4-axis output-mode family `-j`/`-X`/`-csv`/`-t` + 4-flag verbosity ladder `-s`/`-s2`/`-s3`/`-S` + group-name family `-G`/`-G0`/`-G1`/`-G2` + write-mode pair `-overwrite_original`/`-overwrite_original_in_place` + `-delete_original`/`-delete_original!` backup-cleanup pair + per-tag `-All`/`-All=` extract-vs-delete pair + `-tagsFromFile` cross-file copy + 19-charset `-charset` ring + 7-axis introspection family `-listd`/`-listr`/`-listg`/`-execute`/`-common_args`/`-fileOrder`/`-api`), `pdftk` (Sid Steward PDF Toolkit with 19-verb surface covering `cat`/`shuffle`/`burst`/`rotate` 4-axis structural ops + form quartet `generate_fdf`/`fill_form`/`dump_data_fields`/`dump_data_fields_utf8` + watermark+stamp quartet `background`/`multibackground`/`stamp`/`multistamp` + introspection quintet `dump_data`/`dump_data_utf8`/`dump_data_fields`/`dump_data_fields_utf8`/`dump_data_annots` + `update_info`/`update_info_utf8` metadata-write pair + `attach_files`/`unpack_files` attachment pair + 3-algorithm output-encryption ring `encrypt_40bit`/`encrypt_128bit`/`encrypt_aes128` + `allow` 9-permission ring + `flatten`/`compress`/`uncompress`/`drop_xfa`/`drop_xmp` content-mod quintet + `keep_first_id`/`keep_final_id` ID-preservation pair + `need_appearances`/`replacement_font` form-rendering pair), `gs` (Ghostscript PostScript/PDF interpreter with `-sDEVICE` 28-device ring covering pdfwrite/ps2write/epswrite/txtwrite/png*/jpeg*/tiff* + `-r`/`-g` resolution/geometry pair + `-dPDFSETTINGS {/screen,/ebook,/printer,/prepress,/default}` 5-preset ring + 3-axis page-range `-dFirstPage`/`-dLastPage` + font-control triplet `-dEmbedAllFonts`/`-dSubsetFonts`/`-dCompressFonts` + `-dAutoRotatePages {/None,/All,/PageByPage}` 3-mode ring + `-dSAFER`/`-dNOSAFER` security pair + `-dNOPAUSE`/`-dBATCH`/`-dQUIET` headless triplet + `-c`/`-f` inline-PS triplet), `qpdf` (content-preserving PDF library CLI with `--encrypt`/`--decrypt`/`--password`/`--copy-encryption`/`--show-encryption` security quintet + `--pages`/`--split-pages`/`--rotate`/`--overlay`/`--underlay`/`--remove-page-labels` page-manipulation sextet + `--linearize`/`--object-streams {preserve,disable,generate}` web-optimization pair + 4-axis check/inspect family `--check`/`--show-pages`/`--show-object`/`--show-attachments` + `--json`/`--json-output` machine-readable pair + `--stream-data`/`--compress-streams`/`--decode-level` 4-level decode-level ring + `--qdf` QDF-mode + `--allow-weak-crypto`); harvested from `imagemagick.org/script/convert.php`, `exiftool.org/exiftool_pod.html`, `manpages.debian.org/{pdftk,gs}.1`, and `qpdf.readthedocs.io/en/stable/cli.html`.
- **Archive + audio-image-DB encoders / dumpers** — `7z` (shared compdef for `7z`/`7za`/`7zr` archivers with 11-verb action axis `a`/`b`/`d`/`e`/`h`/`l`/`t`/`u`/`x`/`rn`/`i` covering add/benchmark/delete/extract/hash/list/test/update/extract-with-paths/rename/info + `-mx{0,1,3,5,7,9}` 6-level compression ladder + `-m0 {LZMA,LZMA2,PPMd,BZip2,Deflate,Deflate64,Copy}` 7-codec ring + `-t` 30+ archive-type ring covering 7z/zip/tar/xz/iso/dmg/etc + `-mhe`/`-ms` solid+header-encrypt pair + repeatable `-i`/`-x` include/exclude filter pair + 6-axis metadata-preservation family `-snh`/`-snl`/`-stl`/`-spd`/`-spe`/`-spf` + `-bb`/`-bs` output-stream pair + `-y` yes-to-all + `-sdel` source-delete + `-p` password), `cwebp` (libwebp WebP encoder with `-q`/`-z`/`-lossless`/`-near_lossless` 4-axis compression-mode ring + `-preset {default,photo,picture,drawing,icon,text}` 6-preset ring + 5-axis geometry `-crop`/`-resize`/`-s` raw-YUV-size + filter quartet `-f`/`-sharpness`/`-strong`/`-nostrong` + `-sharp_yuv`/`-sns`/`-segments`/`-partition_limit` quality-tuning quartet + size/PSNR/pass target triplet + 4-axis alpha-channel family `-alpha_q`/`-alpha_filter`/`-alpha_method`/`-noalpha` + `-blend_alpha` + `-exact` transparent-RGB-preservation + 4-axis output reporting `-v`/`-print_psnr`/`-print_ssim`/`-print_lsim` + `-progress`/`-quiet`/`-short` verbosity triplet + `-map` ASCII-encoding-map + `-mt`/`-low_memory` threading+memory pair + `-metadata {all,none,exif,icc,xmp}` 5-axis copy ring), `lame` (MP3 encoder with mutually-exclusive `-b`/`-B`/`-V`/`-v`/`--abr`/`--preset` 6-axis bitrate-mode ring + `-q {0..9}` quality ladder + `-m {s,j,f,d,m,l,r}` 7-mode stereo ring + lowpass/highpass filter pair + ReplayGain quartet `--replaygain-{fast,accurate}`/`--noreplaygain`/`--clipdetect` + 8-tag ID3 family `--tt`/`--ta`/`--tl`/`--ty`/`--tc`/`--tn`/`--tg`/`--genre` + `--id3v1-only`/`--id3v2-only` tag-version pair), `mongodump` (BSON dump with `--db`/`--collection` + `--query`/`--queryFile` query pair + `--out`/`--archive` output-mode pair + `--gzip` compression + `--oplog` PIT-recovery + `--dumpDbUsersAndRoles` + repeatable `--excludeCollection`/`--excludeCollectionsWithPrefix` exclusion pair + `-j` parallel collections + `--viewsAsCollections` + `--compressors {snappy,zlib,zstd}` network-compression ring + auth/TLS surface shared with `mongo`), `mongoimport` (JSON/CSV/TSV importer with `--type {json,csv,tsv}` 3-format ring + `--mode {insert,upsert,merge,delete}` 4-mode write-handling ring + `--fields`/`--fieldFile`/`--headerline`/`--columnsHaveTypes`/`--parseGrace {autoCast,skipField,skipRow,stop}` 5-axis schema family + `--drop`/`--stopOnError`/`--maintainInsertionOrder`/`--bypassDocumentValidation` 4-axis safety toggles + `--jsonArray` + `--legacy` extended-JSON-v1 + `--useArrayIndexFields` + `--upsertFields`/`--writeConcern`/`--numInsertionWorkers` 3-axis upsert+performance triplet); harvested from `manpages.debian.org/{7z,lame}.1`, `developers.google.com/speed/webp/docs/cwebp`, and `mongodb.com/docs/database-tools/{mongodump,mongoimport}/`.
- **Multimedia + MongoDB dump-pair siblings** — `ffmpeg` (universal multimedia converter with repeatable `-i input` + `-f` 20-format container ring covering mp4/mov/mkv/webm/avi/flv/mp3/wav/flac/aac/ogg/gif/image2/mpegts/hls/dash/null/pipe/rawvideo/etc + `-c:v` 9-codec video ring covering libx264/libx265/libvpx*/libsvtav1/mpeg4/hevc_videotoolbox/h264_videotoolbox/copy × `-c:a` 8-codec audio ring + `-c:s` 7-codec subtitle ring + `-map` stream-selection + `-vf`/`-af`/`-filter_complex` 3-axis filtergraph family + `-ss`/`-t`/`-to` time-window triplet + `-pix_fmt` 10-format pixel ring + `-r`/`-s`/`-aspect` geometry triplet + `-ar` 10-rate ring + `-ac` 7-channel ring + 4-axis stream-disable `-an`/`-vn`/`-sn`/`-dn` + 9-level `-loglevel` ladder + `-hwaccel` 9-engine ring covering vaapi/vdpau/cuda/nvdec/videotoolbox/d3d11va/dxva2/qsv + `-preset` 11-level x264/x265 speed ladder + `-crf` + `-tune` 8-mode ring + `-re` realtime + `-shortest` + `-movflags` mp4-muxer flags + `-y`/`-n` overwrite-policy pair + `-stats`/`-nostats` + `-hide_banner`), `ffprobe` (multimedia inspector with mutually-exclusive 5-section `-show_{format,streams,packets,frames,chapters,programs}` 6-target axis + `-of`/`-output_format`/`-print_format` 7-output-format ring covering default/csv/ini/json/compact/flat/xml + `-show_entries` selective dump + `-show_data`/`-show_data_hash` payload pair with 5-algorithm hash ring + `-show_log`/`-show_error` diagnostic pair + `-count_frames`/`-count_packets` count pair + `-select_streams`/`-read_intervals` filter pair + `-pretty` numeric pretty-print + `-sections` structure dump + `-bitexact` reproducible-output), `sox` (Sound eXchange with format quintet `-t {30+ types}`/`-r`/`-b`/`-c`/`-e {9 encodings}` + mutually-exclusive endian triplet `-L`/`-B`/`-x` + `-G` clip-guard / `-D` no-dither / `-S`/`-q`/`-V {0..4}` 3-axis verbosity + `--norm`/`--buffer` audio-tuning pair + `--no-glob`/`--no-clobber` filename-safety pair + `--plot {gnuplot,octave,off}` transfer-function viz ring + `--multi-threaded`/`--single-threaded` parallelism pair + `--combine {concatenate,merge,mix,mix-power,multiply,sequence}` 6-mode multi-input combinator ring + `--temp` + effect verbs `trim`/`rate`/`gain`/`fade`/`pitch`/`tempo`/`speed`/`reverb`/`echo`/etc), `mongoexport` (query-and-export with `--type {json,csv}` format pair + `--jsonFormat {canonical,relaxed}` Extended-JSON-mode pair + `--jsonArray`/`--pretty`/`--noHeaderLine` output-shape triplet + `--fields`/`--fieldFile` field-selection pair + `--query`/`--queryFile` query pair + `--skip`/`--limit`/`--sort` pagination triplet + `--readPreference` + auth/TLS surface shared with `mongo*`), `mongorestore` (BSON restorer with `--nsInclude`/`--nsExclude` namespace-filter pair + `--nsFrom`/`--nsTo` namespace-rename pair + `--drop`/`--dryRun` plan-or-replace pair + 3-axis index-policy family `--noIndexRestore`/`--convertLegacyIndexes`/`--keepIndexVersion` + `--noOptionsRestore` collMod-skip + `--maintainInsertionOrder`/`--stopOnError`/`--bypassDocumentValidation`/`--objcheck` 4-axis safety quartet + oplog triplet `--oplogReplay`/`--oplogLimit`/`--oplogFile` for PIT recovery + `--restoreDbUsersAndRoles` + `--numInsertionWorkersPerCollection`/`-j` performance pair + `--gzip`/`--archive` compressed-input pair); harvested from `manpages.debian.org/{ffmpeg,ffprobe,sox}.1` and `mongodb.com/docs/database-tools/{mongoexport,mongorestore}/`. Plus `jpegtran` (libjpeg / mozjpeg lossless JPEG transcoder + transformer with mutually-exclusive 6-axis transformation family `-flip {horizontal,vertical}` × `-rotate {90,180,270}` × `-transpose` × `-transverse` + `-crop WxH+X+Y`/`-wipe WxH+X+Y`/`-drop +X+Y file` 3-axis region ops + entropy-coding triplet `-optimize`/`-progressive`/`-arithmetic` + `-scans` scan-script + `-trim`/`-perfect` 8x8-block-honesty pair + 4-mode `-copy {none,comments,icc,all}` marker-copy ring + `-icc` profile embed + `-maxmemory`/`-maxscans` safety pair + `-strict` warn-as-error + `-report`/`-verbose`/`-debug`), `dwebp` (libwebp WebP decoder with mutually-exclusive 6-format `-bmp`/`-tiff`/`-pam`/`-ppm`/`-pgm`/`-yuv` output ring + `-nofancy`/`-nofilter` quality-skip pair + mutually-exclusive `-dither`/`-nodither` smoothing pair + `-crop`/`-resize`/`-flip` 3-axis geometry family + `-mt` multi-threading + `-noasm` asm-disable + `-v` timing), `bsondump` (BSON → JSON / debug-format converter with `--type {json,debug}` 2-format pair + `--pretty` + `--objcheck` validation + `--bsonFile`/`--outFile` IO pair), `mongostat` (live cluster activity stats with `--humanReadable` human-friendly toggle + `-o`/`-O` field-replace-vs-append pair + `--noheaders` + mutually-exclusive `--json`/`--interactive` output-mode pair + `-n`/`--rowcount` cap + `--discover` cluster-traversal + `--all` full-field-set), `mongotop` (per-collection read+write activity tracker with `--locks` mode-toggle + `-n` row-cap + `--json`); harvested from `manpages.debian.org/jpegtran.1`, `developers.google.com/speed/webp/docs/dwebp`, and `mongodb.com/docs/database-tools/{bsondump,mongostat,mongotop}/`. Plus `cjpeg` (libjpeg encoder with `-quality 0..100` + mutually-exclusive `-grayscale`/`-rgb` 2-colorspace + `-optimize`/`-progressive`/`-arithmetic` entropy-coding triplet + `-dct {int,fast,float}` DCT-method ring + `-icc` profile-embed + `-restart` MCU-row + `-smooth` denoiser + `-baseline` 8-bit-tables-only + quantization quartet `-qtables`/`-qslots`/`-sample`/`-scans` + `-memdst` memory-output + `-targa` raw input + safety pair `-maxmemory`/`-strict`), `djpeg` (libjpeg decoder with mutually-exclusive 6-format output ring `-bmp`/`-gif`/`-gif0`/`-os2`/`-pnm`/`-targa` + `-scale M/N` fractional resize + mutually-exclusive `-grayscale`/`-rgb` colorspace + `-colors`/`-quantize` palette reducer + `-dither {fs,ordered,none}` 3-mode dithering ring + `-fast` quality-skip + `-nosmooth`/`-onepass` quantization-speed pair + `-map` palette-from-file + `-icc` profile-extract + `-skip`/`-crop` region pair + `-memsrc` memory-input + `-maxscans` safety + `-dct {int,fast,float}` matching cjpeg's DCT-method ring), `gifsicle` (Eddie Kohler GIF Swiss-knife with mutually-exclusive `-m`/`-e` merge-vs-explode pair + mutually-exclusive `-O`/`-U` optimize-vs-unoptimize pair with 3-level `-O{1,2,3}` ladder + 4-colormap-preset `--use-colormap {web,bw,gray,halftone}` ring + 4-axis transform `--crop`/`--resize`/`--scale`/`--rotate-{90,180,270}` + 2-axis `--flip-{horizontal,vertical}` + `-d`/`-l`/`-t`/`-B` delay/loopcount/transparent/background quartet + `-k`/`-f` colors+dither pair + `-i` interlace + `-b` in-place batch + 3-strip family `--no-extensions`/`--no-comments`/`--no-names` + `--append`/`--replace`/`--multifile` multi-input triplet + `-I` info-dump), `mongofiles` (GridFS CLI with 8-verb action axis `list`/`search`/`put`/`get`/`get_id`/`get_regex`/`delete`/`delete_id` + `-d db` + `-l local-name` + `--type MIME` + `-r` replace-existing + `--prefix` GridFS-bucket override + auth/TLS surface shared with `mongo*`), `pdftoppm` (poppler-utils PDF-page rasterizer with mutually-exclusive 5-format output ring `-mono`/`-gray`/`-png`/`-jpeg`/`-tiff` + `-f`/`-l` page-range pair + mutually-exclusive `-o`/`-e` odd/even pair + `-singlefile` no-suffix mode + `-r`/`-rx`/`-ry` resolution triplet + 4-axis scale-to family `-scale-to`/`-scale-to-x`/`-scale-to-y`/`-scale-dimension-before-rotation` + 5-axis crop family `-x`/`-y`/`-W`/`-H`/`-sz` + `-cropbox` crop-box-override + 4-axis ICC-profile family `-displayprofile`/`-defaultgrayprofile`/`-defaultrgbprofile`/`-defaultcmykprofile` + `-freetype`/`-thinlinemode`/`-aa`/`-aaVector` 4-axis rendering family + `-opw`/`-upw` password pair + `-jpegopt`/`-tiffcompression` per-format option pair + `-progress`/`-q` verbosity pair + `-sep`/`-forcenum` filename-suffix pair); harvested from `manpages.debian.org/{cjpeg,djpeg,pdftoppm}.1`, `lcdf.org/gifsicle/man.html`, and `mongodb.com/docs/database-tools/mongofiles/`. Plus `x264` (H.264/AVC encoder with `--preset` 11-level speed ladder shared with ffmpeg/x265 + repeatable `--tune` 8-mode source-type ring + `--profile {baseline,main,high,high10,high422,high444}` 6-profile ring + mutually-exclusive `--crf`/`--bitrate`/`--qp` 3-axis rate-control + `--pass {1,2,3}` multipass + `--me {dia,hex,umh,esa,tesa}` 5-method motion-estimation ring + `--subme 0..11` subpixel-ME ladder + `--partitions` 7-partition ring + `--weightp {0,1,2}` 3-level weighted-prediction ring + 4-axis optimization-disable family `--no-cabac`/`--no-deblock`/`--no-scenecut`/`--no-8x8dct` + `--interlaced` + `--tff`/`--bff` field-order pair + `--psnr`/`--ssim` metric pair), `x265` (H.265/HEVC encoder mirroring x264's surface with 7-profile ring `main`/`main10`/`mainstillpicture`/`main422`/`main444`/`main10-intra`/`main12` + same `--crf`/`--bitrate`/`--qp` 3-axis rate-control + `--keyint`/`--bframes`/`--frames` GOP triplet + `--threads`/`--pools` thread pair + `--psnr`/`--ssim`/`--hrd`/`--aud` 4-axis reporting+signaling family + `--hash {0,1,2,3}` 4-mode DPH-SEI ring + 3-axis HDR signaling `--transfer`/`--colorprim`/`--colormatrix` covering bt709/bt2020/smpte2084/arib-std-b67), `pdfunite` (poppler PDF concatenator with `-v`/`-h` and free-form argument list), `pdfseparate` (per-page extractor with `-f`/`-l` page-range pair and `%d` filename pattern), `pdfsig` (PDF digital-signature inspector/creator with mutually-exclusive 4-verb action axis `-dump`/`-add-signature`/`-sign field`/`-list-{nicks,backends}` + NSS-backend triplet `-nssdir`/`-nss-pwd`/`-nick` + `-backend {NSS,GPGME}` 2-backend pair + `-kpw` signing-key password + `-digest {SHA1,SHA256,SHA384,SHA512}` 4-algorithm ring + `-etsi` ETSI.CAdES + 4-axis validation-skip family `-nocert`/`-no-ocsp`/`-no-appearance`/`-aia`); harvested from `manpages.debian.org/{x264,x265,pdfunite,pdfseparate,pdfsig}.1`. Plus `pdftohtml` (poppler PDF→HTML/XML converter with `-c` complex-layout / `-s` single-HTML / `-noframes`/`-stdout`/`-xml` output-mode quartet + `-p` PDF-link-to-HTML-link rewrite + `-dataurls` inline-image-as-data-URL + 4-axis content-skip family `-i` no-images / `-noframes` / `-nomerge` no-paragraph-merge / `-hidden` hidden-text-on + `-zoom` factor + `-fmt {png,jpg}` Splash-image pair + `-wbt` word-break-threshold + `-fontfullname` no-font-substitution + `-nodrm` override + `-enc`/`-opw`/`-upw` standard text-encoding+password triplet), `pdftocairo` (poppler PDF rasterizer/vector-exporter via Cairo with mutually-exclusive 9-format output ring `-png`/`-jpeg`/`-tiff`/`-pdf`/`-ps`/`-eps`/`-svg`/`-print`/`-printdlg` + `-antialias {default,none,gray,subpixel,fast,good,best}` 7-mode AA ring + 6-axis Cairo-specific PS family `-level2`/`-level3`/`-struct`/`-origpagesizes`/`-paper`/`-paperw`/`-paperh` + 4-axis page-fit family `-nocrop`/`-expand`/`-noshrink`/`-nocenter` + `-duplex` + Windows print-driver triplet `-printer`/`-printopt`/`-setupdlg` + same `-f`/`-l`/`-o`/`-e`/`-r`/`-rx`/`-ry`/`-scale-to{,-x,-y}`/`-x`/`-y`/`-W`/`-H`/`-sz`/`-cropbox`/`-mono`/`-gray`/`-transp`/`-icc`/`-jpegopt`/`-opw`/`-upw` surface shared with pdftoppm), `pdfdetach` (embedded-file extractor with mutually-exclusive 4-verb action axis `-list`/`-save N`/`-savefile NAME`/`-saveall` + `-o` sink override + `-enc` 5-encoding ring for filenames + `-opw`/`-upw` password pair), `pdfattach` (embed-file-in-PDF with `-replace` same-name-overwrite toggle), `ffplay` (FFmpeg SDL-based player with `-x`/`-y` window dimensions + `-fs` fullscreen + 3-axis stream-disable `-an`/`-vn`/`-sn` + 3-axis stream-pick `-ast`/`-vst`/`-sst` + `-ss`/`-t` time-window + `-bytes` byte-seek + `-nodisp`/`-noborder` chrome pair + `-volume 0..100` + `-loop`/`-autoexit` playback-end pair + `-exitonkeydown`/`-exitonmousedown` interaction pair + `-showmode {video,waves,rdft}` 3-mode visualization ring + `-sync {audio,video,ext}` 3-clock ring + `-seek_interval` seek-key step + `-window_title` + `-vf`/`-af` filtergraph pair + `-stats`/`-fast`/`-genpts` decode triplet + `-pix_fmt` ring + 9-level `-loglevel` ladder); harvested from `manpages.debian.org/{pdftohtml,pdftocairo,pdfdetach,pdfattach,ffplay}.1`. Plus `mc` (GNU Midnight Commander visual file manager with mutually-exclusive `-b`/`-c` color-vs-mono pair + `-C` custom-colorspec + `-a` stickchars line-drawing toggle + mutually-exclusive `-u`/`-U` subshell-off-vs-on pair + 4-axis startup-mode family `-e file` editor / `-v file` viewer / `-P file` printwd-on-exit / `-K file` keymap + `--nokeymap` skip-keybindings + `-S` skin + `-l file` FTP-dialog log + `-d`/`-g` mouse-disable+oldmouse pair + `-s` slow-terminal + `-t` use-TERMCAP + `-x`/`-X` xterm-mode-vs-no-X11 pair + `-f`/`-F` datadir-print-vs-extended pair + 0–2 dir positional args for left/right panel), `httpie` (HTTPie manager binary distinct from `http`/`https` request commands — 2-verb top axis `plugins` / `cli` with `plugins {install,uninstall,list,upgrade}` 4-action ring on PyPI-name-or-path targets + `cli {sessions,export-args,plugins,check-updates}` 4-action ring with `sessions {upgrade,upgrade-all}` 2-verb inner pair on HOSTNAME+SESSION_NAME_OR_PATH plus `--bind-cookies` + `export-args -f {json}` format ring + top-level `--debug`/`--traceback`/`--version`/`--help` 4-axis diagnostics), `s3fs` (s3fs-fuse FUSE file system backed by Amazon S3 with 80+ `-o key[=value]` mount options spanning auth quartet `passwd_file`/`profile`/`iam_role`/`ibm_iam_auth` + endpoint triplet `url`/`host`/`endpoint`+`servicepath`+`use_path_request_style`+`listobjectsv2`+`requester_pays` + 8-axis upload family `multipart_size`/`multipart_threshold`/`multipart_copy_size`/`singlepart_copy_limit`/`parallel_count`/`nomultipart`/`streamupload`/`max_dirty_data` + 9-axis cache family `use_cache`/`del_cache`/`check_cache_dir_exist`/`ensure_diskfree`/`free_space_ratio`/`max_stat_cache_size`/`stat_cache_expire`/`stat_cache_interval_expire`/`disable_noobj_cache` + 3-axis listing `multireq_max`/`list_object_max_keys`/`nodnscache` + `use_sse`/`load_sse_c` SSE pair + `sigv2`/`sigv4` signature pair + `nocopyapi`/`norenameapi`/`nomixupload`/`noxmlns` 4-axis API-disable family + `connect_timeout`/`readwrite_timeout`/`retries` resilience triplet + `no_check_certificate`/`ssl_verify_hostname`/`ssl_client_cert`/`cipher_suites` 4-axis TLS family + `proxy`/`proxy_cred_file`/`ipresolve`/`noua` 4-axis network family + `default_acl`/`umask`/`mp_umask`/`use_xattr`/`complement_stat`/`compat_dir`/`use_wtf8` 7-axis perms family + `logfile`/`dbglevel`/`curldbg`/`no_time_stamp_msg` 4-axis logging family + top-level `-f`/`-s`/`-d` daemon-disable/single-thread/debug triplet + `-u`/`--incomplete-mpu-list` MPU-list + `--incomplete-mpu-abort=all` cleanup), `ksh` (AT&T KornShell 93u+ with ±a/±b/±e/±f/±h/±i/±k/±l/±m/±n/±p/±r/±s/±t/±u/±v/±x/±B/±C/±E/±G/±H 22-pair set-or-unset axis + `-o option` / `+o option` long-form set-style on/off pair covering 29 options including allexport/bgnice/braceexpand/emacs/errexit/globstar/histexpand/ignoreeof/keyword/markdirs/monitor/noclobber/noexec/noglob/notify/nounset/pipefail/posix/privileged/restricted/showme/trackall/verbose/vi/xtrace + `-c command` string invocation), `tcsh` (TENEX C shell with `-b` break-option-processing + `-c command` + `-d` load-~/.cshdirs + repeatable `-Dname=value` env-set (Domain/OS) + `-e` exit-on-error + `-f` no-rc-or-hash + `-F` fork-not-vfork + `-i` force-interactive + `-l` login + `-m` load-~/.tcshrc-even-if-not-owned + `-n` parse-only + `-q` accept-SIGQUIT-debugger + `-s` stdin + `-t` single-line + `-v`/`-V` verbose-after-history-vs-before-tcshrc pair + `-x`/`-X` echo-mode-before-execution-vs-before-tcshrc pair); harvested from `manpages.debian.org/{mc,s3fs,ksh93,tcsh}.1`, `httpie.io/docs/cli/manager`, and `github.com/httpie/cli` source `httpie/manager/cli.py`.
- **Document converters / generators** (alongside existing `typst`/`tectonic`/`mdbook`/`glow`/`gum`/`frogmouth`) — `asciidoctor` (Ruby AsciiDoc processor with `-b {html5,docbook5,manpage}` 3-backend ring × `-d {article,book,manpage,inline}` 4-doctype axis + `-S {unsafe,safe,server,secure}` 4-level safe-mode ladder + mutually-exclusive `-e`/`-s` embedded-output pair + repeatable `-a name=value` attribute-define + `-E {erb,slim,haml,liquid}` template-engine quartet + `-T template-dir` + `--failure-level {WARN,ERROR,FATAL}` CI-strict ladder + `-r library` Ruby require + `-I load-path` repeatable), `pandoc` (Haskell universal converter with `-f`/`-t` input/output format pair covering ~50-format ring `{markdown*,commonmark*,gfm,html,docx,odt,pptx,beamer,latex,context,epub,jats,rst,org,asciidoc*,man,rtf,docbook*,opml,csljson,bibtex,ris,…}` + standalone `-s` + `--template` override + repeatable `-d` defaults YAML + repeatable `-M`/`-V`/`--metadata-file` metadata triplet + repeatable `-F` executable / `-L` Lua filter pair + `--toc-depth` + `--slide-level` + math-engine triplet `--mathjax`/`--katex`/`--mathml` + `--pdf-engine {pdflatex,lualatex,xelatex,tectonic,latexmk,wkhtmltopdf,weasyprint,prince,pagedjs-cli,context,pdfroff,typst}` 12-engine ring + `--highlight-style` 8-style + `--wrap {auto,none,preserve}` + `--columns`), `doxygen` (multi-language source-doc generator with `-g` Doxyfile-template + `-u` upgrade-Doxyfile + `-x` config-diff + `-w {html,latex,rtf}` style-template emitter + `-d {commentcnv,commentscan,filterevent,pp,tokenizer,code,preprocessor,printtree,time,extcmd,markdown,filteroutput,lex}` 13-category debug ring + `-l` layout-XML template + `-b` stdin-input + `-T`/`-S` timing+source-footer toggle pair), `pydoc` (Python text/HTML docs server with mutually-exclusive `-w`/`-k`/`-p`/`-n`/`-b` write-HTML / keyword-search / HTTP-server / hostname / browser-launch verb ring), `dust` (`du` successor in Rust with mutually-exclusive `-D`/`-F` directories-only / files-only pair + mutually-exclusive `-b`/`-B` no-bars / right-side-bars pair + mutually-exclusive `-c`/`-C` mono / force-color pair + `-n N`/`-d N` directory/depth-cap pair + `-z`/`-e`/`-v`/`-X` size-floor / regex-include / regex-exclude / name-ignore filter quartet + `-x` same-filesystem + `-s` length-vs-disk-usage toggle + `-f`/`-t` files-only-count / group-by-filetype reporting pair + `-p` full-path-output + `-i` hide-hidden + `-r` reverse + `-o {b,k,m,g,t,si}` unit-format ring + `-R` screen-reader mode + `-j` JSON + `--files0-from`/`--files-from`/`--collapse` IO-redirection triplet); harvested from `docs.asciidoctor.org/asciidoctor/latest/cli/man1/asciidoctor/`, `pandoc.org/MANUAL.html`, `doxygen.nl/manual/starting.html`, `docs.python.org/3/library/pydoc.html`, and `github.com/bootandy/dust` README.
- **Android platform-tools + SDK** (alongside existing `aapt2`/`apksigner`/`zipalign`/`dexdump`/`dx`/`r8`/`bundletool`/`apkanalyzer`/`mksdcard`) — `adb` (Android Debug Bridge with 26 subcommand surface covering device-control verbs `devices`/`shell`/`install`/`uninstall`/`push`/`pull`, port-forwarding pair `forward`/`reverse`, wireless-debug triplet `connect`/`disconnect`/`pair`, runtime quartet `logcat`/`bugreport`/`sideload`/`sync`, daemon-control trio `kill-server`/`start-server`/`reconnect` × `root`/`unroot`/`remount` privilege-mode triplet × `enable-verity`/`disable-verity` dm-verity pair, plus mutually-exclusive `-d`/`-e`/`-s serial` device-target ring + `-l` detailed-listing toggle + `tcpip` TCP-listen-port switch + `get-state` query), `fastboot` (bootloader-mode protocol with `flash`/`flashall`/`erase`/`format`/`getvar`/`boot`/`reboot`/`reboot-bootloader`/`continue`/`update`/`devices`/`set_active` 12 verbs + `flashing {lock,unlock,lock_critical,unlock_critical,get_unlock_ability}` security 5-subverb axis + boot-image geometry quartet `--kernel-offset`/`--ramdisk-offset`/`--tags-offset`/`-b base` + `-n page-size` NAND + `-S size` sparse-threshold + `--slot {a,b}` A/B slot selector + `--wipe-and-use-fbe` FBE-enable + `-w` userdata wipe + `-i` custom USB vendor ID), `avdmanager` (AVD lifecycle with 4-verb × 3-object grid: `create`/`delete`/`move`/`list` × `avd`/`target`/`device`, per-AVD `-n name`/`-k sdk_id`/`-p path`/`-c sdcard-spec`/`-f` force + `-r` rename), `sdkmanager` (package manager with mutually-exclusive `--list`/`--install`/`--uninstall`/`--update`/`--licenses` 5-verb axis + `--channel {0,1,2,3}` stable/beta/dev/canary release ring + `--sdk_root` SDK-path override + `--include_obsolete` legacy-EOL packages + `--package_file` batch-install list + `--proxy {http,socks}` proxy-type pair + `--no_https` plain-HTTP fallback + `--newer`/`--verbose`), `d8` (Java bytecode → DEX with mutually-exclusive `--debug`/`--release` build-mode pair + mutually-exclusive `--file-per-class`/`--file-per-class-file` DEX-fan-out pair + `--lib android.jar`/`--classpath`/`--min-api` SDK-target triplet + `--main-dex-list`/`--main-dex-rules`/`--main-dex-list-output` main-DEX-curation triplet + mutually-exclusive 4-way `--force-{ea,da,pa,ah}` Java-assertion-handling ring + `--intermediate` incremental-compile + `--no-desugaring` + `--pg-map`/`--desugared-lib` mapping pair + `--thread-count` + `--map-diagnostics from-to` level-remapping); harvested from `developer.android.com/tools/{adb,avdmanager,sdkmanager,d8}` and `manpages.debian.org/fastboot.1`. Plus `monkey` (on-device pseudo-random user-event stress tester run as `adb shell monkey` with 8-axis percentage mix `--pct-{touch,motion,trackball,nav,majornav,syskeys,appswitch,anyevent}` summing to 100% + repeatable `-p package`/`-c category` allowlist scoping + `-s seed` reproducibility + `--throttle ms` rate-limit + 4-error-class ignore-pass quartet `--ignore-{crashes,timeouts,security-exceptions}` + `--monitor-native-crashes` + `--kill-process-after-error` + `--hprof` profile-around + `--dbg-no-events` baseline-noop + `--wait-dbg` debugger-attach), `screenrecord` (on-device H.264/HEVC video capture with `--size WxH` × `--bit-rate` + `--time-limit` cap + `--display-id` per-display capture + `--output-format {mp4,h264,webm,3gpp,frames,raw-frames}` 6-container ring + `--codec-name` override + `--rotate` 90° + `--bugreport` overlay + diagnostic toggle pair `--show-device-info`/`--show-frame-time` + `--bframes` GOP-tuning + `--persistent-surface`/`--capture-secure` rendering-mode pair + `--monotonic-time` clock-source switch), `screencap` (on-device screenshot with `-a` all-displays-suffix + `-d display-id` per-display + mutually-exclusive `-p`/`-j` PNG/JPEG output pair + `--hint-for-seamless` SurfaceFlinger fast-path); harvested from `developer.android.com/studio/test/monkey` and `android.googlesource.com/platform/frameworks/{av/screenrecord,base/screencap}` `usage()`.
- **macOS system / package / signing CLI** (alongside existing `codesign`/`pkgbuild`/`productbuild`/`altool`/`stapler`/`xcodes`/`spctl`) — `installer` (macOS package installer with `-pkg`/`-target` install target pair + 3-way introspection `-dominfo`/`-volinfo`/`-pkginfo` + XML-choice-state quartet `-showChoicesXML`/`-showChoicesAfterApplyingChangesXML`/`-applyChoiceChangesXML` + `-allowUntrusted` cert-relax + `-store` MAS-mode + `-config`/`-plist`/`-file` parameter-marshal triplet + `-lang`/`-listiso` ISO-locale pair), `security` (27-subcommand keychain CLI covering keychain-lifecycle nonet `list/default/login/create/delete/lock/unlock/set-settings/set-password`, password-manager sextet `add/find/delete-{generic,internet}-password`, certificate quintet `find-certificate`/`find-identity`/`add-certificates`/`delete-certificate`/`create-keypair`, import/export pair, trust-settings quartet `add/remove-trusted-cert`/`dump-trust-settings`/`verify-cert` + `-i`/`-p prompt` interactive-mode + `-l` leaks-on-exit), `iconutil` (`.icns`↔`.iconset` converter with mutually-exclusive `-c {icns,iconset}` direction enum + `-o` output override), `defaults` (preferences CLI with 10-subcommand surface `read`/`read-type`/`write`/`rename`/`delete`/`domains`/`find`/`export`/`import`/`help` + 10-type value-marshal axis `-string`/`-data`/`-int`/`-float`/`-bool`/`-date`/`-array`/`-array-add`/`-dict`/`-dict-add` + domain-axis pair `-app`/`-g`/`NSGlobalDomain` + `-currentHost`/`-host` per-host scoping), `notarytool` (`xcrun notarytool` 7-subcommand Developer-ID notary CLI `submit`/`info`/`log`/`history`/`wait`/`store-credentials`/`help` + 8-flag auth-axis covering App-Store-Connect-API path `-k`/`-d`/`-i` key/key-id/issuer triplet, Apple-ID password path `--apple-id`/`--password`/`--team-id`, and keychain-profile shortcut `-p` + submit-specific `--wait`/`--no-wait` + `--timeout` + `--s3-acceleration`/`--no-s3-acceleration` upload-mode pair + `--force` validation-skip + store-credentials-specific `--validate`/`--sync` iCloud-Keychain toggle + `-f {normal,json,plist}` output-format ring); harvested from `unix.com/man-page/osx/1/{installer,security,iconutil,defaults}/` and `keith.github.io/xcode-man-pages/notarytool.1.html`.
- **Apple Xcode + iOS deploy** — `xcrun` (developer-tool finder/runner with mutually-exclusive `-f`/`-r` find-only-vs-execute pair + `--sdk {macosx,iphoneos,iphonesimulator,appletvos,appletvsimulator,watchos,watchsimulator,driverkit}` 8-SDK ring + `--toolchain` SDK-vs-toolchain orthogonal axis + lookup-cache pair `-n` no-cache / `-k` invalidate + `--show-sdk-{path,version,build-version,platform-path,platform-version}` 5-property introspection + `-l` invocation-tracing), `ios-deploy` (cross-platform iOS-device CLI for install/run/debug with `-b bundle`/`-1 bundle_id` target pair + `--detect`/`--list_bundle_id`/`--exists` device-state introspection triplet + uninstall pair `-r` before-install vs `-9` only + `--debug`/`--nolldb`/`--noninteractive`/`--justlaunch`/`--noinstall` 5-mode lldb-control axis + filesystem ops sextet `--list`/`--upload`/`--download`/`--to`/`--mkdir`/`--rm`/`--rmtree` + provisioning-profile quartet `--list_profiles`/`--profile-download`/`--profile-install`/`--profile-uninstall` + `--check-developer-mode` + `--app_deltas dir` incremental-install + `--symbols dir` OS-symbol download + `--json` machine-readable output); harvested from `developer.apple.com/library/archive/documentation/Darwin/Reference/ManPages/man1/xcrun.1.html` and `github.com/ios-control/ios-deploy` README options. Plus `xcode-select` (active developer-directory manager with mutually-exclusive `-s`/`-r` switch-vs-reset pair + `-p` print-path + `--install` Command-Line-Developer-Tools dialog), `swift-format` (`format`/`lint`/`dump-configuration` 3-subcommand surface with `-i` format-in-place + `-s` lint-strict-exit + `--assume-filename` stdin-path-shim + `--color-diagnostics`/`--no-color-diagnostics` pair + `--configuration` JSON-config + `--ignore-unparsable-files` + `-p`/`-r` parallel+recursive pair).
- **Android reverse-engineering toolchain** — `apktool` (iBotPeaches APK decode/build/install-framework with 8-verb surface `d`/`decode`/`b`/`build`/`if`/`install-framework`/`cf`/`clean-frameworks`/`lf`/`list-frameworks`/`pr`/`publicize-resources` + decode-mode `-a` all-src / `-s`/`-r` no-src/no-res mutually-exclusive selectors / `--only-manifest` / `--res-resolve-mode {default,greedy,lazy}` 3-way ring / `--keep-broken-res` corrupt-tolerant + build-mode `--no-apk`/`--no-crunch`/`--copy-original`/`--debuggable`/`--net-sec-conf`/`--aapt aapt2-path` / `-l pkg=path` shared-lib injection + framework-mode `-p`/`-t`/`-a` path/tag/all-tags triplet), `jadx-gui` (skylot/jadx GUI wrapper sharing the same ~50-flag CLI as `jadx` covering single-class decompile, gradle-export with template auto, decompilation-mode 4-ring auto/restructure/simple/fallback, deobfuscation triplet `--deobf-min`/`--deobf-max`/`--deobf-whitelist` + 8-knob inline-disabling family `--no-inline-*` / `--no-finally` / `--no-replace-consts` / `--respect-bytecode-access-modifiers`, Kotlin-intrinsic var-rename, CFG dot dumps `--cfg`/`--raw-cfg`, log-level 6-axis `quiet`/`progress`/`error`/`warn`/`info`/`debug` ring, plugin disable list), `aapt` (legacy Android Asset Packaging Tool — `l`/`d`/`p`/`r`/`a`/`c`/`s`/`v` 8 subcommands + `dump` 8-target ring `badging`/`permissions`/`resources`/`configurations`/`xmltree`/`xmlstrings`/`strings`/`styleparents` + `package` core surface covering manifest/resources/assets directory triplet + min/target/max SDK + version-code/name + rename-manifest-package + shared-lib + utf16 + debug-mode + auto-add-overlay); harvested from `ibotpeaches.github.io/Apktool` README + `iBotPeaches/Apktool brut.apktool/.../Main.java`, `skylot/jadx` README usage section, and `elinux.org/Android_aapt` reference.
- **conntrack-tools** (Pablo Neira / netfilter — Linux kernel connection-tracking table CLI) — single binary with 11 mutually-exclusive command verbs `-L/-G/-D/-I/-A/-U/-E/-F/-C/-S/-R` (dump/get/delete/create/add/update/event-stream/flush/count/stats/load-file), filter-parameter sextet over source/destination tuples in both original and reply directions (`-s/-d/-r/-q` × `--sport/--dport/--reply-port-src/--reply-port-dst`), L3/L4 protocol selectors (`-p {tcp,udp,sctp,dccp,gre,icmp,icmpv6,udplite}` × `-f {ipv4,ipv6}`), conntrack-mark / SELinux-secmark / label / status filters with `--label-add`/`--label-del` mutation pair, NAT-direction triplet `-n/-g/-j` source/destination/any-NAT, zone-direction triplet `-w`/`--orig-zone`/`--reply-zone`, and per-protocol state/icmp-type-code-id/SCTP-verification-tag/DCCP-role refinements; harvested from `manpages.debian.org/conntrack.8`.
- **libyal Windows-artifact parsers** (Joachim Metz application-layer forensics — shared 15-codepage `-c` ASCII-string-decoding ring across the suite: `ascii,windows-{874,932,936,949,950,1250-1258}` for Cyrillic/Greek/Turkish/Hebrew/Arabic/Baltic/East-Asian case-file localization matching the libewf header-codepage convention) — `regfinfo` (Windows NT Registry .DAT hive reader with `-B` bodyfile-mode key+value dump and `-H` hierarchy view), `evtxexport` (Vista+ `.evtx` EventLog exporter with `-f {xml,text}` × `-m {all,items,recovered}` × `-t {application,security,system}` log-type filter triplet, plus `-T` event-template parsing requiring `-r dir`/`-s SYSTEM`/`-S SOFTWARE` registry-hive paths for message-string resolution), `pffexport` (Outlook PAB/OST/PST mailbox exporter with `-f {all,html,rtf,text}` body-format selector × `-m {all,debug,items,recovered}` mode × `-d` per-item `ItemValues.txt` dump × `-t basename` target-directory naming), `pffinfo` (lightweight PAB/OST/PST inspector with `-a` allocation expansion), `olecfinfo` (legacy MS Office `.doc`/`.xls`/`.ppt` OLE Compound File inspector with `-a` allocation); harvested from `libyal/{libregf,libevtx,libpff,libolecf} manuals/{regf,evtx,pff,olecf}{info,export}.1`. Round 71 extends the suite with `sccainfo` (Windows Prefetch / SuperFetch `.pf` cache reader for application-launch forensics with `-a` allocation expansion), `lnkinfo` (Windows Shell Link `.lnk` shortcut decoder — extracts target path, working directory, distributed link tracking GUIDs, and droid identifiers), `msiecfinfo` (Internet Explorer `index.dat` cache-file reader for visited-URL/cookie history forensics with `-a` allocation), `evtexport` (NT5 `.evt` legacy EventLog exporter pre-Vista — pairs with `evtxexport`'s post-Vista `.evtx` reader and shares the same `-r dir`/`-s SYSTEM`/`-S SOFTWARE` registry-hive resolution + `-t {application,security,system}` log-type triplet + `-m {all,items,recovered}` mode + `-p` resource search PATH, minus `-f`/`-T` since legacy `.evt` has no XML/template structure), `nk2info` (Outlook Nickname `.nk2` autocomplete-cache reader for recipient-suggestion history); harvested from `libyal/{libscca,liblnk,libmsiecf,libevt,libnk2} manuals/*.1`.

Directory layout:

```
src/              main completions
more_src/         additional completions (0-9, a-h)
more_src2/        additional completions (i-r)  split to keep dir under 10k entries
more_src3/        additional completions (s-z)  split to keep dir under 10k entries
man_src/          completions parsed from man pages
override_src/     overrides (prepended to fpath)
architecture_src/ cross-architecture toolchains
```

---

## [0x01] // JACK IN

### Zinit `[RECOMMENDED]`

> `~/.zshrc`

```sh
source "$HOME/.zinit/bin/zinit.zsh"
zinit ice lucid nocompile wait'0e' nocompletions
zinit load MenkeTechnologies/zsh-more-completions
```

> **NOTE:** The `nocompletions` ice prevents zinit from symlinking all completions into `~/.zinit/completions`. Instead the plugin directly manipulates `fpath` — this lets `override_src` completions take priority over defaults while keeping system completions untouched.

### Oh My Zsh

```sh
cd "$HOME/.oh-my-zsh/custom/plugins" && git clone https://github.com/MenkeTechnologies/zsh-more-completions.git
```

Add `zsh-more-completions` to your plugins array in `~/.zshrc`.

OMZ only adds plugin root to fpath, so completions in subdirectories won't load without intervention. You need to wire the nested source dirs into fpath **before** OMZ runs `compinit`:

```sh
# OMZ does not add nested comp dirs to fpath so do it here, assume *src has completions
for plug in ${plugins[@]}; do
    if [[ -d "$ZSH/custom/plugins/$plug" ]]; then
        # null glob - no error
        for dir in "$ZSH/custom/plugins/$plug/"*src(N); do
            if [[ -d "$dir" ]]; then
                if [[ -z ${fpath[(r)$dir]} ]];then
                    if [[ $dir = *override* ]]; then
                        fpath=($dir $fpath)
                    else
                        fpath=($fpath $dir)
                    fi
                fi
            fi
        done
    fi
done
```

### Manual Install

```sh
git clone https://github.com/MenkeTechnologies/zsh-more-completions.git
```

Copy all `_*` files from the source directories to somewhere on your `fpath`.

---

## [0x02] // BOOT SEQUENCE

After install, initialize the completion system:

```sh
autoload -Uz compinit && compinit
```

For faster shell startup, cache the `~/.zcompdump`:

```sh
compinit -C
```

This skips the security check and loads the cached dump directly — **dramatically** faster boot times.

---

## [0x03] // STATS

Counts change constantly; **do not** edit totals by hand. From the repo root:

```sh
zsh scripts/print-repo-stats.zsh
```

This prints `_`-prefixed completion file counts per directory, `total_completions`, and `zunit_tests` (number of `@test` entries under `tests/`).

| Metric | How it is obtained |
|---|---|
| Completion files per dir | `scripts/print-repo-stats.zsh` (uses `find … -name '_*'`) |
| ZUnit tests | `grep '^@test' tests/*.zsh \| wc -l` (same as script output) |
| Generation method | Python scripts parsing `--help` + man pages |
| License | See `license.md` |

---

## [0x04] // TESTING

From the repository root, with [ZUnit](https://github.com/MenkeTechnologies/zunit) on your `PATH`:

```sh
zunit tests/*.zsh
```

GitHub Actions runs the same command on Ubuntu. On macOS, a wrong-case path can still `stat` the file; entries in `tests/t-more-src-existence.zsh` must use the exact basename as on disk (e.g. `_linguist`, not `_Linguist`) so Linux CI passes. `tests/t-repo-invariants.zsh` locks down repo metadata: `blacklist.txt` integrity, `.github/workflows/ci.yml` contract, `license.md`, and absence of tracked `.zwc` files.

---

```
[ SIGNAL LOST ]
[ RECONNECTING... ]
[ LINK ESTABLISHED ]
```

> _Wake up, samurai. Your shell has work to do._
