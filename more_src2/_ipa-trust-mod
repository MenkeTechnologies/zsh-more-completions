#compdef ipa-trust-mod
# Documentation: (subcommand of ipa CLI, not a standalone binary)
# Reference accessed: 2026-05-16
# Verified: no standalone manpage in freeipa upstream. `ipa-trust-mod` is the
# subcommand `ipa trust-mod` of the main IPA CLI tool.

# ipa-trust-mod: subcommand of ipa CLI (use `ipa trust-mod`)
local curcontext="$curcontext" ret=1
_arguments -C \
  '*::extra args:_files' && ret=0
return ret
