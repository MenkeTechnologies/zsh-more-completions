#compdef joyserver
# Documentation: (none - package does not exist in Debian/Ubuntu catalogs)
# Reference accessed: 2026-05-16
# Verified: packages.debian.org search for "joyserver" returns "Sorry, your
# search gave no results" across all Debian suites; ubuntu jammy manpage URL
# returns HTTP 404. No upstream source identified. File kept per "do not
# delete files" directive with conservative completion only.
# Removed from prior file: -d (joystick device), -p (listen port), -v
# (verbose) - none can be source-verified; the entire prior set was
# fabricated since the binary does not exist upstream.

# joyserver: not present in upstream package catalogs
local curcontext="$curcontext" ret=1
_arguments -C \
  '(- *)-h[show help]' \
  '(- *)--help[show help]' \
  '*::extra args:_files' && ret=0
return ret
