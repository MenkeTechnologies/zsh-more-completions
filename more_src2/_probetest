#compdef probetest
# Documentation: (no canonical upstream)
# Reference accessed: 2026-05-17
# Verified: no installed `probetest` binary with documented CLI exists
# in Debian/Ubuntu/Arch/Homebrew package indexes. Closest matches are
# private GStreamer test programs in `gst-plugins-good/tests/icles/`
# that are not packaged.
# Removed from prior file: bogus man.archlinux.org search URL;
# fabricated -f/retry/max-size args.

# probetest: no documented CLI surface
local curcontext="$curcontext" ret=1
_arguments -C \
  ':no documented CLI (test program; check `probetest --help`):' && ret=0
return ret
