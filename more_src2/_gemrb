#compdef gemrb
# Documentation: https://github.com/gemrb/gemrb/blob/master/gemrb/core/InterfaceConfig.cpp
# Reference accessed: 2026-05-16
# Verified: gemrb/core/InterfaceConfig.cpp LoadFromArgs() (lines 317-352).
# Removed from prior file: -h/--help (no handler in source), -v/--version
# (no handler in source).

# GemRB: open-source engine for Infinity Engine games (BG/IWD/PST)
local curcontext="$curcontext" ret=1
_arguments -C \
  '-c[load config file (overrides defaults)]:file:_files -g "*.cfg"' \
  '-q[quiet mode (disable audio)]' \
  '-f[fullscreen]' \
  '--color[set log color]:value:' \
  '*:game path:_files -/' && ret=0
return ret
