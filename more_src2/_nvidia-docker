#compdef nvidia-docker
# Documentation: https://github.com/NVIDIA/nvidia-docker
# Reference accessed: 2026-05-19
# Verified: upstream docs at the URL above. Tool is a thin wrapper that
# forwards arguments to docker; STUB.

# nvidia-docker: legacy CLI wrapper around docker exposing NVIDIA GPUs
local curcontext="$curcontext" ret=1
_arguments -C \
  '*::docker args:_files' && ret=0
return ret
