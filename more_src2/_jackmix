#compdef jackmix
# Documentation: https://github.com/kampfschlaefer/jackmix/blob/master/jackmix/main.cpp
# Reference accessed: 2026-05-19
# Verified: upstream docs at the URL above.

# JackMix: matrix-style audio mixer for JACK (Qt app). Source main()
# only reads a single positional argument: an existing project file
# (else any standard Qt argument). Removed fabricated -h/--help/-i/-o
# flags (not in source). STUB.
local curcontext="$curcontext" ret=1
_arguments -C \
  '1:project file:_files' && ret=0
return ret
