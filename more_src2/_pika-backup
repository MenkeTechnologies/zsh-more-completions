#compdef pika-backup
# Documentation: https://gitlab.gnome.org/World/pika-backup
# Reference accessed: 2026-05-16
# Verified: upstream gitlab.gnome.org/World/pika-backup. GTK app uses
# no add_main_option / GOptionEntry table; functionality lives entirely
# in the GUI and via D-Bus to the separate pika-backup-monitor binary.
# Removed from prior file: fabricated -h/--help/-V/--version flags and
# bogus man.archlinux.org search URL.

# pika-backup: GTK backup tool built on borg backup (GUI-only)
local curcontext="$curcontext" ret=1
_arguments -C && ret=0
return ret
