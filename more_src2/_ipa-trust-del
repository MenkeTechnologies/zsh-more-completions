#compdef ipa-trust-del
# Documentation: (subcommand of ipa CLI, not a standalone binary)
# Reference accessed: 2026-05-16
# Verified: no standalone manpage in freeipa upstream. `ipa-trust-del` is the
# subcommand `ipa trust-del` of the main IPA CLI tool.

# ipa-trust-del: subcommand of ipa CLI (use `ipa trust-del`)
local curcontext="$curcontext" ret=1
_arguments -C \
  '*::extra args:_files' && ret=0
return ret
