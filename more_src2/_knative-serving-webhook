#compdef knative-serving-webhook
# _knative-serving-webhook — Knative Serving admission/conversion webhook (sharedmain)
# upstream: https://github.com/knative/serving
# Flags from cmd/webhook/main.go + knative.dev/pkg sharedmain on tip.

_knative-serving-webhook() {
    _arguments \
        '(- *)'{-h,--help}'[show help]' \
        '--disable-controllers=[csv of disabled controllers]:csv:' \
        '--disable-ha=[disable high-availability mode]:bool:(true false)' \
        '--kubeconfig=[kubeconfig file]:file:_files' \
        '-v[klog verbosity level]:n:'
}

_knative-serving-webhook "$@"
